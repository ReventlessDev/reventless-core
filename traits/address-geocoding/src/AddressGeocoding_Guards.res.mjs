// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

function onSubjectUpdate(r, subject) {
  if (subject === r.subject) {
    return "Ignore";
  } else {
    return "Append";
  }
}

function onSuppliedPair(r, subject, location) {
  if (subject === r.subject && Primitive_object.equal(r.location, location)) {
    return "Ignore";
  } else {
    return "Append";
  }
}

function onLocationReport(r, location, resolvedFrom) {
  if (resolvedFrom !== r.subject || Primitive_object.equal(r.location, location) && Primitive_object.equal(r.resolvedFrom, resolvedFrom)) {
    return "Ignore";
  } else {
    return "Append";
  }
}

function onUnresolvableReport(r, subject) {
  if (subject !== r.subject || Primitive_object.equal(r.resolvedFrom, subject)) {
    return "Ignore";
  } else {
    return "Append";
  }
}

export {
  onSubjectUpdate,
  onSuppliedPair,
  onLocationReport,
  onUnresolvableReport,
}
/* No side effect */
