node_modules/
# ReScript build artifacts — regenerated by `rescript build`, not tracked.
# (The hand-written cross-package bridge ReventlessVscodeProtocol.gen.ts is at the
# package root, so the src/*.gen.ts rule below doesn't touch it.)
lib/
src/*.res.js
src/*.gen.ts
