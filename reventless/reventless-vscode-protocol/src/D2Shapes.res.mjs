// Generated by ReScript, PLEASE EDIT WITH CARE

import * as D2Classes$ReventlessVscodeProtocol from "./D2Classes.res.mjs";

let base64 = (s =>
  typeof Buffer !== "undefined"
    ? Buffer.from(s, "utf8").toString("base64")
    : btoa(String.fromCharCode(...new TextEncoder().encode(s))));

function xmlEscape(s) {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;");
}

let fontAttrs = `font-family="-apple-system,Segoe UI,Roboto,sans-serif" font-weight="600" font-size="16"`;

function textWidth(label) {
  return label.length * 8.6 | 0;
}

function dataUri(svg) {
  return "data:image/svg+xml;base64," + base64(svg);
}

function extensionPointIcon(label) {
  let w = textWidth(label) + 96 | 0;
  let svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ` + w.toString() + ` ` + (70).toString() + `">` + (`<path d="M12 10 L` + (w - 12 | 0).toString() + ` 10 L` + (w - 28 | 0).toString() + ` 35 L` + (w - 12 | 0).toString() + ` 60 L12 60 L28 35 Z" `) + (`fill="` + D2Classes$ReventlessVscodeProtocol.extensionPointFill + `" stroke="` + D2Classes$ReventlessVscodeProtocol.extensionPointStroke + `" stroke-width="3" stroke-linejoin="round"/>`) + (`<text x="` + (w / 2 | 0).toString() + `" y="35" text-anchor="middle" dominant-baseline="central" `) + (`fill="` + D2Classes$ReventlessVscodeProtocol.extensionPointFontColor + `" ` + fontAttrs + `>` + xmlEscape(label) + `</text></svg>`);
  return {
    uri: "data:image/svg+xml;base64," + base64(svg),
    width: w,
    height: 70
  };
}

function extensionIcon(label) {
  let w = textWidth(label) + 80 | 0;
  let svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ` + w.toString() + ` ` + (70).toString() + `">` + (`<path d="M26 10 L` + (w - 10 | 0).toString() + ` 10 L` + (w - 10 | 0).toString() + ` 60 L26 60 L10 35 Z" `) + (`fill="` + D2Classes$ReventlessVscodeProtocol.extensionFill + `" stroke="` + D2Classes$ReventlessVscodeProtocol.extensionStroke + `" stroke-width="3" stroke-linejoin="round"/>`) + (`<text x="` + ((w + 16 | 0) / 2 | 0).toString() + `" y="35" text-anchor="middle" dominant-baseline="central" `) + (`fill="` + D2Classes$ReventlessVscodeProtocol.extensionFontColor + `" ` + fontAttrs + `>` + xmlEscape(label) + `</text></svg>`);
  return {
    uri: "data:image/svg+xml;base64," + base64(svg),
    width: w,
    height: 70
  };
}

let height = 70;

export {
  base64,
  xmlEscape,
  fontAttrs,
  height,
  textWidth,
  dataUri,
  extensionPointIcon,
  extensionIcon,
}
/* No side effect */
