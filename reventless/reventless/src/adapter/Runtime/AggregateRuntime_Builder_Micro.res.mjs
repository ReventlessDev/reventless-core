// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$Reventless from "../../components/Component.res.mjs";
import * as CommandTopic$Reventless from "../../components/CommandTopic/CommandTopic.res.mjs";
import * as ComponentType$Reventless from "../../ComponentType.res.mjs";
import * as EventCollector$Reventless from "../../components/EventCollector/EventCollector.res.mjs";
import * as CommandGenerator$Reventless from "../../components/CommandGenerator/CommandGenerator.res.mjs";

function Make(RuntimeEnvironment) {
  return CommandTopicChannel => (EventCollectorChannel => {
    let forCommandGenerator = (handler, connect, memorySizeOpt, timeoutOpt, commandGenerator) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$Reventless.toPulumiResource(commandGenerator);
      return connect(RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, CommandGenerator$Reventless.componentType), handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, {
        parent: resource
      }));
    };
    let forCommandTopic = (handler, connect, memorySizeOpt, timeoutOpt, commandTopic) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$Reventless.toPulumiResource(commandTopic);
      return connect(RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, CommandTopic$Reventless.componentType), handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, {
        parent: resource
      }));
    };
    let forEventCollector = (handler, eventTopics, resources, memorySizeOpt, timeoutOpt, eventCollector) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$Reventless.toPulumiResource(eventCollector);
      let name = ComponentType$Reventless.nameOpt(resource.__name, EventCollector$Reventless.componentType);
      let opts_parent = resource;
      let opts = {
        parent: opts_parent
      };
      let runtime = RuntimeEnvironment.make(name, handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, opts);
      EventCollectorChannel.connect(name, [{
          channel: eventCollector.channel,
          eventTopics: eventTopics,
          resources: resources
        }], runtime, opts);
    };
    let finish = () => {};
    return {
      CommandTopicChannel: CommandTopicChannel,
      EventCollectorChannel: EventCollectorChannel,
      forCommandGenerator: forCommandGenerator,
      forCommandTopic: forCommandTopic,
      forEventCollector: forEventCollector,
      finish: finish
    };
  });
}

export {
  Make,
}
/* Component-Reventless Not a pure module */
