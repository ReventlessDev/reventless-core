// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Pulumi from "@pulumi/pulumi";
import * as Id$ReventlessSpec from "@reventlessdev/reventless-spec/src/Id.res.mjs";
import * as Component$Reventless from "../Component.res.mjs";
import * as DcbTag$ReventlessSpec from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as DcbEventLog$Reventless from "./DcbEventLog.res.mjs";
import * as Util_Pulumi$Reventless from "../../util/Util_Pulumi.res.mjs";
import * as ComponentType$Reventless from "../../ComponentType.res.mjs";
import * as EventTopic_Builder$Reventless from "../EventTopic/EventTopic_Builder.res.mjs";
import * as DcbEventLog_Operations$Reventless from "./DcbEventLog_Operations.res.mjs";

function Make(Spec) {
  return Storage => (EventTopicPublisher => {
    let eventSchema = Spec.eventSchema;
    let taggedFields = DcbTag$ReventlessSpec.extractTaggedFields(Spec.eventSchema);
    let singleTagIndexes = taggedFields.map(tagKey => `tag_` + tagKey);
    let compositeIndex = taggedFields.length > 1 ? ["tag_composite"] : [];
    let indexes = singleTagIndexes.concat(compositeIndex);
    let construct = (self, name) => {
      let opts_parent = Component$Reventless.toPulumiResource(self);
      let opts = {
        parent: opts_parent
      };
      let storage = Storage.make(ComponentType$Reventless.name(name, DcbEventLog$Reventless.componentType), indexes, opts);
      let SpecificEventTopic = EventTopic_Builder$Reventless.Make({
        Id: Id$ReventlessSpec.$$String,
        eventSchema: eventSchema
      })(EventTopicPublisher);
      let eventTopic = SpecificEventTopic.make(name, storage.resources, Util_Pulumi$Reventless.ComponentResourceOptions.ofCustomResourceOptions(opts));
      Component$Reventless.setOperations(self, Pulumi.all([
        storage.operations,
        Component$Reventless.operations(eventTopic)
      ]).apply(param => {
        let publishJson = param[1].publishJson;
        let Ops = DcbEventLog_Operations$Reventless.Make(Spec)({
          Spec: Spec,
          name: name,
          storage: param[0],
          publishJson: publishJson
        });
        return {
          read: Ops.read,
          append: Ops.append
        };
      }));
      let outputs_resources = storage.resources;
      let outputs_eventTopic = Component$Reventless.outputs(eventTopic);
      let outputs = {
        resources: outputs_resources,
        eventTopic: outputs_eventTopic
      };
      return Component$Reventless.setOutputs(self, outputs);
    };
    let make = (name, opts) => Component$Reventless.make(ComponentType$Reventless.toString(DcbEventLog$Reventless.componentType), name, construct, opts);
    return {
      Spec: Spec,
      make: make
    };
  });
}

export {
  Make,
}
/* @pulumi/pulumi Not a pure module */
