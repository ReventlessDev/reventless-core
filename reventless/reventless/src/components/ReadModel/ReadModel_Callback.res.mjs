// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Message$Reventless from "../../Message.res.mjs";
import * as Projection$Reventless from "../../Projection.res.mjs";
import * as Message$ReventlessSpec from "@reventlessdev/reventless-spec/src/Message.res.mjs";
import * as ProjectionMapper$Reventless from "../../ProjectionMapper.res.mjs";

function Make(ReadModelSpec) {
  return Mappings => (Spec => {
    let EventProjector = ProjectionMapper$Reventless.Make({
      Id: ReadModelSpec.Id,
      name: ReadModelSpec.name,
      stateSchema: ReadModelSpec.stateSchema,
      subIdConfig: ReadModelSpec.subIdConfig
    })(Mappings);
    let eventsHandler = jsons => {
      let eventCount = jsons.length;
      return Projection$Reventless.handleActions(jsons.map((json, idx) => {
        let idx$1 = idx + 1 | 0;
        let sourceName = Message$Reventless.decode(json, Message$ReventlessSpec.contextSchema).meta.service;
        console.log(`ReadModel ` + ReadModelSpec.name + `: handling event ` + idx$1.toString() + `/` + eventCount.toString() + ` from ` + sourceName + `:`, json);
        return EventProjector.map(sourceName, json);
      }).flat(), Spec.operations, ReadModelSpec.subIdConfig);
    };
    return {
      EventProjector: EventProjector,
      eventsHandler: eventsHandler
    };
  });
}

export {
  Make,
}
/* Message-Reventless Not a pure module */
