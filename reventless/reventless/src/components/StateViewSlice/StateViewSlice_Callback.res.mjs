// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Logger$Reventless from "../../util/Logger.res.mjs";
import * as Projection$Reventless from "../../Projection.res.mjs";

function Make(Spec) {
  let eventsHandler = async (queryDbOps, events) => {
    Logger$Reventless.debug("File \"StateViewSlice_Callback.res\", line 21, characters 11-18", undefined, undefined, `StateViewSlice(` + Spec.name + `)`, events.length.toString() + ` event(s) received`);
    let actions = events.map(event => Spec.project(undefined, event.event)).flat();
    return await Projection$Reventless.handleActions(actions, queryDbOps, undefined);
  };
  return {
    Spec: Spec,
    eventsHandler: eventsHandler
  };
}

export {
  Make,
}
/* Logger-Reventless Not a pure module */
