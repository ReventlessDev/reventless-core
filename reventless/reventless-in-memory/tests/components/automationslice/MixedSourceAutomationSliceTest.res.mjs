// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as MixedSourceAutomationSliceFixtures$ReventlessInMemory from "./MixedSourceAutomationSliceFixtures.res.mjs";

describe("MixedSource AutomationSlice", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(MixedSourceAutomationSliceFixtures$ReventlessInMemory.slice));
    await TestRunner$ReventlessInMemory.resolve(MixedSourceAutomationSliceFixtures$ReventlessInMemory.aggregateResource.name);
    await TestRunner$ReventlessInMemory.resolve(MixedSourceAutomationSliceFixtures$ReventlessInMemory.dcbResource.name);
    await Promise.resolve();
    await Promise.resolve();
  });
  beforeEach(() => {
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
  });
  test("Aggregate-source event creates a TODO and produces a command", async () => {
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishAggregateEvent("env-a1", {
      TAG: "OrderShipped",
      orderId: "o1",
      productId: "p1"
    });
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(MixedSourceAutomationSliceFixtures$ReventlessInMemory.slice));
    await ops.processPending();
    expect(MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.length).toBe(1);
    let cmd = MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents[0];
    expect(cmd.id).toBe("o1:p1");
  });
  test("DCB-source StockReserved creates a TODO and produces a command", async () => {
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishDcbEvent("env-d1", {
      TAG: "StockReserved",
      orderId: "o2",
      productId: "p2"
    });
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(MixedSourceAutomationSliceFixtures$ReventlessInMemory.slice));
    await ops.processPending();
    expect(MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.length).toBe(1);
    let cmd = MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents[0];
    expect(cmd.id).toBe("o2:p2");
  });
  test("DCB-source StockReleased after a Processing item is idempotent", async () => {
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishDcbEvent("env-d2", {
      TAG: "StockReserved",
      orderId: "o3",
      productId: "p3"
    });
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishDcbEvent("env-d3", {
      TAG: "StockReleased",
      orderId: "o3",
      productId: "p3"
    });
    expect(MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.length).toBe(1);
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(MixedSourceAutomationSliceFixtures$ReventlessInMemory.slice));
    await ops.processPending();
    expect(MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.length).toBe(0);
  });
  test("both sources can drive items into the same TODO list independently", async () => {
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishAggregateEvent("env-a2", {
      TAG: "OrderShipped",
      orderId: "o4",
      productId: "p4"
    });
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishDcbEvent("env-d4", {
      TAG: "StockReserved",
      orderId: "o5",
      productId: "p5"
    });
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(MixedSourceAutomationSliceFixtures$ReventlessInMemory.slice));
    await ops.processPending();
    expect(MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.length).toBe(2);
    let ids = MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.map(c => c.id);
    expect(ids.includes("o4:p4")).toBe(true);
    expect(ids.includes("o5:p5")).toBe(true);
  });
  test("first-writer-wins idempotency across mappings sharing an ID", async () => {
    MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents = [];
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishAggregateEvent("env-a3", {
      TAG: "OrderShipped",
      orderId: "o6",
      productId: "p6"
    });
    await MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishDcbEvent("env-d5", {
      TAG: "StockReserved",
      orderId: "o6",
      productId: "p6"
    });
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(MixedSourceAutomationSliceFixtures$ReventlessInMemory.slice));
    await ops.processPending();
    expect(MixedSourceAutomationSliceFixtures$ReventlessInMemory.publishedCommands.contents.length).toBe(1);
  });
});

/*  Not a pure module */
