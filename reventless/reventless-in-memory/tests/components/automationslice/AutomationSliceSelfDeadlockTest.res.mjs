// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as AutomationSliceSelfDeadlockFixtures$ReventlessInMemory from "./AutomationSliceSelfDeadlockFixtures.res.mjs";

describe("AutomationSlice self-deadlock regression:", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.DcbLogMaker.operations(AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.dcbEventLog));
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.autoShipSlice));
    await Promise.resolve();
    await Promise.resolve();
    let resource = AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.dcbTopicOutputs.resources[0];
    await TestRunner$ReventlessInMemory.resolve(resource.name);
  });
  test("Place → Placed → AutoShip → Ship → Shipped completes", async () => {
    let outcome = await AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.withTimeout(AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.publishJsons([AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.placeCmdJson("order-1")]), 1500);
    let tmp;
    tmp = outcome.TAG === "Ok" ? "ok" : outcome._0;
    expect(tmp).toBe("ok");
    await Promise.resolve();
    await Promise.resolve();
    await Promise.resolve();
    await Promise.resolve();
    let eventTypes = await AutomationSliceSelfDeadlockFixtures$ReventlessInMemory.readEventTypes("order-1");
    expect(eventTypes.includes("Placed")).toBe(true);
    expect(eventTypes.includes("Shipped")).toBe(true);
  });
});

/*  Not a pure module */
