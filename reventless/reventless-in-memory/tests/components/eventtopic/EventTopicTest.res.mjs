// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as EventTopicFixtures$ReventlessInMemory from "./EventTopicFixtures.res.mjs";

describe("EventTopic (in-memory)", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicFixtures$ReventlessInMemory.eventTopic));
  });
  beforeEach(() => EventTopicFixtures$ReventlessInMemory.reset());
  test("publish sends event to bus subscribers", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicFixtures$ReventlessInMemory.eventTopic));
    let event$p = EventTopicFixtures$ReventlessInMemory.makeEvent$p("item-1", {
      TAG: "ItemPublished",
      name: "Widget"
    });
    await ops.publish([event$p]);
    expect(EventTopicFixtures$ReventlessInMemory.capturedEventCount.contents).toBe(1);
  });
  test("publish multiple events notifies subscribers for each", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicFixtures$ReventlessInMemory.eventTopic));
    let events = [
      EventTopicFixtures$ReventlessInMemory.makeEvent$p("item-1", {
        TAG: "ItemPublished",
        name: "Widget"
      }),
      EventTopicFixtures$ReventlessInMemory.makeEvent$p("item-2", {
        TAG: "ItemRemoved",
        id: "item-2"
      })
    ];
    await ops.publish(events);
    expect(EventTopicFixtures$ReventlessInMemory.capturedEventCount.contents).toBe(2);
  });
  test("publish empty array does not notify subscribers", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicFixtures$ReventlessInMemory.eventTopic));
    await ops.publish([]);
    expect(EventTopicFixtures$ReventlessInMemory.capturedEventCount.contents).toBe(0);
  });
});

/*  Not a pure module */
