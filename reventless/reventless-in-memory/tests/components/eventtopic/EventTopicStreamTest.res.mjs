// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as TestFixtures$ReventlessInMemory from "../../TestFixtures.res.mjs";
import * as EventTopicStreamFixtures$ReventlessInMemory from "./EventTopicStreamFixtures.res.mjs";

describe("EventTopic.publishJsonStream", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicStreamFixtures$ReventlessInMemory.evtTopic));
  });
  beforeEach(() => {
    EventTopicStreamFixtures$ReventlessInMemory.received.contents = 0;
  });
  test("streams 2 events to all subscribers", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicStreamFixtures$ReventlessInMemory.evtTopic));
    let stream = Effect.Stream.fromIterable([
      {
        service: "svc",
        meta: TestFixtures$ReventlessInMemory.testMeta,
        json: JSON.parse("{\"ev\":1}")
      },
      {
        service: "svc",
        meta: TestFixtures$ReventlessInMemory.testMeta,
        json: JSON.parse("{\"ev\":2}")
      }
    ]);
    await Effect.Effect.runPromise(ops.publishJsonStream(stream));
    await Promise.resolve();
    await Promise.resolve();
    expect(EventTopicStreamFixtures$ReventlessInMemory.received.contents).toBe(2);
  });
  test("empty stream publishes nothing to subscribers", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventTopicStreamFixtures$ReventlessInMemory.evtTopic));
    await Effect.Effect.runPromise(ops.publishJsonStream(Effect.Stream.empty));
    await Promise.resolve();
    expect(EventTopicStreamFixtures$ReventlessInMemory.received.contents).toBe(0);
  });
});

/*  Not a pure module */
