// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as SideEffectHandlerFixtures$ReventlessInMemory from "./SideEffectHandlerFixtures.res.mjs";

beforeAll(async () => {
  await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(SideEffectHandlerFixtures$ReventlessInMemory.seh));
  await TestRunner$ReventlessInMemory.resolve(SideEffectHandlerFixtures$ReventlessInMemory.topicResource.name);
});

describe("SideEffectHandler (in-memory)", () => {
  beforeEach(() => SideEffectHandlerFixtures$ReventlessInMemory.resetMocks());
  test("OrderPlaced event triggers execute with correct id and orderId", async () => {
    await SideEffectHandlerFixtures$ReventlessInMemory.publishOrderPlaced("order-1", "ord-abc");
    expect(SideEffectHandlerFixtures$ReventlessInMemory.capturedOrders.contents.length).toBe(1);
    let entry = SideEffectHandlerFixtures$ReventlessInMemory.capturedOrders.contents[0];
    expect(entry[0]).toBe("order-1");
    expect(entry[1]).toBe("ord-abc");
  });
  test("multiple events trigger execute for each", async () => {
    await SideEffectHandlerFixtures$ReventlessInMemory.publishOrderPlaced("order-2", "ord-def");
    await SideEffectHandlerFixtures$ReventlessInMemory.publishOrderPlaced("order-3", "ord-ghi");
    expect(SideEffectHandlerFixtures$ReventlessInMemory.capturedOrders.contents.length).toBe(2);
  });
});

/*  Not a pure module */
