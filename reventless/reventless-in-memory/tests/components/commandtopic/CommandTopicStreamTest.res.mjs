// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect/Effect";
import * as Stream from "effect/Stream";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as CommandTopicStreamFixtures$ReventlessInMemory from "./CommandTopicStreamFixtures.res.mjs";

describe("CommandTopic.publishJsonsStream", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicStreamFixtures$ReventlessInMemory.cmdTopic));
  });
  beforeEach(() => {
    CommandTopicStreamFixtures$ReventlessInMemory.captured.contents = [];
  });
  test("streams 3 commands to the bus in order", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicStreamFixtures$ReventlessInMemory.cmdTopic));
    let stream = Stream.fromIterable([
      CommandTopicStreamFixtures$ReventlessInMemory.makeJson("cmd-1"),
      CommandTopicStreamFixtures$ReventlessInMemory.makeJson("cmd-2"),
      CommandTopicStreamFixtures$ReventlessInMemory.makeJson("cmd-3")
    ]);
    await Effect.runPromise(ops.publishJsonsStream(stream));
    await Promise.resolve();
    expect(CommandTopicStreamFixtures$ReventlessInMemory.captured.contents.length).toBe(3);
  });
  test("empty stream dispatches nothing", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicStreamFixtures$ReventlessInMemory.cmdTopic));
    await Effect.runPromise(ops.publishJsonsStream(Stream.empty));
    await Promise.resolve();
    expect(CommandTopicStreamFixtures$ReventlessInMemory.captured.contents.length).toBe(0);
  });
});

/*  Not a pure module */
