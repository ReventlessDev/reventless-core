// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Message$ReventlessCore from "@reventlessdev/reventless-core/src/Message.res.mjs";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as CommandTopicFixtures$ReventlessInMemory from "./CommandTopicFixtures.res.mjs";

describe("CommandTopic (in-memory)", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicFixtures$ReventlessInMemory.cmdTopic));
  });
  test("publishJsons succeeds without throwing", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicFixtures$ReventlessInMemory.cmdTopic));
    let commandJson = Message$ReventlessCore.encode({
      TAG: "CreateItem",
      name: "Widget"
    }, CommandTopicFixtures$ReventlessInMemory.ItemSpec.commandSchema);
    let didThrow = false;
    try {
      await ops.publishJsons([{
          id: "item-1",
          meta: CommandTopicFixtures$ReventlessInMemory.testMeta,
          commandJson: commandJson
        }]);
    } catch (exn) {
      didThrow = true;
    }
    expect(didThrow).toBe(false);
  });
  test("publishJsons with empty array succeeds", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicFixtures$ReventlessInMemory.cmdTopic));
    let didThrow = false;
    try {
      await ops.publishJsons([]);
    } catch (exn) {
      didThrow = true;
    }
    expect(didThrow).toBe(false);
  });
  test("publishJsons dispatches to bus channel (TestCommandTopicCmdTopic)", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(CommandTopicFixtures$ReventlessInMemory.cmdTopic));
    let received = {
      contents: false
    };
    CommandTopicFixtures$ReventlessInMemory.Bus.registerCommandHandler("TestCommandTopicCmdTopic", async (_json, _ctx) => {
      received.contents = true;
    });
    let commandJson = Message$ReventlessCore.encode({
      TAG: "DeleteItem",
      id: "item-99"
    }, CommandTopicFixtures$ReventlessInMemory.ItemSpec.commandSchema);
    await ops.publishJsons([{
        id: "item-99",
        meta: CommandTopicFixtures$ReventlessInMemory.testMeta,
        commandJson: commandJson
      }]);
    expect(received.contents).toBe(true);
  });
});

/*  Not a pure module */
