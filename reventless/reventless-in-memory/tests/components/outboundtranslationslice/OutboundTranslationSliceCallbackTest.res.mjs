// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as OutboundTranslationSlice_Callback$ReventlessCore from "@reventlessdev/reventless-core/src/components/OutboundTranslationSlice/OutboundTranslationSlice_Callback.res.mjs";
import * as OutboundTranslationSliceFixtures$ReventlessInMemory from "./OutboundTranslationSliceFixtures.res.mjs";

let FireForgetCallback = OutboundTranslationSlice_Callback$ReventlessCore.Make({
  name: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.name,
  moduleUrl: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.moduleUrl,
  DcbEventLogSpec: OutboundTranslationSliceFixtures$ReventlessInMemory.OrderEventLog,
  outboundItemSchema: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.outboundItemSchema,
  inboundCommandSchema: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.inboundCommandSchema,
  collect: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.collect,
  translate: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.translate,
  maxRetries: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.maxRetries,
  heartbeatInterval: OutboundTranslationSliceFixtures$ReventlessInMemory.SendTrackingEmailSpec.heartbeatInterval
});

let CommandBackCallback = OutboundTranslationSlice_Callback$ReventlessCore.Make({
  name: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.name,
  moduleUrl: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.moduleUrl,
  DcbEventLogSpec: OutboundTranslationSliceFixtures$ReventlessInMemory.OrderEventLog,
  outboundItemSchema: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.outboundItemSchema,
  inboundCommandSchema: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.inboundCommandSchema,
  collect: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.collect,
  translate: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translate,
  maxRetries: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.maxRetries,
  heartbeatInterval: OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.heartbeatInterval
});

describe("OutboundTranslationSlice Callback", () => {
  beforeEach(() => {
    FireForgetCallback.todoItems.contents = {};
    CommandBackCallback.todoItems.contents = {};
    OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translateFn.contents = async (id, _item) => ({
      TAG: "Ok",
      _0: [
        id,
        {
          TAG: "ConfirmPayment",
          orderId: id
        }
      ]
    });
  });
  describe("Phase 1 — collect", () => {
    test("OrderShipped event creates a pending outbound item", async () => {
      FireForgetCallback.phase1([{
          TAG: "OrderShipped",
          orderId: "ord-1",
          email: "test@example.com"
        }]);
      let items = FireForgetCallback.todoItems.contents;
      expect(Object.entries(items).length).toBe(1);
      let row = items["ord-1"];
      expect(Stdlib_Option.isSome(row)).toBe(true);
      let r = Stdlib_Option.getOrThrow(row, undefined);
      expect(r.status).toBe("Pending");
      expect(r.retryCount).toBe(0);
    });
    test("duplicate event does not create a second item (idempotent)", async () => {
      FireForgetCallback.phase1([{
          TAG: "OrderShipped",
          orderId: "ord-1",
          email: "test@example.com"
        }]);
      FireForgetCallback.phase1([{
          TAG: "OrderShipped",
          orderId: "ord-1",
          email: "other@example.com"
        }]);
      let items = FireForgetCallback.todoItems.contents;
      expect(Object.entries(items).length).toBe(1);
    });
    test("multiple different events create multiple items", async () => {
      FireForgetCallback.phase1([
        {
          TAG: "OrderShipped",
          orderId: "ord-1",
          email: "a@example.com"
        },
        {
          TAG: "OrderShipped",
          orderId: "ord-2",
          email: "b@example.com"
        }
      ]);
      let items = FireForgetCallback.todoItems.contents;
      expect(Object.entries(items).length).toBe(2);
    });
    test("irrelevant event does not create an item", async () => {
      FireForgetCallback.phase1([{
          TAG: "PaymentReceived",
          orderId: "ord-1",
          amount: 100.0
        }]);
      let items = FireForgetCallback.todoItems.contents;
      expect(Object.entries(items).length).toBe(0);
    });
  });
  describe("Phase 2 — translate (fire-and-forget)", () => {
    test("Ok(None) marks item as Completed, no command published", async () => {
      FireForgetCallback.phase1([{
          TAG: "OrderShipped",
          orderId: "ord-1",
          email: "test@example.com"
        }]);
      let publishedCommands = {
        contents: []
      };
      let mockPublish = async cmds => {
        publishedCommands.contents = cmds;
      };
      await FireForgetCallback.phase2(mockPublish);
      expect(publishedCommands.contents.length).toBe(0);
      let row = Stdlib_Option.getOrThrow(FireForgetCallback.todoItems.contents["ord-1"], undefined);
      expect(row.status).toBe("Completed");
    });
    test("empty TODO list produces no commands", async () => {
      let publishedCommands = {
        contents: []
      };
      let mockPublish = async cmds => {
        publishedCommands.contents = cmds;
      };
      await FireForgetCallback.phase2(mockPublish);
      expect(publishedCommands.contents.length).toBe(0);
    });
  });
  describe("Phase 2 — translate (command-back)", () => {
    test("Ok(Some(...)) marks item as Completed and publishes command", async () => {
      CommandBackCallback.phase1([{
          TAG: "PaymentReceived",
          orderId: "ord-1",
          amount: 50.0
        }]);
      let publishedCommands = {
        contents: []
      };
      let mockPublish = async cmds => {
        publishedCommands.contents = publishedCommands.contents.concat(cmds);
      };
      await CommandBackCallback.phase2(mockPublish);
      expect(publishedCommands.contents.length).toBe(1);
      let cmd = publishedCommands.contents[0];
      expect(cmd.id).toBe("ord-1");
      let row = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row.status).toBe("Completed");
    });
    test("Error marks item as Failed with incremented retryCount", async () => {
      OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translateFn.contents = async (_id, _item) => ({
        TAG: "Error",
        _0: "gateway timeout"
      });
      CommandBackCallback.phase1([{
          TAG: "PaymentReceived",
          orderId: "ord-1",
          amount: 50.0
        }]);
      let mockPublish = async _cmds => {};
      await CommandBackCallback.phase2(mockPublish);
      let row = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row.status).toBe("Failed");
      expect(row.retryCount).toBe(1);
    });
    test("failed items with retryCount < maxRetries are retried", async () => {
      OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translateFn.contents = async (_id, _item) => ({
        TAG: "Error",
        _0: "timeout"
      });
      CommandBackCallback.phase1([{
          TAG: "PaymentReceived",
          orderId: "ord-1",
          amount: 50.0
        }]);
      let mockPublish = async _cmds => {};
      await CommandBackCallback.phase2(mockPublish);
      let row1 = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row1.retryCount).toBe(1);
      OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translateFn.contents = async (id, _item) => ({
        TAG: "Ok",
        _0: [
          id,
          {
            TAG: "ConfirmPayment",
            orderId: id
          }
        ]
      });
      let publishedCommands = {
        contents: []
      };
      let successPublish = async cmds => {
        publishedCommands.contents = cmds;
      };
      await CommandBackCallback.phase2(successPublish);
      expect(publishedCommands.contents.length).toBe(1);
      let row2 = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row2.status).toBe("Completed");
    });
    test("failed items beyond maxRetries are not retried", async () => {
      OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translateFn.contents = async (_id, _item) => ({
        TAG: "Error",
        _0: "timeout"
      });
      CommandBackCallback.phase1([{
          TAG: "PaymentReceived",
          orderId: "ord-1",
          amount: 50.0
        }]);
      let mockPublish = async _cmds => {};
      await CommandBackCallback.phase2(mockPublish);
      await CommandBackCallback.phase2(mockPublish);
      let row = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row.retryCount).toBe(2);
      let publishedCommands = {
        contents: []
      };
      let trackPublish = async cmds => {
        publishedCommands.contents = cmds;
      };
      await CommandBackCallback.phase2(trackPublish);
      expect(publishedCommands.contents.length).toBe(0);
      let row2 = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row2.retryCount).toBe(2);
    });
    test("individual item failure does not affect other items", async () => {
      let callCount = {
        contents: 0
      };
      OutboundTranslationSliceFixtures$ReventlessInMemory.ProcessPaymentSpec.translateFn.contents = async (id, _item) => {
        callCount.contents = callCount.contents + 1 | 0;
        if (callCount.contents === 1) {
          return {
            TAG: "Error",
            _0: "first item fails"
          };
        } else {
          return {
            TAG: "Ok",
            _0: [
              id,
              {
                TAG: "ConfirmPayment",
                orderId: id
              }
            ]
          };
        }
      };
      CommandBackCallback.phase1([
        {
          TAG: "PaymentReceived",
          orderId: "ord-1",
          amount: 50.0
        },
        {
          TAG: "PaymentReceived",
          orderId: "ord-2",
          amount: 75.0
        }
      ]);
      let publishedCommands = {
        contents: []
      };
      let mockPublish = async cmds => {
        publishedCommands.contents = publishedCommands.contents.concat(cmds);
      };
      await CommandBackCallback.phase2(mockPublish);
      let row1 = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-1"], undefined);
      expect(row1.status).toBe("Failed");
      let row2 = Stdlib_Option.getOrThrow(CommandBackCallback.todoItems.contents["ord-2"], undefined);
      expect(row2.status).toBe("Completed");
      expect(publishedCommands.contents.length).toBe(1);
    });
  });
});

export {
  FireForgetCallback,
  CommandBackCallback,
}
/* FireForgetCallback Not a pure module */
