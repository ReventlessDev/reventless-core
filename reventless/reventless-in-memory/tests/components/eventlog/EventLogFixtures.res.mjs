// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as EventLog_Builder$ReventlessCore from "@reventlessdev/reventless-core/src/components/EventLog/EventLog_Builder.res.mjs";
import * as InMemory_Bus$ReventlessInMemory from "../../../src/adapter/InMemory_Bus.res.mjs";
import * as TestFixtures$ReventlessInMemory from "../../TestFixtures.res.mjs";
import * as EventLogStorage_InMemory$ReventlessInMemory from "../../../src/adapter/EventLog/EventLogStorage_InMemory.res.mjs";
import * as EventTopicPublisher_InMemory$ReventlessInMemory from "../../../src/adapter/EventTopic/EventTopicPublisher_InMemory.res.mjs";

let name = "TestItemEventLog";

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "ItemCreated",
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemDeleted",
    id: s.m(S.string)
  }))
]);

let ItemEventLogSpec = {
  Id: undefined,
  name: name,
  eventSchema: eventSchema
};

let Bus = InMemory_Bus$ReventlessInMemory.Make({});

let capturedTopicEventCount = {
  contents: 0
};

Bus.subscribeToEvents("TestItemEventLogEventTopic", async (param, param$1, param$2) => {
  capturedTopicEventCount.contents = capturedTopicEventCount.contents + 1 | 0;
});

TestRunner$ReventlessInMemory.setup();

let EventLogMaker = EventLog_Builder$ReventlessCore.Make({
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  name: name,
  eventSchema: eventSchema
})({
  make: EventLogStorage_InMemory$ReventlessInMemory.make
})(EventTopicPublisher_InMemory$ReventlessInMemory.Make(Bus));

let eventLog = EventLogMaker.make("TestItemEventLog", undefined);

function makeEvent$p(id, event) {
  return {
    id: id,
    meta: TestFixtures$ReventlessInMemory.testMeta,
    event: event
  };
}

function reset() {
  capturedTopicEventCount.contents = 0;
}

export {
  ItemEventLogSpec,
  Bus,
  capturedTopicEventCount,
  EventLogMaker,
  eventLog,
  makeEvent$p,
  reset,
}
/* eventSchema Not a pure module */
