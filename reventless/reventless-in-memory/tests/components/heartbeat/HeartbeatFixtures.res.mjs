// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Globals from "@jest/globals";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as Heartbeat_Builder$ReventlessCore from "@reventlessdev/reventless-core/src/components/Heartbeat/Heartbeat_Builder.res.mjs";
import * as HeartbeatRunner_InMemory$ReventlessInMemory from "../../../src/adapter/Heartbeat/HeartbeatRunner_InMemory.res.mjs";

TestRunner$ReventlessInMemory.setup();

let HeartbeatMaker = Heartbeat_Builder$ReventlessCore.Make({
  make: HeartbeatRunner_InMemory$ReventlessInMemory.make
});

beforeAll(() => {
  Globals.jest.useFakeTimers();
});

afterAll(() => {
  HeartbeatRunner_InMemory$ReventlessInMemory.reset();
  Globals.jest.useRealTimers();
});

let capturedCount = {
  contents: 0
};

async function mockPublish(cmds) {
  capturedCount.contents = capturedCount.contents + cmds.length | 0;
}

let resolvedHandler = {
  contents: undefined
};

beforeAll(async () => {
  let h = await TestRunner$ReventlessInMemory.resolve(HeartbeatMaker.makeHandler("hb-id-1", 1, mockPublish));
  resolvedHandler.contents = h;
});

export {
  HeartbeatMaker,
  capturedCount,
  mockPublish,
  resolvedHandler,
}
/*  Not a pure module */
