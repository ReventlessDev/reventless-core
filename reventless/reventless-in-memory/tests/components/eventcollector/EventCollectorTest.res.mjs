// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as TestFixtures$ReventlessInMemory from "../../TestFixtures.res.mjs";
import * as EventCollectorFixtures$ReventlessInMemory from "./EventCollectorFixtures.res.mjs";

describe("EventCollector (in-memory)", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventCollectorFixtures$ReventlessInMemory.eventCollector));
    await TestRunner$ReventlessInMemory.resolve(EventCollectorFixtures$ReventlessInMemory.topicResource.name);
  });
  beforeEach(() => {
    EventCollectorFixtures$ReventlessInMemory.capturedEvents.contents = [];
  });
  test("EventCollector component exists", async () => {
    let ops = await TestRunner$ReventlessInMemory.resolve(Component$ReventlessCore.operations(EventCollectorFixtures$ReventlessInMemory.eventCollector));
    expect(typeof ops.enqueueEvent).toBe("function");
  });
  test("bus publishes events to topic subscribers", async () => {
    let received = {
      contents: 0
    };
    EventCollectorFixtures$ReventlessInMemory.Bus.subscribeToEvents(EventCollectorFixtures$ReventlessInMemory.topicName, async (param, param$1, param$2) => {
      received.contents = received.contents + 1 | 0;
    });
    let testEvent = Object.fromEntries([[
        "type",
        "TestEvent"
      ]]);
    await EventCollectorFixtures$ReventlessInMemory.Bus.publishEvent(EventCollectorFixtures$ReventlessInMemory.topicName, "test", TestFixtures$ReventlessInMemory.testMeta, testEvent);
    expect(received.contents).toBe(1);
  });
});

/*  Not a pure module */
