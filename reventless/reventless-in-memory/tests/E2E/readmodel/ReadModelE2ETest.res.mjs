// Generated by ReScript, PLEASE EDIT WITH CARE

import * as TestRunner$ReventlessInMemory from "../../../src/test/TestRunner.res.mjs";
import * as ReadModelE2EFixtures$ReventlessInMemory from "./ReadModelE2EFixtures.res.mjs";

describe("ReadModel E2E", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(ReadModelE2EFixtures$ReventlessInMemory.ItemReadModel.operations(ReadModelE2EFixtures$ReventlessInMemory.rm));
    await TestRunner$ReventlessInMemory.resolve(ReadModelE2EFixtures$ReventlessInMemory.topicResource.name);
  });
  test("ItemCreated event projects into ReadModel state", async () => {
    await ReadModelE2EFixtures$ReventlessInMemory.publishItemCreated("item-1", "Widget");
    let states = await ReadModelE2EFixtures$ReventlessInMemory.loadState("item-1");
    expect(states.length).toBe(1);
    let state = states[0];
    expect(state.name).toBe("Widget");
  });
  test("query for unknown ID returns empty", async () => {
    let states = await ReadModelE2EFixtures$ReventlessInMemory.loadState("unknown-id");
    expect(states.length).toBe(0);
  });
  test("multiple events project independently", async () => {
    await ReadModelE2EFixtures$ReventlessInMemory.publishItemCreated("item-2", "Gadget");
    await ReadModelE2EFixtures$ReventlessInMemory.publishItemCreated("item-3", "Doohickey");
    let states2 = await ReadModelE2EFixtures$ReventlessInMemory.loadState("item-2");
    let states3 = await ReadModelE2EFixtures$ReventlessInMemory.loadState("item-3");
    expect(states2.length).toBe(1);
    expect(states3.length).toBe(1);
    let state2 = states2[0];
    let state3 = states3[0];
    expect(state2.name).toBe("Gadget");
    expect(state3.name).toBe("Doohickey");
  });
});

/*  Not a pure module */
