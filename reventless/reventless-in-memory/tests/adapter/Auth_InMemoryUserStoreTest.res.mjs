// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Nodefs from "node:fs";
import * as Nodeos from "node:os";
import * as Nodepath from "node:path";
import * as Stdlib_JsError from "@rescript/runtime/lib/es6/Stdlib_JsError.js";
import * as UserStore$ReventlessInMemory from "../../src/adapter/Auth/UserStore.res.mjs";
import * as Auth_InMemory$ReventlessInMemory from "../../src/adapter/Auth/Auth_InMemory.res.mjs";

function resetAll() {
  Auth_InMemory$ReventlessInMemory.resetUsers();
  Auth_InMemory$ReventlessInMemory.Login.resetStore();
  UserStore$ReventlessInMemory.resetResolution();
}

function _writeYamlFile(contents) {
  let dir = Nodefs.mkdtempSync(Nodepath.join(Nodeos.tmpdir(), "reventless-userstore-"));
  let path = Nodepath.join(dir, "users.yaml");
  Nodefs.writeFileSync(path, contents, "utf8");
  return path;
}

test("parseString decodes a well-formed YAML document", async () => {
  let entries = UserStore$ReventlessInMemory.parseString(`
- username: alice
  password: alice-pw
  groups: [Editor]
- username: bob
  password: bob-pw
  groups: []
`);
  if (entries.TAG !== "Ok") {
    return Stdlib_JsError.throwWithMessage("parseString failed: " + entries._0);
  }
  let entries$1 = entries._0;
  expect(entries$1.length).toEqual(2);
  let first = entries$1[0];
  expect(first.username).toEqual("alice");
  expect(first.password).toEqual("alice-pw");
  expect(first.groups).toEqual(["Editor"]);
});

test("parseString rejects malformed YAML", async () => {
  let match = UserStore$ReventlessInMemory.parseString("not: valid: yaml: at all: ::");
  if (match.TAG === "Ok") {
    return Stdlib_JsError.throwWithMessage("expected Error for malformed YAML");
  }
});

test("parseString rejects entries missing required fields", async () => {
  let match = UserStore$ReventlessInMemory.parseString(`
- username: alice
  groups: [Editor]
`);
  if (match.TAG === "Ok") {
    return Stdlib_JsError.throwWithMessage("expected Error for missing password");
  }
});

test("load(~users) registers credentials and returns InlineUsers", async () => {
  resetAll();
  let entries = [
    {
      username: "alice",
      password: "alice-pw",
      groups: ["Editor"]
    },
    {
      username: "bob",
      password: "bob-pw",
      groups: [],
      userId: "u-bob"
    }
  ];
  let msg = UserStore$ReventlessInMemory.load(entries, undefined, undefined);
  if (msg.TAG === "Ok") {
    let tmp = msg._0;
    if (typeof tmp === "object" || tmp !== "InlineUsers") {
      Stdlib_JsError.throwWithMessage("expected InlineUsers");
    }
  } else {
    Stdlib_JsError.throwWithMessage("load failed: " + msg._0);
  }
  let result = await Auth_InMemory$ReventlessInMemory.Login.issue("alice", "alice-pw");
  if (result.TAG !== "Ok") {
    Stdlib_JsError.throwWithMessage("issue rejected loaded credentials: " + result._0);
  }
  let identity = Auth_InMemory$ReventlessInMemory.lookupUser("bob");
  if (identity !== undefined) {
    expect(identity.userId).toEqual("u-bob");
    return;
  } else {
    return Stdlib_JsError.throwWithMessage("bob not in user registry");
  }
});

test("load(~usersFile) reads a YAML file from disk", async () => {
  resetAll();
  let path = _writeYamlFile(`
- username: carol
  password: carol-pw
  groups: [Admin]
`);
  let msg = UserStore$ReventlessInMemory.load(undefined, path, undefined);
  if (msg.TAG === "Ok") {
    let p = msg._0;
    if (typeof p !== "object" || p.TAG !== "UsersFile") {
      Stdlib_JsError.throwWithMessage("expected UsersFile resolution");
    } else {
      expect(p._0).toEqual(path);
    }
  } else {
    Stdlib_JsError.throwWithMessage("load failed: " + msg._0);
  }
  let result = await Auth_InMemory$ReventlessInMemory.Login.issue("carol", "carol-pw");
  if (result.TAG !== "Ok") {
    Stdlib_JsError.throwWithMessage("issue rejected file-loaded credentials: " + result._0);
  }
  Nodefs.rmSync(path, {
    force: true
  });
});

test("load(~usersFile) returns Error when the file is missing", async () => {
  resetAll();
  let match = UserStore$ReventlessInMemory.load(undefined, "/tmp/does-not-exist-reventless.yaml", undefined);
  if (match.TAG === "Ok") {
    return Stdlib_JsError.throwWithMessage("expected Error for missing file");
  }
});

test("load() with no args + no default file returns Empty", async () => {
  resetAll();
  let msg = UserStore$ReventlessInMemory.load(undefined, undefined, undefined);
  if (msg.TAG !== "Ok") {
    return Stdlib_JsError.throwWithMessage("load failed: " + msg._0);
  }
  let tmp = msg._0;
  if (typeof tmp !== "object") {
    if (tmp === "InlineUsers") {
      return Stdlib_JsError.throwWithMessage("expected Empty or DefaultFile");
    } else {
      return;
    }
  } else if (tmp.TAG === "UsersFile") {
    return Stdlib_JsError.throwWithMessage("expected Empty or DefaultFile");
  } else {
    return;
  }
});

test("inline users win over a file path", async () => {
  resetAll();
  let path = _writeYamlFile(`
- username: file-user
  password: file-pw
  groups: []
`);
  let inline = [{
      username: "inline-user",
      password: "inline-pw",
      groups: []
    }];
  let msg = UserStore$ReventlessInMemory.load(inline, path, undefined);
  if (msg.TAG === "Ok") {
    let tmp = msg._0;
    if (typeof tmp === "object" || tmp !== "InlineUsers") {
      Stdlib_JsError.throwWithMessage("expected InlineUsers (inline should win)");
    }
  } else {
    Stdlib_JsError.throwWithMessage("load failed: " + msg._0);
  }
  let match = Auth_InMemory$ReventlessInMemory.lookupUser("file-user");
  if (match !== undefined) {
    Stdlib_JsError.throwWithMessage("file-user leaked into registry despite inline override");
  }
  let match$1 = Auth_InMemory$ReventlessInMemory.lookupUser("inline-user");
  if (match$1 !== undefined) {
    
  } else {
    Stdlib_JsError.throwWithMessage("inline-user missing from registry");
  }
  Nodefs.rmSync(path, {
    force: true
  });
});

test("autoLoadOnce is a no-op after an explicit load", async () => {
  resetAll();
  UserStore$ReventlessInMemory.load([{
      username: "x",
      password: "x",
      groups: []
    }], undefined, undefined);
  UserStore$ReventlessInMemory.autoLoadOnce();
  let match = Auth_InMemory$ReventlessInMemory.lookupUser("x");
  if (match !== undefined) {
    return;
  } else {
    return Stdlib_JsError.throwWithMessage("autoLoadOnce wiped explicit registration");
  }
});

export {
  resetAll,
  _writeYamlFile,
}
/*  Not a pure module */
