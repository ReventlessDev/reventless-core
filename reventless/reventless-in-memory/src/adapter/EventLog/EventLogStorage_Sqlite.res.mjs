// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect/Effect";
import * as Stream from "effect/Stream";
import * as Pulumi from "@pulumi/pulumi";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";
import * as SqliteDriver$ReventlessInMemory from "../SqliteDriver.res.mjs";

function ensureSchema(db) {
  SqliteDriver$ReventlessInMemory.exec(db, "CREATE TABLE IF NOT EXISTS event_log (log_name TEXT NOT NULL, aggregate_id TEXT NOT NULL, seq_nr INTEGER NOT NULL, payload TEXT NOT NULL, PRIMARY KEY (log_name, aggregate_id, seq_nr))");
}

function decodePayload(row) {
  let match = row["payload"];
  if (match === undefined) {
    return null;
  }
  if (typeof match !== "string") {
    return null;
  }
  try {
    return JSON.parse(match);
  } catch (exn) {
    return null;
  }
}

function makeStorage(db, name, param) {
  SqliteDriver$ReventlessInMemory.exec(db, "CREATE TABLE IF NOT EXISTS event_log (log_name TEXT NOT NULL, aggregate_id TEXT NOT NULL, seq_nr INTEGER NOT NULL, payload TEXT NOT NULL, PRIMARY KEY (log_name, aggregate_id, seq_nr))");
  let insertStmt = SqliteDriver$ReventlessInMemory.prepare(db, "INSERT INTO event_log(log_name, aggregate_id, seq_nr, payload) VALUES(?,?,?,?)");
  let countStmt = SqliteDriver$ReventlessInMemory.prepare(db, "SELECT COUNT(*) AS c FROM event_log WHERE log_name=? AND aggregate_id=?");
  let selectByIdStmt = SqliteDriver$ReventlessInMemory.prepare(db, "SELECT payload FROM event_log WHERE log_name=? AND aggregate_id=? ORDER BY seq_nr ASC");
  let currentCount = id => {
    let row = SqliteDriver$ReventlessInMemory.get(countStmt, [
      name,
      id
    ]);
    if (row === undefined) {
      return 0;
    }
    let match = row["c"];
    if (typeof match === "number") {
      return match | 0;
    } else {
      return 0;
    }
  };
  let append = async (seqNr, id, jsons) => {
    try {
      SqliteDriver$ReventlessInMemory.transaction(db, () => {
        let existing = currentCount(id);
        if (seqNr !== existing) {
          throw {
            RE_EXN_ID: "Failure",
            _1: "conflict",
            Error: new Error()
          };
        }
        jsons.forEach((json, i) => SqliteDriver$ReventlessInMemory.run(insertStmt, [
          name,
          id,
          seqNr + i | 0,
          JSON.stringify(json)
        ]));
      });
      return {
        TAG: "Ok",
        _0: undefined
      };
    } catch (raw_msg) {
      let msg = Primitive_exceptions.internalToException(raw_msg);
      if (msg.RE_EXN_ID === "Failure") {
        return {
          TAG: "Error",
          _0: msg._1
        };
      } else {
        return {
          TAG: "Error",
          _0: "conflict"
        };
      }
    }
  };
  let replayArray = id => SqliteDriver$ReventlessInMemory.all(selectByIdStmt, [
    name,
    id
  ]).map(decodePayload);
  let replay = replayArray;
  let replayStream = id => Stream.fromIterable(replayArray(id));
  let appendStream = (startingSeqNr, id, stream) => {
    let seqNrRef = {
      contents: startingSeqNr
    };
    return Stream.runForEach(stream, json => {
      let currentSeq = seqNrRef.contents;
      return Effect.flatMap(Effect.promise(() => append(currentSeq, id, [json])), result => {
        if (result.TAG !== "Ok") {
          return Effect.fail(result._0);
        }
        seqNrRef.contents = currentSeq + 1 | 0;
        return Effect.succeed();
      });
    });
  };
  return [
    name,
    replay,
    {
      resources: [],
      operations: Pulumi.output({
        append: append,
        replay: replay,
        replayStream: replayStream,
        appendStream: appendStream
      })
    }
  ];
}

function Make(Bus) {
  return DbProvider => {
    let make = (name, opts) => {
      let match = makeStorage(DbProvider.db, name, opts);
      Bus.registerEventLogReplay(match[0], match[1]);
      return match[2];
    };
    return {
      make: make
    };
  };
}

export {
  ensureSchema,
  decodePayload,
  makeStorage,
  Make,
}
/* effect/Effect Not a pure module */
