// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";

let activeTimers = {
  contents: {}
};

function make(name, param, timeout, runtime, param$1) {
  let handlerDeferred = runtime.parts.handlerDeferred;
  let intervalMs = (timeout * 60 | 0) * 1000 | 0;
  let handle = setInterval(() => {
    Effect.Effect.runPromise(Effect.Effect.flatMap(Effect.Deferred.await(handlerDeferred), handler => Effect.Effect.promise(() => handler(undefined, undefined))));
  }, intervalMs);
  activeTimers.contents[name] = handle;
  return {
    resources: []
  };
}

function reset() {
  Object.values(activeTimers.contents).forEach(prim => {
    clearInterval(prim);
  });
  activeTimers.contents = {};
}

export {
  activeTimers,
  make,
  reset,
}
/* effect Not a pure module */
