// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as CommandTopic$ReventlessCore from "@reventlessdev/reventless-core/src/components/CommandTopic/CommandTopic.res.mjs";
import * as ComponentType$ReventlessCore from "@reventlessdev/reventless-core/src/ComponentType.res.mjs";
import * as EventCollector$ReventlessCore from "@reventlessdev/reventless-core/src/components/EventCollector/EventCollector.res.mjs";
import * as CommandGenerator$ReventlessCore from "@reventlessdev/reventless-core/src/components/CommandGenerator/CommandGenerator.res.mjs";
import * as RuntimeEnvironment_InMemory$ReventlessInMemory from "./RuntimeEnvironment_InMemory.res.mjs";

function Make(Bus) {
  return CommandTopicChannel => (EventCollectorChannel => {
    let forCommandGenerator = (handler, connect, param, param$1, commandGenerator) => {
      let resource = Component$ReventlessCore.toPulumiResource(commandGenerator);
      return connect(RuntimeEnvironment_InMemory$ReventlessInMemory.make(ComponentType$ReventlessCore.nameOpt(resource.__name, CommandGenerator$ReventlessCore.componentType), handler.apply(h => h), undefined, undefined, undefined));
    };
    let forCommandTopic = (handler, connect, param, param$1, commandTopic) => {
      let resource = Component$ReventlessCore.toPulumiResource(commandTopic);
      return connect(RuntimeEnvironment_InMemory$ReventlessInMemory.make(ComponentType$ReventlessCore.nameOpt(resource.__name, CommandTopic$ReventlessCore.componentType), handler.apply(h => h), undefined, undefined, undefined));
    };
    let forEventCollector = (handler, eventTopics, resources, param, param$1, eventCollector) => {
      let resource = Component$ReventlessCore.toPulumiResource(eventCollector);
      let name = ComponentType$ReventlessCore.nameOpt(resource.__name, EventCollector$ReventlessCore.componentType);
      let opts_parent = resource;
      let opts = {
        parent: opts_parent
      };
      let runtime = RuntimeEnvironment_InMemory$ReventlessInMemory.make(name, handler.apply(h => h), undefined, undefined, undefined);
      EventCollectorChannel.connect(name, [{
          channel: eventCollector.channel,
          eventTopics: eventTopics,
          resources: resources
        }], runtime, opts);
    };
    let finish = () => {};
    return {
      CommandTopicChannel: CommandTopicChannel,
      EventCollectorChannel: EventCollectorChannel,
      forCommandGenerator: forCommandGenerator,
      forCommandTopic: forCommandTopic,
      forEventCollector: forEventCollector,
      finish: finish
    };
  });
}

export {
  Make,
}
/* Component-ReventlessCore Not a pure module */
