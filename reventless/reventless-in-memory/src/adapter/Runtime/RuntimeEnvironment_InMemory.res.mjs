// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function make(param, handler, $staropt$star, $staropt$star$1, param$1) {
  $staropt$star !== undefined;
  $staropt$star$1 !== undefined;
  let handlerDeferred = Effect.Effect.runSync(Effect.Deferred.make());
  let subscriptionLatch = Effect.Effect.runSync(Effect.Effect.makeLatch(false));
  handler.apply(h => {
    Effect.Effect.runPromise(Effect.Deferred.succeed(handlerDeferred, h));
  });
  return {
    parts: {
      handlerDeferred: handlerDeferred,
      subscriptionLatch: subscriptionLatch
    },
    resources: []
  };
}

function groupBySource(event) {
  let dict = {};
  dict["inmemory"] = event;
  return dict;
}

function extractCorrelationId(event) {
  return Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_JSON.Decode.object(event), obj => obj["meta"]), Stdlib_JSON.Decode.object), meta => meta["correlationId"]), Stdlib_JSON.Decode.string);
}

export {
  make,
  groupBySource,
  extractCorrelationId,
}
/* effect Not a pure module */
