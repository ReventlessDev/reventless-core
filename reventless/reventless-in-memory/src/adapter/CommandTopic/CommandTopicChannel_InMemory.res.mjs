// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Message$ReventlessSpec from "@reventlessdev/reventless-spec/src/Message.res.mjs";

function Make(Bus) {
  let encodeMessage = cmdJson => Object.fromEntries([
    [
      "id",
      cmdJson.id
    ],
    [
      "meta",
      S.reverseConvertToJsonOrThrow(cmdJson.meta, Message$ReventlessSpec.metaSchema)
    ],
    [
      "command",
      cmdJson.commandJson
    ]
  ]);
  let decodeId = body => {
    if (typeof body === "object" && body !== null && !Array.isArray(body)) {
      return Stdlib_Option.getOr(Stdlib_Option.flatMap(body["id"], j => {
        if (typeof j === "string") {
          return j;
        }
      }), "");
    } else {
      return "";
    }
  };
  let make = (name, param) => {
    let publishJsons = async jsons => {
      await Promise.all(jsons.map(async cmdJson => await Bus.dispatchCommand(name, encodeMessage(cmdJson))));
    };
    let handleChannelEvent = handleCmds => Pulumi.output((fullBody, _ctx) => {
      let reference = decodeId(fullBody);
      return handleCmds([{
          command: fullBody,
          reference: reference
        }]).then(param => {});
    });
    let connect = (param, channel, runtime, param$1, param$2) => {
      Bus.registerCommandHandler(channel.parts.name, (json, ctx) => {
        let h = runtime.parts.handlerRef.contents;
        if (h !== undefined) {
          return h(json, ctx);
        } else {
          console.log("InMemory CommandTopic: handler not registered for", channel.parts.name);
          return Promise.resolve();
        }
      });
      return [];
    };
    return {
      parts: {
        name: name
      },
      resources: [],
      publishJsons: Pulumi.output(publishJsons),
      handleChannelEvent: handleChannelEvent,
      connect: connect
    };
  };
  return {
    encodeMessage: encodeMessage,
    decodeId: decodeId,
    make: make
  };
}

export {
  Make,
}
/* S Not a pure module */
