// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let counterStore = {
  contents: {}
};

let targetRefStore = {
  contents: {}
};

function make(param, param$1, param$2, param$3, param$4, param$5, param$6, param$7, param$8, param$9) {
  return {
    addToCounterTarget: async param => {
      let counterId = param.counterId;
      let refKey = counterId + ":" + param.targetRef;
      let match = targetRefStore.contents[refKey];
      if (match !== undefined) {
        return;
      }
      targetRefStore.contents[refKey] = true;
      let current = Stdlib_Option.getOr(counterStore.contents[counterId], 0);
      counterStore.contents[counterId] = current + param.target | 0;
    }
  };
}

function getCount(counterId) {
  return Stdlib_Option.getOr(counterStore.contents[counterId], 0);
}

function reset() {
  counterStore.contents = {};
  targetRefStore.contents = {};
}

export {
  counterStore,
  targetRefStore,
  make,
  getCount,
  reset,
}
/* No side effect */
