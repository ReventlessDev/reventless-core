// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stream from "effect/Stream";
import * as Pulumi from "@pulumi/pulumi";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function Make(Bus) {
  let make = (name, $staropt$star, param, param$1, $staropt$star$1, $staropt$star$2, $staropt$star$3) => {
    $staropt$star !== undefined;
    if ($staropt$star$1 !== undefined) {
      Primitive_option.valFromOption($staropt$star$1);
    }
    if ($staropt$star$2 !== undefined) {
      Primitive_option.valFromOption($staropt$star$2);
    }
    if ($staropt$star$3 === undefined) {
      
    }
    let store = {
      contents: {}
    };
    let allItems = {
      contents: []
    };
    let failNextWrites = {
      contents: 0
    };
    let syncAll = () => {
      allItems.contents = Object.values(store.contents).flatMap(v => v);
    };
    let load = async id => ({
      TAG: "Ok",
      _0: Stdlib_Option.getOr(store.contents[id], [])
    });
    let loadStream = id => Stream.fromIterable(Stdlib_Option.getOr(store.contents[id], []));
    let save = async (id, state, _saveMode, _ttl) => {
      if (failNextWrites.contents > 0) {
        failNextWrites.contents = failNextWrites.contents - 1 | 0;
        return {
          TAG: "Error",
          _0: {
            TAG: "NotSavedToStorage",
            _0: "mock write failure"
          }
        };
      } else {
        store.contents[id] = [state];
        syncAll();
        return {
          TAG: "Ok",
          _0: undefined
        };
      }
    };
    let saveBatch = async batch => {
      if (failNextWrites.contents > 0) {
        failNextWrites.contents = failNextWrites.contents - 1 | 0;
        return {
          TAG: "Error",
          _0: {
            TAG: "BatchNotFullyWrittenToStorage",
            _0: "mock write failure"
          }
        };
      } else {
        batch.forEach(param => {
          store.contents[param[0]] = [param[1]];
        });
        syncAll();
        return {
          TAG: "Ok",
          _0: undefined
        };
      }
    };
    let count = async (_id, _fieldName, inc) => ({
      TAG: "Ok",
      _0: inc
    });
    let $$delete = async (id, _subId) => {
      Stdlib_Dict.$$delete(store.contents, id);
      syncAll();
      return {
        TAG: "Ok",
        _0: undefined
      };
    };
    let deleteBatch = async ids => {
      ids.forEach(param => Stdlib_Dict.$$delete(store.contents, param[0]));
      syncAll();
      return {
        TAG: "Ok",
        _0: undefined
      };
    };
    let ops = {
      load: load,
      loadStream: loadStream,
      save: save,
      saveBatch: saveBatch,
      count: count,
      delete: $$delete,
      deleteBatch: deleteBatch
    };
    Bus.registerQueryDb(name, ops);
    Bus.registerQueryDbScan(name, () => allItems.contents);
    let storage_resources = [];
    let storage_dataSourceName = Pulumi.output("");
    let storage_operations = Pulumi.output(ops);
    let storage = {
      resources: storage_resources,
      dataSourceName: storage_dataSourceName,
      operations: storage_operations
    };
    let reset = () => {
      store.contents = {};
      allItems.contents = [];
      failNextWrites.contents = 0;
    };
    return {
      storage: storage,
      failNextWrites: failNextWrites,
      reset: reset
    };
  };
  return {
    make: make
  };
}

export {
  Make,
}
/* effect/Stream Not a pure module */
