// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Pulumi from "@pulumi/pulumi";
import * as EventLogStorage_Postgres_Ops$ReventlessPostgres from "./EventLogStorage_Postgres_Ops.res.mjs";

function makeStorage(pool, name, opts, onAppendedOpt) {
  let onAppended = onAppendedOpt !== undefined ? onAppendedOpt : EventLogStorage_Postgres_Ops$ReventlessPostgres.noTracking;
  let match = EventLogStorage_Postgres_Ops$ReventlessPostgres.makeOps(pool, name, opts, onAppended);
  let ops = match[1];
  return [
    match[0],
    ops,
    {
      resources: [],
      operations: Pulumi.output(ops)
    }
  ];
}

let noTracking = EventLogStorage_Postgres_Ops$ReventlessPostgres.noTracking;

let decodePayload = EventLogStorage_Postgres_Ops$ReventlessPostgres.decodePayload;

let coerceInt = EventLogStorage_Postgres_Ops$ReventlessPostgres.coerceInt;

let countAll = EventLogStorage_Postgres_Ops$ReventlessPostgres.countAll;

let isUniqueViolation = EventLogStorage_Postgres_Ops$ReventlessPostgres.isUniqueViolation;

let makeOps = EventLogStorage_Postgres_Ops$ReventlessPostgres.makeOps;

export {
  noTracking,
  decodePayload,
  coerceInt,
  countAll,
  isUniqueViolation,
  makeOps,
  makeStorage,
}
/* @pulumi/pulumi Not a pure module */
