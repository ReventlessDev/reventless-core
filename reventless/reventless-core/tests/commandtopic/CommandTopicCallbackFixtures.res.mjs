// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stream from "@reventlessdev/rescript-effect/src/Stream.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as Effect from "effect/Effect";
import * as Stdlib_JsError from "@rescript/runtime/lib/es6/Stdlib_JsError.js";
import * as Message$ReventlessCore from "../../src/Message.res.mjs";
import * as CommandTopic_Callback$ReventlessCore from "../../src/components/CommandTopic/CommandTopic_Callback.res.mjs";

S.enableJson();

let name = "TestCommandTopic";

let commandSchema = S.union([
  S.schema(s => ({
    TAG: "CreateItem",
    itemId: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "DeleteItem",
    itemId: s.m(S.string)
  }))
]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

let TestSpec = {
  Id: undefined,
  name: name,
  commandSchema: commandSchema,
  commandAuthorization: commandAuthorization
};

let capturedItems = {
  contents: []
};

let commandHandlerResults = {
  contents: [{
      TAG: "Ok",
      _0: "ref-1"
    }]
};

let commandHandlerShouldThrow = {
  contents: false
};

function commandsHandler(stream) {
  return Effect.map(Stream.runCollect(stream), items => {
    if (commandHandlerShouldThrow.contents) {
      Stdlib_JsError.throwWithMessage("commandsHandler failed");
    }
    capturedItems.contents = capturedItems.contents.concat(items.map(item => ({
      reference: item.reference,
      command: item.command.command
    })));
    return commandHandlerResults.contents;
  });
}

let TestOps_Spec = {
  Id: Id$Reventless.StringPure,
  name: name,
  commandSchema: commandSchema,
  commandAuthorization: commandAuthorization
};

let TestOps = {
  Spec: TestOps_Spec,
  commandsHandler: commandsHandler
};

let $$let = TestOps_Spec;

let $$let$1 = $$let.Id;

let TestHandler = CommandTopic_Callback$ReventlessCore.Make({
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  name: name,
  commandSchema: commandSchema
})({
  Spec: {
    Id: {
      schema: $$let$1.schema,
      make: prim => prim,
      makeFromString: prim => prim,
      toString: prim => prim,
      cmp: $$let$1.cmp
    },
    name: $$let.name,
    commandSchema: $$let.commandSchema
  },
  commandsHandler: commandsHandler
});

let testMeta = {
  service: "TestService",
  time: "2024-01-01T00:00:00Z",
  ip: "127.0.0.1",
  user: "test-user",
  msgId: "cmd-msg-1",
  correlationId: "cmd-corr-1"
};

function makeCommandJson(id, command) {
  let cmd$p = {
    id: id,
    meta: testMeta,
    command: command
  };
  return Message$ReventlessCore.encodeCommand$p(cmd$p, Id$Reventless.StringPure.schema, commandSchema);
}

function makeTopicItem(id, command) {
  return {
    command: makeCommandJson(id, command),
    reference: id
  };
}

function reset() {
  capturedItems.contents = [];
  commandHandlerResults.contents = [{
      TAG: "Ok",
      _0: "ref-1"
    }];
  commandHandlerShouldThrow.contents = false;
}

export {
  TestSpec,
  capturedItems,
  commandHandlerResults,
  commandHandlerShouldThrow,
  TestOps,
  TestHandler,
  testMeta,
  makeCommandJson,
  makeTopicItem,
  reset,
}
/*  Not a pure module */
