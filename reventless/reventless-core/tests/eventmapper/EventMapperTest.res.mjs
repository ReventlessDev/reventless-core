// Generated by ReScript, PLEASE EDIT WITH CARE

import * as JestGlobals from "@reventlessdev/rescript-jest/src/JestGlobals.res.mjs";
import * as EventMapperFixtures$ReventlessCore from "./EventMapperFixtures.res.mjs";

globalThis.describe("Mapping.Make:", () => {
  globalThis.describe("sourceName", () => {
    globalThis.test("equals source spec name", () => {
      globalThis.expect(EventMapperFixtures$ReventlessCore.ItemMapping.sourceName).toBe("SourceAggregate");
    });
  });
  globalThis.describe("project", () => {
    globalThis.test("ItemCreated maps to Create action", () => {
      let event$p = EventMapperFixtures$ReventlessCore.makeSourceEvent$p("item-1", {
        TAG: "ItemCreated",
        name: "Widget",
        price: 9.99
      });
      let action = EventMapperFixtures$ReventlessCore.ItemMapping.project(event$p);
      if (typeof action !== "object") {
        return JestGlobals.fail("Expected Create action");
      }
      if (action.TAG !== "Create") {
        return JestGlobals.fail("Expected Create action");
      }
      let state = action._1;
      globalThis.expect([
        action._0,
        state.name,
        state.price
      ]).toEqual([
        "item-1",
        "Widget",
        9.99
      ]);
    });
    globalThis.test("ItemPriceUpdated maps to Update action", () => {
      let event$p = EventMapperFixtures$ReventlessCore.makeSourceEvent$p("item-1", {
        TAG: "ItemPriceUpdated",
        newPrice: 14.99
      });
      let action = EventMapperFixtures$ReventlessCore.ItemMapping.project(event$p);
      if (typeof action !== "object") {
        return JestGlobals.fail("Expected Update action");
      }
      if (action.TAG !== "Update") {
        return JestGlobals.fail("Expected Update action");
      }
      globalThis.expect(action._0).toBe("item-1");
    });
    globalThis.test("ItemRemoved maps to Delete action", () => {
      let event$p = EventMapperFixtures$ReventlessCore.makeSourceEvent$p("item-1", "ItemRemoved");
      let action = EventMapperFixtures$ReventlessCore.ItemMapping.project(event$p);
      if (typeof action !== "object") {
        return JestGlobals.fail("Expected Delete action");
      }
      if (action.TAG !== "Delete") {
        return JestGlobals.fail("Expected Delete action");
      }
      globalThis.expect(action._0).toBe("item-1");
    });
    globalThis.test("Update action applies function correctly", () => {
      let event$p = EventMapperFixtures$ReventlessCore.makeSourceEvent$p("item-1", {
        TAG: "ItemPriceUpdated",
        newPrice: 14.99
      });
      let action = EventMapperFixtures$ReventlessCore.ItemMapping.project(event$p);
      if (typeof action !== "object") {
        return JestGlobals.fail("Expected Update action");
      }
      if (action.TAG !== "Update") {
        return JestGlobals.fail("Expected Update action");
      }
      let updated = action._1({
        name: "Widget",
        price: 9.99
      });
      globalThis.expect(updated.price).toBe(14.99);
    });
  });
});

/*  Not a pure module */
