// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as Stdlib_JsError from "@rescript/runtime/lib/es6/Stdlib_JsError.js";
import * as Message$ReventlessCore from "../../src/Message.res.mjs";
import * as SideEffectHandler_Callback$ReventlessCore from "../../src/components/SideEffectHandler/SideEffectHandler_Callback.res.mjs";

let name = "TestSideEffectSource";

let eventSchema = S.schema(s => ({
  TAG: "SomethingHappened",
  value: s.m(S.string)
}));

let TestSource = {
  Id: undefined,
  name: name,
  eventSchema: eventSchema
};

let capturedExecuteCalls = {
  contents: []
};

let executeThrowOnCall = {
  contents: 0
};

let executeCallCount = {
  contents: 0
};

let moduleUrl = import.meta.url;

async function execute(id, _meta, event, _queryEngine) {
  executeCallCount.contents = executeCallCount.contents + 1 | 0;
  if (executeThrowOnCall.contents > 0 && executeCallCount.contents === executeThrowOnCall.contents) {
    Stdlib_JsError.throwWithMessage("side effect failed");
  }
  capturedExecuteCalls.contents = capturedExecuteCalls.contents.concat([{
      id: id,
      event: event
    }]);
}

let TestSideEffect_Source = {
  name: name,
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  eventSchema: eventSchema
};

let TestSideEffect = {
  Source: TestSideEffect_Source,
  moduleUrl: moduleUrl,
  execute: execute
};

let sideEffects = [TestSideEffect];

async function queryEngine_scan(param, param$1, param$2) {
  return [];
}

async function queryEngine_query(param, param$1, param$2, param$3, param$4, param$5, param$6) {
  return [];
}

let queryEngine = {
  scan: queryEngine_scan,
  query: queryEngine_query
};

let TestSpec = {
  sideEffects: sideEffects,
  queryEngine: queryEngine
};

let TestHandler = SideEffectHandler_Callback$ReventlessCore.Make(TestSpec);

let testMeta_ip = "127.0.0.1";

let testMeta_user = "test-user";

let testMeta = {
  service: name,
  time: "2024-01-01T00:00:00Z",
  ip: testMeta_ip,
  user: testMeta_user,
  msgId: "se-msg-1",
  correlationId: "se-corr-1"
};

function makeEventJson(serviceOpt, id, event) {
  let service = serviceOpt !== undefined ? serviceOpt : name;
  let newrecord = {...testMeta};
  return Object.fromEntries([
    [
      "id",
      id
    ],
    [
      "meta",
      Message$ReventlessCore.encode((newrecord.service = service, newrecord), Message$ReventlessCore.metaSchema)
    ],
    [
      "event",
      Message$ReventlessCore.encode(event, eventSchema)
    ]
  ]);
}

function reset() {
  capturedExecuteCalls.contents = [];
  executeThrowOnCall.contents = 0;
  executeCallCount.contents = 0;
}

export {
  TestSource,
  capturedExecuteCalls,
  executeThrowOnCall,
  executeCallCount,
  TestSideEffect,
  TestSpec,
  TestHandler,
  testMeta,
  makeEventJson,
  reset,
}
/* eventSchema Not a pure module */
