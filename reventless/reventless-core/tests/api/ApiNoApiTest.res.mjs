// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";
import * as ApiNoApiHelpers$ReventlessCore from "../../src/components/Api/ApiNoApiHelpers.res.mjs";

globalThis.describe("ApiNoApiHelpers:", () => {
  globalThis.test("isNoApi returns false for schema without noApi metadata", () => {
    globalThis.expect(ApiNoApiHelpers$ReventlessCore.isNoApi(S.string)).toBe(false);
  });
  globalThis.test("isNoApi returns true for schema with noApi metadata set to true", () => {
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiId, true);
    globalThis.expect(ApiNoApiHelpers$ReventlessCore.isNoApi(schema$p)).toBe(true);
  });
  globalThis.test("getExcludedVariants returns None for schema without noApiVariants metadata", () => {
    globalThis.expect(ApiNoApiHelpers$ReventlessCore.getExcludedVariants(S.string)).toBe(undefined);
  });
  globalThis.test("getExcludedVariants returns Some with DeleteItem when set", () => {
    let excluded = new Set([
      "DeleteItem",
      "UpdateItem"
    ]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let result = ApiNoApiHelpers$ReventlessCore.getExcludedVariants(schema$p);
    globalThis.expect(Stdlib_Option.getOr(Stdlib_Option.map(result, set => set.has("DeleteItem")), false)).toBe(true);
  });
  globalThis.test("filterNoApiVariants returns all field names when no variants are excluded", () => {
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, S.string);
    globalThis.expect(filtered).toEqual([
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ]);
  });
  globalThis.test("filterNoApiVariants filters out excluded variants", () => {
    let excluded = new Set(["DeleteItem"]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, schema$p);
    globalThis.expect(filtered).toEqual([
      "CreateItem",
      "UpdateItem"
    ]);
  });
  globalThis.test("filterNoApiVariants filters out multiple excluded variants", () => {
    let excluded = new Set([
      "DeleteItem",
      "UpdateItem"
    ]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem",
      "GetItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, schema$p);
    globalThis.expect(filtered).toEqual([
      "CreateItem",
      "GetItem"
    ]);
  });
  globalThis.test("filterNoApiVariants returns empty array when all variants are excluded", () => {
    let excluded = new Set([
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, schema$p);
    globalThis.expect(filtered).toEqual([]);
  });
});

/*  Not a pure module */
