// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";
import * as ApiNoApiHelpers$ReventlessCore from "../../src/components/Api/ApiNoApiHelpers.res.mjs";

Jest.describe("ApiNoApiHelpers:", () => {
  Jest.test("isNoApi returns false for schema without noApi metadata", () => Jest.Expect.toBe(Jest.Expect.expect(ApiNoApiHelpers$ReventlessCore.isNoApi(S.string)), false));
  Jest.test("isNoApi returns true for schema with noApi metadata set to true", () => {
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiId, true);
    return Jest.Expect.toBe(Jest.Expect.expect(ApiNoApiHelpers$ReventlessCore.isNoApi(schema$p)), true);
  });
  Jest.test("getExcludedVariants returns None for schema without noApiVariants metadata", () => Jest.Expect.toBe(Jest.Expect.expect(ApiNoApiHelpers$ReventlessCore.getExcludedVariants(S.string)), undefined));
  Jest.test("getExcludedVariants returns Some with DeleteItem when set", () => {
    let excluded = new Set([
      "DeleteItem",
      "UpdateItem"
    ]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let result = ApiNoApiHelpers$ReventlessCore.getExcludedVariants(schema$p);
    return Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.getOr(Stdlib_Option.map(result, set => set.has("DeleteItem")), false)), true);
  });
  Jest.test("filterNoApiVariants returns all field names when no variants are excluded", () => {
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, S.string);
    return Jest.Expect.toEqual(Jest.Expect.expect(filtered), [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ]);
  });
  Jest.test("filterNoApiVariants filters out excluded variants", () => {
    let excluded = new Set(["DeleteItem"]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, schema$p);
    return Jest.Expect.toEqual(Jest.Expect.expect(filtered), [
      "CreateItem",
      "UpdateItem"
    ]);
  });
  Jest.test("filterNoApiVariants filters out multiple excluded variants", () => {
    let excluded = new Set([
      "DeleteItem",
      "UpdateItem"
    ]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem",
      "GetItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, schema$p);
    return Jest.Expect.toEqual(Jest.Expect.expect(filtered), [
      "CreateItem",
      "GetItem"
    ]);
  });
  Jest.test("filterNoApiVariants returns empty array when all variants are excluded", () => {
    let excluded = new Set([
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ]);
    let schema$p = S.Metadata.set(S.string, Api$ReventlessInfra.noApiVariantsId, excluded);
    let fieldNames = [
      "CreateItem",
      "DeleteItem",
      "UpdateItem"
    ];
    let filtered = ApiNoApiHelpers$ReventlessCore.filterNoApiVariants(fieldNames, schema$p);
    return Jest.Expect.toEqual(Jest.Expect.expect(filtered), []);
  });
});

/*  Not a pure module */
