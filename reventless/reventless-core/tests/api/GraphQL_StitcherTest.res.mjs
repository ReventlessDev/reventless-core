// Generated by ReScript, PLEASE EDIT WITH CARE

import * as AdminApi$ReventlessCore from "../../src/admin/AdminApi.res.mjs";
import * as GraphQL_Stitcher$ReventlessCore from "../../src/components/Api/GraphQL_Stitcher.res.mjs";

let fullSchemaSdl = `type Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
  Catalog_products: [Product]
  Ordering_orders: [Order]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
  Catalog_addProduct(input: AddInput): Product
  Catalog_updateProduct(input: UpdateInput): Product
  Ordering_placeOrder(input: OrderInput): Order
}`;

let collapsedSchemaSdl = `type Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
}`;

let authAnnotatedSdl = `type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
    @aws_auth(cognito_groups: ["Admin"])
  Catalog_addProduct(input: AddInput): Product
    @aws_auth(cognito_groups: ["User"])
}

type Query {
  node(id: ID!): Node @aws_auth(cognito_groups: ["Admin"])
}`;

globalThis.describe("GraphQL_Stitcher.countRootTypeFields", () => {
  globalThis.test("counts Mutation fields", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Mutation")).toBe(4);
  });
  globalThis.test("counts Query fields", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Query")).toBe(4);
  });
  globalThis.test("ignores @aws_auth directive lines, counts each Mutation field once", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(authAnnotatedSdl, "Mutation")).toBe(2);
  });
  globalThis.test("ignores @aws_auth directive lines, counts each Query field once", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(authAnnotatedSdl, "Query")).toBe(1);
  });
  globalThis.test("missing type returns 0", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Subscription")).toBe(0);
  });
  globalThis.test("empty SDL returns 0", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields("", "Mutation")).toBe(0);
  });
});

globalThis.describe("GraphQL_Stitcher.isCatastrophicSchemaShrink", () => {
  globalThis.test("rejects the all-disconnected collapse (8 → 3 root fields, < 50%)", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(fullSchemaSdl, collapsedSchemaSdl, 0.5)).toBe(true);
  });
  globalThis.test("allows an intentional small removal above threshold (8 → 7)", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(fullSchemaSdl, `type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
  Catalog_products: [Product]
  Ordering_orders: [Order]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
  Catalog_addProduct(input: AddInput): Product
  Ordering_placeOrder(input: OrderInput): Order
}`, 0.5)).toBe(false);
  });
  globalThis.test("allows the first push when there is no live schema to protect", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink("", fullSchemaSdl, 0.5)).toBe(false);
  });
  globalThis.test("allows an unchanged re-push (same SDL)", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(fullSchemaSdl, fullSchemaSdl, 0.5)).toBe(false);
  });
  globalThis.test("allows a purely additive push even below threshold cardinality", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(collapsedSchemaSdl, fullSchemaSdl, 0.5)).toBe(false);
  });
  globalThis.test("includes Subscription fields when judging a shrink", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(`type Query {
  node(id: ID!): Node
}

type Mutation {
  Ordering_placeOrder(input: OrderInput): Order
}

type Subscription {
  onOrdering_Customer_Register: Customer
  onOrdering_Customer_UpdateAddress: Customer
  onCatalog_Product_Added: Product
}`, `type Query {
  node(id: ID!): Node
}

type Mutation {
  Ordering_placeOrder(input: OrderInput): Order
}`, 0.5)).toBe(true);
  });
});

globalThis.describe("GraphQL_Stitcher.rootTypeFieldNames", () => {
  globalThis.test("lists Mutation field names", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.rootTypeFieldNames(fullSchemaSdl, "Mutation")).toEqual([
      "Platform_connectPlugin",
      "Catalog_addProduct",
      "Catalog_updateProduct",
      "Ordering_placeOrder"
    ]);
  });
  globalThis.test("strips args and @aws_auth directive lines", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.rootTypeFieldNames(authAnnotatedSdl, "Mutation")).toEqual([
      "Platform_connectPlugin",
      "Catalog_addProduct"
    ]);
  });
  globalThis.test("missing type returns []", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.rootTypeFieldNames(fullSchemaSdl, "Subscription")).toEqual([]);
  });
});

globalThis.describe("GraphQL_Stitcher.missingRootFields", () => {
  globalThis.test("reports plugin fields absent from an admin-base clobber", () => {
    let missing = GraphQL_Stitcher$ReventlessCore.missingRootFields(fullSchemaSdl, collapsedSchemaSdl);
    globalThis.expect(missing.includes("Catalog_addProduct")).toBe(true);
    globalThis.expect(missing.includes("Ordering_placeOrder")).toBe(true);
    globalThis.expect(missing.includes("Ordering_orders")).toBe(true);
  });
  globalThis.test("empty when live schema is a superset (intact)", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.missingRootFields(fullSchemaSdl, fullSchemaSdl)).toEqual([]);
  });
  globalThis.test("detects an equal-cardinality field swap the count test misses", () => {
    let swapped = `type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
  Catalog_products: [Product]
  Ordering_orders: [Order]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
  Catalog_addProduct(input: AddInput): Product
  Catalog_updateProduct(input: UpdateInput): Product
  Foreign_strayField(input: OrderInput): Order
}`;
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(swapped, "Mutation")).toBe(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Mutation"));
    let missing = GraphQL_Stitcher$ReventlessCore.missingRootFields(fullSchemaSdl, swapped);
    globalThis.expect(missing.includes("Ordering_placeOrder")).toBe(true);
  });
});

globalThis.describe("GraphQL_Stitcher.subscriptionSources", () => {
  globalThis.test("encode/decode round-trips the subscription→mutation source mapping", () => {
    let fragment = GraphQL_Stitcher$ReventlessCore.encode({
      types: [],
      mutations: [`  Catalog_AddProduct(input: AddInput): CommandResult`],
      queries: [],
      subscriptions: [`  onCatalog_AddProduct(id: ID): CommandResult`],
      subscriptionSources: [{
          field: "onCatalog_AddProduct",
          mutations: ["Catalog_AddProduct"]
        }]
    });
    let parts = GraphQL_Stitcher$ReventlessCore.decode(fragment);
    globalThis.expect(parts.subscriptionSources).toEqual([{
        field: "onCatalog_AddProduct",
        mutations: ["Catalog_AddProduct"]
      }]);
  });
  globalThis.test("decode tolerates fragments encoded before the field existed", () => {
    globalThis.expect(GraphQL_Stitcher$ReventlessCore.decode({
      encoded: `{"types":[],"mutations":[],"queries":[],"subscriptions":["  onX(id: ID): CommandResult"]}`,
      protocol: "graphql"
    }).subscriptionSources).toEqual([]);
  });
  globalThis.test("collectSubscriptionSources unions across fragments, first wins per field", () => {
    let base = GraphQL_Stitcher$ReventlessCore.encode({
      types: [],
      mutations: [],
      queries: [],
      subscriptions: [`  onUIFragmentChange: UIFragmentChangeEvent`],
      subscriptionSources: [{
          field: "onUIFragmentChange",
          mutations: ["Platform_UIFragmentRegistered"]
        }]
    });
    let pluginA = GraphQL_Stitcher$ReventlessCore.encode({
      types: [],
      mutations: [],
      queries: [],
      subscriptions: [`  onA(id: ID): CommandResult`],
      subscriptionSources: [
        {
          field: "onA",
          mutations: ["A"]
        },
        {
          field: "onUIFragmentChange",
          mutations: ["Rogue_Mutation"]
        }
      ]
    });
    let collected = GraphQL_Stitcher$ReventlessCore.collectSubscriptionSources(base, [pluginA]);
    globalThis.expect(collected).toEqual([
      {
        field: "onUIFragmentChange",
        mutations: ["Platform_UIFragmentRegistered"]
      },
      {
        field: "onA",
        mutations: ["A"]
      }
    ]);
  });
  globalThis.test("core-emitted admin base fragment carries no provider directives", () => {
    let base = AdminApi$ReventlessCore.baseFragment(true);
    globalThis.expect(base.encoded.includes("@aws_subscribe")).toBe(false);
    globalThis.expect(base.encoded.includes("@aws_auth")).toBe(false);
    let parts = GraphQL_Stitcher$ReventlessCore.decode(base);
    let fields = parts.subscriptionSources.map(s => s.field);
    globalThis.expect(fields.includes("onUIFragmentChange")).toBe(true);
    globalThis.expect(fields.includes("onPluginStatusChange")).toBe(true);
  });
});

export {
  fullSchemaSdl,
  collapsedSchemaSdl,
  authAnnotatedSdl,
}
/*  Not a pure module */
