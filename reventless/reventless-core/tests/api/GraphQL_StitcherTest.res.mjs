// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as GraphQL_Stitcher$ReventlessCore from "../../src/components/Api/GraphQL_Stitcher.res.mjs";

let fullSchemaSdl = `type Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
  Catalog_products: [Product]
  Ordering_orders: [Order]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
  Catalog_addProduct(input: AddInput): Product
  Catalog_updateProduct(input: UpdateInput): Product
  Ordering_placeOrder(input: OrderInput): Order
}`;

let collapsedSchemaSdl = `type Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
}`;

let authAnnotatedSdl = `type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
    @aws_auth(cognito_groups: ["Admin"])
  Catalog_addProduct(input: AddInput): Product
    @aws_auth(cognito_groups: ["User"])
}

type Query {
  node(id: ID!): Node @aws_auth(cognito_groups: ["Admin"])
}`;

Jest.describe("GraphQL_Stitcher.countRootTypeFields", () => {
  Jest.test("counts Mutation fields", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Mutation")), 4));
  Jest.test("counts Query fields", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Query")), 4));
  Jest.test("ignores @aws_auth directive lines, counts each Mutation field once", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(authAnnotatedSdl, "Mutation")), 2));
  Jest.test("ignores @aws_auth directive lines, counts each Query field once", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(authAnnotatedSdl, "Query")), 1));
  Jest.test("missing type returns 0", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields(fullSchemaSdl, "Subscription")), 0));
  Jest.test("empty SDL returns 0", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.countRootTypeFields("", "Mutation")), 0));
});

Jest.describe("GraphQL_Stitcher.isCatastrophicSchemaShrink", () => {
  Jest.test("rejects the all-disconnected collapse (8 → 3 root fields, < 50%)", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(fullSchemaSdl, collapsedSchemaSdl, 0.5)), true));
  Jest.test("allows an intentional small removal above threshold (8 → 7)", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(fullSchemaSdl, `type Query {
  node(id: ID!): Node
  Platform_plugins: [Plugin]
  Catalog_products: [Product]
  Ordering_orders: [Order]
}

type Mutation {
  Platform_connectPlugin(input: ConnectInput): ConnectResult
  Catalog_addProduct(input: AddInput): Product
  Ordering_placeOrder(input: OrderInput): Order
}`, 0.5)), false));
  Jest.test("allows the first push when there is no live schema to protect", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink("", fullSchemaSdl, 0.5)), false));
  Jest.test("allows an unchanged re-push (same SDL)", () => Jest.Expect.toBe(Jest.Expect.expect(GraphQL_Stitcher$ReventlessCore.isCatastrophicSchemaShrink(fullSchemaSdl, fullSchemaSdl, 0.5)), false));
});

export {
  fullSchemaSdl,
  collapsedSchemaSdl,
  authAnnotatedSdl,
}
/*  Not a pure module */
