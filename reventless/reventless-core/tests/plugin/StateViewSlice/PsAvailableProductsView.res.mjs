// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "CatalogProductSynced",
    productId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "CatalogProductPriceChanged",
    productId: s.m(DcbTag$Reventless.string)
  }))
]);

let stateSchema = S.schema(s => ({
  productId: s.m(S.string),
  name: s.m(S.string)
}));

function project(event) {
  if (event.TAG !== "CatalogProductSynced") {
    return [{
        TAG: "Update",
        _0: event.productId,
        _1: s => s
      }];
  }
  let productId = event.productId;
  return [{
      TAG: "Set",
      _0: productId,
      _1: {
        productId: productId,
        name: event.name
      }
    }];
}

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "AvailableProducts";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/reventless-core/tests/plugin/StateViewSlice/PsAvailableProductsView.res.mjs";

let authorization = "AllowAuthenticated";

export {
  name,
  Id,
  consumedEventSchema,
  stateSchema,
  project,
  config,
  subIdConfig,
  moduleUrl,
  authorization,
}
/* consumedEventSchema Not a pure module */
