// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";
import * as DisplayName$Reventless from "@reventlessdev/reventless-spec/src/components/DisplayName.res.mjs";

let consumedEventSchema = S.schema(s => ({
  TAG: "CustomerAdded",
  customerId: s.m(DcbTag$Reventless.string),
  firstName: s.m(S.string),
  lastName: s.m(S.string)
}));

let stateSchema = S.schema(s => ({
  customerId: s.m(S.string),
  firstName: s.m(S.string),
  lastName: s.m(S.string),
  displayName: s.m(S.option(S.string))
}));

let stateSchema$1 = S.Metadata.set(stateSchema, DisplayName$Reventless.displayNameId, {
  fields: [
    "firstName",
    "lastName"
  ],
  separator: " "
});

function project(event) {
  let customerId = event.customerId;
  return [{
      TAG: "Set",
      _0: customerId,
      _1: {
        customerId: customerId,
        firstName: event.firstName,
        lastName: event.lastName
      }
    }];
}

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "Customers";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/reventless-core/tests/plugin/StateViewSlice/PsCustomersView.res.mjs";

let authorization = "AllowAuthenticated";

let visibility = "Public";

export {
  name,
  Id,
  consumedEventSchema,
  stateSchema$1 as stateSchema,
  project,
  config,
  subIdConfig,
  moduleUrl,
  authorization,
  visibility,
}
/* consumedEventSchema Not a pure module */
