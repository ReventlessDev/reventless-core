// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Plugin$ReventlessCore from "../../src/plugin/component/Plugin.res.mjs";

globalThis.describe("Plugin.version", () => {
  globalThis.test("extracts the version segment from name@version", () => {
    globalThis.expect(Plugin$ReventlessCore.version("Catalog@0.10.0-alpha.73")).toBe("0.10.0-alpha.73");
  });
  globalThis.test("returns empty string when the id carries no @", () => {
    globalThis.expect(Plugin$ReventlessCore.version("Platform")).toBe("");
  });
});

globalThis.describe("Plugin.compareVersions", () => {
  globalThis.test("equal versions compare as 0", () => {
    globalThis.expect(Plugin$ReventlessCore.compareVersions("0.10.0", "0.10.0")).toBe(0);
  });
  globalThis.test("higher patch is newer", () => {
    globalThis.expect(Plugin$ReventlessCore.compareVersions("0.10.1", "0.10.0")).toBe(1);
  });
  globalThis.test("lower minor is older", () => {
    globalThis.expect(Plugin$ReventlessCore.compareVersions("0.9.0", "0.10.0")).toBe(-1);
  });
  globalThis.test("orders prerelease counters numerically, not lexically", () => {
    globalThis.expect(Plugin$ReventlessCore.compareVersions("0.10.0-alpha.9", "0.10.0-alpha.73")).toBe(-1);
    globalThis.expect(Plugin$ReventlessCore.compareVersions("0.10.0-alpha.73", "0.10.0-alpha.7")).toBe(1);
  });
});

/*  Not a pure module */
