// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";
import * as Plugin_Structure$ReventlessCore from "../../src/components/Plugin/Plugin_Structure.res.mjs";

let name = "PublicView";

let moduleUrl = "";

let stateSchema = S.schema(s => ({
  id: s.m(S.string),
  name: s.m(S.string)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let PublicSpec = {
  Id: undefined,
  name: name,
  moduleUrl: moduleUrl,
  stateSchema: stateSchema,
  config: config,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Public"
};

let name$1 = "InternalView";

let moduleUrl$1 = "";

let stateSchema$1 = S.schema(s => ({
  id: s.m(S.string),
  name: s.m(S.string)
}));

let config$1 = ReadModel$Reventless.config(undefined, undefined, undefined);

let InternalSpec = {
  Id: undefined,
  name: name$1,
  moduleUrl: moduleUrl$1,
  stateSchema: stateSchema$1,
  config: config$1,
  subIdConfig: undefined,
  visibility: "Internal",
  authorization: "AllowAuthenticated"
};

let sourceNames = [];

function make(param, param$1, param$2, param$3) {
  return 0;
}

function outputs(param) {
  return 0;
}

function operations(param) {
  return 0;
}

function finish() {
  
}

let PublicReadModel_Spec = {
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  name: name,
  moduleUrl: moduleUrl,
  stateSchema: stateSchema,
  config: config,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Public"
};

let PublicReadModel = {
  Spec: PublicReadModel_Spec,
  sourceNames: sourceNames,
  make: make,
  outputs: outputs,
  operations: operations,
  finish: finish
};

let sourceNames$1 = [];

function make$1(param, param$1, param$2, param$3) {
  return 0;
}

function outputs$1(param) {
  return 0;
}

function operations$1(param) {
  return 0;
}

function finish$1() {
  
}

let InternalReadModel_Spec = {
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  name: name$1,
  moduleUrl: moduleUrl$1,
  stateSchema: stateSchema$1,
  config: config$1,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Internal"
};

let InternalReadModel = {
  Spec: InternalReadModel_Spec,
  sourceNames: sourceNames$1,
  make: make$1,
  outputs: outputs$1,
  operations: operations$1,
  finish: finish$1
};

function isVisible(R) {
  let match = R.Spec.visibility;
  return match === "Public";
}

let readModels = [
  PublicReadModel,
  InternalReadModel
];

Jest.describe("ReadModel visibility filter", () => {
  Jest.test("PPX defaults visibility to Public when no attribute is present", () => Jest.Expect.toEqual(Jest.Expect.expect("Public"), "Public"));
  Jest.test("Internal spec retains Internal value", () => Jest.Expect.toEqual(Jest.Expect.expect("Internal"), "Internal"));
  Jest.test("filter predicate excludes Internal", () => {
    let visible = readModels.filter(isVisible);
    return Jest.Expect.toBe(Jest.Expect.expect(visible.length), 1);
  });
  Jest.test("filter predicate retains Public name", () => {
    let visible = readModels.filter(isVisible);
    let first = visible[0];
    return Jest.Expect.toEqual(Jest.Expect.expect(first.Spec.name), "PublicView");
  });
  Jest.test("unfiltered list still contains both entries (no over-filtering)", () => Jest.Expect.toBe(Jest.Expect.expect(readModels.length), 2));
});

Jest.describe("Plugin_Structure.make — visibility filter", () => {
  let structure = Plugin_Structure$ReventlessCore.make("VisibilityPlugin", undefined, readModels, undefined, undefined, undefined, undefined, undefined, undefined, undefined);
  Jest.test("readModels excludes Internal entries", () => Jest.Expect.toBe(Jest.Expect.expect(structure.readModels.length), 1));
  Jest.test("readModels retains the Public entry by name", () => {
    let first = structure.readModels[0];
    return Jest.Expect.toEqual(Jest.Expect.expect(first.name), "PublicView");
  });
  Jest.test("aggregates / stateViewSlices stay empty when none are passed (sanity)", () => Jest.Expect.toEqual(Jest.Expect.expect([
    structure.aggregates.length,
    structure.stateViewSlices.length
  ]), [
    0,
    0
  ]));
});

export {
  PublicSpec,
  InternalSpec,
  PublicReadModel,
  InternalReadModel,
  isVisible,
  readModels,
}
/* stateSchema Not a pure module */
