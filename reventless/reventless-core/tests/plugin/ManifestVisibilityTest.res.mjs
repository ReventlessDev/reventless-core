// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";
import * as Plugin_Structure$ReventlessCore from "../../src/plugin/component/Plugin_Structure.res.mjs";

let name = "PublicView";

let moduleUrl = "";

let stateSchema = S.schema(s => ({
  id: s.m(S.string),
  name: s.m(S.string)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let PublicSpec = {
  Id: undefined,
  name: name,
  moduleUrl: moduleUrl,
  stateSchema: stateSchema,
  config: config,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Public"
};

let name$1 = "InternalView";

let moduleUrl$1 = "";

let stateSchema$1 = S.schema(s => ({
  id: s.m(S.string),
  name: s.m(S.string)
}));

let config$1 = ReadModel$Reventless.config(undefined, undefined, undefined);

let InternalSpec = {
  Id: undefined,
  name: name$1,
  moduleUrl: moduleUrl$1,
  stateSchema: stateSchema$1,
  config: config$1,
  subIdConfig: undefined,
  visibility: "Internal",
  authorization: "AllowAuthenticated"
};

let sourceNames = [];

let consumedEventNames = ["OrderPlaced"];

function make(param, param$1, param$2, param$3) {
  return 0;
}

function outputs(param) {
  return 0;
}

function operations(param) {
  return 0;
}

function finish() {
  
}

let PublicReadModel_Spec = {
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  name: name,
  moduleUrl: moduleUrl,
  stateSchema: stateSchema,
  config: config,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Public"
};

let PublicReadModel = {
  Spec: PublicReadModel_Spec,
  sourceNames: sourceNames,
  consumedEventNames: consumedEventNames,
  make: make,
  outputs: outputs,
  operations: operations,
  finish: finish
};

let sourceNames$1 = [];

let consumedEventNames$1 = [];

function make$1(param, param$1, param$2, param$3) {
  return 0;
}

function outputs$1(param) {
  return 0;
}

function operations$1(param) {
  return 0;
}

function finish$1() {
  
}

let InternalReadModel_Spec = {
  Id: {
    schema: Id$Reventless.StringPure.schema,
    make: prim => prim,
    makeFromString: prim => prim,
    toString: prim => prim,
    cmp: Id$Reventless.StringPure.cmp
  },
  name: name$1,
  moduleUrl: moduleUrl$1,
  stateSchema: stateSchema$1,
  config: config$1,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Internal"
};

let InternalReadModel = {
  Spec: InternalReadModel_Spec,
  sourceNames: sourceNames$1,
  consumedEventNames: consumedEventNames$1,
  make: make$1,
  outputs: outputs$1,
  operations: operations$1,
  finish: finish$1
};

function isVisible(R) {
  let match = R.Spec.visibility;
  return match === "Public";
}

let readModels = [
  PublicReadModel,
  InternalReadModel
];

globalThis.describe("ReadModel visibility filter", () => {
  globalThis.test("PPX defaults visibility to Public when no attribute is present", () => {
    globalThis.expect("Public").toEqual("Public");
  });
  globalThis.test("Internal spec retains Internal value", () => {
    globalThis.expect("Internal").toEqual("Internal");
  });
  globalThis.test("filter predicate excludes Internal", () => {
    let visible = readModels.filter(isVisible);
    globalThis.expect(visible.length).toBe(1);
  });
  globalThis.test("filter predicate retains Public name", () => {
    let visible = readModels.filter(isVisible);
    let first = visible[0];
    globalThis.expect(first.Spec.name).toEqual("PublicView");
  });
  globalThis.test("unfiltered list still contains both entries (no over-filtering)", () => {
    globalThis.expect(readModels.length).toBe(2);
  });
});

globalThis.describe("Plugin_Structure.make — visibility tagging", () => {
  let structure = Plugin_Structure$ReventlessCore.make("VisibilityPlugin", undefined, readModels, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined);
  globalThis.test("readModels carries both Public and Internal entries", () => {
    globalThis.expect(structure.readModels.length).toBe(2);
  });
  globalThis.test("the Public entry is tagged visibility = None", () => {
    let $$public = structure.readModels.find(d => d.name === "PublicView");
    globalThis.expect(Stdlib_Option.map($$public, d => d.visibility)).toEqual(Primitive_option.some(undefined));
  });
  globalThis.test("the Internal entry is carried and tagged visibility = Some(\"Internal\")", () => {
    let internal = structure.readModels.find(d => d.name === "InternalView");
    globalThis.expect(Stdlib_Option.map(internal, d => d.visibility)).toEqual("Internal");
  });
  globalThis.test("a read model's mapping-consumed events surface as qualified consumedEventTypes", () => {
    let $$public = structure.readModels.find(d => d.name === "PublicView");
    globalThis.expect(Stdlib_Option.map($$public, d => d.consumedEventTypes)).toEqual(["VisibilityPlugin.OrderPlaced"]);
  });
  globalThis.test("aggregates / stateViewSlices stay empty when none are passed (sanity)", () => {
    globalThis.expect([
      structure.aggregates.length,
      structure.stateViewSlices.length
    ]).toEqual([
      0,
      0
    ]);
  });
});

export {
  PublicSpec,
  InternalSpec,
  PublicReadModel,
  InternalReadModel,
  isVisible,
  readModels,
}
/* stateSchema Not a pure module */
