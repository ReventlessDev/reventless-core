// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let consumedEventSchema = S.union([
  S.literal("OrderPlaced"),
  S.literal("OrderShipped"),
  S.literal("OrderCancelled")
]);

function evolve(_state, _event) {
  return true;
}

let commandSchema = S.schema(s => ({
  TAG: "ShipOrder",
  orderId: s.m(DcbTag$Reventless.string)
}));

let errorSchema = S.literal("OrderNotFound");

let eventSchema = S.schema(s => ({
  TAG: "OrderShipped",
  orderId: s.m(DcbTag$Reventless.string)
}));

function decide(_state, _command) {
  return {
    TAG: "Ok",
    _0: []
  };
}

let name = "ShipOrder";

let Id;

let initialState = false;

let moduleUrl = "@reventlessdev/reventless-core/tests/plugin/StateChangeSlice/PsShipOrder.res.mjs";

export {
  name,
  Id,
  initialState,
  consumedEventSchema,
  evolve,
  commandSchema,
  errorSchema,
  eventSchema,
  decide,
  moduleUrl,
}
/* consumedEventSchema Not a pure module */
