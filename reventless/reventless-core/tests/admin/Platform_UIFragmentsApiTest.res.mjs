// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_string from "@rescript/runtime/lib/es6/Primitive_string.js";
import * as Platform_UIFragmentsApi$ReventlessCore from "../../src/admin/Platform_UIFragmentsApi.res.mjs";

let panel_positions = ["platform-summary"];

let panel = {
  fragmentId: "Catalog.Categories.list",
  title: "Categories",
  description: "",
  positions: panel_positions,
  requiredAccess: undefined
};

let page = {
  fragmentId: "Catalog.Categories.list",
  title: "Categories",
  menuEntry: {
    label: "Categories",
    icon: undefined,
    group: undefined,
    sortOrder: 0
  },
  requiredAccess: "admin"
};

let state_panels = [panel];

let state_pages = [page];

let state = {
  pluginId: "Catalog",
  remoteEntryUrl: "https://cdn.example.com/catalog@1.0/remoteEntry.js",
  panels: state_panels,
  pages: state_pages,
  registeredAt: "2026-05-14T00:00:00.000Z",
  updatedAt: "2026-05-14T00:00:00.000Z"
};

let encoded = Platform_UIFragmentsApi$ReventlessCore.encodeUIFragmentEntry(state);

let json = JSON.stringify(encoded);

Jest.describe("encodeUIFragmentEntry", () => {
  Jest.test("produces a JSON object with the expected top-level keys", () => {
    let dict = Stdlib_Option.getOr(Stdlib_JSON.Decode.object(encoded), {});
    let keys = Object.keys(dict).toSorted(Primitive_string.compare);
    return Jest.Expect.toEqual(Jest.Expect.expect(keys), [
      "pages",
      "panels",
      "pluginId",
      "registeredAt",
      "remoteEntryUrl",
      "updatedAt"
    ]);
  });
  Jest.test("encodes pluginId", () => Jest.Expect.toEqual(Jest.Expect.expect(json.includes("\"pluginId\":\"Catalog\"")), true));
  Jest.test("encodes remoteEntryUrl", () => Jest.Expect.toEqual(Jest.Expect.expect(json.includes("\"remoteEntryUrl\":\"https://cdn.example.com/catalog@1.0/remoteEntry.js\"")), true));
  Jest.test("encodes None requiredAccess on panel as null", () => Jest.Expect.toEqual(Jest.Expect.expect(json.includes("\"requiredAccess\":null")), true));
  Jest.test("encodes Some requiredAccess on page as its inner string", () => Jest.Expect.toEqual(Jest.Expect.expect(json.includes("\"requiredAccess\":\"admin\"")), true));
  Jest.test("encodes nested menuEntry as an object with sortOrder", () => Jest.Expect.toEqual(Jest.Expect.expect(json.includes("\"sortOrder\":0")), true));
  Jest.test("encodes panel positions as an array of strings", () => Jest.Expect.toEqual(Jest.Expect.expect(json.includes("\"positions\":[\"platform-summary\"]")), true));
});

Jest.describe("Platform_UIFragmentsApi.sdl", () => {
  Jest.test("query field declares the list type", () => Jest.Expect.toEqual(Jest.Expect.expect(Platform_UIFragmentsApi$ReventlessCore.sdlQueryField.includes("[Platform_UIFragmentEntry!]!")), true));
  Jest.test("sdlTypes includes Platform_UIFragmentEntry", () => {
    let allTypes = Platform_UIFragmentsApi$ReventlessCore.sdlTypes.join("\n");
    return Jest.Expect.toEqual(Jest.Expect.expect(allTypes.includes("Platform_UIFragmentEntry")), true);
  });
  Jest.test("sdlTypes includes Platform_UIPanel with positions field", () => {
    let allTypes = Platform_UIFragmentsApi$ReventlessCore.sdlTypes.join("\n");
    return Jest.Expect.toEqual(Jest.Expect.expect(allTypes.includes("positions: [String!]!")), true);
  });
});

export {
  panel,
  page,
  state,
  encoded,
  json,
}
/* encoded Not a pure module */
