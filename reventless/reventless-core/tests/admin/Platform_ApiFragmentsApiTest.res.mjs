// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_string from "@rescript/runtime/lib/es6/Primitive_string.js";
import * as Platform_ApiFragmentsApi$ReventlessCore from "../../src/admin/Platform_ApiFragmentsApi.res.mjs";

let state = {
  pluginId: "Catalog",
  encoded: `{"types":[],"mutations":[],"queries":[],"subscriptions":[],"subscriptionSources":[]}`,
  protocol: "graphql",
  apiTarget: "Domain",
  registeredAt: "2026-05-14T00:00:00.000Z",
  updatedAt: "2026-05-14T00:00:00.000Z",
  pushStatus: "ok",
  pushMessage: "",
  pushedAt: "2026-05-14T00:00:01.000Z"
};

let encoded = Platform_ApiFragmentsApi$ReventlessCore.encodeApiFragmentEntry(state);

let json = JSON.stringify(encoded);

globalThis.describe("encodeApiFragmentEntry", () => {
  globalThis.test("produces a JSON object with the expected top-level keys (status only, no SDL)", () => {
    let dict = Stdlib_Option.getOr(Stdlib_JSON.Decode.object(encoded), {});
    let keys = Object.keys(dict).toSorted(Primitive_string.compare);
    globalThis.expect(keys).toEqual([
      "apiTarget",
      "pluginId",
      "pushMessage",
      "pushStatus",
      "pushedAt",
      "registeredAt",
      "updatedAt"
    ]);
  });
  globalThis.test("encodes pluginId", () => {
    globalThis.expect(json.includes("\"pluginId\":\"Catalog\"")).toEqual(true);
  });
  globalThis.test("encodes the apiTarget variant as its bare string", () => {
    globalThis.expect(json.includes("\"apiTarget\":\"Domain\"")).toEqual(true);
  });
  globalThis.test("encodes pushStatus", () => {
    globalThis.expect(json.includes("\"pushStatus\":\"ok\"")).toEqual(true);
  });
  globalThis.test("does not leak the encoded SDL into the status shape", () => {
    globalThis.expect(json.includes("subscriptionSources")).toEqual(false);
  });
});

globalThis.describe("Platform_ApiFragmentsApi.sdl", () => {
  globalThis.test("query field declares the list type", () => {
    globalThis.expect(Platform_ApiFragmentsApi$ReventlessCore.sdlQueryField.includes("[Platform_ApiFragmentEntry!]!")).toEqual(true);
  });
  globalThis.test("sdlTypes includes Platform_ApiFragmentEntry with pushStatus field", () => {
    let allTypes = Platform_ApiFragmentsApi$ReventlessCore.sdlTypes.join("\n");
    globalThis.expect(allTypes.includes("Platform_ApiFragmentEntry")).toEqual(true);
    globalThis.expect(allTypes.includes("pushStatus: String!")).toEqual(true);
  });
});

export {
  state,
  encoded,
  json,
}
/* encoded Not a pure module */
