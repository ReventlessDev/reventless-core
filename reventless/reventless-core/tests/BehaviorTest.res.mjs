// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";
import * as Message$ReventlessCore from "../src/Message.res.mjs";
import * as TestFixtures$ReventlessCore from "./TestFixtures.res.mjs";

function Make(Spec) {
  return Behavior => {
    S.enableJson();
    let apply$p = (state, event) => Behavior.apply(state, event);
    let currentState = events => Stdlib_Array.reduce(events.slice(1), Behavior.init(events[0]), apply$p);
    let errors = {
      contents: []
    };
    let errorHandler = (error, param, param$1) => {
      errors.contents = errors.contents.concat([error]);
      return [];
    };
    let exec = (history, context, command) => {
      errors.contents = [];
      if (history.length === 0) {
        return Behavior.create(command, context, errorHandler);
      }
      try {
        return Behavior.execute(currentState(history), command, TestFixtures$ReventlessCore.context, errorHandler);
      } catch (raw_exn) {
        let exn = Primitive_exceptions.internalToException(raw_exn);
        if (exn.RE_EXN_ID === Message$ReventlessCore.InvalidEvent) {
          return [];
        }
        throw exn;
      }
    };
    let givenEvents = events => events;
    let whenCmd = (history, cmd) => exec(history, TestFixtures$ReventlessCore.context, cmd);
    let whenCmdWithId = (history, id, cmd) => exec(history, {
      id: id,
      meta: TestFixtures$ReventlessCore.context.meta
    }, cmd);
    let thenEvents = (events, expectedEvents) => Jest.Expect.toEqual(Jest.Expect.expect([
      errors.contents.length,
      events
    ]), [
      0,
      expectedEvents
    ]);
    let compare = (cmp, e1, e2) => {
      let cmpResult = cmp(e1, e2);
      if (!cmpResult) {
        console.log("Events do not match:", e1, e2);
      }
      return cmpResult;
    };
    let thenCompareEvents = (events, expectedEvents, cmp) => Jest.Expect.toEqual(Jest.Expect.expect([
      errors.contents.length,
      events.length,
      Stdlib_Array.zip(events, expectedEvents).map(param => compare(cmp, param[0], param[1])).every(result => result)
    ]), [
      0,
      expectedEvents.length,
      true
    ]);
    let listErrors = () => "Errors occured: " + Stdlib_Array.reduce(errors.contents.map(err => Stdlib_Option.getOrThrow(Stdlib_JSON.Decode.string(Stdlib_Option.getOrThrow(Stdlib_JSON.Decode.array(Message$ReventlessCore.encode(err, Spec.errorSchema)), undefined)[0]), undefined)), "", (a, b) => a + (b + " "));
    let thenEvent = (events, expectedEvent) => {
      if (events.length !== 0) {
        return Jest.Expect.toEqual(Jest.Expect.expect([
          errors.contents.length,
          events.length,
          events[0]
        ]), [
          0,
          1,
          Primitive_option.some(expectedEvent)
        ]);
      } else if (errors.contents.length !== 0) {
        return Jest.fail(listErrors());
      } else {
        return Jest.fail("thenEvent: No event present to validate");
      }
    };
    let thenCompareEvent = (events, expectedEvent, cmp) => {
      if (events.length === 0) {
        if (errors.contents.length !== 0) {
          return Jest.fail(listErrors());
        } else {
          return Jest.fail("thenEvent: No event present to validate");
        }
      }
      let firstEvent = Stdlib_Option.getOrThrow(events[0], undefined);
      return Jest.Expect.toEqual(Jest.Expect.expect([
        errors.contents.length,
        events.length,
        compare(cmp, firstEvent, expectedEvent)
      ]), [
        0,
        1,
        true
      ]);
    };
    let thenNoEvent = events => thenEvents(events, []);
    let thenEventWithError = (events, expectedEvent, expectedError) => Jest.Expect.toEqual(Jest.Expect.expect([
      events.length,
      events[0],
      errors.contents.length,
      errors.contents[0]
    ]), [
      1,
      Primitive_option.some(expectedEvent),
      1,
      Primitive_option.some(expectedError)
    ]);
    let thenEventsWithError = (events, expectedEvents, expectedError) => Jest.Expect.toEqual(Jest.Expect.expect([
      events,
      errors.contents.length,
      errors.contents[0]
    ]), [
      expectedEvents,
      1,
      Primitive_option.some(expectedError)
    ]);
    let thenError = (events, expectedError) => Jest.Expect.toEqual(Jest.Expect.expect([
      events,
      errors.contents.length,
      errors.contents[0]
    ]), [
      [],
      1,
      Primitive_option.some(expectedError)
    ]);
    return {
      Spec: Spec,
      describe: Jest.describe,
      test: Jest.test,
      givenEvents: givenEvents,
      whenCmd: whenCmd,
      whenCmdWithId: whenCmdWithId,
      thenEvent: thenEvent,
      thenCompareEvent: thenCompareEvent,
      thenNoEvent: thenNoEvent,
      thenEventWithError: thenEventWithError,
      thenEvents: thenEvents,
      thenCompareEvents: thenCompareEvents,
      thenEventsWithError: thenEventsWithError,
      thenError: thenError
    };
  };
}

export {
  Make,
}
/* S Not a pure module */
