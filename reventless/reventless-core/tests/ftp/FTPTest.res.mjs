// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as FTP$ReventlessCore from "../../src/util/FTP.res.mjs";

Jest.describe("FTP.SSH2 should", () => {
  Jest.test("parse valid url, only url given", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      undefined,
      ""
    ]);
  });
  Jest.test("parse valid url, url and relative path given", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net/some/path");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      undefined,
      "some/path"
    ]);
  });
  Jest.test("parse valid url, url and absolute path given", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net//some/path");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      undefined,
      "/some/path"
    ]);
  });
  Jest.test("parse valid url, url and port given", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net:8080");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      8080,
      ""
    ]);
  });
  Jest.test("parse valid url, all parts given (relative path)", () => {
    let t = FTP$ReventlessCore.parseUrl("reventless.atos.net:8025/some/path");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "reventless.atos.net",
      8025,
      "some/path"
    ]);
  });
  Jest.test("parse valid url, all parts given (absolute path)", () => {
    let t = FTP$ReventlessCore.parseUrl("reventless.atos.net:8025//some/path");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "reventless.atos.net",
      8025,
      "/some/path"
    ]);
  });
  Jest.test("parse valid url, all parts given (relative path first)", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net/some/path:8080");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      8080,
      "some/path"
    ]);
  });
  Jest.test("parse valid url, all parts given (absolute path first)", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net//some/path:8080");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      8080,
      "/some/path"
    ]);
  });
  Jest.test("parse url with invalid port (relative path)", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net/some/path:invalidPort");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      undefined,
      "some/path"
    ]);
  });
  Jest.test("parse url with invalid port (absolute path)", () => {
    let t = FTP$ReventlessCore.parseUrl("atos.net//some/path:invalidPort");
    return Jest.Expect.toEqual(Jest.Expect.expect(t), [
      "atos.net",
      undefined,
      "/some/path"
    ]);
  });
});

/*  Not a pure module */
