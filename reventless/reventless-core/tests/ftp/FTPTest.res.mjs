// Generated by ReScript, PLEASE EDIT WITH CARE

import * as FTP$ReventlessCore from "../../src/util/FTP.res.mjs";

globalThis.describe("FTP.SSH2 should", () => {
  globalThis.test("parse valid url, only url given", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com");
    globalThis.expect(t).toEqual([
      "example.com",
      undefined,
      ""
    ]);
  });
  globalThis.test("parse valid url, url and relative path given", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com/some/path");
    globalThis.expect(t).toEqual([
      "example.com",
      undefined,
      "some/path"
    ]);
  });
  globalThis.test("parse valid url, url and absolute path given", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com//some/path");
    globalThis.expect(t).toEqual([
      "example.com",
      undefined,
      "/some/path"
    ]);
  });
  globalThis.test("parse valid url, url and port given", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com:8080");
    globalThis.expect(t).toEqual([
      "example.com",
      8080,
      ""
    ]);
  });
  globalThis.test("parse valid url, all parts given (relative path)", () => {
    let t = FTP$ReventlessCore.parseUrl("reventless.example.com:8025/some/path");
    globalThis.expect(t).toEqual([
      "reventless.example.com",
      8025,
      "some/path"
    ]);
  });
  globalThis.test("parse valid url, all parts given (absolute path)", () => {
    let t = FTP$ReventlessCore.parseUrl("reventless.example.com:8025//some/path");
    globalThis.expect(t).toEqual([
      "reventless.example.com",
      8025,
      "/some/path"
    ]);
  });
  globalThis.test("parse valid url, all parts given (relative path first)", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com/some/path:8080");
    globalThis.expect(t).toEqual([
      "example.com",
      8080,
      "some/path"
    ]);
  });
  globalThis.test("parse valid url, all parts given (absolute path first)", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com//some/path:8080");
    globalThis.expect(t).toEqual([
      "example.com",
      8080,
      "/some/path"
    ]);
  });
  globalThis.test("parse url with invalid port (relative path)", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com/some/path:invalidPort");
    globalThis.expect(t).toEqual([
      "example.com",
      undefined,
      "some/path"
    ]);
  });
  globalThis.test("parse url with invalid port (absolute path)", () => {
    let t = FTP$ReventlessCore.parseUrl("example.com//some/path:invalidPort");
    globalThis.expect(t).toEqual([
      "example.com",
      undefined,
      "/some/path"
    ]);
  });
});

/*  Not a pure module */
