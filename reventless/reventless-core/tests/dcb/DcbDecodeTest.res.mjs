// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Primitive_string from "@rescript/runtime/lib/es6/Primitive_string.js";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as DcbDecode$Reventless from "@reventlessdev/reventless-spec/src/components/DcbDecode.res.mjs";

let producedEventSchema = S.union([
  S.schema(s => ({
    TAG: "ItemCreated",
    itemId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemRenamed",
    itemId: s.m(DcbTag$Reventless.string),
    newName: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemArchived",
    itemId: s.m(DcbTag$Reventless.string)
  }))
]);

let consumedPayloadLessSchema = S.union([
  S.literal("ItemCreated"),
  S.literal("ItemArchived")
]);

let consumedPartialSchema = S.union([
  S.schema(s => ({
    TAG: "ItemCreated",
    itemId: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemRenamed",
    itemId: s.m(S.string)
  }))
]);

let consumedFullSchema = S.union([
  S.schema(s => ({
    TAG: "ItemCreated",
    itemId: s.m(S.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemRenamed",
    itemId: s.m(S.string),
    newName: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemArchived",
    itemId: s.m(S.string)
  }))
]);

Jest.describe("DcbDecode:", () => {
  Jest.describe("makeDecoder — payload-less", () => {
    let match = DcbDecode$Reventless.makeDecoder(consumedPayloadLessSchema);
    let eventTypes = match.eventTypes;
    let decode = match.decode;
    Jest.test("returns correct eventTypes", () => {
      let sorted = eventTypes.toSorted(Primitive_string.compare);
      return Jest.Expect.toEqual(Jest.Expect.expect(sorted), [
        "ItemArchived",
        "ItemCreated"
      ]);
    });
    Jest.test("decodes ItemCreated as payload-less variant", () => {
      let result = decode("ItemCreated", Object.fromEntries([
        [
          "itemId",
          "i-1"
        ],
        [
          "name",
          "Test"
        ]
      ]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), "ItemCreated");
    });
    Jest.test("decodes ItemArchived as payload-less variant", () => {
      let result = decode("ItemArchived", Object.fromEntries([[
          "itemId",
          "i-1"
        ]]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), "ItemArchived");
    });
    Jest.test("returns None for unknown event type", () => {
      let result = decode("ItemRenamed", Object.fromEntries([[
          "itemId",
          "i-1"
        ]]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), undefined);
    });
  });
  Jest.describe("makeDecoder — partial projection", () => {
    let match = DcbDecode$Reventless.makeDecoder(consumedPartialSchema);
    let decode = match.decode;
    Jest.test("decodes ItemCreated with only itemId field", () => {
      let result = decode("ItemCreated", Object.fromEntries([
        [
          "itemId",
          "i-1"
        ],
        [
          "name",
          "Test"
        ]
      ]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "ItemCreated",
        itemId: "i-1"
      });
    });
    Jest.test("decodes ItemRenamed with only itemId field", () => {
      let result = decode("ItemRenamed", Object.fromEntries([
        [
          "itemId",
          "i-1"
        ],
        [
          "newName",
          "New"
        ]
      ]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "ItemRenamed",
        itemId: "i-1"
      });
    });
    Jest.test("returns None for unknown event type", () => {
      let result = decode("ItemArchived", Object.fromEntries([]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), undefined);
    });
  });
  Jest.describe("makeDecoder — full shape", () => {
    let match = DcbDecode$Reventless.makeDecoder(consumedFullSchema);
    let decode = match.decode;
    Jest.test("decodes ItemCreated with all fields", () => {
      let result = decode("ItemCreated", Object.fromEntries([
        [
          "itemId",
          "i-1"
        ],
        [
          "name",
          "Test"
        ]
      ]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "ItemCreated",
        itemId: "i-1",
        name: "Test"
      });
    });
    Jest.test("decodes ItemRenamed with all fields", () => {
      let result = decode("ItemRenamed", Object.fromEntries([
        [
          "itemId",
          "i-1"
        ],
        [
          "newName",
          "New"
        ]
      ]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "ItemRenamed",
        itemId: "i-1",
        newName: "New"
      });
    });
    Jest.test("decodes ItemArchived with all fields", () => {
      let result = decode("ItemArchived", Object.fromEntries([[
          "itemId",
          "i-1"
        ]]));
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "ItemArchived",
        itemId: "i-1"
      });
    });
  });
});

export {
  producedEventSchema,
  consumedPayloadLessSchema,
  consumedPartialSchema,
  consumedFullSchema,
}
/* producedEventSchema Not a pure module */
