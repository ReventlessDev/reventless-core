// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as AggregateFixtures$ReventlessCore from "./AggregateFixtures.res.mjs";

beforeEach(() => AggregateFixtures$ReventlessCore.mock.reset());

describe("Aggregate_Callback.handleCommands:", () => {
  describe("single command — new aggregate", () => {
    test("Create returns Ok(reference) and stores 1 event", async () => {
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([AggregateFixtures$ReventlessCore.makeTopicItem(undefined, "ref-1", {
          TAG: "Create",
          name: "Widget"
        })])));
      expect([
        results,
        AggregateFixtures$ReventlessCore.mock.getAll().length
      ]).toEqual([
        [{
            TAG: "Ok",
            _0: "ref-1"
          }],
        1
      ]);
    });
  });
  describe("command on existing aggregate", () => {
    test("Rename after Create replays history and appends new event", async () => {
      await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([AggregateFixtures$ReventlessCore.makeTopicItem(undefined, "ref-1", {
          TAG: "Create",
          name: "Widget"
        })])));
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([AggregateFixtures$ReventlessCore.makeTopicItem(undefined, "ref-2", {
          TAG: "Rename",
          newName: "Gadget"
        })])));
      expect([
        results,
        AggregateFixtures$ReventlessCore.mock.getAll().length
      ]).toEqual([
        [{
            TAG: "Ok",
            _0: "ref-2"
          }],
        2
      ]);
    });
  });
  describe("behavior returns no events", () => {
    test("Rename on new aggregate returns Ok with no event stored", async () => {
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([AggregateFixtures$ReventlessCore.makeTopicItem(undefined, "ref-1", {
          TAG: "Rename",
          newName: "Gadget"
        })])));
      expect([
        results,
        AggregateFixtures$ReventlessCore.mock.getAll().length
      ]).toEqual([
        [{
            TAG: "Ok",
            _0: "ref-1"
          }],
        0
      ]);
    });
  });
  describe("append failure", () => {
    test("eventLog.append fails → reference returned Error", async () => {
      AggregateFixtures$ReventlessCore.mock.failNextAppend.contents = true;
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([AggregateFixtures$ReventlessCore.makeTopicItem(undefined, "ref-1", {
          TAG: "Create",
          name: "Widget"
        })])));
      expect(results).toEqual([{
          TAG: "Error",
          _0: "ref-1"
        }]);
    });
  });
  describe("multiple commands for same aggregate ID", () => {
    test("Create then Rename in one batch accumulate state correctly", async () => {
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([
        AggregateFixtures$ReventlessCore.makeTopicItem("agg-1", "ref-1", {
          TAG: "Create",
          name: "Widget"
        }),
        AggregateFixtures$ReventlessCore.makeTopicItem("agg-1", "ref-2", {
          TAG: "Rename",
          newName: "Gadget"
        })
      ])));
      expect([
        results,
        AggregateFixtures$ReventlessCore.mock.getAll().length
      ]).toEqual([
        [
          {
            TAG: "Ok",
            _0: "ref-1"
          },
          {
            TAG: "Ok",
            _0: "ref-2"
          }
        ],
        2
      ]);
    });
  });
  describe("commands for different aggregate IDs", () => {
    test("each ID processed independently", async () => {
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([
        AggregateFixtures$ReventlessCore.makeTopicItem("agg-a", "ref-a", {
          TAG: "Create",
          name: "Alpha"
        }),
        AggregateFixtures$ReventlessCore.makeTopicItem("agg-b", "ref-b", {
          TAG: "Create",
          name: "Beta"
        })
      ])));
      expect([
        results.length,
        AggregateFixtures$ReventlessCore.mock.getAll().length
      ]).toEqual([
        2,
        2
      ]);
    });
  });
  describe("empty batch", () => {
    test("returns empty array", async () => {
      let results = await Effect.Effect.runPromise(AggregateFixtures$ReventlessCore.TestHandler.handleCommands(Effect.Stream.fromIterable([])));
      expect(results).toEqual([]);
    });
  });
});

/*  Not a pure module */
