// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Message$ReventlessCore from "../../src/Message.res.mjs";
import * as PluginSpec$ReventlessCore from "../../src/plugin/lifecycle/PluginSpec.res.mjs";

globalThis.describe("Message should", () => {
  globalThis.test("create a valid sequenceNr", () => {
    globalThis.expect(Message$ReventlessCore.hrtimeToString([
      1,
      1
    ], 123456789)).toBe("123456789-000000001");
  });
  globalThis.test("get variant name of json without payload", () => {
    let variantJson = Message$ReventlessCore.encode("Heartbeat", PluginSpec$ReventlessCore.commandSchema);
    let variantName = Message$ReventlessCore.variantNameOfJson(variantJson);
    globalThis.expect(variantName).toBe("Heartbeat");
  });
  globalThis.test("get variant name of json with payload", () => {
    let variant = {
      TAG: "Connect",
      _0: {
        id: "id",
        name: "testName",
        version: "testVersion",
        extensionPoints: [{
            name: "testExtensionPoint",
            commandTopic: "testCommandTopic",
            eventTopic: "testEventTopic"
          }],
        extensions: [{
            name: "testExtension",
            extensionPointName: "testExtensionPoint",
            dcbSources: []
          }],
        eventCollector: "testEventCollector",
        extensionProtocols: [],
        apiSchemaFragment: undefined,
        apiTarget: undefined,
        uiFragments: undefined,
        structure: undefined,
        dcbEventLog: undefined
      }
    };
    let variantJson = Message$ReventlessCore.encode(variant, PluginSpec$ReventlessCore.commandSchema);
    let variantName = Message$ReventlessCore.variantNameOfJson(variantJson);
    globalThis.expect(variantName).toBe("Connect");
  });
  globalThis.test("get event name of eventJson'", () => {
    let eventJson$p = Message$ReventlessCore.encodeEvent$p({
      id: "testId",
      meta: {
        service: "testService",
        time: "testTime",
        ip: "testIp",
        user: "testUser",
        msgId: "testMsgId",
        correlationId: "testCorrelationId"
      },
      event: "UnknownPluginDetected"
    }, S.string, PluginSpec$ReventlessCore.eventSchema);
    let eventName = Message$ReventlessCore.eventNameOfEvent$pJson(eventJson$p);
    globalThis.expect(eventName).toBe("UnknownPluginDetected");
  });
  globalThis.test("get id of eventJson'", () => {
    let eventJson$p = Message$ReventlessCore.encodeEvent$p({
      id: "testId",
      meta: {
        service: "testService",
        time: "testTime",
        ip: "testIp",
        user: "testUser",
        msgId: "testMsgId",
        correlationId: "testCorrelationId"
      },
      event: "UnknownPluginDetected"
    }, S.string, PluginSpec$ReventlessCore.eventSchema);
    let eventId = Stdlib_Option.getOrThrow(Message$ReventlessCore.idOfEvent$pJson(eventJson$p), undefined);
    globalThis.expect(eventId).toBe("testId");
  });
});

/*  Not a pure module */
