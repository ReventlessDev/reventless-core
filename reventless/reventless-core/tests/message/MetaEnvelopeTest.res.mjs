// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Util_Sury$Reventless from "@reventlessdev/reventless-spec/src/util/Util_Sury.res.mjs";
import * as Message$ReventlessCore from "../../src/Message.res.mjs";

Jest.describe("Message.meta optional fields:", () => {
  Jest.test("generateMeta() with no ~ip/~user omits those keys from JSON", () => {
    let meta = Message$ReventlessCore.generateMeta("svc", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let json = Util_Sury$Reventless.toJson(meta, Message$ReventlessCore.metaSchema);
    let obj = Stdlib_Option.getOrThrow(Stdlib_JSON.Decode.object(json), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["ip"]), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["user"]), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["causationId"]), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["traceparent"]), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["schemaVersion"]), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["headers"]), undefined);
    Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.isSome(obj["service"])), true);
    Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.isSome(obj["time"])), true);
    Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.isSome(obj["msgId"])), true);
    return Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.isSome(obj["correlationId"])), true);
  });
  Jest.test("generateMeta() correlationId defaults to msgId", () => {
    let meta = Message$ReventlessCore.generateMeta("svc", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    return Jest.Expect.toBe(Jest.Expect.expect(meta.correlationId), meta.msgId);
  });
  Jest.test("generateMeta(~user=alice) round-trips Some(alice)", () => {
    let meta = Message$ReventlessCore.generateMeta("svc", undefined, "alice", undefined, undefined, undefined, undefined, undefined);
    let json = Util_Sury$Reventless.toJson(meta, Message$ReventlessCore.metaSchema);
    let decoded = Util_Sury$Reventless.fromJson(json, Message$ReventlessCore.metaSchema);
    return Jest.Expect.toEqual(Jest.Expect.expect(decoded.user), "alice");
  });
  Jest.test("all new optional fields round-trip when set", () => {
    let headers = Object.fromEntries([
      [
        "tenantId",
        "t1"
      ],
      [
        "feature.x",
        "on"
      ]
    ]);
    let meta = Message$ReventlessCore.generateMeta("svc", "10.0.0.1", "bob", "parent-msg-id", "00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01", "root-msg-id", "1.2.0", headers);
    let json = Util_Sury$Reventless.toJson(meta, Message$ReventlessCore.metaSchema);
    let decoded = Util_Sury$Reventless.fromJson(json, Message$ReventlessCore.metaSchema);
    Jest.Expect.toEqual(Jest.Expect.expect(decoded.ip), "10.0.0.1");
    Jest.Expect.toEqual(Jest.Expect.expect(decoded.user), "bob");
    Jest.Expect.toEqual(Jest.Expect.expect(decoded.causationId), "parent-msg-id");
    Jest.Expect.toEqual(Jest.Expect.expect(decoded.traceparent), "00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01");
    Jest.Expect.toBe(Jest.Expect.expect(decoded.correlationId), "root-msg-id");
    Jest.Expect.toEqual(Jest.Expect.expect(decoded.schemaVersion), "1.2.0");
    return Jest.Expect.toEqual(Jest.Expect.expect(Stdlib_Option.getOr(decoded.headers, {})["tenantId"]), "t1");
  });
});

Jest.describe("Message.deriveMeta:", () => {
  Jest.test("emits a new msgId distinct from the parent", () => {
    let parent = Message$ReventlessCore.generateMeta("svc", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let child = Message$ReventlessCore.deriveMeta(parent, undefined);
    return Jest.Expect.toBe(Jest.Expect.expect(child.msgId === parent.msgId), false);
  });
  Jest.test("sets causationId = parent.msgId", () => {
    let parent = Message$ReventlessCore.generateMeta("svc", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let child = Message$ReventlessCore.deriveMeta(parent, undefined);
    return Jest.Expect.toEqual(Jest.Expect.expect(child.causationId), parent.msgId);
  });
  Jest.test("inherits correlationId (chain-root id stays stable)", () => {
    let root = Message$ReventlessCore.generateMeta("svc", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let mid = Message$ReventlessCore.deriveMeta(root, undefined);
    let leaf = Message$ReventlessCore.deriveMeta(mid, undefined);
    Jest.Expect.toBe(Jest.Expect.expect(mid.correlationId), root.correlationId);
    Jest.Expect.toBe(Jest.Expect.expect(leaf.correlationId), root.correlationId);
    return Jest.Expect.toEqual(Jest.Expect.expect(leaf.causationId), mid.msgId);
  });
  Jest.test("inherits ip/user/traceparent/schemaVersion/headers from parent", () => {
    let parent = Message$ReventlessCore.generateMeta("svc", "10.0.0.1", "alice", undefined, "tp", undefined, "1", Object.fromEntries([[
        "k",
        "v"
      ]]));
    let child = Message$ReventlessCore.deriveMeta(parent, undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(child.ip), "10.0.0.1");
    Jest.Expect.toEqual(Jest.Expect.expect(child.user), "alice");
    Jest.Expect.toEqual(Jest.Expect.expect(child.traceparent), "tp");
    Jest.Expect.toEqual(Jest.Expect.expect(child.schemaVersion), "1");
    return Jest.Expect.toEqual(Jest.Expect.expect(Stdlib_Option.getOr(child.headers, {})["k"]), "v");
  });
  Jest.test("~service overrides parent's service; defaults to parent's service", () => {
    let parent = Message$ReventlessCore.generateMeta("parent-svc", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let inherited = Message$ReventlessCore.deriveMeta(parent, undefined);
    let overridden = Message$ReventlessCore.deriveMeta(parent, "child-svc");
    Jest.Expect.toBe(Jest.Expect.expect(inherited.service), "parent-svc");
    return Jest.Expect.toBe(Jest.Expect.expect(overridden.service), "child-svc");
  });
});

Jest.describe("StoredEvent encode/decode round-trip:", () => {
  Jest.test("aggregate-style record (no tags) round-trips through the flat-dict bridge", () => {
    let meta = Message$ReventlessCore.generateMeta("catalog", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let stored_data = Object.fromEntries([
      [
        "itemId",
        "agg-1"
      ],
      [
        "name",
        "Widget"
      ]
    ]);
    let stored = {
      id: "agg-1",
      position: "000000001",
      event: "ItemCreated",
      data: stored_data,
      meta: meta,
      recordedAt: "2026-05-13T10:00:00.000Z"
    };
    let flat = Message$ReventlessCore.storedEventToFlatJson(stored, S.string);
    let roundTrip = Message$ReventlessCore.flatJsonToStoredEvent(flat, S.string);
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.id), "agg-1");
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.position), "000000001");
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.event), "ItemCreated");
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.recordedAt), "2026-05-13T10:00:00.000Z");
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.meta.msgId), meta.msgId);
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.meta.correlationId), meta.correlationId);
    return Jest.Expect.toEqual(Jest.Expect.expect(roundTrip.tags), undefined);
  });
  Jest.test("DCB-style record (with tags) round-trips and preserves the tag list", () => {
    let meta = Message$ReventlessCore.generateMeta("catalog", undefined, undefined, "cmd-1", undefined, undefined, undefined, undefined);
    let stored_data = Object.fromEntries([[
        "productId",
        "p-1"
      ]]);
    let stored_tags = [
      {
        key: "productId",
        value: "p-1"
      },
      {
        key: "categoryId",
        value: "c-1"
      }
    ];
    let stored = {
      id: "productId:p-1",
      position: "1736782800000-abc-uuid",
      event: "ProductAdded",
      data: stored_data,
      meta: meta,
      recordedAt: "2026-05-13T10:00:00.000Z",
      tags: stored_tags
    };
    let flat = Message$ReventlessCore.storedEventToFlatJson(stored, S.string);
    let roundTrip = Message$ReventlessCore.flatJsonToStoredEvent(flat, S.string);
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.id), "productId:p-1");
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.position), "1736782800000-abc-uuid");
    Jest.Expect.toBe(Jest.Expect.expect(roundTrip.event), "ProductAdded");
    Jest.Expect.toEqual(Jest.Expect.expect(roundTrip.meta.causationId), "cmd-1");
    let tags = Stdlib_Option.getOrThrow(roundTrip.tags, undefined);
    Jest.Expect.toBe(Jest.Expect.expect(tags.length), 2);
    let first = tags[0];
    Jest.Expect.toBe(Jest.Expect.expect(first.key), "productId");
    return Jest.Expect.toBe(Jest.Expect.expect(first.value), "p-1");
  });
  Jest.test("meta is flattened to top-level keys in the on-disk JSON (DynamoDB GSI projectability)", () => {
    let meta = Message$ReventlessCore.generateMeta("catalog", undefined, undefined, undefined, undefined, undefined, undefined, undefined);
    let stored_data = {};
    let stored = {
      id: "agg-1",
      position: "000000001",
      event: "ItemCreated",
      data: stored_data,
      meta: meta,
      recordedAt: "2026-05-13T10:00:00.000Z"
    };
    let flat = Message$ReventlessCore.storedEventToFlatJson(stored, S.string);
    let obj = Stdlib_Option.getOrThrow(Stdlib_JSON.Decode.object(flat), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["meta"]), undefined);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["service"]), "catalog");
    Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.isSome(obj["msgId"])), true);
    Jest.Expect.toEqual(Jest.Expect.expect(obj["position"]), "000000001");
    Jest.Expect.toEqual(Jest.Expect.expect(obj["event"]), "ItemCreated");
    return Jest.Expect.toEqual(Jest.Expect.expect(obj["recordedAt"]), "2026-05-13T10:00:00.000Z");
  });
});

/*  Not a pure module */
