// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stream from "effect/Stream";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let moduleUrl = import.meta.url;

let stateSchema = S.schema(s => ({
  name: s.m(S.string),
  count: s.m(S.int)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let ItemQueryDbSpec = {
  Id: undefined,
  name: "TestItemQueryDb",
  moduleUrl: moduleUrl,
  stateSchema: stateSchema,
  config: config,
  subIdConfig: undefined,
  authorization: "AllowAuthenticated",
  visibility: "Public"
};

let store = {
  contents: {}
};

let failNextWrite = {
  contents: false
};

async function mockJsonOps_load(id) {
  return {
    TAG: "Ok",
    _0: Stdlib_Option.getOr(store.contents[id], [])
  };
}

function mockJsonOps_loadStream(id) {
  return Stream.fromIterable(Stdlib_Option.getOr(store.contents[id], []));
}

async function mockJsonOps_save(id, state, _saveMode, _ttl) {
  if (failNextWrite.contents) {
    failNextWrite.contents = false;
    return {
      TAG: "Error",
      _0: {
        TAG: "NotSavedToStorage",
        _0: "mock failure"
      }
    };
  } else {
    store.contents[id] = [state];
    return {
      TAG: "Ok",
      _0: undefined
    };
  }
}

async function mockJsonOps_saveBatch(batch) {
  if (failNextWrite.contents) {
    failNextWrite.contents = false;
    return {
      TAG: "Error",
      _0: {
        TAG: "BatchNotFullyWrittenToStorage",
        _0: "mock failure"
      }
    };
  } else {
    batch.forEach(param => {
      store.contents[param[0]] = [param[1]];
    });
    return {
      TAG: "Ok",
      _0: undefined
    };
  }
}

async function mockJsonOps_count(_id, _field, inc) {
  return {
    TAG: "Ok",
    _0: inc
  };
}

async function mockJsonOps_delete(id, _subId) {
  Stdlib_Dict.$$delete(store.contents, id);
  return {
    TAG: "Ok",
    _0: undefined
  };
}

async function mockJsonOps_deleteBatch(ids) {
  ids.forEach(param => Stdlib_Dict.$$delete(store.contents, param[0]));
  return {
    TAG: "Ok",
    _0: undefined
  };
}

let mockJsonOps = {
  load: mockJsonOps_load,
  loadStream: mockJsonOps_loadStream,
  save: mockJsonOps_save,
  saveBatch: mockJsonOps_saveBatch,
  count: mockJsonOps_count,
  delete: mockJsonOps_delete,
  deleteBatch: mockJsonOps_deleteBatch
};

function reset() {
  store.contents = {};
  failNextWrite.contents = false;
}

export {
  ItemQueryDbSpec,
  store,
  failNextWrite,
  mockJsonOps,
  reset,
}
/* moduleUrl Not a pure module */
