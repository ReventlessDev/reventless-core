// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Effect from "effect";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

S.enableJson();

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "ItemCreated",
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ItemDeleted",
    id: s.m(S.string)
  }))
]);

let ItemEventLogSpec = {
  Id: undefined,
  name: "TestItemEventLog",
  eventSchema: eventSchema
};

let storedEvents = {
  contents: {}
};

let failNextAppend = {
  contents: false
};

let failNextAppendsWithTransient = {
  contents: 0
};

let appendCallCount = {
  contents: 0
};

async function mockStorage_append(_seqNr, id, jsons) {
  appendCallCount.contents = appendCallCount.contents + 1 | 0;
  if (failNextAppend.contents) {
    failNextAppend.contents = false;
    return {
      TAG: "Error",
      _0: "mock storage failure"
    };
  }
  if (failNextAppendsWithTransient.contents > 0) {
    failNextAppendsWithTransient.contents = failNextAppendsWithTransient.contents - 1 | 0;
    return {
      TAG: "Error",
      _0: "ThrottlingException: Rate exceeded"
    };
  }
  let existing = Stdlib_Option.getOr(storedEvents.contents[id], []);
  storedEvents.contents[id] = existing.concat(jsons);
  return {
    TAG: "Ok",
    _0: undefined
  };
}

async function mockStorage_replay(id) {
  return Stdlib_Option.getOr(storedEvents.contents[id], []);
}

function mockStorage_replayStream(id) {
  return Effect.Stream.fromIterable(Stdlib_Option.getOr(storedEvents.contents[id], []));
}

let mockStorage = {
  append: mockStorage_append,
  replay: mockStorage_replay,
  replayStream: mockStorage_replayStream
};

let capturedPublishes = {
  contents: []
};

function make(param, param$1, param$2) {
  return 0;
}

let MockEventTopic_Spec = {
  Id: Id$Reventless.StringPure,
  eventSchema: eventSchema
};

let MockEventTopic = {
  Spec: MockEventTopic_Spec,
  make: make
};

async function mockEventTopicOps_publish(events) {
  capturedPublishes.contents = capturedPublishes.contents.concat(events);
}

async function mockEventTopicOps_publishJson(_service, _meta, _json) {
  
}

let mockEventTopicOps = {
  publish: mockEventTopicOps_publish,
  publishJson: mockEventTopicOps_publishJson
};

let testMeta = {
  service: "test",
  time: "2024-01-01T00:00:00.000Z",
  ip: "127.0.0.1",
  user: "testuser",
  msgId: "msg-001",
  correlationId: "corr-001"
};

function makeEvent$p(id, event) {
  return {
    id: id,
    meta: testMeta,
    event: event
  };
}

function reset() {
  storedEvents.contents = {};
  failNextAppend.contents = false;
  failNextAppendsWithTransient.contents = 0;
  appendCallCount.contents = 0;
  capturedPublishes.contents = [];
}

export {
  ItemEventLogSpec,
  storedEvents,
  failNextAppend,
  failNextAppendsWithTransient,
  appendCallCount,
  mockStorage,
  capturedPublishes,
  MockEventTopic,
  mockEventTopicOps,
  testMeta,
  makeEvent$p,
  reset,
}
/*  Not a pure module */
