// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as Logger$ReventlessCore from "../../util/Logger.res.mjs";

function Make(Spec) {
  let handleJsonEvents = stream => Effect.Stream.runDrain(Effect.Stream.mapEffect(stream, eventJson$p => Effect.Effect.promise(async () => {
    Logger$ReventlessCore.logJsonEvent(undefined, undefined, eventJson$p, `Core handleJsonEvents: outgoing event:`);
    await Promise.all(Spec.outgoingExtensionPointJsonEventsHandlers.map(handleEvent => handleEvent(eventJson$p, Spec.pluginDefinition)));
  })));
  return {
    handleJsonEvents: handleJsonEvents
  };
}

export {
  Make,
}
/* effect Not a pure module */
