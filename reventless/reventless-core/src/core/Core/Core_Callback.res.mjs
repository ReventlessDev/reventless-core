// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Logger$Reventless from "../../util/Logger.res.mjs";
import * as Util_Promise$Reventless from "../../util/Util_Promise.res.mjs";

function Make(Spec) {
  let handleJsonEvents = eventsJson$p => {
    let count = eventsJson$p.length;
    return Util_Promise$Reventless.toUnit(Promise.all(eventsJson$p.map(async (eventJson$p, idx) => {
      let idx$1 = idx + 1 | 0;
      Logger$Reventless.logJsonEvent(undefined, undefined, eventJson$p, `Core eventHandler: outgoing event ` + idx$1.toString() + `/` + count.toString() + `:`);
      return Util_Promise$Reventless.toUnit(Promise.all(Spec.outgoingExtensionPointEventHandlers.map(handleEvent => handleEvent(eventJson$p, Spec.pluginDefinition))));
    })));
  };
  return {
    handleJsonEvents: handleJsonEvents
  };
}

export {
  Make,
}
/* Logger-Reventless Not a pure module */
