// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Logger$ReventlessCore from "../../util/Logger.res.mjs";
import * as Util_Promise$ReventlessCore from "../../util/Util_Promise.res.mjs";

function Make(Spec) {
  let handleJsonEvents = eventsJson$p => {
    let count = eventsJson$p.length;
    return Util_Promise$ReventlessCore.toUnit(Promise.all(eventsJson$p.map(async (eventJson$p, idx) => {
      let idx$1 = idx + 1 | 0;
      Logger$ReventlessCore.logJsonEvent(undefined, undefined, eventJson$p, `Core eventHandler: outgoing event ` + idx$1.toString() + `/` + count.toString() + `:`);
      return Util_Promise$ReventlessCore.toUnit(Promise.all(Spec.outgoingExtensionPointEventHandlers.map(handleEvent => handleEvent(eventJson$p, Spec.pluginDefinition))));
    })));
  };
  return {
    handleJsonEvents: handleJsonEvents
  };
}

export {
  Make,
}
/* Logger-ReventlessCore Not a pure module */
