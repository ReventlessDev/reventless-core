// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Api_Naming$ReventlessCore from "../../components/Api/Api_Naming.res.mjs";
import * as PluginBaseFragment$ReventlessCore from "./PluginBaseFragment.res.mjs";
import * as GraphQL_FragmentGenerator$ReventlessCore from "../../components/Api/GraphQL_FragmentGenerator.res.mjs";

let cloneArgsSchema = S.schema(s => ({
  restoreDateTime: s.m(S.option(S.string))
}));

let mutationEntries = PluginBaseFragment$ReventlessCore.mutationEntries.concat([{
    fieldNames: [Api_Naming$ReventlessCore.coreField("Clone")],
    commandSchema: cloneArgsSchema,
    description: "Clone the system to a specific point in time"
  }]);

let baseFragment = GraphQL_FragmentGenerator$ReventlessCore.generate(mutationEntries, PluginBaseFragment$ReventlessCore.queryEntries);

function generateFragment(dcbMutationEntries, dcbQueryEntries, dcbEventLogEntries) {
  let allMutationEntries = mutationEntries.concat(dcbMutationEntries);
  let allQueryEntries = PluginBaseFragment$ReventlessCore.queryEntries.concat(dcbQueryEntries);
  return GraphQL_FragmentGenerator$ReventlessCore.generate(allMutationEntries, allQueryEntries);
}

let queryEntries = PluginBaseFragment$ReventlessCore.queryEntries;

export {
  cloneArgsSchema,
  mutationEntries,
  queryEntries,
  baseFragment,
  generateFragment,
}
/* cloneArgsSchema Not a pure module */
