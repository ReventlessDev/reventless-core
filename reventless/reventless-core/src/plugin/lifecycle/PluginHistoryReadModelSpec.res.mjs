// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Plugin$Reventless from "@reventlessdev/reventless-spec/src/components/Plugin.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let transitionSchema = S.union([
  S.literal("Detected"),
  S.literal("Connected"),
  S.literal("Superseded"),
  S.literal("Promoted"),
  S.literal("Disconnected"),
  S.literal("Activated"),
  S.literal("Deactivated"),
  S.literal("Retired"),
  S.literal("IncompatibleDetected")
]);

let stateSchema = S.schema(s => ({
  name: s.m(Plugin$Reventless.nameSchema),
  version: s.m(Plugin$Reventless.versionSchema),
  transitionKey: s.m(S.string),
  transition: s.m(transitionSchema),
  transitionAt: s.m(S.string),
  by: s.m(S.string),
  supersededVersion: s.m(S.option(Plugin$Reventless.versionSchema))
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let subIdConfig = {
  subIdField: "transitionKey",
  getSubId: s => s.transitionKey
};

let name = "PluginHistory";

let Id;

let moduleUrl = "@reventlessdev/reventless-core/src/plugin/lifecycle/PluginHistoryReadModelSpec.res.mjs";

let authorization = "AllowAuthenticated";

let visibility = "Internal";

export {
  name,
  Id,
  transitionSchema,
  stateSchema,
  config,
  subIdConfig,
  moduleUrl,
  authorization,
  visibility,
}
/* transitionSchema Not a pure module */
