// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Int from "@rescript/runtime/lib/es6/Stdlib_Int.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function makeId(name, version) {
  return name + `@` + version;
}

function name(id) {
  return Stdlib_Option.getOr(id.split("@")[0], id);
}

function version(id) {
  return Stdlib_Option.getOr(id.split("@")[1], "");
}

function compareVersions(a, b) {
  let parts = s => s.replace(/[-+]/g, ".").split(".");
  let pa = parts(a);
  let pb = parts(b);
  let len = pa.length > pb.length ? pa.length : pb.length;
  let result = 0;
  let i = 0;
  while (result === 0 && i < len) {
    let sa = Stdlib_Option.getOr(pa[i], "");
    let sb = Stdlib_Option.getOr(pb[i], "");
    let match = Stdlib_Int.fromString(sa, undefined);
    let match$1 = Stdlib_Int.fromString(sb, undefined);
    let exit = 0;
    if (match !== undefined && match$1 !== undefined) {
      if (match > match$1) {
        result = 1;
      } else if (match < match$1) {
        result = -1;
      }
    } else {
      exit = 1;
    }
    if (exit === 1) {
      if (sa > sb) {
        result = 1;
      } else if (sa < sb) {
        result = -1;
      }
    }
    i = i + 1 | 0;
  };
  return result;
}

let componentType = "Plugin";

export {
  componentType,
  makeId,
  name,
  version,
  compareVersions,
}
/* No side effect */
