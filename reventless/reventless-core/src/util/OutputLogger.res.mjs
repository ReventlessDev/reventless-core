// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Pulumi from "@pulumi/pulumi";
import * as Logger$ReventlessCore from "./Logger.res.mjs";

function logOutput(loc, map, stringify, level, desc, output) {
  if (Pulumi.Output.isInstance(output)) {
    output.apply(item => Logger$ReventlessCore.log(loc, map, stringify, level, desc, item));
    return;
  }
  let itemType = typeof output;
  Logger$ReventlessCore.log(loc, map, stringify, "Error", desc + (" ~}> was expected to be a Pulumi.Output.t, but is " + (itemType + "!")), output);
}

export {
  logOutput,
}
/* @pulumi/pulumi Not a pure module */
