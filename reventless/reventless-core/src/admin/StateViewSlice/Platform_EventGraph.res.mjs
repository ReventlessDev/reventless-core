// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Plugin$Reventless from "@reventlessdev/reventless-spec/src/components/Plugin.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "Connected",
    _0: s.m(Plugin$Reventless.pluginDefinitionSchema)
  })),
  S.schema(s => ({
    TAG: "Reconnected",
    _0: s.m(Plugin$Reventless.pluginDefinitionSchema)
  })),
  S.schema(s => ({
    TAG: "Disconnected",
    _0: s.m(Plugin$Reventless.pluginDefinitionSchema)
  })),
  S.schema(s => ({
    TAG: "Activated",
    _0: s.m(Plugin$Reventless.pluginDefinitionSchema)
  })),
  S.schema(s => ({
    TAG: "Deactivated",
    _0: s.m(Plugin$Reventless.pluginDefinitionSchema)
  }))
]);

let stateSchema = S.schema(s => ({
  pluginName: s.m(S.string),
  nodes: s.m(S.array(Plugin$Reventless.graphNodeSchema)),
  edges: s.m(S.array(Plugin$Reventless.graphEdgeSchema))
}));

function nodeFor(pluginName, componentName, kind) {
  return {
    pluginName: pluginName,
    componentName: componentName,
    kind: kind
  };
}

function writeKind(structure, name) {
  if (structure.aggregates.some(a => a.name === name)) {
    return "Aggregate";
  } else {
    return "StateChangeSlice";
  }
}

function nodesFromStructure(pluginName, structure) {
  return [
    structure.aggregates.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "Aggregate"
    })),
    structure.stateChangeSlices.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "StateChangeSlice"
    })),
    structure.stateViewSlices.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "StateViewSlice"
    })),
    structure.readModels.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "ReadModel"
    })),
    structure.automationSlices.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "AutomationSlice"
    })),
    structure.outboundTranslationSlices.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "OutboundTranslationSlice"
    })),
    structure.inboundTranslationSlices.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "InboundTranslationSlice"
    })),
    structure.extensions.map(param => ({
      pluginName: pluginName,
      componentName: param.name,
      kind: "Extension"
    }))
  ].flat();
}

function edgesFromStructure(pluginName, structure) {
  let writeToView = structure.aggregates.map(w => [
    w,
    "Aggregate"
  ]).concat(structure.stateChangeSlices.map(w => [
    w,
    "StateChangeSlice"
  ])).flatMap(param => {
    let kind = param[1];
    let w = param[0];
    return w.linkedViews.map(viewName => ({
      source: {
        pluginName: pluginName,
        componentName: w.name,
        kind: kind
      },
      target: {
        pluginName: pluginName,
        componentName: viewName,
        kind: "StateViewSlice"
      },
      mechanism: "EventTypeMatch",
      viaEvents: w.producedEventTypes,
      implicit: false
    }));
  });
  let autoToTarget = structure.automationSlices.map(a => ({
    source: {
      pluginName: pluginName,
      componentName: a.name,
      kind: "AutomationSlice"
    },
    target: {
      pluginName: pluginName,
      componentName: a.targetName,
      kind: writeKind(structure, a.targetName)
    },
    mechanism: "AutomationSlice",
    viaEvents: a.producedCommandTypes,
    implicit: false
  }));
  let inboundToTarget = structure.inboundTranslationSlices.map(its => ({
    source: {
      pluginName: pluginName,
      componentName: its.name,
      kind: "InboundTranslationSlice"
    },
    target: {
      pluginName: pluginName,
      componentName: its.targetName,
      kind: writeKind(structure, its.targetName)
    },
    mechanism: "InboundTranslation",
    viaEvents: its.commandTypes,
    implicit: false
  }));
  let extensionToDelegate = structure.extensions.flatMap(ext => ext.delegateNames.map(delegateName => ({
    source: {
      pluginName: pluginName,
      componentName: ext.name,
      kind: "Extension"
    },
    target: {
      pluginName: pluginName,
      componentName: delegateName,
      kind: writeKind(structure, delegateName)
    },
    mechanism: "Extension",
    viaEvents: ext.commandTypes,
    implicit: false
  })));
  return [
    writeToView,
    autoToTarget,
    inboundToTarget,
    extensionToDelegate
  ].flat();
}

function buildEntry(pluginName, structure) {
  return {
    pluginName: pluginName,
    nodes: nodesFromStructure(pluginName, structure),
    edges: edgesFromStructure(pluginName, structure)
  };
}

function project(event) {
  let exit = 0;
  let name;
  let structure;
  let name$1;
  switch (event.TAG) {
    case "Connected" :
      let match = event._0;
      let name$2 = match.name;
      let structure$1 = match.structure;
      if (structure$1 !== undefined) {
        name = name$2;
        structure = structure$1;
        exit = 1;
      } else {
        name$1 = name$2;
        exit = 2;
      }
      break;
    case "Reconnected" :
      let match$1 = event._0;
      let name$3 = match$1.name;
      let structure$2 = match$1.structure;
      if (structure$2 !== undefined) {
        name = name$3;
        structure = structure$2;
        exit = 1;
      } else {
        name$1 = name$3;
        exit = 2;
      }
      break;
    case "Activated" :
      let match$2 = event._0;
      let name$4 = match$2.name;
      let structure$3 = match$2.structure;
      if (structure$3 !== undefined) {
        name = name$4;
        structure = structure$3;
        exit = 1;
      } else {
        name$1 = name$4;
        exit = 2;
      }
      break;
    case "Disconnected" :
    case "Deactivated" :
      return [{
          TAG: "Delete",
          _0: event._0.name
        }];
  }
  switch (exit) {
    case 1 :
      return [{
          TAG: "Set",
          _0: name,
          _1: buildEntry(name, structure)
        }];
    case 2 :
      return [{
          TAG: "Set",
          _0: name$1,
          _1: {
            pluginName: name$1,
            nodes: [],
            edges: []
          }
        }];
  }
}

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "PlatformEventGraph";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/reventless-core/src/admin/StateViewSlice/Platform_EventGraph.res.mjs";

export {
  name,
  Id,
  consumedEventSchema,
  stateSchema,
  nodeFor,
  writeKind,
  nodesFromStructure,
  edgesFromStructure,
  buildEntry,
  project,
  config,
  subIdConfig,
  moduleUrl,
}
/* consumedEventSchema Not a pure module */
