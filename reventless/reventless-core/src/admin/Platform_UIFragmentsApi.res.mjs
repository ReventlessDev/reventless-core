// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let sdlTypes = [
  `type Platform_UIPanel {\n  fragmentId: String!\n  title: String!\n  description: String!\n  positions: [String!]!\n  requiredAccess: String\n}`,
  `type Platform_UIMenuEntry {\n  label: String!\n  icon: String\n  group: String\n  sortOrder: Int!\n}`,
  `type Platform_UIPage {\n  fragmentId: String!\n  title: String!\n  menuEntry: Platform_UIMenuEntry!\n  requiredAccess: String\n}`,
  `type Platform_UIFragmentEntry {\n  pluginId: String!\n  remoteEntryUrl: String!\n  panels: [Platform_UIPanel!]!\n  pages: [Platform_UIPage!]!\n  registeredAt: String!\n  updatedAt: String!\n}`
];

function encodeStrings(ss) {
  return ss.map(prim => prim);
}

function encodePanel(p) {
  return Object.fromEntries([
    [
      "fragmentId",
      p.fragmentId
    ],
    [
      "title",
      p.title
    ],
    [
      "description",
      p.description
    ],
    [
      "positions",
      p.positions.map(prim => prim)
    ],
    [
      "requiredAccess",
      Stdlib_Option.mapOr(p.requiredAccess, null, prim => prim)
    ]
  ]);
}

function encodeMenuEntry(m) {
  return Object.fromEntries([
    [
      "label",
      m.label
    ],
    [
      "icon",
      Stdlib_Option.mapOr(m.icon, null, prim => prim)
    ],
    [
      "group",
      Stdlib_Option.mapOr(m.group, null, prim => prim)
    ],
    [
      "sortOrder",
      m.sortOrder
    ]
  ]);
}

function encodePage(p) {
  return Object.fromEntries([
    [
      "fragmentId",
      p.fragmentId
    ],
    [
      "title",
      p.title
    ],
    [
      "menuEntry",
      encodeMenuEntry(p.menuEntry)
    ],
    [
      "requiredAccess",
      Stdlib_Option.mapOr(p.requiredAccess, null, prim => prim)
    ]
  ]);
}

function encodeUIFragmentEntry(entry) {
  return Object.fromEntries([
    [
      "pluginId",
      entry.pluginId
    ],
    [
      "remoteEntryUrl",
      entry.remoteEntryUrl
    ],
    [
      "panels",
      entry.panels.map(encodePanel)
    ],
    [
      "pages",
      entry.pages.map(encodePage)
    ],
    [
      "registeredAt",
      entry.registeredAt
    ],
    [
      "updatedAt",
      entry.updatedAt
    ]
  ]);
}

let sdlQueryField = `  Platform_UIFragments: [Platform_UIFragmentEntry!]!`;

export {
  sdlTypes,
  sdlQueryField,
  encodeStrings,
  encodePanel,
  encodeMenuEntry,
  encodePage,
  encodeUIFragmentEntry,
}
/* No side effect */
