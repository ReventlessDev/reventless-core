// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as LogFormat$ReventlessCore from "../util/LogFormat.res.mjs";

function Make(Spec) {
  let handleJsonEvents = stream => Effect.Stream.runDrain(Effect.Stream.mapEffect(stream, eventJson$p => Effect.Effect.zipRight(Effect.Effect.logInfo(`Admin handleJsonEvents: outgoing event: ` + LogFormat$ReventlessCore.event$pJsonToLogMessage(eventJson$p)), Effect.Effect.map(Effect.Effect.all(Spec.outgoingExtensionPointJsonEventsHandlers.map(handleEvent => Effect.Effect.promise(() => handleEvent(eventJson$p, Spec.pluginDefinition))), {
    concurrency: "unbounded"
  }), param => {}))));
  return {
    handleJsonEvents: handleJsonEvents
  };
}

export {
  Make,
}
/* effect Not a pure module */
