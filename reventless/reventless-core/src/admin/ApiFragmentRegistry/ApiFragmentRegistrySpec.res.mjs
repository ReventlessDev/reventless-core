// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Plugin$Reventless from "@reventlessdev/reventless-spec/src/components/Plugin.res.mjs";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";

let fragmentSnapshotEntrySchema = S.schema(s => ({
  pluginId: s.m(S.string),
  encoded: s.m(S.string),
  protocol: s.m(S.string),
  apiTarget: s.m(Plugin$Reventless.apiTargetSchema)
}));

let commandSchema = S.union([
  S.schema(s => ({
    TAG: "RegisterApiFragment",
    pluginId: s.m(S.string),
    fragment: s.m(Plugin$Reventless.apiSchemaFragmentSchema),
    apiTarget: s.m(Plugin$Reventless.apiTargetSchema),
    at: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "DeregisterApiFragment",
    pluginId: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "RecordApiFragmentPush",
    pluginId: s.m(S.string),
    ok: s.m(S.bool),
    message: s.m(S.string),
    at: s.m(S.string)
  }))
]);

let errorSchema = S.literal("RegistryError");

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "ApiFragmentRegistered",
    pluginId: s.m(S.string),
    fragment: s.m(Plugin$Reventless.apiSchemaFragmentSchema),
    apiTarget: s.m(Plugin$Reventless.apiTargetSchema),
    at: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ApiFragmentUpdated",
    pluginId: s.m(S.string),
    previousFragment: s.m(Plugin$Reventless.apiSchemaFragmentSchema),
    newFragment: s.m(Plugin$Reventless.apiSchemaFragmentSchema),
    apiTarget: s.m(Plugin$Reventless.apiTargetSchema),
    at: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ApiFragmentDeregistered",
    pluginId: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ApiFragmentPushRecorded",
    pluginId: s.m(S.string),
    ok: s.m(S.bool),
    message: s.m(S.string),
    at: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ApiSchemaComputed",
    snapshot: s.m(S.array(fragmentSnapshotEntrySchema))
  }))
]);

let commandSchema$1 = Api$ReventlessInfra.markNoApiVariants(commandSchema, ["RecordApiFragmentPush"]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

let name = "ApiFragmentRegistry";

let Id;

let moduleUrl = "@reventlessdev/reventless-core/src/admin/ApiFragmentRegistry/ApiFragmentRegistrySpec.res.mjs";

export {
  name,
  Id,
  fragmentSnapshotEntrySchema,
  errorSchema,
  eventSchema,
  commandSchema$1 as commandSchema,
  moduleUrl,
  commandAuthorization,
}
/* fragmentSnapshotEntrySchema Not a pure module */
