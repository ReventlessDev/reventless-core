// Generated by ReScript, PLEASE EDIT WITH CARE


function project(event) {
  switch (event.TAG) {
    case "ApiFragmentRegistered" :
      let at = event.at;
      let fragment = event.fragment;
      let pluginId = event.pluginId;
      return [{
          TAG: "Set",
          _0: pluginId,
          _1: {
            pluginId: pluginId,
            encoded: fragment.encoded,
            protocol: fragment.protocol,
            registeredAt: at,
            updatedAt: at,
            pushStatus: "pending",
            pushMessage: "",
            pushedAt: ""
          }
        }];
    case "ApiFragmentUpdated" :
      let at$1 = event.at;
      let newFragment = event.newFragment;
      return [{
          TAG: "Update",
          _0: event.pluginId,
          _1: state => ({
            pluginId: state.pluginId,
            encoded: newFragment.encoded,
            protocol: newFragment.protocol,
            registeredAt: state.registeredAt,
            updatedAt: at$1,
            pushStatus: "pending",
            pushMessage: "",
            pushedAt: state.pushedAt
          })
        }];
    case "ApiFragmentDeregistered" :
      return [{
          TAG: "Delete",
          _0: event.pluginId
        }];
    case "ApiFragmentPushRecorded" :
      let at$2 = event.at;
      let message = event.message;
      let ok = event.ok;
      return [{
          TAG: "Update",
          _0: event.pluginId,
          _1: state => ({
            pluginId: state.pluginId,
            encoded: state.encoded,
            protocol: state.protocol,
            registeredAt: state.registeredAt,
            updatedAt: state.updatedAt,
            pushStatus: ok ? "ok" : "error",
            pushMessage: message,
            pushedAt: at$2
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/reventless-core/src/admin/ApiFragmentRegistry/StateViewSlice/ApiFragments_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
