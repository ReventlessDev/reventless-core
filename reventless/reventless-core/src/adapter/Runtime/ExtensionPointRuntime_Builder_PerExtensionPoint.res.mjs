// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "../../components/Component.res.mjs";
import * as CommandTopic$ReventlessCore from "../../components/CommandTopic/CommandTopic.res.mjs";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";

function Make(RuntimeEnvironment) {
  return CommandTopicChannel => {
    let forCommandTopic = (handler, connect, memorySizeOpt, timeoutOpt, commandTopic) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$ReventlessCore.toPulumiResource(commandTopic);
      return connect(RuntimeEnvironment.make(ComponentType$ReventlessCore.nameOpt(resource.__name, CommandTopic$ReventlessCore.componentType), handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, {
        parent: resource
      }));
    };
    return {
      CommandTopicChannel: CommandTopicChannel,
      forCommandTopic: forCommandTopic
    };
  };
}

export {
  Make,
}
/* Component-ReventlessCore Not a pure module */
