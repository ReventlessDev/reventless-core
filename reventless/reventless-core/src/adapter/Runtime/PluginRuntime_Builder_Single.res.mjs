// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Runtime$ReventlessCore from "./Runtime.res.mjs";
import * as Component$ReventlessCore from "../../components/Component.res.mjs";
import * as Heartbeat$ReventlessCore from "../../components/Heartbeat/Heartbeat.res.mjs";
import * as CommandTopic$ReventlessCore from "../../components/CommandTopic/CommandTopic.res.mjs";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";
import * as EventCollector$ReventlessCore from "../../components/EventCollector/EventCollector.res.mjs";

function Make(RuntimeEnvironment) {
  return EventCollectorChannel => {
    let forPluginEventCollector = (handler, eventTopics, resources, memorySizeOpt, timeoutOpt, eventCollector) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$ReventlessCore.toPulumiResource(eventCollector);
      let name = ComponentType$ReventlessCore.nameOpt(resource.__name, EventCollector$ReventlessCore.componentType);
      let opts_parent = resource;
      let opts = {
        parent: opts_parent
      };
      let runtime = RuntimeEnvironment.make(name, handler.apply(handler => Runtime$ReventlessCore.runEffectHandler(RuntimeEnvironment.asEffectHandler(handler))), memorySize, timeout, {
        parent: resource
      });
      EventCollectorChannel.connect(name, [{
          channel: eventCollector.channel,
          eventTopics: eventTopics,
          resources: resources
        }], runtime, opts);
    };
    let forPluginHeartbeat = (handler, connect, memorySizeOpt, timeoutOpt, heartbeat) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$ReventlessCore.toPulumiResource(heartbeat);
      return connect(RuntimeEnvironment.make(ComponentType$ReventlessCore.nameOpt(resource.__name, Heartbeat$ReventlessCore.componentType), handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, {
        parent: resource
      }));
    };
    let registerPluginName = param => {};
    let forDcbCommandTopic = (handler, connect, memorySizeOpt, timeoutOpt, dcbCommandTopic) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$ReventlessCore.toPulumiResource(dcbCommandTopic);
      return connect(RuntimeEnvironment.make(ComponentType$ReventlessCore.nameOpt(resource.__name, CommandTopic$ReventlessCore.componentType), handler.apply(handler => Runtime$ReventlessCore.runEffectHandler(RuntimeEnvironment.asEffectHandler(handler))), memorySize, timeout, {
        parent: resource
      }));
    };
    let finish = () => {};
    return {
      EventCollectorChannel: EventCollectorChannel,
      registerPluginName: registerPluginName,
      forPluginEventCollector: forPluginEventCollector,
      forPluginHeartbeat: forPluginHeartbeat,
      forDcbCommandTopic: forDcbCommandTopic,
      finish: finish
    };
  };
}

export {
  Make,
}
/* Runtime-ReventlessCore Not a pure module */
