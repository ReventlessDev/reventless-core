// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "../Component.res.mjs";
import * as Scheduler$ReventlessCore from "./Scheduler.res.mjs";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";

function Make(ScheduledPublisher) {
  let construct = (self, name) => {
    let opts_parent = Component$ReventlessCore.toPulumiResource(self);
    let opts = {
      parent: opts_parent
    };
    let scheduledPublisher = ScheduledPublisher.make(name, opts);
    Component$ReventlessCore.setOperations(self, scheduledPublisher.operations);
    let outputs = {
      resource: scheduledPublisher.resource
    };
    return Component$ReventlessCore.setOutputs(self, outputs);
  };
  let make = opts => Component$ReventlessCore.make(ComponentType$ReventlessCore.toString(Scheduler$ReventlessCore.componentType), ComponentType$ReventlessCore.toName(Scheduler$ReventlessCore.componentType), construct, opts);
  return {
    make: make
  };
}

export {
  Make,
}
/* Component-ReventlessCore Not a pure module */
