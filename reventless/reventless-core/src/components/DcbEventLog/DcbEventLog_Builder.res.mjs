// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as Pulumi from "@pulumi/pulumi";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Component$ReventlessCore from "../Component.res.mjs";
import * as DcbEventLog$ReventlessCore from "./DcbEventLog.res.mjs";
import * as Util_Pulumi$ReventlessCore from "../../util/Util_Pulumi.res.mjs";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";
import * as EventTopic_Builder$ReventlessCore from "../EventTopic/EventTopic_Builder.res.mjs";
import * as DcbEventLog_Operations$ReventlessCore from "./DcbEventLog_Operations.res.mjs";

function Make(Spec) {
  return Storage => (EventTopicPublisher => {
    let eventSchema = Spec.eventSchema;
    let taggedFields = DcbTag$Reventless.extractTaggedFields(Spec.eventSchema);
    let singleTagIndexes = taggedFields.map(tagKey => `tag_` + tagKey);
    let compositeIndex = taggedFields.length > 1 ? ["tag_composite"] : [];
    let indexes = singleTagIndexes.concat(compositeIndex);
    let construct = (self, name) => {
      let opts_parent = Component$ReventlessCore.toPulumiResource(self);
      let opts = {
        parent: opts_parent
      };
      let storage = Storage.make(ComponentType$ReventlessCore.name(name, DcbEventLog$ReventlessCore.componentType), indexes, opts);
      let SpecificEventTopic = EventTopic_Builder$ReventlessCore.Make({
        Id: Id$Reventless.$$String,
        eventSchema: eventSchema
      })(EventTopicPublisher);
      let eventTopic = SpecificEventTopic.make(name, storage.resources, Util_Pulumi$ReventlessCore.ComponentResourceOptions.ofCustomResourceOptions(opts));
      Component$ReventlessCore.setOperations(self, Pulumi.all([
        storage.operations,
        Component$ReventlessCore.operations(eventTopic)
      ]).apply(param => {
        let publishJson = param[1].publishJson;
        let Ops = DcbEventLog_Operations$ReventlessCore.Make(Spec)({
          Spec: Spec,
          name: name,
          storage: param[0],
          publishJson: publishJson
        });
        return {
          read: Ops.read,
          append: Ops.append,
          readStream: Ops.readStream,
          appendStream: Ops.appendStream
        };
      }));
      let outputs_resources = storage.resources;
      let outputs_eventTopic = Component$ReventlessCore.outputs(eventTopic);
      let outputs = {
        resources: outputs_resources,
        eventTopic: outputs_eventTopic
      };
      return Component$ReventlessCore.setOutputs(self, outputs);
    };
    let make = (name, opts) => Component$ReventlessCore.make(ComponentType$ReventlessCore.toString(DcbEventLog$ReventlessCore.componentType), name, construct, opts);
    return {
      Spec: Spec,
      make: make
    };
  });
}

export {
  Make,
}
/* Id-Reventless Not a pure module */
