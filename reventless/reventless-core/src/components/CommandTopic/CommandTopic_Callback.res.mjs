// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "@reventlessdev/rescript-effect/src/Effect.res.mjs";
import * as Stream from "@reventlessdev/rescript-effect/src/Stream.res.mjs";
import * as Effect$1 from "effect";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";
import * as Logger$ReventlessCore from "../../util/Logger.res.mjs";
import * as Message$ReventlessCore from "../../Message.res.mjs";

function Make(Spec) {
  return Ops => {
    let handleJsonCommands = stream => Effect$1.Effect.flatMap(Stream.runCollect(Effect$1.Stream.flatMap(Effect$1.Stream.mapEffect(stream, param => {
      let reference = param.reference;
      let json = param.command;
      return Effect$1.Effect.sync(() => {
        let command$p;
        try {
          command$p = Message$ReventlessCore.decodeCommand$p(json, Spec.Id.schema, Spec.commandSchema);
        } catch (raw_err) {
          let err = Primitive_exceptions.internalToException(raw_err);
          let commandStr = JSON.stringify(json);
          Logger$ReventlessCore.error("File \"CommandTopic_Callback.res\", line 19, characters 28-35", undefined, undefined, `Couldn't decode command ` + commandStr + `:`, err);
          return;
        }
        return {
          command: command$p,
          reference: reference
        };
      });
    }), opt => {
      if (opt !== undefined) {
        return Effect$1.Stream.fromIterable([opt]);
      } else {
        return Effect$1.Stream.empty;
      }
    })), topicItems => Effect$1.Effect.map(Effect.tryPromise(e => {
      Logger$ReventlessCore.error("File \"CommandTopic_Callback.res\", line 35, characters 28-35", undefined, undefined, "Couldn't handle commands", e);
      return Stdlib_Option.getOr(Stdlib_JsExn.message(e), "Couldn't handle commands");
    }, () => Ops.commandsHandler(topicItems)), res => {
      Logger$ReventlessCore.debug("File \"CommandTopic_Callback.res\", line 41, characters 26-33", undefined, undefined, "finished", "CommandTopic.handleCommands");
      return res;
    }));
    return {
      handleJsonCommands: handleJsonCommands
    };
  };
}

export {
  Make,
}
/* Effect Not a pure module */
