// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";
import * as Message$ReventlessCore from "../../Message.res.mjs";

function Make(Spec) {
  return Ops => {
    let handleJsonCommands = stream => Effect.Effect.tap(Ops.commandsHandler(Effect.Stream.flatMap(Effect.Stream.mapEffect(stream, param => {
      let reference = param.reference;
      let json = param.command;
      return Effect.Effect.sync(() => {
        let command$p;
        try {
          command$p = Message$ReventlessCore.decodeCommand$p(json, Spec.Id.schema, Spec.commandSchema);
        } catch (raw_err) {
          let err = Primitive_exceptions.internalToException(raw_err);
          let commandStr = JSON.stringify(json);
          let errMsg = Stdlib_Option.getOr(Stdlib_Option.flatMap(Stdlib_JsExn.fromException(err), Stdlib_JsExn.message), "unknown");
          Effect.Effect.runSync(Effect.Effect.logError(`Couldn't decode command ` + commandStr + `: ` + errMsg));
          return;
        }
        return {
          command: command$p,
          reference: reference
        };
      });
    }), opt => {
      if (opt !== undefined) {
        return Effect.Stream.fromIterable([opt]);
      } else {
        return Effect.Stream.empty;
      }
    })), param => Effect.Effect.logInfo("finished CommandTopic.handleCommands"));
    return {
      handleJsonCommands: handleJsonCommands
    };
  };
}

export {
  Make,
}
/* effect Not a pure module */
