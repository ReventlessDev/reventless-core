// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";
import * as Logger$ReventlessCore from "../../util/Logger.res.mjs";
import * as Message$ReventlessCore from "../../Message.res.mjs";

function Make(Spec) {
  return Ops => {
    let handleJsonCommands = stream => Effect.Effect.map(Ops.commandsHandler(Effect.Stream.flatMap(Effect.Stream.mapEffect(stream, param => {
      let reference = param.reference;
      let json = param.command;
      return Effect.Effect.sync(() => {
        let command$p;
        try {
          command$p = Message$ReventlessCore.decodeCommand$p(json, Spec.Id.schema, Spec.commandSchema);
        } catch (raw_err) {
          let err = Primitive_exceptions.internalToException(raw_err);
          let commandStr = JSON.stringify(json);
          Logger$ReventlessCore.error("File \"CommandTopic_Callback.res\", line 19, characters 28-35", undefined, undefined, `Couldn't decode command ` + commandStr + `:`, err);
          return;
        }
        return {
          command: command$p,
          reference: reference
        };
      });
    }), opt => {
      if (opt !== undefined) {
        return Effect.Stream.fromIterable([opt]);
      } else {
        return Effect.Stream.empty;
      }
    })), res => {
      Logger$ReventlessCore.debug("File \"CommandTopic_Callback.res\", line 32, characters 24-31", undefined, undefined, "finished", "CommandTopic.handleCommands");
      return res;
    });
    return {
      handleJsonCommands: handleJsonCommands
    };
  };
}

export {
  Make,
}
/* effect Not a pure module */
