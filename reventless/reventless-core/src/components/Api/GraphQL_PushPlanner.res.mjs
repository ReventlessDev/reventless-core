// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as GraphQL_Stitcher$ReventlessCore from "./GraphQL_Stitcher.res.mjs";

let planTargetSchema = S.union([
  S.literal("DomainApi"),
  S.literal("PlatformApi")
]);

let emptyBase = GraphQL_Stitcher$ReventlessCore.encode({
  types: [],
  mutations: [],
  queries: [],
  subscriptions: [],
  subscriptionSources: []
});

function planPushes(adminBase, fragments, splitApi) {
  if (splitApi) {
    let platformFrags = fragments.filter(f => f.target === "Platform").map(f => f.fragment);
    let domainFrags = fragments.filter(f => f.target === "Domain").map(f => f.fragment);
    return [
      {
        api: "PlatformApi",
        sdl: GraphQL_Stitcher$ReventlessCore.stitch(adminBase, platformFrags)
      },
      {
        api: "DomainApi",
        sdl: GraphQL_Stitcher$ReventlessCore.stitch(emptyBase, domainFrags)
      }
    ];
  }
  let allFrags = fragments.map(f => f.fragment);
  return [{
      api: "DomainApi",
      sdl: GraphQL_Stitcher$ReventlessCore.stitch(adminBase, allFrags)
    }];
}

export {
  planTargetSchema,
  emptyBase,
  planPushes,
}
/* planTargetSchema Not a pure module */
