// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Component$ReventlessCore from "../Component.res.mjs";

function Make(Provider) {
  let construct = (name, baseFragment, self) => {
    let opts_parent = Component$ReventlessCore.toPulumiResource(self);
    let opts = {
      parent: opts_parent
    };
    let match = Provider.makeApiResource(name, opts);
    let api = match[0];
    let apiId = api.apply(a => Stdlib_Option.getOr(a.id, name));
    Component$ReventlessCore.setOutputs(self, {
      apiId: apiId
    });
    Component$ReventlessCore.setOperations(self, api.apply(apiValue => ({
      updateSchema: pluginFragments => Provider.updateSchema(Pulumi.output(apiValue), baseFragment, pluginFragments)
    })));
  };
  let make = (name, baseFragment, opts) => Component$ReventlessCore.make("reventless:index:Api", name, extra => construct(name, baseFragment, extra), opts);
  return {
    construct: construct,
    make: make
  };
}

export {
  Make,
}
/* @pulumi/pulumi Not a pure module */
