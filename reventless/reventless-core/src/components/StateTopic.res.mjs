// Generated by ReScript, PLEASE EDIT WITH CARE

import Component from "./Component";
import * as Component$Reventless from "./Component.res.mjs";
import * as ComponentType$Reventless from "../ComponentType.res.mjs";

let Adapter = {};

function Make(Spec) {
  return Publisher => {
    let construct = (self, name, allQueryDbs) => {
      let opts_parent = Component$Reventless.toPulumiResource(self);
      let opts = {
        parent: opts_parent
      };
      let publisher = Publisher.make(ComponentType$Reventless.name(name, "EventTopic"), opts, allQueryDbs);
      let outputs = {
        resource: publisher.resource
      };
      self.setOutputs(outputs);
      return self.registerOutputs(outputs);
    };
    let make = (name, opts, allQueryDbs) => {
      let prim0 = ComponentType$Reventless.toString("EventTopic");
      return new Component(prim0, name, construct, opts, allQueryDbs);
    };
    return {
      Spec: Spec,
      make: make
    };
  };
}

let componentType = "EventTopic";

export {
  componentType,
  Adapter,
  Make,
}
/* ./Component Not a pure module */
