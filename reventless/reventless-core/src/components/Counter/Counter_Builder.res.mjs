// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as Pulumi from "@pulumi/pulumi";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";
import * as Counter$ReventlessCore from "./Counter.res.mjs";
import * as Component$ReventlessCore from "../Component.res.mjs";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";
import * as QueryDb_Adapter$ReventlessCore from "../QueryDb/QueryDb_Adapter.res.mjs";
import * as QueryDb_Builder$ReventlessCore from "../QueryDb/QueryDb_Builder.res.mjs";
import * as Counter_Callback$ReventlessCore from "./Counter_Callback.res.mjs";
import * as Counter_Operations$ReventlessCore from "./Counter_Operations.res.mjs";

function Make(QueryDbStorage) {
  return ApiValues => (Handler => {
    let make = (name, counterEventsHandler, ttlOpt, opts) => {
      let ttl = ttlOpt !== undefined ? ttlOpt : 604800;
      return Component$ReventlessCore.make(ComponentType$ReventlessCore.toString(Counter$ReventlessCore.componentType), ComponentType$ReventlessCore.name(name, Counter$ReventlessCore.componentType), (extra, extra$1) => {
        let ttl$1 = ttl;
        let opts_parent = Component$ReventlessCore.toPulumiResource(extra);
        let opts = {
          parent: opts_parent
        };
        let opts2_parent = Component$ReventlessCore.toPulumiResource(extra);
        let opts2 = {
          parent: opts2_parent
        };
        let name = extra$1 + "References";
        let config = ReadModel$Reventless.config(undefined, undefined, undefined);
        let ReferencesDb = QueryDb_Builder$ReventlessCore.Make({
          Id: {
            schema: Id$Reventless.StringPure.schema,
            make: prim => prim,
            makeFromString: prim => prim,
            toString: prim => prim,
            cmp: Id$Reventless.StringPure.cmp
          },
          name: name,
          stateSchema: Counter_Operations$ReventlessCore.referencesStateSchema,
          config: config,
          subIdConfig: undefined
        })(QueryDbStorage)(QueryDb_Adapter$ReventlessCore.NoResolvers(QueryDbStorage));
        let name$1 = extra$1 + "Counts";
        let config$1 = ReadModel$Reventless.config(undefined, undefined, undefined);
        let CountsDb = QueryDb_Builder$ReventlessCore.Make({
          Id: {
            schema: Id$Reventless.StringPure.schema,
            make: prim => prim,
            makeFromString: prim => prim,
            toString: prim => prim,
            cmp: Id$Reventless.StringPure.cmp
          },
          name: name$1,
          stateSchema: Counter_Callback$ReventlessCore.countsStateSchema,
          config: config$1,
          subIdConfig: undefined
        })(QueryDbStorage)(QueryDb_Adapter$ReventlessCore.NoResolvers(QueryDbStorage));
        let referencesDb = ReferencesDb.make(ApiValues.api, ApiValues.apiRole, ttl$1, opts);
        let countsDb = CountsDb.make(ApiValues.api, ApiValues.apiRole, ttl$1, opts);
        let handler = Component$ReventlessCore.operations(countsDb).apply(param => {
          let Callback = Counter_Callback$ReventlessCore.Make({
            name: extra$1,
            countsDbCount: param.count,
            counterEventsHandler: counterEventsHandler
          });
          return Handler.make(extra$1, name, Component$ReventlessCore.outputs(referencesDb), name$1, Component$ReventlessCore.outputs(countsDb), Callback.counterHandler, opts2);
        });
        Component$ReventlessCore.setOperations(extra, Pulumi.all([
          Component$ReventlessCore.operations(referencesDb),
          handler
        ]).apply(param => {
          let Operations = Counter_Operations$ReventlessCore.Make({
            ttl: ttl$1,
            saveBatch: param[0].saveBatch
          });
          return {
            count: Operations.count,
            addToCounterTarget: param[1].addToCounterTarget
          };
        }));
        let outputs_referencesDb = Component$ReventlessCore.outputs(referencesDb);
        let outputs_countsDb = Component$ReventlessCore.outputs(countsDb);
        let outputs = {
          referencesDb: outputs_referencesDb,
          countsDb: outputs_countsDb
        };
        return Component$ReventlessCore.setOutputs(extra, outputs);
      }, opts);
    };
    return {
      make: make
    };
  });
}

export {
  Make,
}
/* Id-Reventless Not a pure module */
