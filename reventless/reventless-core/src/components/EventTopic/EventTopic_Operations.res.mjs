// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Logger$ReventlessCore from "../../util/Logger.res.mjs";
import * as Message$ReventlessCore from "../../Message.res.mjs";
import * as Util_Promise$ReventlessCore from "../../util/Util_Promise.res.mjs";

function Make(Spec) {
  return Ops => {
    let publish = async events$p => {
      let eventCount = events$p.length;
      return await Util_Promise$ReventlessCore.toUnit(Promise.all(events$p.map(async (event$p, idx) => {
        let eventJson$p = Message$ReventlessCore.encodeEvent$p(event$p, Spec.Id.schema, Spec.eventSchema);
        let id = event$p.id;
        let idx$1 = idx + 1 | 0;
        let val;
        try {
          val = await Ops.publishJson(Spec.Id.toString(id), event$p.meta, eventJson$p);
        } catch (e) {
          Logger$ReventlessCore.logJsonEvent("File \"EventTopic_Operations.res\", line 18, characters 15-22", "Error", eventJson$p, `Couldn't publish event ` + idx$1.toString() + `/` + eventCount.toString() + `:`);
          throw e;
        }
        return Logger$ReventlessCore.logJsonEvent("File \"EventTopic_Operations.res\", line 25, characters 15-22", undefined, eventJson$p, `Published event ` + idx$1.toString() + `/` + eventCount.toString() + `:`);
      })));
    };
    return {
      publish: publish
    };
  };
}

export {
  Make,
}
/* Logger-ReventlessCore Not a pure module */
