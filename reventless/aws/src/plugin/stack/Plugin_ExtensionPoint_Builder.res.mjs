// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as ExtensionPoint_Builder$ReventlessAws from "../../components/ExtensionPoint_Builder.res.mjs";
import * as EventTopicPublisher_SNS$ReventlessAws from "../../adapter/EventTopic/EventTopicPublisher_SNS.res.mjs";
import * as PluginRuntimeOperations$ReventlessAws from "../runtime/PluginRuntimeOperations.res.mjs";
import * as RuntimeEnvironment_Lambda$ReventlessAws from "../../adapter/Runtime/RuntimeEnvironment_Lambda.res.mjs";
import * as CommandTopicChannel_SQS_Sync$ReventlessAws from "../../adapter/CommandTopic/CommandTopicChannel_SQS_Sync.res.mjs";
import * as PluginExtensionPoint_Builder$ReventlessCore from "@reventlessdev/reventless-core/src/plugin/connect/PluginExtensionPoint_Builder.res.mjs";
import * as PluginExtensionPointRuntime_Builder$ReventlessAws from "../runtime/PluginExtensionPointRuntime_Builder.res.mjs";

function MakeWithConfig(Config) {
  let environment = Stdlib_Option.getOr(process.env.Environment, "unknown");
  return PluginExtensionPoint_Builder$ReventlessCore.Make({
    runtimeOps: PluginRuntimeOperations$ReventlessAws.operations,
    environment: environment,
    updateApiSchema: Config.updateApiSchema,
    manageSubscriptions: Config.manageSubscriptions
  })({
    make: RuntimeEnvironment_Lambda$ReventlessAws.make,
    groupBySource: RuntimeEnvironment_Lambda$ReventlessAws.groupBySource,
    extractCorrelationId: RuntimeEnvironment_Lambda$ReventlessAws.extractCorrelationId,
    extractCausationId: RuntimeEnvironment_Lambda$ReventlessAws.extractCausationId,
    extractSentTimestamp: RuntimeEnvironment_Lambda$ReventlessAws.extractSentTimestamp,
    extractRetryCount: RuntimeEnvironment_Lambda$ReventlessAws.extractRetryCount,
    asEventHandler: prim => prim,
    asEffectHandler: prim => prim
  })({
    make: CommandTopicChannel_SQS_Sync$ReventlessAws.make
  })({
    make: EventTopicPublisher_SNS$ReventlessAws.make
  })({
    forCommandTopic: PluginExtensionPointRuntime_Builder$ReventlessAws.forCommandTopic
  })(ExtensionPoint_Builder$ReventlessAws.Defaults);
}

let environment = Stdlib_Option.getOr(process.env.Environment, "unknown");

let Make = PluginExtensionPoint_Builder$ReventlessCore.Make({
  runtimeOps: PluginRuntimeOperations$ReventlessAws.operations,
  environment: environment,
  updateApiSchema: undefined,
  manageSubscriptions: undefined
})({
  make: RuntimeEnvironment_Lambda$ReventlessAws.make,
  groupBySource: RuntimeEnvironment_Lambda$ReventlessAws.groupBySource,
  extractCorrelationId: RuntimeEnvironment_Lambda$ReventlessAws.extractCorrelationId,
  extractCausationId: RuntimeEnvironment_Lambda$ReventlessAws.extractCausationId,
  extractSentTimestamp: RuntimeEnvironment_Lambda$ReventlessAws.extractSentTimestamp,
  extractRetryCount: RuntimeEnvironment_Lambda$ReventlessAws.extractRetryCount,
  asEventHandler: prim => prim,
  asEffectHandler: prim => prim
})({
  make: CommandTopicChannel_SQS_Sync$ReventlessAws.make
})({
  make: EventTopicPublisher_SNS$ReventlessAws.make
})({
  forCommandTopic: PluginExtensionPointRuntime_Builder$ReventlessAws.forCommandTopic
})(ExtensionPoint_Builder$ReventlessAws.Defaults);

let CommandTopicChannel;

let RuntimeEnvironment;

let ExtensionPointRuntimeBuilder;

export {
  CommandTopicChannel,
  RuntimeEnvironment,
  ExtensionPointRuntimeBuilder,
  MakeWithConfig,
  Make,
}
/* Make Not a pure module */
