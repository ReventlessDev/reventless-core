// Generated by ReScript, PLEASE EDIT WITH CARE


let dependencies = {
  contents: []
};

function getDependencies() {
  let currentLen = dependencies.contents.length;
  let idx = currentLen / 4 | 0;
  let match = idx !== 0 ? [
      ((idx - 1 | 0) << 2),
      (idx << 2)
    ] : [
      0,
      0
    ];
  let deps = Promise.all(dependencies.contents.slice(match[0], match[1]));
  let registerResource = {
    contents: param => {}
  };
  let promise = new Promise((resolve, param) => {
    registerResource.contents = resolve;
  });
  dependencies.contents = dependencies.contents.concat([promise]);
  return [
    deps,
    registerResource.contents
  ];
}

export {
  getDependencies,
}
/* No side effect */
