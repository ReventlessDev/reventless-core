// Generated by ReScript, PLEASE EDIT WITH CARE


let defaultProdStacks = [
  "prod",
  "main"
];

function deriveFqdn(baseName, stack, baseDomain, prodStacks) {
  if (prodStacks.includes(stack)) {
    return baseName + `.` + baseDomain;
  } else {
    return baseName + `-` + stack + `.` + baseDomain;
  }
}

function parseProdStacks(csv) {
  return csv.split(",").map(prim => prim.trim()).filter(s => s !== "");
}

export {
  defaultProdStacks,
  deriveFqdn,
  parseProdStacks,
}
/* No side effect */
