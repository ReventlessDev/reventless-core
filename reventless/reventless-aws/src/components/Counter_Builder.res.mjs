// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Counter$ReventlessCore from "@reventlessdev/reventless-core/src/components/Counter/Counter.res.mjs";
import * as ComponentType$ReventlessCore from "@reventlessdev/reventless-core/src/ComponentType.res.mjs";
import * as Counter_Builder$ReventlessCore from "@reventlessdev/reventless-core/src/components/Counter/Counter_Builder.res.mjs";
import * as CounterHandler_DynamoDbStream$ReventlessAws from "../adapter/Counter/CounterHandler_DynamoDbStream.res.mjs";
import * as QueryDbStorage_DynamoDbStream$ReventlessAws from "../adapter/QueryDb/QueryDbStorage_DynamoDbStream.res.mjs";

function Make(Api) {
  return Config => {
    let Inner = Counter_Builder$ReventlessCore.Make(QueryDbStorage_DynamoDbStream$ReventlessAws)(Api)({
      make: CounterHandler_DynamoDbStream$ReventlessAws.make
    });
    let make = (name, jsonEventsHandler, ttl, opts) => {
      CounterHandler_DynamoDbStream$ReventlessAws.registerCounter(ComponentType$ReventlessCore.name(name, Counter$ReventlessCore.componentType), Config.specModulePath, Config.mappingsModulePath, Config.publishQueueUrl);
      return Inner.make(name, jsonEventsHandler, ttl, opts);
    };
    return {
      Inner: Inner,
      make: make,
      outputs: Inner.outputs,
      operations: Inner.operations
    };
  };
}

export {
  Make,
}
/* Counter-ReventlessCore Not a pure module */
