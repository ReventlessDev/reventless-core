// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Aws from "@pulumi/aws";
import * as AWS_Tags$ReventlessAws from "../AWS_Tags.res.mjs";
import * as Util_SNS$ReventlessAws from "../../util/Util_SNS.res.mjs";
import * as EventTopic$ReventlessCore from "@reventlessdev/reventless-core/src/components/EventTopic/EventTopic.res.mjs";
import * as Util_SNS_FIFO$ReventlessAws from "../../util/Util_SNS_FIFO.res.mjs";
import * as EventTopicPublisher_SNS_Runtime$ReventlessAws from "./EventTopicPublisher_SNS_Runtime.res.mjs";

function make(name, param, opts) {
  let topic = new (Aws.sns.Topic)(name, {
    contentBasedDeduplication: true,
    fifoTopic: true,
    tags: AWS_Tags$ReventlessAws.make(name, EventTopic$ReventlessCore.componentType)
  }, opts);
  return {
    resources: [Util_SNS_FIFO$ReventlessAws.toResource(topic)],
    publishJson: Util_SNS$ReventlessAws.toRuntimeTopicOutput(topic).apply(runtimeTopic => ((extra, extra$1, extra$2) => EventTopicPublisher_SNS_Runtime$ReventlessAws.publishFifo(runtimeTopic, extra, extra$1, extra$2)))
  };
}

export {
  make,
}
/* @pulumi/aws Not a pure module */
