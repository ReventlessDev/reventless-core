// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as Util_Bundle$ReventlessAws from "../../util/Util_Bundle.res.mjs";
import * as Util_EntryPoint$ReventlessAws from "../../util/Util_EntryPoint.res.mjs";
import * as RuntimeEnvironment_Lambda$ReventlessAws from "./RuntimeEnvironment_Lambda.res.mjs";

let bundledTaskBucketInfos = {};

function registerBundledTaskBucket(bucketName, callbackModulePath, publishToAggregatesQueueUrls) {
  bundledTaskBucketInfos[bucketName] = {
    callbackModulePath: callbackModulePath,
    publishToAggregatesQueueUrls: publishToAggregatesQueueUrls
  };
}

function forBucketCallback(param, connect, memorySizeOpt, timeoutOpt, name, task) {
  let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 4096;
  let timeout = timeoutOpt !== undefined ? timeoutOpt : 600;
  let resource = Component$ReventlessCore.toPulumiResource(task);
  let fullName = Stdlib_Option.getOr(resource.__name, "UnnamedTask") + name;
  let info = bundledTaskBucketInfos[name];
  if (info !== undefined) {
    let factoryModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-aws/src/adapter/Runtime/BundledTaskHandlerFactory.mjs");
    let requestContextModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-core/src/RequestContext.res.mjs");
    let envVars = {};
    let publishToAggregatesEnvVars = {};
    Stdlib_Dict.forEachWithKey(info.publishToAggregatesQueueUrls, (queueUrlOutput, aggName) => {
      let envVar = `PUBLISH_` + aggName + `_QUEUE_URL`;
      envVars[envVar] = queueUrlOutput;
      publishToAggregatesEnvVars[aggName] = envVar;
    });
    let entryPointCode = Util_EntryPoint$ReventlessAws.generateBundledTaskBucketEntryPoint({
      name: fullName,
      callbackModulePath: info.callbackModulePath,
      factoryModule: factoryModulePath,
      requestContextModule: requestContextModulePath,
      publishToAggregatesEnvVars: publishToAggregatesEnvVars
    });
    return connect(RuntimeEnvironment_Lambda$ReventlessAws.makeBundledFromEntryPoint(fullName, entryPointCode, envVars, memorySize, timeout, {
      parent: resource
    }));
  }
  console.warn(`TaskRuntime_Builder_PerBucket_Bundled: no bundled info registered for bucket "` + name + `"`);
}

function finish() {
  
}

let RuntimeEnvironment;

let TaskBucket;

export {
  RuntimeEnvironment,
  TaskBucket,
  bundledTaskBucketInfos,
  registerBundledTaskBucket,
  forBucketCallback,
  finish,
}
/* Component-ReventlessCore Not a pure module */
