// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as Util_Bundle$ReventlessAws from "../../util/Util_Bundle.res.mjs";
import * as CommandTopic$ReventlessCore from "@reventlessdev/reventless-core/src/components/CommandTopic/CommandTopic.res.mjs";
import * as ComponentType$ReventlessCore from "@reventlessdev/reventless-core/src/ComponentType.res.mjs";
import * as Util_EntryPoint$ReventlessAws from "../../util/Util_EntryPoint.res.mjs";
import * as RuntimeEnvironment_Lambda$ReventlessAws from "./RuntimeEnvironment_Lambda.res.mjs";

let bundledInfos = {};

function registerBundledExtensionPoint(name, specModulePath, mappingsModulePath, publishToAggregatesQueueUrls) {
  bundledInfos[name] = {
    specModulePath: specModulePath,
    mappingsModulePath: mappingsModulePath,
    publishToAggregatesQueueUrls: publishToAggregatesQueueUrls
  };
}

function forCommandTopic(param, connect, memorySizeOpt, timeoutOpt, commandTopic) {
  let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
  let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
  let commandTopicResource = Component$ReventlessCore.toPulumiResource(commandTopic);
  let epName = Stdlib_Option.getOr(commandTopicResource.__name, "Unnamed");
  let hit = bundledInfos[epName];
  let matchedInfo = hit !== undefined ? hit : Stdlib_Option.map(Object.entries(bundledInfos).find(param => epName.includes(param[0].replaceAll(".", ""))), param => param[1]);
  if (matchedInfo !== undefined) {
    let channel = commandTopic.channel;
    let channelParts = channel.parts;
    let queue = channelParts.queue;
    let factoryModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-aws/src/adapter/Runtime/BundledExtensionPointHandlerFactory.mjs");
    let requestContextModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-core/src/RequestContext.res.mjs");
    let name = ComponentType$ReventlessCore.nameOpt(commandTopicResource.__name, CommandTopic$ReventlessCore.componentType);
    let opts_parent = commandTopicResource;
    let opts = {
      parent: opts_parent
    };
    let envVars = {};
    envVars["EP_QUEUE_URL"] = queue.id;
    envVars["EP_QUEUE_ARN"] = queue.arn;
    let publishToAggregatesEnvVars = {};
    Stdlib_Dict.forEachWithKey(matchedInfo.publishToAggregatesQueueUrls, (queueUrlOutput, aggName) => {
      let envVar = `PTA_` + aggName + `_QUEUE_URL`;
      envVars[envVar] = queueUrlOutput;
      publishToAggregatesEnvVars[aggName] = envVar;
    });
    let registration_specModulePath = matchedInfo.specModulePath;
    let registration_mappingsModulePath = matchedInfo.mappingsModulePath;
    let registration = {
      specModulePath: registration_specModulePath,
      mappingsModulePath: registration_mappingsModulePath,
      queueUrlEnvVar: "EP_QUEUE_URL",
      queueArnEnvVar: "EP_QUEUE_ARN",
      publishToAggregatesEnvVars: publishToAggregatesEnvVars
    };
    let entryPointCode = Util_EntryPoint$ReventlessAws.generateBundledExtensionPointEntryPoint({
      name: epName,
      handler: registration,
      factoryModule: factoryModulePath,
      requestContextModule: requestContextModulePath
    });
    return connect(RuntimeEnvironment_Lambda$ReventlessAws.makeBundledFromEntryPoint(name, entryPointCode, envVars, memorySize, timeout, opts));
  }
  console.warn(`ExtensionPointRuntime_Builder_PerExtensionPoint_Bundled: no bundled info for ` + epName);
}

let CommandTopicChannel;

let RuntimeEnvironment;

export {
  CommandTopicChannel,
  RuntimeEnvironment,
  bundledInfos,
  registerBundledExtensionPoint,
  forCommandTopic,
}
/* Component-ReventlessCore Not a pure module */
