// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Stdlib_JsError from "@rescript/runtime/lib/es6/Stdlib_JsError.js";
import * as Aggregate$ReventlessCore from "@reventlessdev/reventless-core/src/components/Aggregate/Aggregate.res.mjs";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as Util_Bundle$ReventlessAws from "../../util/Util_Bundle.res.mjs";
import * as ComponentType$ReventlessCore from "@reventlessdev/reventless-core/src/ComponentType.res.mjs";
import * as Util_EntryPoint$ReventlessAws from "../../util/Util_EntryPoint.res.mjs";
import * as RuntimeEnvironment_Lambda$ReventlessAws from "./RuntimeEnvironment_Lambda.res.mjs";
import * as EventCollectorChannel_DynamoDbStream$ReventlessAws from "../EventCollector/EventCollectorChannel_DynamoDbStream.res.mjs";

let bundledAggregateInfos = {};

function registerBundledAggregate(aggregateName, specModulePath, behaviorModulePath, eventLogTableName, mappingsModulePath) {
  bundledAggregateInfos[aggregateName] = {
    specModulePath: specModulePath,
    behaviorModulePath: behaviorModulePath,
    eventLogTableName: eventLogTableName,
    mappingsModulePath: mappingsModulePath
  };
}

let storedSpecs = {};

function getStoredSpec(aggregateName, aggregateResource) {
  return Stdlib_Option.getOr(storedSpecs[aggregateName], {
    aggregateName: aggregateName,
    aggregateResource: aggregateResource,
    queueUrl: Pulumi.output(""),
    queueArn: Pulumi.output(""),
    commandTopicConnects: [],
    commandGeneratorConnects: [],
    eventCollectorChannelSpec: undefined,
    commandTopicMemorySize: 0,
    commandTopicTimeout: 0,
    commandGeneratorMemorySize: 0,
    commandGeneratorTimeout: 0,
    eventCollectorMemorySize: 0,
    eventCollectorTimeout: 0
  });
}

function forCommandGenerator(param, connect, memorySizeOpt, timeoutOpt, commandGenerator) {
  let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
  let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
  let resource = Component$ReventlessCore.toPulumiResource(commandGenerator);
  let aggregateResource = resource.__parentResource;
  if (aggregateResource === undefined) {
    return;
  }
  let aggregateName = Stdlib_Option.getOr(aggregateResource.__name, "Unnamed");
  let spec = getStoredSpec(aggregateName, aggregateResource);
  storedSpecs[aggregateName] = {
    aggregateName: spec.aggregateName,
    aggregateResource: spec.aggregateResource,
    queueUrl: spec.queueUrl,
    queueArn: spec.queueArn,
    commandTopicConnects: spec.commandTopicConnects,
    commandGeneratorConnects: spec.commandGeneratorConnects.concat([connect]),
    eventCollectorChannelSpec: spec.eventCollectorChannelSpec,
    commandTopicMemorySize: spec.commandTopicMemorySize,
    commandTopicTimeout: spec.commandTopicTimeout,
    commandGeneratorMemorySize: Math.max(spec.commandGeneratorMemorySize, memorySize),
    commandGeneratorTimeout: Math.max(spec.commandGeneratorTimeout, timeout),
    eventCollectorMemorySize: spec.eventCollectorMemorySize,
    eventCollectorTimeout: spec.eventCollectorTimeout
  };
}

function forCommandTopic(param, connect, memorySizeOpt, timeoutOpt, commandTopic) {
  let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
  let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
  let commandTopicResource = Component$ReventlessCore.toPulumiResource(commandTopic);
  let aggregateResource = commandTopicResource.__parentResource;
  if (aggregateResource !== undefined) {
    let aggregateName = Stdlib_Option.getOr(aggregateResource.__name, "Unnamed");
    let channel = commandTopic.channel;
    let channelParts = channel.parts;
    let queue = channelParts.queue;
    let spec = getStoredSpec(aggregateName, aggregateResource);
    storedSpecs[aggregateName] = {
      aggregateName: spec.aggregateName,
      aggregateResource: spec.aggregateResource,
      queueUrl: queue.id,
      queueArn: queue.arn,
      commandTopicConnects: spec.commandTopicConnects.concat([connect]),
      commandGeneratorConnects: spec.commandGeneratorConnects,
      eventCollectorChannelSpec: spec.eventCollectorChannelSpec,
      commandTopicMemorySize: Math.max(spec.commandTopicMemorySize, memorySize),
      commandTopicTimeout: Math.max(spec.commandTopicTimeout, timeout),
      commandGeneratorMemorySize: spec.commandGeneratorMemorySize,
      commandGeneratorTimeout: spec.commandGeneratorTimeout,
      eventCollectorMemorySize: spec.eventCollectorMemorySize,
      eventCollectorTimeout: spec.eventCollectorTimeout
    };
    return;
  }
  let name = Stdlib_Option.getOr(commandTopicResource.__name, "Unnamed");
  Stdlib_JsError.throwWithMessage(`forCommandTopic(bundled-micro): commandTopic ` + name + ` has no Aggregate parent`);
}

function forEventCollector(param, eventTopics, resources, memorySizeOpt, timeoutOpt, eventCollector) {
  let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 2048;
  let timeout = timeoutOpt !== undefined ? timeoutOpt : 180;
  let eventCollectorResource = Component$ReventlessCore.toPulumiResource(eventCollector);
  let channel = eventCollector.channel;
  let aggregateResource = Stdlib_Option.flatMap(eventCollectorResource.__parentResource, parent => parent.__parentResource);
  if (aggregateResource !== undefined) {
    let aggregateName = Stdlib_Option.getOr(aggregateResource.__name, "Unnamed");
    let spec = getStoredSpec(aggregateName, aggregateResource);
    storedSpecs[aggregateName] = {
      aggregateName: spec.aggregateName,
      aggregateResource: spec.aggregateResource,
      queueUrl: spec.queueUrl,
      queueArn: spec.queueArn,
      commandTopicConnects: spec.commandTopicConnects,
      commandGeneratorConnects: spec.commandGeneratorConnects,
      eventCollectorChannelSpec: {
        channel: channel,
        eventTopics: eventTopics,
        resources: resources
      },
      commandTopicMemorySize: spec.commandTopicMemorySize,
      commandTopicTimeout: spec.commandTopicTimeout,
      commandGeneratorMemorySize: spec.commandGeneratorMemorySize,
      commandGeneratorTimeout: spec.commandGeneratorTimeout,
      eventCollectorMemorySize: Math.max(spec.eventCollectorMemorySize, memorySize),
      eventCollectorTimeout: Math.max(spec.eventCollectorTimeout, timeout)
    };
    return;
  }
  let name = Stdlib_Option.getOr(eventCollectorResource.__name, "Unnamed");
  Stdlib_JsError.throwWithMessage(`forEventCollector(bundled-micro): eventCollector ` + name + ` has no Aggregate parent`);
}

let finished = {
  contents: false
};

function finish() {
  if (finished.contents) {
    return;
  }
  let specs = Object.values(storedSpecs);
  if (specs.length !== 0) {
    let requestContextModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-core/src/RequestContext.res.mjs");
    let commandTopicFactoryModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-aws/src/adapter/Runtime/BundledAggregateHandlerFactory.mjs");
    let commandGeneratorFactoryModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-aws/src/adapter/Runtime/BundledCommandGeneratorHandlerFactory.mjs");
    let eventMapperFactoryModulePath = Util_Bundle$ReventlessAws.resolveModule("@reventlessdev/reventless-aws/src/adapter/Runtime/BundledEventMapperHandlerFactory.mjs");
    specs.forEach(spec => {
      let info = bundledAggregateInfos[spec.aggregateName];
      if (info !== undefined) {
        let aggregateOpts_parent = spec.aggregateResource;
        let aggregateOpts = {
          parent: aggregateOpts_parent
        };
        let baseName = ComponentType$ReventlessCore.name(spec.aggregateName, Aggregate$ReventlessCore.componentType);
        let cmdTopicEnvVars = {};
        cmdTopicEnvVars["HANDLER_0_TABLE"] = info.eventLogTableName;
        cmdTopicEnvVars["HANDLER_0_QUEUE_URL"] = spec.queueUrl;
        cmdTopicEnvVars["HANDLER_0_QUEUE_ARN"] = spec.queueArn;
        let cmdTopicRegistration_specModulePath = info.specModulePath;
        let cmdTopicRegistration_behaviorModulePath = info.behaviorModulePath;
        let cmdTopicRegistration = {
          specModulePath: cmdTopicRegistration_specModulePath,
          behaviorModulePath: cmdTopicRegistration_behaviorModulePath,
          eventLogTableEnvVar: "HANDLER_0_TABLE",
          queueUrlEnvVar: "HANDLER_0_QUEUE_URL",
          queueArnEnvVar: "HANDLER_0_QUEUE_ARN"
        };
        let cmdTopicEntryPointCode = Util_EntryPoint$ReventlessAws.generateBundledAggregateEntryPoint({
          name: spec.aggregateName + "CmdTopic",
          handlers: [cmdTopicRegistration],
          factoryModule: commandTopicFactoryModulePath,
          requestContextModule: requestContextModulePath
        });
        let cmdTopicName = baseName + "CmdTopic";
        let cmdTopicRuntime = RuntimeEnvironment_Lambda$ReventlessAws.makeBundledFromEntryPoint(cmdTopicName, cmdTopicEntryPointCode, cmdTopicEnvVars, Math.max(spec.commandTopicMemorySize, 1024), Math.max(spec.commandTopicTimeout, 30), aggregateOpts);
        spec.commandTopicConnects.forEach(connect => connect(cmdTopicRuntime));
        if (spec.commandGeneratorConnects.length !== 0) {
          let cmdGenEnvVars = {};
          cmdGenEnvVars["QUEUE_URL"] = spec.queueUrl;
          let cmdGenEntryPointCode = Util_EntryPoint$ReventlessAws.generateBundledCommandGeneratorEntryPoint({
            name: spec.aggregateName + "CmdGen",
            factoryModule: commandGeneratorFactoryModulePath,
            requestContextModule: requestContextModulePath,
            specModulePath: info.specModulePath,
            behaviorModulePath: info.behaviorModulePath,
            queueUrlEnvVar: "QUEUE_URL"
          });
          let cmdGenName = baseName + "CmdGen";
          let cmdGenRuntime = RuntimeEnvironment_Lambda$ReventlessAws.makeBundledFromEntryPoint(cmdGenName, cmdGenEntryPointCode, cmdGenEnvVars, Math.max(spec.commandGeneratorMemorySize, 1024), Math.max(spec.commandGeneratorTimeout, 30), aggregateOpts);
          spec.commandGeneratorConnects.forEach(connect => connect(cmdGenRuntime));
        }
        let match = spec.eventCollectorChannelSpec;
        let match$1 = info.mappingsModulePath;
        if (match === undefined) {
          return;
        }
        if (match$1 !== undefined) {
          let evtMapperEnvVars = {};
          evtMapperEnvVars["QUEUE_URL"] = spec.queueUrl;
          let evtMapperEntryPointCode = Util_EntryPoint$ReventlessAws.generateBundledEventMapperEntryPoint({
            name: spec.aggregateName + "EvtMapper",
            factoryModule: eventMapperFactoryModulePath,
            requestContextModule: requestContextModulePath,
            targetSpecModulePath: info.specModulePath,
            mappingsModulePath: match$1,
            queueUrlEnvVar: "QUEUE_URL"
          });
          let evtMapperName = baseName + "EvtMapper";
          let evtMapperRuntime = RuntimeEnvironment_Lambda$ReventlessAws.makeBundledFromEntryPoint(evtMapperName, evtMapperEntryPointCode, evtMapperEnvVars, Math.max(spec.eventCollectorMemorySize, 2048), Math.max(spec.eventCollectorTimeout, 180), aggregateOpts);
          EventCollectorChannel_DynamoDbStream$ReventlessAws.connect(evtMapperName, [match], evtMapperRuntime, aggregateOpts);
          return;
        }
        console.warn(`AggregateRuntime_Builder_Micro_Bundled: eventCollector registered for ` + spec.aggregateName + ` but no mappingsModulePath — skipping EventMapper Lambda`);
        return;
      }
      console.warn(`AggregateRuntime_Builder_Micro_Bundled: no bundled info registered for ` + spec.aggregateName);
    });
  }
  finished.contents = true;
}

let CommandTopicChannel;

let EventCollectorChannel;

let RuntimeEnvironment;

export {
  CommandTopicChannel,
  EventCollectorChannel,
  RuntimeEnvironment,
  bundledAggregateInfos,
  registerBundledAggregate,
  storedSpecs,
  getStoredSpec,
  forCommandGenerator,
  forCommandTopic,
  forEventCollector,
  finished,
  finish,
}
/* @pulumi/pulumi Not a pure module */
