// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Util_EntryPoint$ReventlessAws from "../../util/Util_EntryPoint.res.mjs";
import * as ProjectionRuntime_Builder_Single$ReventlessAws from "./ProjectionRuntime_Builder_Single.res.mjs";

let bundledSideEffectInfos = {};

function registerSideEffectHandler(sideEffectHandlerName, sideEffectModulePaths) {
  bundledSideEffectInfos[sideEffectHandlerName] = {
    sideEffectModulePaths: sideEffectModulePaths
  };
}

function processHandler(param, info, param$1, sourceUrnEnvVar) {
  return {
    sideEffectModulePaths: info.sideEffectModulePaths,
    sourceUrnEnvVar: sourceUrnEnvVar
  };
}

function generateEntryPoint(name, handlers, factoryModule, requestContextModule) {
  return Util_EntryPoint$ReventlessAws.generateSideEffectEntryPoint({
    name: name,
    handlers: handlers,
    factoryModule: factoryModule,
    requestContextModule: requestContextModule
  });
}

let include = ProjectionRuntime_Builder_Single$ReventlessAws.Make({
  name: "AllSideEffectHandlers",
  builderName: "SideEffectHandlerRuntime_Builder_Single",
  factoryModulePath: "@reventlessdev/reventless-aws/src/adapter/Runtime/SideEffectHandlerFactory.mjs",
  infos: bundledSideEffectInfos,
  processHandler: processHandler,
  generateEntryPoint: generateEntryPoint
});

let EventCollectorChannel;

let RuntimeEnvironment;

let storedSpecs = include.storedSpecs;

let grandParent = include.grandParent;

let forEventCollector = include.forEventCollector;

let finished = include.finished;

let finish = include.finish;

export {
  EventCollectorChannel,
  RuntimeEnvironment,
  bundledSideEffectInfos,
  registerSideEffectHandler,
  storedSpecs,
  grandParent,
  forEventCollector,
  finished,
  finish,
}
/* include Not a pure module */
