// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "@reventlessdev/rescript-effect/src/Effect.res.mjs";
import * as Effect$1 from "effect/Effect";
import * as SNS_Helpers$AwsSdk from "@reventlessdev/rescript-aws-sdk/src/SNS_Helpers.res.mjs";
import * as SQS_Error$ReventlessAws from "../errors/SQS_Error.res.mjs";

function subscribe(channelId, topicId) {
  return Effect$1.runPromise(Effect$1.catchAll(Effect$1.retry(Effect$1.map(Effect.tryPromise(SQS_Error$ReventlessAws.classify, () => SNS_Helpers$AwsSdk.subscribeQueueToTopic(channelId, topicId)), () => {}), SQS_Error$ReventlessAws.retrySchedule), err => {
    let msg = SQS_Error$ReventlessAws.message(err);
    return Effect$1.flatMap(Effect$1.logError("Failed to subscribe channel to topic: " + msg), () => Effect$1.fail(err));
  }));
}

function unsubscribe(channelId, topicId) {
  return Effect$1.runPromise(Effect$1.catchAll(Effect$1.retry(Effect$1.map(Effect.tryPromise(SQS_Error$ReventlessAws.classify, () => SNS_Helpers$AwsSdk.unsubscribeQueueFromTopic(channelId, topicId)), () => {}), SQS_Error$ReventlessAws.retrySchedule), err => {
    let msg = SQS_Error$ReventlessAws.message(err);
    return Effect$1.flatMap(Effect$1.logError("Failed to unsubscribe channel from topic: " + msg), () => Effect$1.fail(err));
  }));
}

export {
  subscribe,
  unsubscribe,
}
/* Effect Not a pure module */
