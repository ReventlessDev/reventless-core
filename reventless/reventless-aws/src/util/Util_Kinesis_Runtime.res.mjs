// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "@reventlessdev/rescript-effect/src/Effect.res.mjs";
import * as Effect$1 from "effect";
import * as Kinesis$AwsSdk from "@reventlessdev/rescript-aws-sdk/src/Kinesis.res.mjs";
import * as ClientKinesis from "@aws-sdk/client-kinesis";
import * as Kinesis_Error$ReventlessAws from "../errors/Kinesis_Error.res.mjs";

function putRecord(stream, data) {
  return Effect$1.Effect.runPromise(Effect$1.Effect.catchAll(Effect$1.Effect.retry(Effect$1.Effect.map(Effect.tryPromise(Kinesis_Error$ReventlessAws.classify, () => Kinesis$AwsSdk.PutRecordCommand.send(new ClientKinesis.PutRecordCommand({
    Data: data,
    StreamName: stream.name.get(),
    PartitionKey: ""
  }))), param => {}), Kinesis_Error$ReventlessAws.retrySchedule), err => {
    let msg = Kinesis_Error$ReventlessAws.message(err);
    return Effect$1.Effect.flatMap(Effect$1.Effect.logError(`Util_Kinesis_Runtime.putRecord: ` + msg), () => Effect$1.Effect.fail(msg));
  }));
}

export {
  putRecord,
}
/* Effect Not a pure module */
