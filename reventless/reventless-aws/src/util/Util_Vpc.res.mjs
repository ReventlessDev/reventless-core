// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Pulumi from "@pulumi/pulumi";
import * as Stdlib_JsError from "@rescript/runtime/lib/es6/Stdlib_JsError.js";

function getVpcConfig(stackName, outputName) {
  let stackReference = new Pulumi.StackReference(stackName);
  let vpcOutput = stackReference.requireOutput(outputName);
  return vpcOutput.apply(vpc => {
    let match = vpc.securityGroup.id;
    let match$1 = vpc.privateSubnet.id;
    let match$2 = vpc.vpc.id;
    if (match !== undefined && match$1 !== undefined && match$2 !== undefined) {
      return {
        securityGroupIds: [match],
        subnetIds: [match$1]
      };
    } else {
      return Stdlib_JsError.throwWithMessage("Output is not a ReventlessCore Vpc Component");
    }
  });
}

export {
  getVpcConfig,
}
/* @pulumi/pulumi Not a pure module */
