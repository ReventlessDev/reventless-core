node_modules/
# ReScript build artifacts — regenerated by `rescript build`, not tracked.
# (The hand-written genType bridges are exceptions: the cross-package one at the
# package root, and the in-package one in src/, un-ignored below.)
lib/
src/*.res.js
src/*.gen.ts
!src/ReventlessDomainProtocol.gen.ts
