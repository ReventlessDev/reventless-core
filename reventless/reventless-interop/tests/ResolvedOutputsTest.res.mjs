// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Plugin$ReventlessInterop from "../src/components/Plugin.res.mjs";
import * as QueryDb$ReventlessInterop from "../src/components/QueryDb.res.mjs";
import * as EventLog$ReventlessInterop from "../src/components/EventLog.res.mjs";
import * as Aggregate$ReventlessInterop from "../src/components/Aggregate.res.mjs";
import * as DcbEventLog$ReventlessInterop from "../src/components/DcbEventLog.res.mjs";
import * as StateViewSlice$ReventlessInterop from "../src/components/StateViewSlice.res.mjs";
import * as AutomationSlice$ReventlessInterop from "../src/components/AutomationSlice.res.mjs";
import * as CommandGenerator$ReventlessInterop from "../src/components/CommandGenerator.res.mjs";
import * as StateChangeSlice$ReventlessInterop from "../src/components/StateChangeSlice.res.mjs";
import * as InboundTranslationSlice$ReventlessInterop from "../src/components/InboundTranslationSlice.res.mjs";
import * as OutboundTranslationSlice$ReventlessInterop from "../src/components/OutboundTranslationSlice.res.mjs";

let resource_resourceInfo = {
  TAG: "StorageKeys",
  partitionKey: "id",
  sortKey: undefined
};

let resource_configuration = {};

let resource_tags = {};

let resource = {
  name: "my-table",
  id: "abc123",
  urn: "urn:pulumi:stack::project::resource",
  resourceInfo: resource_resourceInfo,
  service: "dynamodb",
  role: "",
  region: "",
  resourceType: "",
  configuration: resource_configuration,
  tags: resource_tags
};

let queryDb = {
  resources: [resource]
};

let readModelQueryDb = {
  resources: [resource]
};

let eventTopicOutputs = {
  resources: [resource]
};

globalThis.describe("DCB interop types — round-trip serialization", () => {
  globalThis.test("DcbEventLog.resolvedOutputs round-trips", () => {
    let original_resources = [resource];
    let original = {
      resources: original_resources,
      eventTopic: eventTopicOutputs
    };
    let json = S.reverseConvertToJsonOrThrow(original, DcbEventLog$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, DcbEventLog$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("StateChangeSlice.resolvedOutputs round-trips", () => {
    let original = {
      resources: [resource]
    };
    let json = S.reverseConvertToJsonOrThrow(original, StateChangeSlice$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, StateChangeSlice$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("StateViewSlice.resolvedOutputs round-trips", () => {
    let original_resources = [resource];
    let original = {
      resources: original_resources,
      queryDb: queryDb
    };
    let json = S.reverseConvertToJsonOrThrow(original, StateViewSlice$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, StateViewSlice$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("AutomationSlice.resolvedOutputs round-trips", () => {
    let original_resources = [resource];
    let original = {
      resources: original_resources,
      queryDb: queryDb
    };
    let json = S.reverseConvertToJsonOrThrow(original, AutomationSlice$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, AutomationSlice$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("OutboundTranslationSlice.resolvedOutputs round-trips", () => {
    let original_resources = [resource];
    let original = {
      resources: original_resources,
      queryDb: queryDb
    };
    let json = S.reverseConvertToJsonOrThrow(original, OutboundTranslationSlice$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, OutboundTranslationSlice$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("InboundTranslationSlice.resolvedOutputs round-trips", () => {
    let original_resources = [resource];
    let original = {
      resources: original_resources,
      queryDb: queryDb
    };
    let json = S.reverseConvertToJsonOrThrow(original, InboundTranslationSlice$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, InboundTranslationSlice$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("QueryDb.resolvedOutputs round-trips", () => {
    let original = {
      resources: [resource]
    };
    let json = S.reverseConvertToJsonOrThrow(original, QueryDb$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, QueryDb$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
});

let commandTopicOutputs = {
  resources: [resource]
};

let commandGeneratorOutputs = {
  resources: [resource]
};

let eventLogOutputs_resources = [resource];

let eventLogOutputs = {
  resources: eventLogOutputs_resources,
  eventTopic: eventTopicOutputs
};

globalThis.describe("Aggregate interop types — round-trip serialization", () => {
  globalThis.test("Aggregate.resolvedOutputs round-trips (without eventMapper)", () => {
    let original = {
      name: "Customer",
      commandGenerator: commandGeneratorOutputs,
      commandTopic: commandTopicOutputs,
      eventLog: eventLogOutputs
    };
    let json = S.reverseConvertToJsonOrThrow(original, Aggregate$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Aggregate$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("Aggregate.resolvedOutputs round-trips (with eventMapper)", () => {
    let em_eventCollector = {
      name: "CustomerMapperEC",
      resources: [resource]
    };
    let em = {
      name: "CustomerMapper",
      eventCollector: em_eventCollector
    };
    let original_eventMapper = em;
    let original = {
      name: "Customer",
      commandGenerator: commandGeneratorOutputs,
      commandTopic: commandTopicOutputs,
      eventLog: eventLogOutputs,
      eventMapper: original_eventMapper
    };
    let json = S.reverseConvertToJsonOrThrow(original, Aggregate$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Aggregate$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("EventLog.resolvedOutputs round-trips", () => {
    let json = S.reverseConvertToJsonOrThrow(eventLogOutputs, EventLog$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, EventLog$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(eventLogOutputs);
  });
  globalThis.test("CommandGenerator.resolvedOutputs round-trips", () => {
    let json = S.reverseConvertToJsonOrThrow(commandGeneratorOutputs, CommandGenerator$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, CommandGenerator$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(commandGeneratorOutputs);
  });
});

globalThis.describe("Plugin.resolvedOutputs — round-trip with DCB fields", () => {
  globalThis.test("minimal plugin (no optional fields) round-trips", () => {
    let original = {
      id: "test@1.0",
      version: "1.0.0"
    };
    let json = S.reverseConvertToJsonOrThrow(original, Plugin$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Plugin$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("plugin with readModels round-trips", () => {
    let rm_sourceNames = ["Agg1"];
    let rm = {
      name: "MyRM",
      queryDb: readModelQueryDb,
      sourceNames: rm_sourceNames
    };
    let original_readModels = Object.fromEntries([[
        "MyRM",
        rm
      ]]);
    let original = {
      id: "test@1.0",
      version: "1.0.0",
      readModels: original_readModels
    };
    let json = S.reverseConvertToJsonOrThrow(original, Plugin$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Plugin$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("plugin with DCB slices round-trips", () => {
    let dcbLog_resources = [resource];
    let dcbLog = {
      resources: dcbLog_resources,
      eventTopic: eventTopicOutputs
    };
    let scs = {
      resources: [resource]
    };
    let svs_resources = [resource];
    let svs = {
      resources: svs_resources,
      queryDb: queryDb
    };
    let auto_resources = [resource];
    let auto = {
      resources: auto_resources,
      queryDb: queryDb
    };
    let ots_resources = [resource];
    let ots = {
      resources: ots_resources,
      queryDb: queryDb
    };
    let its_resources = [resource];
    let its = {
      resources: its_resources,
      queryDb: queryDb
    };
    let original_dcbEventLog = dcbLog;
    let original_stateChangeSlices = Object.fromEntries([[
        "AddItem",
        scs
      ]]);
    let original_stateViewSlices = Object.fromEntries([[
        "ItemsView",
        svs
      ]]);
    let original_automationSlices = Object.fromEntries([[
        "ShipOrder",
        auto
      ]]);
    let original_outboundTranslationSlices = Object.fromEntries([[
        "SendEmail",
        ots
      ]]);
    let original_inboundTranslationSlices = Object.fromEntries([[
        "PaymentHook",
        its
      ]]);
    let original = {
      id: "dcb-plugin@1.0",
      version: "1.0.0",
      dcbEventLog: original_dcbEventLog,
      stateChangeSlices: original_stateChangeSlices,
      stateViewSlices: original_stateViewSlices,
      automationSlices: original_automationSlices,
      outboundTranslationSlices: original_outboundTranslationSlices,
      inboundTranslationSlices: original_inboundTranslationSlices
    };
    let json = S.reverseConvertToJsonOrThrow(original, Plugin$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Plugin$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("plugin with aggregates round-trips", () => {
    let agg = {
      name: "Customer",
      commandGenerator: commandGeneratorOutputs,
      commandTopic: commandTopicOutputs,
      eventLog: eventLogOutputs
    };
    let original_aggregates = Object.fromEntries([[
        "Customer",
        agg
      ]]);
    let original = {
      id: "agg@1.0",
      version: "1.0.0",
      aggregates: original_aggregates
    };
    let json = S.reverseConvertToJsonOrThrow(original, Plugin$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Plugin$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("plugin with all fields round-trips", () => {
    let ep_commandTopic = {
      resources: [resource]
    };
    let ep = {
      name: "MyEP",
      commandTopic: ep_commandTopic,
      eventTopic: eventTopicOutputs
    };
    let rm_sourceNames = ["Agg1"];
    let rm = {
      name: "RM1",
      queryDb: readModelQueryDb,
      sourceNames: rm_sourceNames
    };
    let agg = {
      name: "Agg1",
      commandGenerator: commandGeneratorOutputs,
      commandTopic: commandTopicOutputs,
      eventLog: eventLogOutputs
    };
    let dcbLog_resources = [resource];
    let dcbLog = {
      resources: dcbLog_resources,
      eventTopic: eventTopicOutputs
    };
    let scs = {
      resources: [resource]
    };
    let svs_resources = [resource];
    let svs = {
      resources: svs_resources,
      queryDb: queryDb
    };
    let auto_resources = [resource];
    let auto = {
      resources: auto_resources,
      queryDb: queryDb
    };
    let ots_resources = [resource];
    let ots = {
      resources: ots_resources,
      queryDb: queryDb
    };
    let its_resources = [resource];
    let its = {
      resources: its_resources,
      queryDb: queryDb
    };
    let original_aggregates = Object.fromEntries([[
        "Agg1",
        agg
      ]]);
    let original_readModels = Object.fromEntries([[
        "RM1",
        rm
      ]]);
    let original_extensionPoints = Object.fromEntries([[
        "MyEP",
        ep
      ]]);
    let original_dcbEventLog = dcbLog;
    let original_stateChangeSlices = Object.fromEntries([[
        "SCS1",
        scs
      ]]);
    let original_stateViewSlices = Object.fromEntries([[
        "SVS1",
        svs
      ]]);
    let original_automationSlices = Object.fromEntries([[
        "AS1",
        auto
      ]]);
    let original_outboundTranslationSlices = Object.fromEntries([[
        "OTS1",
        ots
      ]]);
    let original_inboundTranslationSlices = Object.fromEntries([[
        "ITS1",
        its
      ]]);
    let original = {
      id: "full@2.0",
      version: "2.0.0",
      aggregates: original_aggregates,
      readModels: original_readModels,
      extensionPoints: original_extensionPoints,
      dcbEventLog: original_dcbEventLog,
      stateChangeSlices: original_stateChangeSlices,
      stateViewSlices: original_stateViewSlices,
      automationSlices: original_automationSlices,
      outboundTranslationSlices: original_outboundTranslationSlices,
      inboundTranslationSlices: original_inboundTranslationSlices
    };
    let json = S.reverseConvertToJsonOrThrow(original, Plugin$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Plugin$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
  globalThis.test("minimal plugin without optional fields round-trips", () => {
    let original = {
      id: "empty@1.0",
      version: "1.0.0"
    };
    let json = S.reverseConvertToJsonOrThrow(original, Plugin$ReventlessInterop.resolvedOutputsSchema);
    let parsed = S.parseOrThrow(json, Plugin$ReventlessInterop.resolvedOutputsSchema);
    globalThis.expect(parsed).toEqual(original);
  });
});

export {
  resource,
  queryDb,
  readModelQueryDb,
  eventTopicOutputs,
  commandTopicOutputs,
  commandGeneratorOutputs,
  eventLogOutputs,
}
/*  Not a pure module */
