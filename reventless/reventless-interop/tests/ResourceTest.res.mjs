// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Resource$ReventlessInterop from "../src/Resource.res.mjs";

function roundTripInfo(v) {
  let json = S.reverseConvertToJsonOrThrow(v, Resource$ReventlessInterop.resourceInfoSchema);
  globalThis.expect(S.parseOrThrow(json, Resource$ReventlessInterop.resourceInfoSchema)).toEqual(v);
}

globalThis.describe("Resource.resourceInfo round-trip", () => {
  globalThis.test("StorageKeys with a sort key (Some)", () => roundTripInfo({
    TAG: "StorageKeys",
    partitionKey: "pk",
    sortKey: "sk"
  }));
  globalThis.test("StorageKeys without a sort key (None)", () => roundTripInfo({
    TAG: "StorageKeys",
    partitionKey: "pk",
    sortKey: undefined
  }));
  globalThis.test("StreamSource", () => roundTripInfo({
    TAG: "StreamSource",
    sourceUrn: "urn:x"
  }));
  globalThis.test("ApiResolver", () => roundTripInfo({
    TAG: "ApiResolver",
    typeName: "Query",
    fieldName: "products"
  }));
  globalThis.test("NoInfo", () => roundTripInfo("NoInfo"));
});

globalThis.describe("Resource.t round-trip", () => {
  globalThis.test("a full resource carrying a StorageKeys(Some) resourceInfo", () => {
    let original_resourceInfo = {
      TAG: "StorageKeys",
      partitionKey: "orderId",
      sortKey: "seq"
    };
    let original_configuration = Object.fromEntries([[
        "billingMode",
        "PAY_PER_REQUEST"
      ]]);
    let original_tags = Object.fromEntries([[
        "env",
        "prod"
      ]]);
    let original = {
      name: "OrdersTable",
      id: "orders-123",
      urn: "urn:pulumi:stack::proj::aws:dynamodb/table:Table::OrdersTable",
      resourceInfo: original_resourceInfo,
      service: "dynamodb",
      role: "table",
      region: "eu-west-1",
      resourceType: "aws:dynamodb/table:Table",
      configuration: original_configuration,
      tags: original_tags
    };
    let json = S.reverseConvertToJsonOrThrow(original, Resource$ReventlessInterop.schema);
    globalThis.expect(S.parseOrThrow(json, Resource$ReventlessInterop.schema)).toEqual(original);
  });
});

export {
  roundTripInfo,
}
/*  Not a pure module */
