// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Output$Pulumi from "@reventlessdev/rescript-pulumi-pulumi/src/Output.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";
import * as Compat$ReventlessInterop from "./Compat.res.mjs";
import * as ExportMeta$ReventlessInterop from "./ExportMeta.res.mjs";

let coreEntry = Stdlib_Option.map(new Pulumi.Config("core").get("stack"), name => [
  name,
  new Pulumi.StackReference(name)
]);

let stackEntries = Stdlib_Option.getOr(new Pulumi.Config("interstack").getObject("dependencies"), []).map(name => [
  name,
  new Pulumi.StackReference(name)
]).concat(Stdlib_Option.mapOr(coreEntry, [], e => [e]));

function parseMeta(raw) {
  try {
    return {
      TAG: "Ok",
      _0: S.parseOrThrow(raw, ExportMeta$ReventlessInterop.schema)
    };
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    let msg = Stdlib_Option.getOr(Stdlib_Option.flatMap(Stdlib_JsExn.fromException(exn), Stdlib_JsExn.message), "failed to parse interop meta");
    return {
      TAG: "Error",
      _0: msg
    };
  }
}

function queryAllArray(outputName, requiredFields, fromJson) {
  return Pulumi.all(stackEntries.map(param => {
    let stackRef = param[1];
    let stackName = param[0];
    let metaOutput = stackRef.getOutput("_interopMeta");
    let dataOutput = stackRef.getOutput(outputName);
    return Output$Pulumi.flatMap(metaOutput, metaOpt => dataOutput.apply(dataOpt => {
      if (metaOpt === undefined) {
        return [{
            TAG: "Error",
            _0: {
              TAG: "MetaMissing",
              stackName: stackName
            }
          }];
      }
      if (dataOpt === undefined) {
        return [];
      }
      let meta = parseMeta(metaOpt);
      if (meta.TAG !== "Ok") {
        return [{
            TAG: "Error",
            _0: {
              TAG: "MetaMissing",
              stackName: stackName
            }
          }];
      }
      let meta$1 = meta._0;
      let items = Stdlib_JSON.Decode.array(dataOpt);
      if (items !== undefined) {
        return items.map(item => Compat$ReventlessInterop.validateAndProject(stackName, meta$1, outputName, item, requiredFields, fromJson));
      } else {
        return [{
            TAG: "Error",
            _0: {
              TAG: "DecodeFailed",
              stackName: stackName,
              reason: `"` + outputName + `" stack export is not an array`
            }
          }];
      }
    }));
  })).apply(results => results.flatMap(x => x));
}

function queryAllSingle(outputName, requiredFields, fromJson) {
  return Pulumi.all(stackEntries.map(param => {
    let stackRef = param[1];
    let stackName = param[0];
    let metaOutput = stackRef.getOutput("_interopMeta");
    let dataOutput = stackRef.getOutput(outputName);
    return Output$Pulumi.flatMap(metaOutput, metaOpt => dataOutput.apply(dataOpt => {
      if (metaOpt === undefined) {
        return {
          TAG: "Error",
          _0: {
            TAG: "MetaMissing",
            stackName: stackName
          }
        };
      }
      if (dataOpt === undefined) {
        return;
      }
      let meta = parseMeta(metaOpt);
      if (meta.TAG === "Ok") {
        return Compat$ReventlessInterop.validateAndProject(stackName, meta._0, outputName, dataOpt, requiredFields, fromJson);
      } else {
        return {
          TAG: "Error",
          _0: {
            TAG: "MetaMissing",
            stackName: stackName
          }
        };
      }
    }));
  })).apply(results => Stdlib_Array.filterMap(results, x => x));
}

function Make(P) {
  let queryAll = () => queryAllArray("tasks", P.requiredFields, P.fromJson);
  let mergeWith = locals => queryAllArray("tasks", P.requiredFields, P.fromJson).apply(results => {
    let remotes = Stdlib_Array.filterMap(results, r => {
      if (r.TAG === "Ok") {
        return Primitive_option.some(r._0);
      }
    });
    return locals.concat(remotes);
  });
  return {
    queryAll: queryAll,
    mergeWith: mergeWith
  };
}

let Task = {
  Make: Make
};

function Make$1(P) {
  let queryAll = () => queryAllArray("eventMappers", P.requiredFields, P.fromJson);
  let mergeWith = locals => queryAllArray("eventMappers", P.requiredFields, P.fromJson).apply(results => {
    let remotes = Stdlib_Array.filterMap(results, r => {
      if (r.TAG === "Ok") {
        return Primitive_option.some(r._0);
      }
    });
    return locals.concat(remotes);
  });
  return {
    queryAll: queryAll,
    mergeWith: mergeWith
  };
}

let EventMapper = {
  Make: Make$1
};

function Make$2(P) {
  let queryAll = () => queryAllSingle("plugin", P.requiredFields, P.fromJson);
  let mergeWith = locals => queryAllSingle("plugin", P.requiredFields, P.fromJson).apply(results => {
    let remotes = Stdlib_Array.filterMap(results, r => {
      if (r.TAG === "Ok") {
        return Primitive_option.some(r._0);
      }
    });
    return locals.concat(remotes);
  });
  return {
    queryAll: queryAll,
    mergeWith: mergeWith
  };
}

let Plugin = {
  Make: Make$2
};

export {
  stackEntries,
  parseMeta,
  queryAllArray,
  queryAllSingle,
  Task,
  EventMapper,
  Plugin,
}
/* coreEntry Not a pure module */
