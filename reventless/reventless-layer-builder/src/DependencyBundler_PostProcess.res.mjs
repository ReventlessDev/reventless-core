// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Rimraf from "rimraf";
import * as Nodepath from "node:path";

async function rescriptDependent(_node, cwd) {
  let rmRes = Rimraf.rimraf("**/*.res", {
    glob: {
      cwd: cwd
    }
  });
  let rmResi = Rimraf.rimraf("**/*.resi", {
    glob: {
      cwd: cwd
    }
  });
  await Promise.all([
    rmRes,
    rmResi
  ]);
}

async function reventlessCore(_node, cwd) {
  return await Rimraf.rimraf([
    Nodepath.resolve(cwd, "coverage"),
    Nodepath.resolve(cwd, "scripts"),
    Nodepath.resolve(cwd, "test-helper"),
    Nodepath.resolve(cwd, "tests")
  ]);
}

async function deleteTests(_node, cwd) {
  return await Rimraf.rimraf(Nodepath.resolve(cwd, "tests"));
}

async function deleteEffectSrc(_node, cwd) {
  return await Rimraf.rimraf(Nodepath.resolve(cwd, "src"));
}

export {
  rescriptDependent,
  reventlessCore,
  deleteTests,
  deleteEffectSrc,
}
/* rimraf Not a pure module */
