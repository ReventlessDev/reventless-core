// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Message$Reventless from "@reventlessdev/reventless-spec/src/types/Message.res.mjs";
import * as Util_Sury$Reventless from "@reventlessdev/reventless-spec/src/util/Util_Sury.res.mjs";

function Make(Bus) {
  let make = resources => ({
    resources: resources,
    remotePublish: async jsons => {
      await Promise.all(jsons.map(async cmdJson => {
        let body = Object.fromEntries([
          [
            "id",
            cmdJson.id
          ],
          [
            "meta",
            Util_Sury$Reventless.toJson(cmdJson.meta, Message$Reventless.metaSchema)
          ],
          [
            "command",
            cmdJson.commandJson
          ]
        ]);
        let channelName = resources.length !== 0 ? resources[0].name : "";
        return await Bus.dispatchCommand(channelName, body);
      }));
    }
  });
  return {
    make: make
  };
}

export {
  Make,
}
/* Message-Reventless Not a pure module */
