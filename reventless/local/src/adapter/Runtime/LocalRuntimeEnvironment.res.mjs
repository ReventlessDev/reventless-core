// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Effect from "effect/Effect";
import * as Deferred from "effect/Deferred";

function make(param, handler, $staropt$star, $staropt$star$1, param$1) {
  $staropt$star !== undefined;
  $staropt$star$1 !== undefined;
  let handlerDeferred = Effect.runSync(Deferred.make());
  let subscriptionLatch = Effect.runSync(Effect.makeLatch(false));
  handler.apply(h => {
    Effect.runPromise(Deferred.succeed(handlerDeferred, h));
  });
  return {
    parts: {
      handlerDeferred: handlerDeferred,
      subscriptionLatch: subscriptionLatch
    },
    resources: []
  };
}

function groupBySource(event) {
  let dict = {};
  dict["inmemory"] = event;
  return dict;
}

function extractMetaField(event, field) {
  return Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_JSON.Decode.object(event), obj => obj["meta"]), Stdlib_JSON.Decode.object), meta => meta[field]), Stdlib_JSON.Decode.string);
}

function extractCorrelationId(event) {
  return extractMetaField(event, "correlationId");
}

function extractCausationId(event) {
  return extractMetaField(event, "causationId");
}

export {
  make,
  groupBySource,
  extractMetaField,
  extractCorrelationId,
  extractCausationId,
}
/* effect/Effect Not a pure module */
