// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Globals from "@jest/globals";
import * as LocalBus$ReventlessLocal from "../../src/adapter/LocalBus.res.mjs";
import * as TestRunner$ReventlessLocal from "../../src/test/TestRunner.res.mjs";
import * as LocalScheduledPublisher$ReventlessLocal from "../../src/adapter/Scheduler/LocalScheduledPublisher.res.mjs";

globalThis.beforeAll(() => {
  Globals.jest.useFakeTimers();
});

TestRunner$ReventlessLocal.setup();

globalThis.afterAll(() => {
  Globals.jest.useRealTimers();
});

function makeTopicResource(topicName) {
  return {
    name: topicName,
    id: topicName,
    urn: topicName,
    resourceInfo: "NoInfo",
    service: "memory:InMemory",
    role: "",
    region: "",
    resourceType: "",
    configuration: {},
    tags: {}
  };
}

globalThis.describe("LocalScheduledPublisher", () => {
  globalThis.describe("createSchedule", () => {
    globalThis.test("single-shot schedule fires event after runAllTimers", async () => {
      let TestBus = LocalBus$ReventlessLocal.Make({});
      let SP = LocalScheduledPublisher$ReventlessLocal.Make(TestBus);
      let count = {
        contents: 0
      };
      TestBus.subscribeToEvents("sched-topic", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      let pub = SP.make("scheduler", {});
      let ops = await TestRunner$ReventlessLocal.resolve(pub.operations);
      await ops.createSchedule([makeTopicResource("sched-topic")], {
        name: "one-shot",
        rate: {
          TAG: "Single",
          _0: 2024,
          _1: 1,
          _2: 1,
          _3: 0,
          _4: 0
        },
        payload: "{}"
      });
      Globals.jest.runAllTimers();
      await Promise.resolve();
      await Promise.resolve();
      globalThis.expect(count.contents).toBe(1);
      return SP.reset();
    });
    globalThis.test("recurring schedule fires on each interval advance", async () => {
      let TestBus = LocalBus$ReventlessLocal.Make({});
      let SP = LocalScheduledPublisher$ReventlessLocal.Make(TestBus);
      let count = {
        contents: 0
      };
      TestBus.subscribeToEvents("repeat-topic", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      let pub = SP.make("scheduler2", {});
      let ops = await TestRunner$ReventlessLocal.resolve(pub.operations);
      await ops.createSchedule([makeTopicResource("repeat-topic")], {
        name: "every-minute",
        rate: {
          TAG: "Minutes",
          _0: 1
        },
        payload: "{}"
      });
      Globals.jest.advanceTimersByTime(60000);
      await Promise.resolve();
      await Promise.resolve();
      Globals.jest.advanceTimersByTime(60000);
      await Promise.resolve();
      await Promise.resolve();
      globalThis.expect(count.contents).toBe(2);
      return SP.reset();
    });
  });
  globalThis.describe("deleteSchedule", () => {
    globalThis.test("clears a named schedule; advancing timers does not fire it", async () => {
      let TestBus = LocalBus$ReventlessLocal.Make({});
      let SP = LocalScheduledPublisher$ReventlessLocal.Make(TestBus);
      let count = {
        contents: 0
      };
      TestBus.subscribeToEvents("del-topic", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      let pub = SP.make("scheduler3", {});
      let ops = await TestRunner$ReventlessLocal.resolve(pub.operations);
      await ops.createSchedule([makeTopicResource("del-topic")], {
        name: "to-delete",
        rate: {
          TAG: "Minutes",
          _0: 1
        },
        payload: "{}"
      });
      await ops.deleteSchedule([], "to-delete");
      Globals.jest.advanceTimersByTime(60000);
      globalThis.expect(count.contents).toBe(0);
      return SP.reset();
    });
  });
  globalThis.describe("reset", () => {
    globalThis.test("clears all active timers; subsequent advance does not fire", async () => {
      let TestBus = LocalBus$ReventlessLocal.Make({});
      let SP = LocalScheduledPublisher$ReventlessLocal.Make(TestBus);
      let count = {
        contents: 0
      };
      TestBus.subscribeToEvents("reset-topic", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      let pub = SP.make("scheduler4", {});
      let ops = await TestRunner$ReventlessLocal.resolve(pub.operations);
      await ops.createSchedule([makeTopicResource("reset-topic")], {
        name: "reset-sched",
        rate: {
          TAG: "Minutes",
          _0: 5
        },
        payload: "{}"
      });
      SP.reset();
      Globals.jest.advanceTimersByTime(300000);
      globalThis.expect(count.contents).toBe(0);
    });
  });
  globalThis.describe("rateToMs", () => {
    globalThis.test("Minutes(n) fires after n minutes", async () => {
      let TestBus = LocalBus$ReventlessLocal.Make({});
      let SP = LocalScheduledPublisher$ReventlessLocal.Make(TestBus);
      let count = {
        contents: 0
      };
      TestBus.subscribeToEvents("rate-topic", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      let pub = SP.make("rate-scheduler", {});
      let ops = await TestRunner$ReventlessLocal.resolve(pub.operations);
      await ops.createSchedule([makeTopicResource("rate-topic")], {
        name: "rate-sched",
        rate: {
          TAG: "Minutes",
          _0: 3
        },
        payload: "{}"
      });
      Globals.jest.advanceTimersByTime(179999);
      globalThis.expect(count.contents).toBe(0);
      Globals.jest.advanceTimersByTime(1);
      await Promise.resolve();
      await Promise.resolve();
      globalThis.expect(count.contents).toBe(1);
      return SP.reset();
    });
    globalThis.test("Hours(n) fires after n hours", async () => {
      let TestBus = LocalBus$ReventlessLocal.Make({});
      let SP = LocalScheduledPublisher$ReventlessLocal.Make(TestBus);
      let count = {
        contents: 0
      };
      TestBus.subscribeToEvents("hours-topic", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      let pub = SP.make("hours-scheduler", {});
      let ops = await TestRunner$ReventlessLocal.resolve(pub.operations);
      await ops.createSchedule([makeTopicResource("hours-topic")], {
        name: "hours-sched",
        rate: {
          TAG: "Hours",
          _0: 2
        },
        payload: "{}"
      });
      Globals.jest.advanceTimersByTime(7200000);
      await Promise.resolve();
      await Promise.resolve();
      globalThis.expect(count.contents).toBe(1);
      return SP.reset();
    });
  });
});

export {
  makeTopicResource,
}
/*  Not a pure module */
