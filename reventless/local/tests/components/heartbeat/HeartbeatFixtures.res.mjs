// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Globals from "@jest/globals";
import * as TestRunner$ReventlessLocal from "../../../src/test/TestRunner.res.mjs";
import * as Heartbeat_Builder$ReventlessCore from "@reventlessdev/reventless-core/src/components/Heartbeat/Heartbeat_Builder.res.mjs";
import * as LocalHeartbeatRunner$ReventlessLocal from "../../../src/adapter/Heartbeat/LocalHeartbeatRunner.res.mjs";

TestRunner$ReventlessLocal.setup();

let HeartbeatMaker = Heartbeat_Builder$ReventlessCore.Make({
  make: LocalHeartbeatRunner$ReventlessLocal.make
});

globalThis.beforeAll(() => {
  Globals.jest.useFakeTimers();
});

globalThis.afterAll(() => {
  LocalHeartbeatRunner$ReventlessLocal.reset();
  Globals.jest.useRealTimers();
});

let capturedCount = {
  contents: 0
};

async function mockPublish(cmds) {
  capturedCount.contents = capturedCount.contents + cmds.length | 0;
}

let resolvedHandler = {
  contents: undefined
};

globalThis.beforeAll(async () => {
  let h = await TestRunner$ReventlessLocal.resolve(HeartbeatMaker.makeHandler("hb-id-1", 1, mockPublish));
  resolvedHandler.contents = h;
});

export {
  HeartbeatMaker,
  capturedCount,
  mockPublish,
  resolvedHandler,
}
/*  Not a pure module */
