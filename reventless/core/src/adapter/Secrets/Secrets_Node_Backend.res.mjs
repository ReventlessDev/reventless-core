// Generated by ReScript, PLEASE EDIT WITH CARE

import * as NodeCrypto from "@reventlessdev/rescript-node/src/NodeCrypto.res.mjs";
import * as Stdlib_Int from "@rescript/runtime/lib/es6/Stdlib_Int.js";
import * as Nodecrypto from "node:crypto";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Primitive_int from "@rescript/runtime/lib/es6/Primitive_int.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Secrets$Reventless from "@reventlessdev/reventless-spec/src/semantic/Secrets.res.mjs";

function _bytes(count) {
  let hex = Nodecrypto.randomBytes(count).toString("hex");
  return Stdlib_Array.fromInitializer(count, i => Stdlib_Option.getOr(Stdlib_Int.fromString(hex.substring((i << 1), (i << 1) + 2 | 0), 16), 0));
}

function accepts(byte, size) {
  return byte < (256 - Primitive_int.mod_(256, size) | 0);
}

function _draw(length, from) {
  let size = from.length;
  let out = [];
  while (out.length < length) {
    _bytes(((length - out.length | 0) << 1) + 8 | 0).forEach(byte => {
      if (accepts(byte, size) && out.length < length) {
        out.push(from.charAt(Primitive_int.mod_(byte, size)));
        return;
      }
    });
  };
  return out.join("");
}

function provider_randomToken(length, alphabet) {
  if (length <= 0) {
    return {
      TAG: "Error",
      _0: {
        TAG: "Unavailable",
        _0: `a token needs a positive length, not ` + length.toString()
      }
    };
  } else {
    return {
      TAG: "Ok",
      _0: _draw(length, Secrets$Reventless.characters(alphabet))
    };
  }
}

function provider_hash(input) {
  return {
    TAG: "Ok",
    _0: NodeCrypto.sha256Hex(input)
  };
}

let provider = {
  randomToken: provider_randomToken,
  hash: provider_hash
};

export {
  _bytes,
  accepts,
  _draw,
  provider,
}
/* NodeCrypto Not a pure module */
