// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_JsError from "@rescript/runtime/lib/es6/Stdlib_JsError.js";
import * as ResourceAttribution$ReventlessCore from "../../ResourceAttribution.res.mjs";

function onProvisioned(param, param$1, param$2, param$3, param$4, param$5, param$6) {
  
}

let Noop = {
  onProvisioned: onProvisioned
};

let backend = {
  contents: Noop
};

let registered = {
  contents: false
};

function use(b) {
  if (registered.contents) {
    Stdlib_JsError.throwWithMessage("EventLogProvisioning.use called twice: only one event-log backend can be registered. A DynamoDB stream supports two readers per shard and core's event collector already holds one, so a second backend would throttle the first. Compose the two backends into one, or fan out behind a single registration.");
  }
  registered.contents = true;
  backend.contents = b;
}

function reset() {
  backend.contents = Noop;
  registered.contents = false;
}

function notify(logStyle, name, owner, resources, opts) {
  let B = backend.contents;
  let match = ResourceAttribution$ReventlessCore.current.contents;
  B.onProvisioned(logStyle, name, owner, match.plugin, match.platform, resources, opts);
}

export {
  Noop,
  backend,
  registered,
  use,
  reset,
  notify,
}
/* No side effect */
