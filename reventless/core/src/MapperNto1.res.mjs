// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as Message$ReventlessCore from "./Message.res.mjs";

function makeGenericMap(decode, map) {
  return json => map(decode(json));
}

function Mapper(Spec) {
  return Target => (Mappings => {
    let findMappings = (sourceNameOpt, mappings) => Stdlib_Option.mapOr(sourceNameOpt, [], sourceName => mappings.filter(Mapping => Mapping.sourceName === sourceName));
    let variantTagOfEnvelope = json => Stdlib_Option.getOr(Stdlib_Option.map(Stdlib_Option.flatMap(Stdlib_JSON.Decode.object(json), d => d["event"]), Message$ReventlessCore.variantNameOfJson), "unknown");
    let map = (sourceName, json) => {
      let tag = variantTagOfEnvelope(json);
      return Stdlib_Array.filterMap(findMappings(sourceName, Mappings.mappings), Mapping => {
        if (Mapping.acceptedTags.includes(tag)) {
          return Primitive_option.some(Mapping.project(json));
        }
      });
    };
    return {
      map: map
    };
  });
}

export {
  makeGenericMap,
  Mapper,
}
/* Message-ReventlessCore Not a pure module */
