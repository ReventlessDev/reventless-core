// Generated by ReScript, PLEASE EDIT WITH CARE


function toString(scope) {
  switch (scope) {
    case "Component" :
      return "component";
    case "Plugin" :
      return "plugin";
    case "Platform" :
      return "platform";
  }
}

let Scope = {
  toString: toString
};

function toString$1(role) {
  if (typeof role === "object") {
    return role._0;
  }
  switch (role) {
    case "CommandTopic" :
      return "CommandTopic";
    case "EventTopic" :
      return "EventTopic";
    case "StateTopic" :
      return "StateTopic";
    case "EventLog" :
      return "EventLog";
    case "DcbEventLog" :
      return "DcbEventLog";
    case "EventLogSubscription" :
      return "EventLogSubscription";
    case "EventCollector" :
      return "EventCollector";
    case "QueryDb" :
      return "QueryDb";
    case "Runtime" :
      return "Runtime";
    case "DeadLetter" :
      return "DeadLetter";
    case "Scheduler" :
      return "Scheduler";
    case "Logs" :
      return "Logs";
    case "Identity" :
      return "Identity";
    case "Api" :
      return "Api";
    case "Auth" :
      return "Auth";
    case "Hosting" :
      return "Hosting";
    case "Network" :
      return "Network";
    case "EventSourceMapping" :
      return "EventSourceMapping";
    case "DataTransfer" :
      return "DataTransfer";
  }
}

let Role = {
  toString: toString$1
};

let current = {
  contents: {
    platform: undefined,
    plugin: undefined
  }
};

function enter(platform, plugin) {
  let previous = current.contents;
  current.contents = {
    platform: platform,
    plugin: plugin
  };
  return previous;
}

function restore(previous) {
  current.contents = previous;
}

function enterCaptured(captured) {
  let previous = current.contents;
  current.contents = captured;
  return previous;
}

function within(captured, fn) {
  let previous = enterCaptured(captured);
  try {
    let result = fn();
    current.contents = previous;
    return result;
  } catch (e) {
    current.contents = previous;
    throw e;
  }
}

function deferred(fn) {
  let captured = current.contents;
  return () => within(captured, fn);
}

export {
  Scope,
  Role,
  current,
  enter,
  restore,
  enterCaptured,
  within,
  deferred,
}
/* No side effect */
