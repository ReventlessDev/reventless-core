// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Context from "effect/Context";
import * as Identity$Reventless from "@reventlessdev/reventless-spec/src/types/Identity.res.mjs";

let tag = Context.GenericTag("reventless/RequestContext");

function make(correlationId, causationId, component, pluginName, timestamp, retryCount, identityOpt, claimsOpt) {
  let identity = identityOpt !== undefined ? identityOpt : Identity$Reventless.anonymous;
  let claims = claimsOpt !== undefined ? claimsOpt : ({});
  return {
    correlationId: correlationId,
    causationId: causationId,
    component: component,
    pluginName: pluginName,
    timestamp: timestamp,
    retryCount: retryCount,
    identity: identity,
    claims: claims
  };
}

function fromOptions(o) {
  return make(Stdlib_Option.getOr(o.correlationId, "unknown"), o.causationId, o.component, o.pluginName, o.timestamp, o.retryCount, undefined, undefined);
}

function getClaim(ctx, key) {
  return ctx.claims[key];
}

function withClaim(ctx, key, value) {
  let next = Object.fromEntries(Object.entries(ctx.claims));
  next[key] = value;
  let newrecord = {...ctx};
  newrecord.claims = next;
  return newrecord;
}

function test(correlationIdOpt, timestamp, retryCount, identityOpt, claimsOpt) {
  let correlationId = correlationIdOpt !== undefined ? correlationIdOpt : "test-correlation-id";
  let identity = identityOpt !== undefined ? identityOpt : Identity$Reventless.anonymous;
  let claims = claimsOpt !== undefined ? claimsOpt : ({});
  return {
    correlationId: correlationId,
    timestamp: timestamp,
    retryCount: retryCount,
    identity: identity,
    claims: claims
  };
}

export {
  tag,
  make,
  fromOptions,
  getClaim,
  withClaim,
  test,
}
/* tag Not a pure module */
