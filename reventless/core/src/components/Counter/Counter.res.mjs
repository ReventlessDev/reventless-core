// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";

let counterEventSchema = Sury.literal("CountFinished");

let name = ComponentType$ReventlessCore.toName("Counter");

let Source = {
  Id: undefined,
  name: name,
  eventSchema: counterEventSchema
};

let separator = "#";

function makeId(param) {
  return param[0] + (separator + param[1]);
}

function unmakeId(id) {
  let x = id.split(separator);
  let len = x.length;
  if (len !== 1) {
    if (len !== 0) {
      return [
        x[0],
        x[1]
      ];
    } else {
      return [
        "",
        ""
      ];
    }
  }
  let counterId = x[0];
  return [
    counterId,
    ""
  ];
}

let componentType = "Counter";

export {
  componentType,
  counterEventSchema,
  Source,
  separator,
  makeId,
  unmakeId,
}
/* counterEventSchema Not a pure module */
