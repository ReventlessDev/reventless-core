// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Pulumi from "@pulumi/pulumi";
import * as Component$ReventlessCore from "../Component.res.mjs";
import * as EventTopic$ReventlessCore from "./EventTopic.res.mjs";
import * as ComponentType$ReventlessCore from "../../ComponentType.res.mjs";
import * as EventTopic_Operations$ReventlessCore from "./EventTopic_Operations.res.mjs";

function Make(Spec) {
  return Publisher => {
    let make = (name, storageResources, opts) => Component$ReventlessCore.make(ComponentType$ReventlessCore.toString(EventTopic$ReventlessCore.componentType), name, (extra, extra$1) => {
      let opts_parent = Component$ReventlessCore.toPulumiResource(extra);
      let opts = {
        parent: opts_parent
      };
      let publisher = Publisher.make(ComponentType$ReventlessCore.name(extra$1, EventTopic$ReventlessCore.componentType), storageResources, opts);
      Component$ReventlessCore.setOperations(extra, Pulumi.all([
        publisher.publishJson,
        publisher.publishJsonStream
      ]).apply(param => {
        let publishJson = param[0];
        let Operations = EventTopic_Operations$ReventlessCore.Make(Spec)({
          publishJson: publishJson
        });
        return {
          publish: Operations.publish,
          publishJson: publishJson,
          publishJsonStream: param[1]
        };
      }));
      let outputs = {
        resources: publisher.resources
      };
      return Component$ReventlessCore.setOutputs(extra, outputs);
    }, opts);
    return {
      Spec: Spec,
      make: make
    };
  };
}

export {
  Make,
}
/* @pulumi/pulumi Not a pure module */
