// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Pulumi from "@pulumi/pulumi";

function toResolvedOutputs(outputs) {
  let match = outputs.bucketNames;
  let match$1 = outputs.sideEffectSources;
  if (match !== undefined) {
    if (match$1 !== undefined) {
      return Pulumi.all(Object.entries(match).map(param => {
        let k = param[0];
        return param[1].apply(resolved => [
          k,
          resolved
        ]);
      })).apply(pairs => ({
        name: outputs.name,
        bucketNames: Object.fromEntries(pairs),
        sideEffectSources: match$1
      }));
    } else {
      return Pulumi.all(Object.entries(match).map(param => {
        let k = param[0];
        return param[1].apply(resolved => [
          k,
          resolved
        ]);
      })).apply(pairs => ({
        name: outputs.name,
        bucketNames: Object.fromEntries(pairs)
      }));
    }
  } else if (match$1 !== undefined) {
    return Pulumi.output({
      name: outputs.name,
      sideEffectSources: match$1
    });
  } else {
    return Pulumi.output({
      name: outputs.name
    });
  }
}

let componentType = "Task";

export {
  componentType,
  toResolvedOutputs,
}
/* @pulumi/pulumi Not a pure module */
