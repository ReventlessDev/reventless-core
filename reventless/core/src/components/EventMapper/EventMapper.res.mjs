// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Output$Pulumi from "@reventlessdev/rescript-pulumi-pulumi/src/Output.res.mjs";
import * as Pulumi from "@pulumi/pulumi";
import * as Adapter$ReventlessCore from "../../adapter/Adapter.res.mjs";

function toResolvedOutputs(outputs) {
  let eventCollectorOutput = Output$Pulumi.flatMap(outputs.eventCollector, ec => Adapter$ReventlessCore.resourcesToInterop(ec.resources).apply(resources => ({
    name: ec.name,
    resources: resources
  })));
  let counter = outputs.counter;
  let counterOutput = counter !== undefined ? Pulumi.all([
      Adapter$ReventlessCore.resourcesToInterop(counter.referencesDb.resources),
      Adapter$ReventlessCore.resourcesToInterop(counter.countsDb.resources)
    ]).apply(param => ({
      referencesDb: {
        resources: param[0]
      },
      countsDb: {
        resources: param[1]
      }
    })) : Pulumi.output(undefined);
  return Pulumi.all([
    eventCollectorOutput,
    counterOutput
  ]).apply(param => {
    let counter = param[1];
    let eventCollector = param[0];
    if (counter !== undefined) {
      return {
        name: outputs.name,
        eventCollector: eventCollector,
        counter: counter
      };
    } else {
      return {
        name: outputs.name,
        eventCollector: eventCollector
      };
    }
  });
}

let ReventlessEventCollector;

let componentType = "EventMapper";

export {
  ReventlessEventCollector,
  componentType,
  toResolvedOutputs,
}
/* Output-Pulumi Not a pure module */
