// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Belt_SetString from "@rescript/runtime/lib/es6/Belt_SetString.js";
import * as Primitive_string from "@rescript/runtime/lib/es6/Primitive_string.js";
import * as StorageRef$Reventless from "@reventlessdev/reventless-spec/src/semantic/StorageRef.res.mjs";

function fromVariant(plugin, variant) {
  if (variant.type !== "object") {
    return;
  }
  let properties = variant.properties;
  let match = properties["TAG"];
  if (match === undefined) {
    return;
  }
  if (match.type !== "string") {
    return;
  }
  let eventType = match.const;
  if (eventType === undefined) {
    return;
  }
  let fields = Stdlib_Array.filterMap(Object.entries(properties), param => {
    let field = param[0];
    return Stdlib_Option.map(StorageRef$Reventless.getFieldStore(param[1]), param => {
      let target = param[0];
      return {
        field: field,
        arity: param[1],
        store: Stdlib_Option.getOr(target.plugin, plugin) + "." + target.store
      };
    });
  }).toSorted((a, b) => Primitive_string.compare(a.field, b.field));
  if (fields.length === 0) {
    return;
  } else {
    return {
      eventType: eventType,
      fields: fields
    };
  }
}

function fromEventSchema(plugin, eventSchema) {
  if (eventSchema.type === "union") {
    return Stdlib_Array.filterMap(eventSchema.anyOf, v => fromVariant(plugin, v));
  } else {
    return Stdlib_Option.mapOr(fromVariant(plugin, eventSchema), [], e => [e]);
  }
}

function toJson(entries) {
  return Object.fromEntries(entries.map(param => [
    param.eventType,
    param.fields.map(param => {
      let tmp;
      tmp = param.arity === "Single" ? "one" : "many";
      return Object.fromEntries([
        [
          "field",
          param.field
        ],
        [
          "arity",
          tmp
        ],
        [
          "store",
          param.store
        ]
      ]);
    })
  ]));
}

function storesOf(entries) {
  return Belt_SetString.toArray(Belt_SetString.fromArray(entries.flatMap(param => param.fields.map(param => param.store))));
}

export {
  fromVariant,
  fromEventSchema,
  toJson,
  storesOf,
}
/* StorageRef-Reventless Not a pure module */
