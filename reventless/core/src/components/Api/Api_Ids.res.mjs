// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function encode(typeName, localId) {
  return btoa(typeName + `:` + localId);
}

function decode(globalId) {
  try {
    let decoded = atob(globalId);
    let idx = decoded.indexOf(":");
    if (idx > 0) {
      return [
        decoded.slice(0, idx),
        decoded.slice(idx + 1 | 0, decoded.length)
      ];
    } else {
      return;
    }
  } catch (exn) {
    return;
  }
}

function toLocalId(id) {
  return Stdlib_Option.map(decode(id), param => param[1]);
}

function alternateKey(id) {
  let localId = toLocalId(id);
  if (localId !== undefined && localId !== id) {
    return localId;
  }
}

export {
  encode,
  decode,
  toLocalId,
  alternateKey,
}
/* No side effect */
