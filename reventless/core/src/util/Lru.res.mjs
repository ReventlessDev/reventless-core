// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function make(capacity) {
  return {
    capacity: capacity,
    store: new Map()
  };
}

function get(cache, key) {
  let value = cache.store.get(key);
  if (value === undefined) {
    return;
  }
  let value$1 = Primitive_option.valFromOption(value);
  cache.store.delete(key);
  cache.store.set(key, value$1);
  return Primitive_option.some(value$1);
}

function put(cache, key, value) {
  if (cache.capacity <= 0) {
    return;
  }
  cache.store.delete(key);
  cache.store.set(key, value);
  if (cache.store.size <= cache.capacity) {
    return;
  }
  let oldest = Array.from(cache.store.keys())[0];
  if (oldest !== undefined) {
    cache.store.delete(Primitive_option.valFromOption(oldest));
    return;
  }
}

function invalidate(cache, key) {
  cache.store.delete(key);
}

function clear(cache) {
  cache.store.clear();
}

function size(cache) {
  return cache.store.size;
}

export {
  make,
  get,
  put,
  invalidate,
  clear,
  size,
}
/* No side effect */
