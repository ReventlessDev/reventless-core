// Generated by ReScript, PLEASE EDIT WITH CARE

import * as AnsiStyle$Reventless from "@reventlessdev/reventless-spec/src/AnsiStyle.res.mjs";

let discriminator = "reventlessMetric";

function metricLine(metric, slice, valueOpt) {
  let value = valueOpt !== undefined ? valueOpt : 1;
  return Object.fromEntries([
    [
      discriminator,
      metric
    ],
    [
      "slice",
      slice
    ],
    [
      "value",
      value
    ],
    [
      "unit",
      "Count"
    ]
  ]);
}

function emitCount(metric, slice, valueOpt) {
  let value = valueOpt !== undefined ? valueOpt : 1;
  if (AnsiStyle$Reventless.isJsonSink()) {
    console.log(JSON.stringify(metricLine(metric, slice, value)));
    return;
  }
}

export {
  discriminator,
  metricLine,
  emitCount,
}
/* AnsiStyle-Reventless Not a pure module */
