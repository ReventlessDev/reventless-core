// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

let stackRefs = {};

let platformStackName = new Pulumi.Config("platform").get("stack");

function stackName(pluginName) {
  return Stdlib_Option.map(platformStackName, name => {
    let parts = name.split("/");
    parts[1] = pluginName;
    return parts.join("/");
  });
}

function get(pluginName) {
  let stackRef = stackRefs[pluginName];
  if (stackRef !== undefined) {
    return stackRef;
  }
  let stackRef$1 = Stdlib_Option.map(stackName(pluginName), stack => new Pulumi.StackReference(stack));
  if (stackRef$1 === undefined) {
    return;
  }
  let stackRef$2 = Primitive_option.valFromOption(stackRef$1);
  stackRefs[pluginName] = stackRef$2;
  return Primitive_option.some(stackRef$2);
}

export {
  stackRefs,
  platformStackName,
  stackName,
  get,
}
/* platformStackName Not a pure module */
