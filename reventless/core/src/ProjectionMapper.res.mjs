// Generated by ReScript, PLEASE EDIT WITH CARE

import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Projection$Reventless from "@reventlessdev/reventless-spec/src/types/Projection.res.mjs";
import * as Message$ReventlessCore from "./Message.res.mjs";
import * as MapperNto1$ReventlessCore from "./MapperNto1.res.mjs";

function MakeGenericTargetFromStateTarget(StateTarget) {
  let decode = json => Message$ReventlessCore.decode(json, StateTarget.stateSchema);
  let encode = value => Message$ReventlessCore.encode(value, StateTarget.stateSchema);
  return {
    name: StateTarget.name,
    decode: decode,
    encode: encode
  };
}

function Make(Target) {
  return Mappings => {
    let decode = json => Message$ReventlessCore.decode(json, Target.stateSchema);
    let encode = value => Message$ReventlessCore.encode(value, Target.stateSchema);
    let GenericTarget_name = Target.name;
    let GenericTarget = {
      name: GenericTarget_name,
      decode: decode,
      encode: encode
    };
    let mappings = Mappings.mappings.map(M => {
      let acceptedTags = DcbTag$Reventless.extractAllVariantNames(M.sourceEventSchema);
      let decode = json => Message$ReventlessCore.decodeEvent$p(json, M.SourceId.schema, M.sourceEventSchema);
      let project = MapperNto1$ReventlessCore.makeGenericMap(decode, msg => Projection$Reventless.mapActionId(M.project(msg), M.targetIdToString, M.targetIdFromString));
      return {
        sourceName: M.sourceName,
        acceptedTags: acceptedTags,
        project: project
      };
    });
    return MapperNto1$ReventlessCore.Mapper({})(GenericTarget)({
      mappings: mappings
    });
  };
}

let Spec;

export {
  Spec,
  MakeGenericTargetFromStateTarget,
  Make,
}
/* DcbTag-Reventless Not a pure module */
