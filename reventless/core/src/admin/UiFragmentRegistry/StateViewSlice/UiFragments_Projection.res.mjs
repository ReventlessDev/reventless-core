// Generated by ReScript, PLEASE EDIT WITH CARE


function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "UiFragmentRegistered" :
      let at = event.at;
      let manifest = event.manifest;
      let pluginId = event.pluginId;
      return [{
          TAG: "Set",
          _0: pluginId,
          _1: {
            pluginId: pluginId,
            remoteEntryUrl: manifest.remoteEntryUrl,
            panels: manifest.panels,
            pages: manifest.pages,
            registeredAt: at,
            updatedAt: at
          }
        }];
    case "UiFragmentUpdated" :
      let at$1 = event.at;
      let newManifest = event.newManifest;
      return [{
          TAG: "Update",
          _0: event.pluginId,
          _1: state => ({
            pluginId: state.pluginId,
            remoteEntryUrl: newManifest.remoteEntryUrl,
            panels: newManifest.panels,
            pages: newManifest.pages,
            registeredAt: state.registeredAt,
            updatedAt: at$1
          })
        }];
    case "UiFragmentDeregistered" :
      return [{
          TAG: "Delete",
          _0: event.pluginId
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/reventless-core/src/admin/UiFragmentRegistry/StateViewSlice/UiFragments_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
