// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

function evolve(_state, event) {
  switch (event.TAG) {
    case "UiFragmentRegistered" :
      return event.manifest;
    case "UiFragmentUpdated" :
      return event.newManifest;
    case "UiFragmentDeregistered" :
      return;
  }
}

function decide(state, command) {
  if (command.TAG !== "RegisterUiFragment") {
    if (state !== undefined) {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "UiFragmentDeregistered",
            pluginId: command.pluginId
          }]
      };
    } else {
      return {
        TAG: "Ok",
        _0: []
      };
    }
  }
  let at = command.at;
  let manifest = command.manifest;
  let pluginId = command.pluginId;
  if (state !== undefined) {
    if (Primitive_object.equal(state, manifest)) {
      return {
        TAG: "Ok",
        _0: []
      };
    } else {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "UiFragmentUpdated",
            pluginId: pluginId,
            previousManifest: state,
            newManifest: manifest,
            at: at
          }]
      };
    }
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "UiFragmentRegistered",
          pluginId: pluginId,
          manifest: manifest,
          at: at
        }]
    };
  }
}

let Spec;

let initialState;

let moduleUrl = "@reventlessdev/reventless-core/src/admin/UiFragmentRegistry/StateChangeSlice/UiFragmentRegistry_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
