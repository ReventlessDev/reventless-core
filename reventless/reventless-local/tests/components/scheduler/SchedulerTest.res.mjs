// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Globals from "@jest/globals";
import * as SchedulerFixtures$ReventlessLocal from "./SchedulerFixtures.res.mjs";

describe("Scheduler_Builder.Make:", () => {
  afterEach(() => SchedulerFixtures$ReventlessLocal.SP.reset());
  describe("createSchedule:", () => {
    test("recurring schedule fires event on each interval advance", async () => {
      let ops = SchedulerFixtures$ReventlessLocal.schedulerOps.contents;
      let count = {
        contents: 0
      };
      SchedulerFixtures$ReventlessLocal.Bus.subscribeToEvents("sched-recurring", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      ops.createSchedule([SchedulerFixtures$ReventlessLocal.makeTopicResource("sched-recurring")], {
        name: "test-recurring",
        rate: {
          TAG: "Minutes",
          _0: 1
        },
        payload: "{}"
      });
      Globals.jest.advanceTimersByTime(60000);
      await Promise.resolve();
      await Promise.resolve();
      expect(count.contents).toBe(1);
      Globals.jest.advanceTimersByTime(60000);
      await Promise.resolve();
      await Promise.resolve();
      expect(count.contents).toBe(2);
    });
    test("single-shot schedule fires once then stops", async () => {
      let ops = SchedulerFixtures$ReventlessLocal.schedulerOps.contents;
      let count = {
        contents: 0
      };
      SchedulerFixtures$ReventlessLocal.Bus.subscribeToEvents("sched-single", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      ops.createSchedule([SchedulerFixtures$ReventlessLocal.makeTopicResource("sched-single")], {
        name: "test-single",
        rate: {
          TAG: "Single",
          _0: 2024,
          _1: 1,
          _2: 1,
          _3: 0,
          _4: 0
        },
        payload: "{}"
      });
      Globals.jest.advanceTimersByTime(0);
      await Promise.resolve();
      await Promise.resolve();
      expect(count.contents).toBe(1);
      Globals.jest.advanceTimersByTime(60000);
      expect(count.contents).toBe(1);
    });
  });
  describe("deleteSchedule:", () => {
    test("deleted schedule does not fire after deletion", async () => {
      let ops = SchedulerFixtures$ReventlessLocal.schedulerOps.contents;
      let count = {
        contents: 0
      };
      SchedulerFixtures$ReventlessLocal.Bus.subscribeToEvents("sched-delete", async (param, param$1, param$2) => {
        count.contents = count.contents + 1 | 0;
      });
      ops.createSchedule([SchedulerFixtures$ReventlessLocal.makeTopicResource("sched-delete")], {
        name: "test-delete",
        rate: {
          TAG: "Minutes",
          _0: 2
        },
        payload: "{}"
      });
      ops.deleteSchedule([], "test-delete");
      Globals.jest.advanceTimersByTime(120000);
      expect(count.contents).toBe(0);
    });
  });
});

/*  Not a pure module */
