// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stream from "@reventlessdev/rescript-effect/src/Stream.res.mjs";
import * as Effect from "effect/Effect";
import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessLocal from "../../../src/test/TestRunner.res.mjs";
import * as QueryDbSubIdFixtures$ReventlessLocal from "./QueryDbSubIdFixtures.res.mjs";

globalThis.describe("QueryDb sub-ID (in-memory)", () => {
  globalThis.beforeAll(async () => {
    await TestRunner$ReventlessLocal.resolve(Component$ReventlessCore.operations(QueryDbSubIdFixtures$ReventlessLocal.metricQueryDb));
  });
  globalThis.test("loadStream returns items sorted by sub-key", async () => {
    let ops = await TestRunner$ReventlessLocal.resolve(Component$ReventlessCore.operations(QueryDbSubIdFixtures$ReventlessLocal.metricQueryDb));
    await ops.save("user-sort", {
      userId: "user-sort",
      region: "us-east",
      date: "2026-03",
      value: 1.0
    }, "Init", undefined);
    await ops.save("user-sort", {
      userId: "user-sort",
      region: "us-east",
      date: "2026-01",
      value: 2.0
    }, "Init", undefined);
    await ops.save("user-sort", {
      userId: "user-sort",
      region: "eu-west",
      date: "2026-02",
      value: 3.0
    }, "Init", undefined);
    let result = await Effect.runPromise(Stream.runCollect(ops.loadStream("user-sort")));
    globalThis.expect(result.length).toBe(3);
    let s0 = result[0];
    let s1 = result[1];
    let s2 = result[2];
    globalThis.expect(s0.region + "/" + s0.date).toBe("eu-west/2026-02");
    globalThis.expect(s1.region + "/" + s1.date).toBe("us-east/2026-01");
    globalThis.expect(s2.region + "/" + s2.date).toBe("us-east/2026-03");
  });
  globalThis.test("delete with subId removes only the targeted item", async () => {
    let ops = await TestRunner$ReventlessLocal.resolve(Component$ReventlessCore.operations(QueryDbSubIdFixtures$ReventlessLocal.metricQueryDb));
    await ops.save("user-del", {
      userId: "user-del",
      region: "us-east",
      date: "2026-01",
      value: 10.0
    }, "Init", undefined);
    await ops.save("user-del", {
      userId: "user-del",
      region: "us-east",
      date: "2026-02",
      value: 20.0
    }, "Init", undefined);
    await ops.delete("user-del", [
      "_subId",
      "us-east/2026-01"
    ]);
    let result = await Effect.runPromise(Stream.runCollect(ops.loadStream("user-del")));
    globalThis.expect(result.length).toBe(1);
    let remaining = result[0];
    globalThis.expect(remaining.date).toBe("2026-02");
  });
  globalThis.test("delete without subId removes entire partition", async () => {
    let ops = await TestRunner$ReventlessLocal.resolve(Component$ReventlessCore.operations(QueryDbSubIdFixtures$ReventlessLocal.metricQueryDb));
    await ops.save("user-delall", {
      userId: "user-delall",
      region: "us-east",
      date: "2026-01",
      value: 5.0
    }, "Init", undefined);
    await ops.save("user-delall", {
      userId: "user-delall",
      region: "eu-west",
      date: "2026-01",
      value: 7.0
    }, "Init", undefined);
    await ops.delete("user-delall", undefined);
    let result = await Effect.runPromise(Stream.runCollect(ops.loadStream("user-delall")));
    globalThis.expect(result.length).toBe(0);
  });
  globalThis.test("save overwrites item with same sub-key", async () => {
    let ops = await TestRunner$ReventlessLocal.resolve(Component$ReventlessCore.operations(QueryDbSubIdFixtures$ReventlessLocal.metricQueryDb));
    await ops.save("user-overwrite", {
      userId: "user-overwrite",
      region: "us-east",
      date: "2026-01",
      value: 1.0
    }, "Init", undefined);
    await ops.save("user-overwrite", {
      userId: "user-overwrite",
      region: "us-east",
      date: "2026-01",
      value: 99.0
    }, "Overwrite", undefined);
    let result = await Effect.runPromise(Stream.runCollect(ops.loadStream("user-overwrite")));
    globalThis.expect(result.length).toBe(1);
    let s = result[0];
    globalThis.expect(s.value).toBe(99.0);
  });
});

/*  Not a pure module */
