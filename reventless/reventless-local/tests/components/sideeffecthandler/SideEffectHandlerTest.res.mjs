// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessCore from "@reventlessdev/reventless-core/src/components/Component.res.mjs";
import * as TestRunner$ReventlessLocal from "../../../src/test/TestRunner.res.mjs";
import * as SideEffectHandlerFixtures$ReventlessLocal from "./SideEffectHandlerFixtures.res.mjs";

globalThis.beforeAll(async () => {
  await TestRunner$ReventlessLocal.resolve(Component$ReventlessCore.operations(SideEffectHandlerFixtures$ReventlessLocal.seh));
  await TestRunner$ReventlessLocal.resolve(SideEffectHandlerFixtures$ReventlessLocal.topicResource.name);
});

globalThis.describe("SideEffectHandler (in-memory)", () => {
  globalThis.beforeEach(() => SideEffectHandlerFixtures$ReventlessLocal.resetMocks());
  globalThis.test("OrderPlaced event triggers execute with correct id and orderId", async () => {
    await SideEffectHandlerFixtures$ReventlessLocal.publishOrderPlaced("order-1", "ord-abc");
    globalThis.expect(SideEffectHandlerFixtures$ReventlessLocal.capturedOrders.contents.length).toBe(1);
    let entry = SideEffectHandlerFixtures$ReventlessLocal.capturedOrders.contents[0];
    globalThis.expect(entry[0]).toBe("order-1");
    globalThis.expect(entry[1]).toBe("ord-abc");
  });
  globalThis.test("multiple events trigger execute for each", async () => {
    await SideEffectHandlerFixtures$ReventlessLocal.publishOrderPlaced("order-2", "ord-def");
    await SideEffectHandlerFixtures$ReventlessLocal.publishOrderPlaced("order-3", "ord-ghi");
    globalThis.expect(SideEffectHandlerFixtures$ReventlessLocal.capturedOrders.contents.length).toBe(2);
  });
});

/*  Not a pure module */
