// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function make(seed) {
  return {
    state: seed
  };
}

function float(r) {
  r.state = r.state + 1831565813 | 0;
  let a = r.state;
  let b = Math.imul(a ^ (a >>> 15), a | 1);
  let c = b ^ (b + Math.imul(b ^ (b >>> 7), b | 61) | 0);
  let bits = c ^ (c >>> 14);
  let unsigned = bits < 0 ? bits + 4294967296.0 : bits;
  return unsigned / 4294967296.0;
}

function int(r, min, max) {
  return min + (float(r) * ((max - min | 0) + 1 | 0) | 0) | 0;
}

function pick(r, xs) {
  return xs[float(r) * xs.length | 0];
}

function pickOr(r, fallback, xs) {
  return Stdlib_Option.getOr(pick(r, xs), fallback);
}

function sampleWeighted(r, entries, count) {
  let pool = entries.slice();
  let chosen = [];
  let remaining = count < pool.length ? count : pool.length;
  for (let _for = 1; _for <= remaining; ++_for) {
    let total = Stdlib_Array.reduce(pool, 0.0, (sum, param) => sum + param[1]);
    let cursor = {
      contents: float(r) * total
    };
    let index = {
      contents: pool.length - 1 | 0
    };
    let found = {
      contents: false
    };
    pool.forEach((param, i) => {
      if (!found.contents) {
        cursor.contents = cursor.contents - param[1];
        if (cursor.contents <= 0.0) {
          index.contents = i;
          found.contents = true;
          return;
        } else {
          return;
        }
      }
    });
    let match = pool[index.contents];
    if (match !== undefined) {
      chosen.push(match[0]);
      pool.splice(index.contents, 1);
    }
  }
  return chosen;
}

function zipfWeights(items, exponent) {
  return items.map((item, rank) => [
    item,
    1.0 / Math.pow(rank + 1 | 0, exponent)
  ]);
}

export {
  make,
  float,
  int,
  pick,
  pickOr,
  sampleWeighted,
  zipfWeights,
}
/* No side effect */
