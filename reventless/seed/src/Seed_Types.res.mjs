// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

let Failed = /* @__PURE__ */Primitive_exceptions.create("Seed_Types-ReventlessSeed.Failed");

function quote(s) {
  return JSON.stringify(s);
}

function toLiteral(v) {
  switch (v.TAG) {
    case "String" :
    case "Id" :
      return JSON.stringify(v._0);
    case "Int" :
    case "Float" :
      return v._0.toString();
    case "Bool" :
      if (v._0) {
        return "true";
      } else {
        return "false";
      }
    case "Enum" :
      return v._0;
    case "List" :
      return `[` + v._0.map(toLiteral).join(", ") + `]`;
    case "Object" :
      return `{` + v._0.map(param => param[0] + `: ` + toLiteral(param[1])).join(", ") + `}`;
  }
}

function ids(xs) {
  return {
    TAG: "List",
    _0: xs.map(x => ({
      TAG: "Id",
      _0: x
    }))
  };
}

function strings(xs) {
  return {
    TAG: "List",
    _0: xs.map(x => ({
      TAG: "String",
      _0: x
    }))
  };
}

function mutation(field, args) {
  return {
    field: field,
    args: args
  };
}

function renderArgs(args) {
  return args.map(param => param[0] + `: ` + toLiteral(param[1])).join(", ");
}

function describe(m) {
  return m.field + `(` + renderArgs(m.args) + `)`;
}

export {
  Failed,
  quote,
  toLiteral,
  ids,
  strings,
  mutation,
  renderArgs,
  describe,
}
/* No side effect */
