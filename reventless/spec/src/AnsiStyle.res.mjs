// Generated by ReScript, PLEASE EDIT WITH CARE


let _default = {
  contents: undefined
};

function _resolveFormat() {
  let match = process.env.REVENTLESS_LOG_FORMAT;
  let match$1 = _default.contents;
  let match$2 = process.stdout.isTTY;
  if (match !== undefined) {
    switch (match) {
      case "json" :
        return "json";
      case "text" :
        return "text";
    }
  }
  if (match$1 !== undefined) {
    return match$1;
  } else if (match$2 !== undefined && match$2) {
    return "text";
  } else {
    return "json";
  }
}

let _format = {
  contents: _resolveFormat()
};

function setDefaultFormat(format) {
  _default.contents = format === "text" ? "text" : "json";
  _format.contents = _resolveFormat();
}

function reload() {
  _format.contents = _resolveFormat();
}

function isJsonSink() {
  return _format.contents === "json";
}

function useAnsi() {
  return _format.contents === "text";
}

function bold(s) {
  if (_format.contents === "text") {
    return `\x1b[1m` + s + `\x1b[0m`;
  } else {
    return s;
  }
}

export {
  _default,
  _resolveFormat,
  _format,
  setDefaultFormat,
  reload,
  isJsonSink,
  useAnsi,
  bold,
}
/* _format Not a pure module */
