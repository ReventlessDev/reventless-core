// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";

let displayNameId = Sury.$Metadata_Id_make("reventless", "displayName");

function getSpec(schema) {
  return Sury.$Metadata_get(schema, displayNameId);
}

function computeLabel(spec, state) {
  return Stdlib_Array.filterMap(spec.fields, fieldName => {
    let match = state[fieldName];
    if (typeof match === "string" && match !== "") {
      return match;
    }
  }).join(spec.separator);
}

export {
  displayNameId,
  getSpec,
  computeLabel,
}
/* displayNameId Not a pure module */
