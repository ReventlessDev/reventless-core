// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Semantic$Reventless from "../semantic/Semantic.res.mjs";

let defaultId = Sury.$Metadata_Id_make("reventless", "default");

function mark(schema, value) {
  return Sury.$Metadata_set(schema, defaultId, value);
}

function int(schema, value) {
  return Sury.$Metadata_set(schema, defaultId, value);
}

function float(schema, value) {
  return Sury.$Metadata_set(schema, defaultId, value);
}

function string(schema, value) {
  return Sury.$Metadata_set(schema, defaultId, value);
}

function bool(schema, value) {
  return Sury.$Metadata_set(schema, defaultId, value);
}

function getFrom(schema) {
  let found = Sury.$Metadata_get(schema, defaultId);
  if (found !== undefined) {
    return found;
  } else {
    return Stdlib_Option.flatMap(Semantic$Reventless.unwrapOptional(schema), getFrom);
  }
}

let get = getFrom;

export {
  defaultId,
  mark,
  int,
  float,
  string,
  bool,
  getFrom,
  get,
}
/* defaultId Not a pure module */
