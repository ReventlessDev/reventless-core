// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let kindSchema = S.literal("ObjectStore");

let provenanceSchema = S.schema(s => ({
  component: s.m(S.string),
  field: s.m(S.string),
  annotation: s.m(S.option(S.string))
}));

let entrySchema = S.schema(s => ({
  kind: s.m(kindSchema),
  key: s.m(S.string),
  declaredBy: s.m(S.array(provenanceSchema))
}));

let schema = S.schema(s => ({
  capabilities: s.m(S.array(entrySchema))
}));

function fromStructure(structure) {
  let declarations = Stdlib_Option.getOr(structure.requiredStoreDeclarations, []);
  return {
    capabilities: Stdlib_Option.getOr(structure.requiredStores, []).map(key => ({
      kind: "ObjectStore",
      key: key,
      declaredBy: Stdlib_Array.filterMap(declarations, d => {
        if (d.store === key) {
          return {
            component: d.component,
            field: d.field,
            annotation: d.annotation
          };
        }
      })
    }))
  };
}

function render(manifest) {
  return JSON.stringify(S.reverseConvertToJsonOrThrow(manifest, schema), undefined, 2) + "\n";
}

function renderForStructure(structure) {
  return render(fromStructure(structure));
}

export {
  kindSchema,
  provenanceSchema,
  entrySchema,
  schema,
  fromStructure,
  render,
  renderForStructure,
}
/* kindSchema Not a pure module */
