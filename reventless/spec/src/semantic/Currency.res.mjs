// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";

let schema = Sury.union([
  Sury.literal("AUD"),
  Sury.literal("CAD"),
  Sury.literal("CHF"),
  Sury.literal("CNY"),
  Sury.literal("EUR"),
  Sury.literal("GBP"),
  Sury.literal("JPY"),
  Sury.literal("NOK"),
  Sury.literal("SEK"),
  Sury.literal("USD")
]);

let all = [
  "AUD",
  "CAD",
  "CHF",
  "CNY",
  "EUR",
  "GBP",
  "JPY",
  "NOK",
  "SEK",
  "USD"
];

function toString(currency) {
  switch (currency) {
    case "AUD" :
      return "AUD";
    case "CAD" :
      return "CAD";
    case "CHF" :
      return "CHF";
    case "CNY" :
      return "CNY";
    case "EUR" :
      return "EUR";
    case "GBP" :
      return "GBP";
    case "JPY" :
      return "JPY";
    case "NOK" :
      return "NOK";
    case "SEK" :
      return "SEK";
    case "USD" :
      return "USD";
  }
}

function exponent(currency) {
  if (currency === "JPY") {
    return 0;
  } else {
    return 2;
  }
}

let d = {};

all.forEach(c => {
  d[toString(c)] = c;
});

function fromString(raw) {
  let c = d[raw];
  if (c !== undefined) {
    return {
      TAG: "Ok",
      _0: c
    };
  } else {
    return {
      TAG: "Error",
      _0: `expected one of the ISO 4217 codes this framework admits (` + all.map(toString).join(", ") + `), got ` + JSON.stringify(raw) + `. Codes are upper-case and exactly three letters.`
    };
  }
}

let byCode = d;

export {
  schema,
  all,
  toString,
  exponent,
  byCode,
  fromString,
}
/* schema Not a pure module */
