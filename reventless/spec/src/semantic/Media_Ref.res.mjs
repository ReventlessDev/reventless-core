// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Semantic$Reventless from "./Semantic.res.mjs";

function hasWebScheme(raw) {
  let lower = raw.toLowerCase();
  if (lower.startsWith("http://")) {
    return true;
  } else {
    return lower.startsWith("https://");
  }
}

function check(what, raw) {
  if (raw === "") {
    return {
      TAG: "Error",
      _0: `expected a ` + what + ` reference, got an empty string`
    };
  } else if (hasWebScheme(raw) || raw.startsWith("/") && !raw.startsWith("//")) {
    return {
      TAG: "Ok",
      _0: raw
    };
  } else {
    return {
      TAG: "Error",
      _0: `expected an http:// or https:// address, or an origin-relative path, got ` + Semantic$Reventless.showString(raw) + `. A data: URI is not a ` + what + ` reference — it inlines the bytes into the event log, permanently.`
    };
  }
}

export {
  hasWebScheme,
  check,
}
/* Semantic-Reventless Not a pure module */
