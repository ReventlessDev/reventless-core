// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as Semantic$Reventless from "./Semantic.res.mjs";

let grammar = Sury.email;

function fromString(raw) {
  let value;
  try {
    value = Sury.parseOrThrow(raw, grammar);
  } catch (exn) {
    return {
      TAG: "Error",
      _0: `expected an email address, got ` + Semantic$Reventless.showString(raw)
    };
  }
  return {
    TAG: "Ok",
    _0: value
  };
}

let schema = Semantic$Reventless.refined(Sury.string, Semantic$Reventless.Id.email, fromString);

export {
  grammar,
  fromString,
  schema,
}
/* grammar Not a pure module */
