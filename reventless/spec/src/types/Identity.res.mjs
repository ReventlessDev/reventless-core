// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

S.enableJson();

let providerSchema = S.union([
  S.literal("Cognito"),
  S.literal("InMemory"),
  S.schema(s => ({
    TAG: "Custom",
    _0: s.m(S.string)
  }))
]);

let schema = S.schema(s => ({
  userId: s.m(S.string),
  username: s.m(S.string),
  groups: s.m(S.array(S.string)),
  claims: s.m(S.option(S.dict(S.string))),
  provider: s.m(providerSchema)
}));

let anonymous_groups = [];

let anonymous = {
  userId: "anonymous",
  username: "anonymous",
  groups: anonymous_groups,
  provider: "InMemory"
};

function hasGroup(identity, group) {
  return identity.groups.includes(group);
}

function getClaim(identity, key) {
  let claims = identity.claims;
  if (claims !== undefined) {
    return claims[key];
  }
}

export {
  providerSchema,
  schema,
  anonymous,
  hasGroup,
  getClaim,
}
/*  Not a pure module */
