// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";

let providerSchema = Sury.union([
  Sury.literal("Cognito"),
  Sury.literal("InMemory"),
  Sury.$schema(s => ({
    TAG: "Custom",
    _0: s.m(Sury.string)
  }))
]);

let schema = Sury.$schema(s => ({
  userId: s.m(Sury.string),
  username: s.m(Sury.string),
  groups: s.m(Sury.array(Sury.string)),
  claims: s.m(Sury.$option(Sury.dict(Sury.string))),
  provider: s.m(providerSchema)
}));

let anonymous_groups = [];

let anonymous = {
  userId: "anonymous",
  username: "anonymous",
  groups: anonymous_groups,
  provider: "InMemory"
};

function hasGroup(identity, group) {
  return identity.groups.includes(group);
}

function getClaim(identity, key) {
  let claims = identity.claims;
  if (claims !== undefined) {
    return claims[key];
  }
}

export {
  providerSchema,
  schema,
  anonymous,
  hasGroup,
  getClaim,
}
/* providerSchema Not a pure module */
