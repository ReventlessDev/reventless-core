// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";

let permissionSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "AllowGroups",
    _0: s.m(Sury.array(Sury.string))
  })),
  Sury.literal("AllowAuthenticated"),
  Sury.literal("AllowAnonymous"),
  Sury.literal("DenyAll")
]);

function isAllowed(rule, identity) {
  if (typeof rule === "object") {
    return rule._0.some(group => identity.groups.includes(group));
  }
  switch (rule) {
    case "AllowAuthenticated" :
      return identity.userId !== "anonymous";
    case "AllowAnonymous" :
      return true;
    case "DenyAll" :
      return false;
  }
}

export {
  permissionSchema,
  isAllowed,
}
/* permissionSchema Not a pure module */
