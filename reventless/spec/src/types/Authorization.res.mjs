// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

let permissionSchema = S.union([
  S.schema(s => ({
    TAG: "AllowGroups",
    _0: s.m(S.array(S.string))
  })),
  S.literal("AllowAuthenticated"),
  S.literal("AllowAnonymous"),
  S.literal("DenyAll")
]);

function isAllowed(rule, identity) {
  if (typeof rule === "object") {
    return rule._0.some(group => identity.groups.includes(group));
  }
  switch (rule) {
    case "AllowAuthenticated" :
      return identity.userId !== "anonymous";
    case "AllowAnonymous" :
      return true;
    case "DenyAll" :
      return false;
  }
}

export {
  permissionSchema,
  isAllowed,
}
/* permissionSchema Not a pure module */
