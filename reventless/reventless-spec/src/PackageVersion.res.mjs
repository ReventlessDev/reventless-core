// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Nodefs from "node:fs";
import * as Nodeurl from "node:url";
import * as Nodepath from "node:path";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function readVersion(path) {
  return Stdlib_Option.getOr(Stdlib_Option.flatMap(Stdlib_Option.flatMap(Stdlib_JSON.Decode.object(JSON.parse(Nodefs.readFileSync(path, "utf8"))), o => o["version"]), Stdlib_JSON.Decode.string), "0.0.0");
}

function fromCwd() {
  return readVersion("./package.json");
}

function findVersion(dir) {
  let _dir = dir;
  while (true) {
    let dir$1 = _dir;
    let candidate = Nodepath.join(dir$1, "package.json");
    if (Nodefs.existsSync(candidate)) {
      return readVersion(candidate);
    }
    let parent = Nodepath.dirname(dir$1);
    if (parent === dir$1) {
      return "0.0.0";
    }
    _dir = parent;
    continue;
  };
}

function fromModuleUrl(url) {
  return findVersion(Nodepath.dirname(Nodeurl.fileURLToPath(url)));
}

let callerFile = (function() {
    var frameworkMarkers = [
      '/reventless/reventless-',
      '/@reventlessdev/reventless-',
    ];
    var oldPrepare = Error.prepareStackTrace;
    Error.prepareStackTrace = function(_, stack) { return stack; };
    var err = new Error();
    var stack = err.stack;
    Error.prepareStackTrace = oldPrepare;
    for (var i = 0; i < stack.length; i++) {
      var file = stack[i].getFileName();
      if (!file) continue;
      var isFramework = false;
      for (var j = 0; j < frameworkMarkers.length; j++) {
        if (file.indexOf(frameworkMarkers[j]) !== -1) { isFramework = true; break; }
      }
      if (!isFramework) return file;
    }
    return undefined;
  });

function fromCaller() {
  let file = callerFile();
  if (file !== undefined) {
    if (file.startsWith("file://")) {
      return findVersion(Nodepath.dirname(Nodeurl.fileURLToPath(file)));
    } else {
      return findVersion(Nodepath.dirname(file));
    }
  } else {
    return readVersion("./package.json");
  }
}

export {
  readVersion,
  fromCwd,
  findVersion,
  fromModuleUrl,
  callerFile,
  fromCaller,
}
/* node:fs Not a pure module */
