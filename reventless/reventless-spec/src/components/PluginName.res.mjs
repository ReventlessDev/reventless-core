// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function fromPackageName(packageName) {
  let parts = packageName.split("/");
  let len = parts.length;
  let base;
  if (len >= 3) {
    base = parts[parts.length - 1 | 0];
  } else {
    switch (len) {
      case 0 :
        base = parts[parts.length - 1 | 0];
        break;
      case 1 :
        base = parts[0];
        break;
      case 2 :
        base = parts[1];
        break;
    }
  }
  return base.split("-").flatMap(part => part.split("_")).map(word => {
    if (word === "") {
      return "";
    } else {
      return word.slice(0, 1).toUpperCase() + word.slice(1, word.length);
    }
  }).join("");
}

function resolve(pluginJsonName, packageJsonName) {
  if (pluginJsonName !== undefined) {
    return pluginJsonName;
  } else {
    return Stdlib_Option.getOr(Stdlib_Option.map(packageJsonName, fromPackageName), "Plugin");
  }
}

export {
  fromPackageName,
  resolve,
}
/* No side effect */
