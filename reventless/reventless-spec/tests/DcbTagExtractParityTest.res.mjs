// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as DcbTag$Reventless from "../src/components/DcbTag.res.mjs";

function viaStringify(value) {
  return JSON.parse(Stdlib_Option.getOrThrow(JSON.stringify(value), undefined));
}

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "CategoryAdded",
    categoryId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "CategoryArchived",
    categoryId: s.m(DcbTag$Reventless.string)
  }))
]);

let Scalar = {
  eventSchema: eventSchema
};

let eventSchema$1 = S.schema(s => ({
  TAG: "Counted",
  widgetId: s.m(DcbTag$Reventless.int)
}));

let IntTag = {
  eventSchema: eventSchema$1
};

let commandSchema = S.schema(s => ({
  TAG: "PlaceOrder",
  orderId: s.m(DcbTag$Reventless.string),
  productIds: s.m(S.array(DcbTag$Reventless.stringForKey("productId")))
}));

let ArrayTag = {
  commandSchema: commandSchema
};

let eventSchema$2 = S.schema(s => ({
  TAG: "Renamed",
  category_id: s.m(DcbTag$Reventless.string)
}));

let Renamed = {
  eventSchema: eventSchema$2
};

let eventSchema$3 = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    productId: s.m(DcbTag$Reventless.string)
  })),
  S.literal("Discontinued")
]);

let PayloadLess = {
  eventSchema: eventSchema$3
};

let eventSchema$4 = S.schema(s => ({
  orderId: s.m(DcbTag$Reventless.string),
  note: s.m(S.string)
}));

let PlainObject = {
  eventSchema: eventSchema$4
};

function unk(s) {
  return s;
}

globalThis.describe("DcbTag.extractTags reverseConvert parity", () => {
  globalThis.test("scalar string tag: reverse-json == stringify-json for extraction", () => {
    let v = {
      TAG: "CategoryAdded",
      categoryId: "cat-1",
      name: "Electronics"
    };
    let r = S.reverseConvertToJsonOrThrow(v, eventSchema);
    let s = viaStringify(v);
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema, r)).toEqual(DcbTag$Reventless.extractTagsFromJson(eventSchema, s));
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema, r)).toEqual([{
        key: "categoryId",
        value: "cat-1"
      }]);
  });
  globalThis.test("int tag", () => {
    let v = {
      TAG: "Counted",
      widgetId: 42
    };
    let r = S.reverseConvertToJsonOrThrow(v, eventSchema$1);
    let s = viaStringify(v);
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema$1, r)).toEqual(DcbTag$Reventless.extractTagsFromJson(eventSchema$1, s));
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema$1, r)).toEqual([{
        key: "widgetId",
        value: "42"
      }]);
  });
  globalThis.test("payload-less variant present in the union", () => {
    let v = {
      TAG: "ProductAdded",
      productId: "p-1"
    };
    let r = S.reverseConvertToJsonOrThrow(v, eventSchema$3);
    let s = viaStringify(v);
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema$3, r)).toEqual(DcbTag$Reventless.extractTagsFromJson(eventSchema$3, s));
  });
  globalThis.test("plain object (non-union) event", () => {
    let v = {
      orderId: "o-1",
      note: "hi"
    };
    let r = S.reverseConvertToJsonOrThrow(v, eventSchema$4);
    let s = viaStringify(v);
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema$4, r)).toEqual(DcbTag$Reventless.extractTagsFromJson(eventSchema$4, s));
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema$4, r)).toEqual([{
        key: "orderId",
        value: "o-1"
      }]);
  });
  globalThis.test("array tag expands per element (extractTagsFromJsonExpanded)", () => {
    let v_1 = [
      "p1",
      "p2"
    ];
    let v = {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      productIds: v_1
    };
    let r = S.reverseConvertToJsonOrThrow(v, commandSchema);
    let s = viaStringify(v);
    globalThis.expect(DcbTag$Reventless.extractTagsFromJsonExpanded(commandSchema, r)).toEqual(DcbTag$Reventless.extractTagsFromJsonExpanded(commandSchema, s));
    globalThis.expect(DcbTag$Reventless.extractTagsFromJsonExpanded(commandSchema, r)).toEqual([
      {
        key: "orderId",
        value: "ord-1"
      },
      {
        key: "productId",
        value: "p1"
      },
      {
        key: "productId",
        value: "p2"
      }
    ]);
  });
  globalThis.test("@as-renamed tagged field: reverseConvert feeds the serialized key", () => {
    let r = S.reverseConvertToJsonOrThrow({
      TAG: "Renamed",
      category_id: "cat-9"
    }, eventSchema$2);
    globalThis.expect(DcbTag$Reventless.extractTagsFromJson(eventSchema$2, r)).toEqual([{
        key: "category_id",
        value: "cat-9"
      }]);
  });
});

export {
  viaStringify,
  Scalar,
  IntTag,
  ArrayTag,
  Renamed,
  PayloadLess,
  PlainObject,
  unk,
}
/* eventSchema Not a pure module */
