// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "../src/components/DcbTag.res.mjs";

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    productId: s.m(S.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ProductArchived",
    productId: s.m(S.string)
  })),
  S.literal("Discontinued")
]);

globalThis.describe("DcbTag variant extraction (via variantTagName)", () => {
  globalThis.test("extractVariantNames returns record-payload constructors in order", () => {
    globalThis.expect(DcbTag$Reventless.extractVariantNames(eventSchema)).toEqual([
      "ProductAdded",
      "ProductArchived"
    ]);
  });
  globalThis.test("extractVariantNames excludes the payload-less constructor", () => {
    globalThis.expect(DcbTag$Reventless.extractVariantNames(eventSchema).includes("Discontinued")).toEqual(false);
  });
  globalThis.test("extractAllVariantNames includes the payload-less constructor", () => {
    globalThis.expect(DcbTag$Reventless.extractAllVariantNames(eventSchema)).toEqual([
      "ProductAdded",
      "ProductArchived",
      "Discontinued"
    ]);
  });
  globalThis.test("isVariantPayloadBearing is true for a record variant", () => {
    globalThis.expect(DcbTag$Reventless.isVariantPayloadBearing(eventSchema, "ProductAdded")).toEqual(true);
  });
  globalThis.test("isVariantPayloadBearing is false for a payload-less variant", () => {
    globalThis.expect(DcbTag$Reventless.isVariantPayloadBearing(eventSchema, "Discontinued")).toEqual(false);
  });
  globalThis.test("isVariantPayloadBearing is false for an unknown constructor name", () => {
    globalThis.expect(DcbTag$Reventless.isVariantPayloadBearing(eventSchema, "Nope")).toEqual(false);
  });
});

export {
  eventSchema,
}
/* eventSchema Not a pure module */
