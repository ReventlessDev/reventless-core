// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbValidation$Reventless from "../src/components/DcbValidation.res.mjs";

function u(prim) {
  return prim;
}

let flatSchema = S.schema(s => ({
  productId: s.m(S.string),
  qty: s.m(S.int)
}));

let flatSameShapeSchema = S.schema(s => ({
  productId: s.m(S.string),
  qty: s.m(S.int)
}));

let flatTypeDriftSchema = S.schema(s => ({
  productId: s.m(S.string),
  qty: s.m(S.string)
}));

let flatNameDriftSchema = S.schema(s => ({
  productId: s.m(S.string),
  quantity: s.m(S.int)
}));

let innerSchema = S.schema(s => ({
  tag: s.m(S.string)
}));

let innerDriftSchema = S.schema(s => ({
  tag: s.m(S.int)
}));

let nestedSchema = S.schema(s => ({
  productId: s.m(S.string),
  meta: s.m(innerSchema)
}));

let nestedDriftSchema = S.schema(s => ({
  productId: s.m(S.string),
  meta: s.m(innerDriftSchema)
}));

globalThis.describe("DcbValidation.schemasAreCompatible", () => {
  globalThis.test("identical object shapes are compatible", () => {
    globalThis.expect(DcbValidation$Reventless.schemasAreCompatible(flatSchema, flatSameShapeSchema)).toEqual(true);
  });
  globalThis.test("a differing field type is incompatible (the A8 fix — not every Object pair passes)", () => {
    globalThis.expect(DcbValidation$Reventless.schemasAreCompatible(flatSchema, flatTypeDriftSchema)).toEqual(false);
  });
  globalThis.test("a differing field name is incompatible", () => {
    globalThis.expect(DcbValidation$Reventless.schemasAreCompatible(flatSchema, flatNameDriftSchema)).toEqual(false);
  });
  globalThis.test("identical nested payloads are compatible", () => {
    globalThis.expect(DcbValidation$Reventless.schemasAreCompatible(nestedSchema, nestedSchema)).toEqual(true);
  });
  globalThis.test("nested payload drift is caught by the recursion", () => {
    globalThis.expect(DcbValidation$Reventless.schemasAreCompatible(nestedSchema, nestedDriftSchema)).toEqual(false);
  });
});

export {
  u,
  flatSchema,
  flatSameShapeSchema,
  flatTypeDriftSchema,
  flatNameDriftSchema,
  innerSchema,
  innerDriftSchema,
  nestedSchema,
  nestedDriftSchema,
}
/* flatSchema Not a pure module */
