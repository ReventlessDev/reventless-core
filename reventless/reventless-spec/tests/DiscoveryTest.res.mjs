// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Discovery$Reventless from "../src/generator/Discovery.res.mjs";

globalThis.describe("Discovery.chapterOf", () => {
  globalThis.test("src/<Chapter>/<Kind>/<Component>.res -> Some(<Chapter>)", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("Product/Aggregate/Product.res")).toEqual("Product");
  });
  globalThis.test("read model under a chapter folder", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("Product/ReadModel/Products.res")).toEqual("Product");
  });
  globalThis.test("DCB slice under a chapter folder", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("Order/StateChangeSlice/PlaceOrder.res")).toEqual("Order");
  });
  globalThis.test("src/<Kind>/<Component>.res -> None", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("Aggregate/Product.res")).toEqual(undefined);
  });
  globalThis.test("plural / short kind-folder spelling is still a kind folder (no chapter)", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("StateChange/PlaceOrder.res")).toEqual(undefined);
  });
  globalThis.test("entity subfolder under a kind folder -> None", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("Aggregate/Product/Product.res")).toEqual(undefined);
  });
  globalThis.test("file at src root -> None", () => {
    globalThis.expect(Discovery$Reventless.chapterOf("Plugin.res")).toEqual(undefined);
  });
});

globalThis.describe("Discovery.chaptersByStem", () => {
  let files = [
    {
      stem: "Product",
      componentType: "Aggregate",
      epGroup: undefined,
      relPath: "Product/Aggregate/Product.res"
    },
    {
      stem: "Product_Behavior",
      componentType: "Aggregate",
      epGroup: undefined,
      relPath: "Product/Aggregate/Product_Behavior.res"
    },
    {
      stem: "Products",
      componentType: "ReadModel",
      epGroup: undefined,
      relPath: "Product/ReadModel/Products.res"
    },
    {
      stem: "Category",
      componentType: "Aggregate",
      epGroup: undefined,
      relPath: "Category/Aggregate/Category.res"
    },
    {
      stem: "ImportProducts",
      componentType: "Task",
      epGroup: undefined,
      relPath: "Task/ImportProducts.res"
    }
  ];
  globalThis.test("maps every chaptered stem (incl. body files) to its chapter, sorted by stem", () => {
    globalThis.expect(Discovery$Reventless.chaptersByStem(files)).toEqual([
      [
        "Category",
        "Category"
      ],
      [
        "Product",
        "Product"
      ],
      [
        "Product_Behavior",
        "Product"
      ],
      [
        "Products",
        "Product"
      ]
    ]);
  });
  globalThis.test("a component with no chapter is absent from the map", () => {
    globalThis.expect(Discovery$Reventless.chaptersByStem(files).some(param => param[0] === "ImportProducts")).toEqual(false);
  });
});

let D;

export {
  D,
}
/*  Not a pure module */
