// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Compat$ReventlessInterop from "../src/Compat.res.mjs";

function makeMeta(fields) {
  return {
    version: "0.1.0",
    fields: Object.fromEntries(fields)
  };
}

function alwaysOkFromJson(_json) {
  return {
    TAG: "Ok",
    _0: "ok"
  };
}

function alwaysErrorFromJson(_json) {
  return {
    TAG: "Error",
    _0: "decode failed"
  };
}

let emptyJsonObject = {};

Jest.describe("Compat.validateAndProject", () => {
  Jest.describe("required field validation", () => {
    Jest.test("succeeds when all required fields are in the manifest", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          [
            "name",
            "bucketNames"
          ]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, [
        "name",
        "bucketNames"
      ], alwaysOkFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Ok",
        _0: "ok"
      });
    });
    Jest.test("succeeds when requiredFields is empty", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          []
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, [], alwaysOkFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Ok",
        _0: "ok"
      });
    });
    Jest.test("fails with MissingRequiredField when a required field is absent", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          ["name"]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, [
        "name",
        "bucketNames"
      ], alwaysOkFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Error",
        _0: {
          TAG: "MissingRequiredField",
          stackName: "org/plugin/prod",
          outputName: "tasks",
          field: "bucketNames"
        }
      });
    });
    Jest.test("fails when manifest for outputName is absent entirely", () => {
      let meta_fields = Object.fromEntries([[
          "plugin",
          [
            "id",
            "version"
          ]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, ["name"], alwaysOkFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Error",
        _0: {
          TAG: "MissingRequiredField",
          stackName: "org/plugin/prod",
          outputName: "tasks",
          field: "name"
        }
      });
    });
    Jest.test("includes the first missing field in the error (not all missing fields)", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          []
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, [
        "name",
        "bucketNames"
      ], alwaysOkFromJson);
      if (result.TAG !== "Ok") {
        let match = result._0;
        switch (match.TAG) {
          case "MissingRequiredField" :
            return Jest.Expect.toBe(Jest.Expect.expect(match.field), "name");
          case "MetaMissing" :
          case "DecodeFailed" :
            break;
        }
      }
      return Jest.Expect.toBe(Jest.Expect.expect(true), false);
    });
  });
  Jest.describe("fromJson decoding", () => {
    Jest.test("calls fromJson when all required fields are present", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          ["name"]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let called = {
        contents: false
      };
      let trackingFromJson = json => {
        called.contents = true;
        return {
          TAG: "Ok",
          _0: "ok"
        };
      };
      Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, ["name"], trackingFromJson);
      return Jest.Expect.toBe(Jest.Expect.expect(called.contents), true);
    });
    Jest.test("does not call fromJson when a required field is missing", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          []
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let called = {
        contents: false
      };
      let trackingFromJson = json => {
        called.contents = true;
        return {
          TAG: "Ok",
          _0: "ok"
        };
      };
      Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, ["name"], trackingFromJson);
      return Jest.Expect.toBe(Jest.Expect.expect(called.contents), false);
    });
    Jest.test("wraps fromJson error in DecodeFailed", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          ["name"]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, ["name"], alwaysErrorFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Error",
        _0: {
          TAG: "DecodeFailed",
          stackName: "org/plugin/prod",
          reason: "decode failed"
        }
      });
    });
    Jest.test("passes the raw JSON value to fromJson unchanged", () => {
      let meta_fields = Object.fromEntries([[
          "plugin",
          ["id"]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let jsonInput = {"id": "test-plugin", "version": "1.0.0"};
      let capturedJson = {
        contents: undefined
      };
      let capturingFromJson = json => {
        capturedJson.contents = json;
        return {
          TAG: "Ok",
          _0: "captured"
        };
      };
      Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "plugin", jsonInput, ["id"], capturingFromJson);
      return Jest.Expect.toBe(Jest.Expect.expect(Stdlib_Option.isSome(capturedJson.contents)), true);
    });
  });
  Jest.describe("optional fields", () => Jest.test("does not require optional fields to be in the manifest", () => {
    let meta_fields = Object.fromEntries([[
        "tasks",
        ["name"]
      ]]);
    let meta = {
      version: "0.1.0",
      fields: meta_fields
    };
    let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, ["name"], alwaysOkFromJson);
    return Jest.Expect.toEqual(Jest.Expect.expect(result), {
      TAG: "Ok",
      _0: "ok"
    });
  }));
  Jest.describe("stackName in errors", () => {
    Jest.test("uses the provided stackName in MissingRequiredField error", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          []
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("myorg/my-plugin/staging", meta, "tasks", emptyJsonObject, ["name"], alwaysOkFromJson);
      if (result.TAG !== "Ok") {
        let match = result._0;
        switch (match.TAG) {
          case "MissingRequiredField" :
            return Jest.Expect.toBe(Jest.Expect.expect(match.stackName), "myorg/my-plugin/staging");
          case "MetaMissing" :
          case "DecodeFailed" :
            break;
        }
      }
      return Jest.Expect.toBe(Jest.Expect.expect(true), false);
    });
    Jest.test("uses the provided stackName in DecodeFailed error", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          ["name"]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("myorg/my-plugin/staging", meta, "tasks", emptyJsonObject, ["name"], alwaysErrorFromJson);
      if (result.TAG !== "Ok") {
        let match = result._0;
        switch (match.TAG) {
          case "MissingRequiredField" :
          case "MetaMissing" :
            break;
          case "DecodeFailed" :
            return Jest.Expect.toBe(Jest.Expect.expect(match.stackName), "myorg/my-plugin/staging");
        }
      }
      return Jest.Expect.toBe(Jest.Expect.expect(true), false);
    });
  });
  Jest.describe("field manifest boundary cases", () => {
    Jest.test("succeeds when manifest has more fields than required", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          [
            "name",
            "bucketNames",
            "sideEffectSources"
          ]
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, ["name"], alwaysOkFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Ok",
        _0: "ok"
      });
    });
    Jest.test("succeeds with empty required fields even when manifest is empty", () => {
      let meta_fields = Object.fromEntries([[
          "tasks",
          []
        ]]);
      let meta = {
        version: "0.1.0",
        fields: meta_fields
      };
      let result = Compat$ReventlessInterop.validateAndProject("org/plugin/prod", meta, "tasks", emptyJsonObject, [], alwaysOkFromJson);
      return Jest.Expect.toEqual(Jest.Expect.expect(result), {
        TAG: "Ok",
        _0: "ok"
      });
    });
  });
});

export {
  makeMeta,
  alwaysOkFromJson,
  alwaysErrorFromJson,
  emptyJsonObject,
}
/*  Not a pure module */
