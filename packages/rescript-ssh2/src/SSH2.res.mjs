// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function onReady(client, handler) {
  return client.on("ready", () => handler(client));
}

let Client = {
  onReady: onReady
};

let ReadStreamOptions = {};

function extendedError(originalError, customError, sftp) {
  if (customError !== undefined ? sftp.emit("error", Primitive_option.valFromOption(customError)) : false) {
    return true;
  } else {
    return sftp.emit("error", originalError);
  }
}

export {
  Client,
  ReadStreamOptions,
  extendedError,
}
/* No side effect */
