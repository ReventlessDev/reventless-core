// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';


var dependencies = {
  contents: []
};

function getDependencies() {
  var currentLen = dependencies.contents.length;
  var idx = currentLen / 4 | 0;
  var match = idx !== 0 ? [
      ((idx - 1 | 0) << 2),
      (idx << 2)
    ] : [
      0,
      0
    ];
  var deps = Promise.all(dependencies.contents.slice(match[0], match[1]));
  var registerResource = {
    contents: (function (param) {
        
      })
  };
  var promise = new Promise((function (resolve, param) {
          registerResource.contents = resolve;
        }));
  dependencies.contents = dependencies.contents.concat([promise]);
  return [
          deps,
          registerResource.contents
        ];
}

exports.getDependencies = getDependencies;
/* No side effect */
