// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Aws from "@pulumi/aws";
import * as Output$Pulumi from "@reventless/rescript-pulumi-pulumi/src/Output.res.mjs";
import * as Pulumi from "@pulumi/pulumi";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as PolicyDocument$PulumiAws from "@reventless/rescript-pulumi-aws/src/IAM/PolicyDocument.res.mjs";

function emailIdentity(name, email, opts) {
  return new (Aws.ses.EmailIdentity)("emailIdentity" + name, {
    email: email
  }, opts !== undefined ? Primitive_option.valFromOption(opts) : undefined);
}

function fromCustomResourceOptions(x) {
  if (x !== undefined) {
    return {
      parent: x.parent,
      provider: x.provider
    };
  } else {
    return {};
  }
}

function sesPolicyDocument(name, identity, opts) {
  return Output$Pulumi.flatMap(identity.arn, identityArn => Pulumi.output(PolicyDocument$PulumiAws.make(undefined, name + "SESPolicy", [{
      Sid: "AllowSES",
      Principal: {
        AWS: "*"
      },
      Effect: "Allow",
      Action: [
        "SES:SendEmail",
        "SES:SendRawEmail"
      ],
      Resource: identityArn
    }])));
}

function identityWithPolicy(name, email, opts) {
  let identity = emailIdentity(name, email, opts);
  return new (Aws.ses.IdentityPolicy)("identityPolicy" + name, {
    identity: identity.arn,
    policy: sesPolicyDocument(name, identity, opts).apply(PolicyDocument$PulumiAws.toJsonString)
  }, opts !== undefined ? Primitive_option.valFromOption(opts) : undefined);
}

export {
  emailIdentity,
  fromCustomResourceOptions,
  sesPolicyDocument,
  identityWithPolicy,
}
/* @pulumi/aws Not a pure module */
