// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_dict = require("@rescript/std/lib/js/js_dict.js");
var Aws = require("@pulumi/aws");
var Core__Array = require("@rescript/core/src/Core__Array.res.js");
var Output$Pulumi = require("@reventless/rescript-pulumi-pulumi/src/Output.res.js");
var Pulumi = require("@pulumi/pulumi");
var Lambda$PulumiAws = require("@reventless/rescript-pulumi-aws/src/Lambda/Lambda.res.js");
var Adapter$Reventless = require("@reventless/reventless/src/adapter/Adapter.res.js");
var Util_SQS$ReventlessAws = require("../../util/Util_SQS.res.js");
var PolicyDocument$PulumiAws = require("@reventless/rescript-pulumi-aws/src/IAM/PolicyDocument.res.js");
var Adapter_Helpers$ReventlessAws = require("../Adapter_Helpers.res.js");

function createQueuePolicy(queue, name, lambda, opts) {
  Pulumi.all([
          queue.arn,
          queue.id,
          Output$Pulumi.flatMap(lambda, (function (lambda) {
                  return lambda.arn;
                }))
        ]).apply(function (param) {
        var queueArn = param[0];
        var queuePolicyDocument = PolicyDocument$PulumiAws.toJsonString(PolicyDocument$PulumiAws.make(undefined, name + "QueuePolicy", [
                  {
                    Sid: "AllowLambdaToAccessQueue",
                    Principal: {
                      Service: "lambda.amazonaws.com"
                    },
                    Effect: "Allow",
                    Action: [
                      "sqs:ReceiveMessage",
                      "sqs:DeleteMessage",
                      "sqs:GetQueueAttributes"
                    ],
                    Resource: queueArn,
                    Condition: {
                      ArnEquals: Object.fromEntries([[
                              "AWS:SourceArn",
                              param[2]
                            ]])
                    }
                  },
                  {
                    Sid: "AllowCloudWatchEventsToSendToQueue",
                    Principal: {
                      Service: "events.amazonaws.com"
                    },
                    Effect: "Allow",
                    Action: ["sqs:SendMessage"],
                    Resource: queueArn
                  }
                ]));
        new (Aws.sqs.QueuePolicy)(name, {
              policy: queuePolicyDocument,
              queueUrl: param[1]
            }, opts);
      });
}

function createLambdaPolicy(lambdaRole, name, queue, resources, opts) {
  Pulumi.all([
          queue.arn,
          Adapter$Reventless.resourcesToUnwrappedOutput(resources)
        ]).apply(function (param) {
        var resources = param[1];
        var allowSQSSendLambda = Adapter_Helpers$ReventlessAws.sqsResources(resources).length > 0 ? PolicyDocument$PulumiAws.make(undefined, name + "RemoteSQSLambdaPolicy", [{
                  Sid: "AllowLambdaSendMessageSQS",
                  Effect: "Allow",
                  Action: "sqs:SendMessage",
                  Resource: Adapter_Helpers$ReventlessAws.sqsResources(resources).map(function (sqsResource) {
                        return sqsResource.urn;
                      })
                }]) : undefined;
        var allowLambdaReadWriteDynamoDb = Adapter_Helpers$ReventlessAws.dynamoDbResources(resources).length > 0 ? PolicyDocument$PulumiAws.make(undefined, name + "LambdaDynamoDbAccessPolicy", [{
                  Sid: "AllowLambdaReadWriteDynamoDb",
                  Effect: "Allow",
                  Action: [
                    "dynamodb:GetItem",
                    "dynamodb:Query",
                    "dynamodb:Scan",
                    "dynamodb:BatchGetItem",
                    "dynamodb:PutItem",
                    "dynamodb:UpdateItem",
                    "dynamodb:DeleteItem",
                    "dynamodb:BatchWriteItem"
                  ],
                  Resource: Adapter_Helpers$ReventlessAws.dynamoDbResources(resources).map(function (dynamoDbResource) {
                        return dynamoDbResource.urn;
                      })
                }]) : undefined;
        var allowLambdaWriteCloudWatchEvents = PolicyDocument$PulumiAws.make(undefined, name + "LambdaCloudWatchEventsPolicy", [{
                Sid: "AllowLambdaWriteCloudWatchEvents",
                Effect: "Allow",
                Action: [
                  "events:PutRule",
                  "events:PutTargets",
                  "events:DeleteRule",
                  "events:RemoveTargets"
                ],
                Resource: "*"
              }]);
        var allowLambdaReceiveSQS = PolicyDocument$PulumiAws.make(undefined, name + "SQSLambdaPolicy", [{
                Sid: "AllowLambdaReceiveSQS",
                Effect: "Allow",
                Action: [
                  "sqs:ReceiveMessage",
                  "sqs:DeleteMessage",
                  "sqs:GetQueueAttributes",
                  "sqs:ChangeMessageVisibility"
                ],
                Resource: param[0]
              }]);
        var allowLambdaPassRoleToEvents = PolicyDocument$PulumiAws.make(undefined, name + "LambdaPassRoleToCloudWatchEventsRule", [{
                Sid: "AllowLambdaPassRoleToEvents",
                Effect: "Allow",
                Action: "iam:PassRole",
                Resource: "*",
                Condition: {
                  StringEquals: Js_dict.fromArray([[
                          "iam:PassedToService",
                          "events.amazonaws.com"
                        ]])
                }
              }]);
        new (Aws.iam.RolePolicy)(name, {
              policy: PolicyDocument$PulumiAws.mergePolicyDocuments(name + "LambdaPolicy", Core__Array.keepSome([
                        Lambda$PulumiAws.defaultLoggingPolicyDocument,
                        allowLambdaReceiveSQS,
                        allowLambdaWriteCloudWatchEvents,
                        allowLambdaPassRoleToEvents,
                        allowLambdaReadWriteDynamoDb,
                        allowSQSSendLambda
                      ])),
              role: lambdaRole.id
            }, opts);
      });
}

function subscribeLambda2SqsTopic(lambda, name, queue, opts) {
  return Adapter$Reventless.outputToResource(lambda.apply(function (lambda) {
                  return Util_SQS$ReventlessAws.Subscription.toResource(queue.onEvent(name, lambda, undefined, opts));
                }));
}

exports.createQueuePolicy = createQueuePolicy;
exports.createLambdaPolicy = createLambdaPolicy;
exports.subscribeLambda2SqsTopic = subscribeLambda2SqsTopic;
/* @pulumi/aws Not a pure module */
