// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Aws = require("@pulumi/aws");
var Core__Array = require("@rescript/core/src/Core__Array.res.js");
var Pulumi = require("@pulumi/pulumi");
var Lambda$PulumiAws = require("@reventless/bs-pulumi-aws/src/Lambda/Lambda.res.js");
var Adapter$Reventless = require("@reventless/reventless/src/adapter/Adapter.res.js");
var Util_S3$ReventlessAws = require("../../util/Util_S3.res.js");
var Util_Pulumi$Reventless = require("@reventless/reventless/src/util/Util_Pulumi.res.js");
var PolicyDocument$PulumiAws = require("@reventless/bs-pulumi-aws/src/IAM/PolicyDocument.res.js");
var Adapter_Helpers$ReventlessAws = require("../Adapter_Helpers.res.js");
var TaskBucket_S3_Runtime$ReventlessAws = require("./TaskBucket_S3_Runtime.res.js");

function subscribeLambda2S3Bucket(lambda, name, bucket, opts) {
  lambda.apply(function (lambda) {
        [
          bucket.onObjectCreated(name + "Created", lambda, undefined, opts),
          bucket.onObjectRemoved(name + "Deleted", lambda, undefined, opts)
        ];
      });
}

function createLambdaPolicy(lambdaRole, name, bucket, bucketMode, resources, opts) {
  Pulumi.all([
          bucket.arn,
          Adapter$Reventless.resourcesToUnwrappedOutput(resources)
        ]).apply(function (param) {
        var resources = param[1];
        var bucketArn = param[0];
        var allowLambdaWriteS3 = bucketMode === "Write" || bucketMode === "ReadWrite" ? PolicyDocument$PulumiAws.make(undefined, name + "WriteS3", [{
                  Sid: "allowLambdaWriteS3",
                  Effect: "Allow",
                  Action: [
                    "s3:PutObject",
                    "s3:DeleteObject"
                  ],
                  Resource: bucketArn
                }]) : undefined;
        var allowLambdaReadS3 = bucketMode === "Read" || bucketMode === "ReadWrite" ? PolicyDocument$PulumiAws.make(undefined, name + "ReadS3", [{
                  Sid: "allowLambdaReadS3",
                  Effect: "Allow",
                  Action: ["s3:GetObject"],
                  Resource: bucketArn
                }]) : undefined;
        var allowLambdaSendSQS = resources.length > 0 ? PolicyDocument$PulumiAws.make(undefined, name + "SendSQS", [{
                  Sid: "AllowLambdaSendSQS",
                  Effect: "Allow",
                  Action: "sqs:SendMessage",
                  Resource: Adapter$Reventless.urns(Adapter_Helpers$ReventlessAws.sqsResources(resources))
                }]) : undefined;
        new (Aws.iam.RolePolicy)(name, {
              policy: PolicyDocument$PulumiAws.mergePolicyDocuments(name + "LambdaPolicy", Core__Array.keepSome([
                        Lambda$PulumiAws.defaultLoggingPolicyDocument,
                        allowLambdaReadS3,
                        allowLambdaWriteS3,
                        allowLambdaSendSQS
                      ])),
              role: lambdaRole.id
            }, opts);
      });
}

function connect(name, bucket, bucketMode, commandTopics, runtime, opts) {
  var opts$1 = Util_Pulumi$Reventless.ComponentResourceOptions.toCustomResourceOptions(opts);
  var lambda = runtime.parts.lambda;
  var lambdaRole = runtime.parts.lambdaRole;
  commandTopics.apply(function (allCommandTopics) {
        subscribeLambda2S3Bucket(lambda, name, bucket.parts, opts$1);
        var resources = Object.values(allCommandTopics).flatMap(function (commandTopic) {
              return commandTopic.resources;
            });
        createLambdaPolicy(lambdaRole, name, bucket.parts, bucketMode, resources, opts$1);
      });
}

function make(name, opts) {
  var opts$1 = Util_Pulumi$Reventless.ComponentResourceOptions.toCustomResourceOptions(opts);
  var bucket = new (Aws.s3.Bucket)(name, {
        corsRules: [{
            allowedHeaders: ["*"],
            allowedMethods: [
              "HEAD",
              "GET"
            ],
            allowedOrigins: ["*"],
            exposeHeaders: [
              "x-amz-server-side-encryption",
              ">x-amz-request-id",
              "x-amz-id-2",
              "ETag"
            ],
            maxAgeSeconds: 3000
          }]
      }, opts$1);
  return {
          resources: [Util_S3$ReventlessAws.toResource(bucket)],
          parts: bucket
        };
}

function makeHandler(handleEvents) {
  return TaskBucket_S3_Runtime$ReventlessAws.handleBucketEvent(handleEvents);
}

exports.subscribeLambda2S3Bucket = subscribeLambda2S3Bucket;
exports.createLambdaPolicy = createLambdaPolicy;
exports.connect = connect;
exports.make = make;
exports.makeHandler = makeHandler;
/* @pulumi/aws Not a pure module */
