// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Aws = require("@pulumi/aws");
var Pulumi = require("@pulumi/pulumi");
var Util_S3$ReventlessAws = require("../../util/Util_S3.res.js");
var Util_Pulumi$Reventless = require("@reventless/reventless/src/util/Util_Pulumi.res.js");
var TaskBucket_S3_Runtime$ReventlessAws = require("./TaskBucket_S3_Runtime.res.js");

function connect(name, bucket, runtime, opts) {
  var opts$1 = Util_Pulumi$Reventless.ComponentResourceOptions.toCustomResourceOptions(opts);
  var lambda = runtime.parts.lambda;
  lambda.apply(function (lambda) {
        [
          bucket.parts.onObjectCreated(name + "Created", lambda, undefined, opts$1),
          bucket.parts.onObjectRemoved(name + "Deleted", lambda, undefined, opts$1)
        ];
      });
}

function make(name, opts) {
  var opts$1 = Util_Pulumi$Reventless.ComponentResourceOptions.toCustomResourceOptions(opts);
  var bucket = new (Aws.s3.Bucket)(name, {
        corsRules: [{
            allowedHeaders: ["*"],
            allowedMethods: [
              "HEAD",
              "GET"
            ],
            allowedOrigins: ["*"],
            exposeHeaders: [
              "x-amz-server-side-encryption",
              ">x-amz-request-id",
              "x-amz-id-2",
              "ETag"
            ],
            maxAgeSeconds: 3000
          }]
      }, opts$1);
  return {
          resources: [Util_S3$ReventlessAws.toResource(bucket)],
          parts: bucket
        };
}

function makeHandler(handleEvents) {
  return Pulumi.output(TaskBucket_S3_Runtime$ReventlessAws.handleBucketEvent(handleEvents));
}

exports.connect = connect;
exports.make = make;
exports.makeHandler = makeHandler;
/* @pulumi/aws Not a pure module */
