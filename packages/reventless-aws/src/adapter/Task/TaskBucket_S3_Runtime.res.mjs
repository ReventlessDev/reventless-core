// Generated by ReScript, PLEASE EDIT WITH CARE


function handleBucketEvent(handleEvent) {
  return (event, param) => Promise.all(event.Records.map(record => {
    let eventName = record.eventName;
    let key = decodeURIComponent(record.s3.object.key);
    return handleEvent(eventName, key);
  })).then(actions => actions.flat());
}

export {
  handleBucketEvent,
}
/* No side effect */
