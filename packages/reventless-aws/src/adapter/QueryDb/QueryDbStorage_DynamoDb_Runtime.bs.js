// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Block = require("bs-platform/lib/js/block.js");
var Curry = require("bs-platform/lib/js/curry.js");
var Js_exn = require("bs-platform/lib/js/js_exn.js");
var Js_dict = require("bs-platform/lib/js/js_dict.js");
var Js_json = require("bs-platform/lib/js/js_json.js");
var Belt_List = require("bs-platform/lib/js/belt_List.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Belt_Option = require("bs-platform/lib/js/belt_Option.js");
var Caml_option = require("bs-platform/lib/js/caml_option.js");
var Message$Reventless = require("@reventless/reventless/src/Message.bs.js");
var DynamoDb_DocumentClient$AwsSdk = require("@reventless/bs-aws-sdk/src/DynamoDb_DocumentClient.bs.js");

function load(table) {
  return (function (id) {
      return DynamoDb_DocumentClient$AwsSdk.queryByIdWithTableName(table.name.get(), id).then((function (arr) {
                      return Promise.resolve(/* Ok */Block.__(0, [arr.length !== 0 ? Belt_List.fromArray(arr) : /* [] */0]));
                    })).catch((function (err) {
                    var tableName = table.name.get();
                    console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".load: Error: Couldn\'t load state for " + (String(id) + (" from " + (String(tableName) + (": " + (String(err) + "")))))));
                    return Promise.resolve(/* Error */Block.__(1, [/* NotLoadedFromStorage */Block.__(1, [err.message])]));
                  }));
    });
}

function calcPurgeTime(ttl) {
  var now_ms = Message$Reventless.now(/* () */0);
  var now_s = now_ms / 1000.0;
  var now_s_rounded = now_s | 0;
  return now_s_rounded + ttl | 0;
}

var purgeTimeAttributeName = "reventlessPurgeTime";

function insertTtl(json, ttl) {
  return Belt_Option.getWithDefault(Belt_Option.flatMap(ttl, (function (ttl) {
                    return Curry._1(Belt_Option.mapWithDefault(Js_json.decodeObject(json), (function (param) {
                                      console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws.insertTtl: Error: Couldn't decode JSON", JSON.stringify(json));
                                      return ;
                                    }), (function (obj, param) {
                                      obj[purgeTimeAttributeName] = calcPurgeTime(ttl);
                                      return Caml_option.some(obj);
                                    })), /* () */0);
                  })), json);
}

function save(table) {
  return (function (_id, json, saveMode, ttl) {
      var tableName = table.name.get();
      var stateStr = JSON.stringify(json);
      var json$1 = insertTtl(json, ttl);
      if (saveMode) {
        return DynamoDb_DocumentClient$AwsSdk.putWithTableName(tableName, json$1).then((function (param) {
                        console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".save: saved Overwrite state to " + (String(tableName) + (": " + (String(stateStr) + "")))));
                        return Promise.resolve(/* Ok */Block.__(0, [/* () */0]));
                      })).catch((function (err) {
                      console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".save: Error: Couldn\'t save Overwrite state to " + (String(tableName) + (": " + (String(err) + "")))));
                      return Promise.resolve(/* Error */Block.__(1, [/* NotSavedToStorage */Block.__(0, [err.message])]));
                    }));
      } else {
        return DynamoDb_DocumentClient$AwsSdk.putIfNotExists(tableName, table.hashKey.get(), table.rangeKey.get(), json$1).then((function (param) {
                        console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".save: saved Init state to " + (String(tableName) + (": " + (String(stateStr) + "")))));
                        return Promise.resolve(/* Ok */Block.__(0, [/* () */0]));
                      })).catch((function (err) {
                      var tableName = table.name.get();
                      var match = err.code;
                      if (match === "ConditionalCheckFailedException") {
                        console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".save: Error: Stale State in " + (String(tableName) + "")));
                        return Promise.resolve(/* Error */Block.__(1, [/* StaleState */0]));
                      } else {
                        console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".save: Error: Couldn\'t save Init state to " + (String(tableName) + (": " + (String(err) + "")))));
                        return Promise.resolve(/* Error */Block.__(1, [/* NotSavedToStorage */Block.__(0, [err.message])]));
                      }
                    }));
      }
    });
}

function saveBatch($staropt$star, table) {
  var maxRetries = $staropt$star !== undefined ? $staropt$star : 3;
  return (function (items) {
      var tableName = table.name.get();
      var batchWrite$prime = function (itemRequestMap) {
        return DynamoDb_DocumentClient$AwsSdk.batchWrite({
                    RequestItems: itemRequestMap,
                    ReturnConsumedCapacity: "NONE",
                    ReturnItemCollectionMetris: "NONE"
                  });
      };
      var wrapWithCount = function (promise, count) {
        return promise.then((function (pContent) {
                      return Promise.resolve(/* tuple */[
                                  pContent,
                                  count
                                ]);
                    }));
      };
      var hasUnprocessedItems = function (writeOutput) {
        return Object.keys(writeOutput.UnprocessedItems).length !== 0;
      };
      var retryIfNecessary = function (p) {
        return p.then((function (originalPromiseContent) {
                      var numberOfRetries = originalPromiseContent[1];
                      var writeOutput = originalPromiseContent[0];
                      var unprocessedItems = writeOutput.UnprocessedItems;
                      var unprocessedItemsPresent = hasUnprocessedItems(writeOutput);
                      var numberOfRetriesReached = numberOfRetries >= maxRetries;
                      if (unprocessedItemsPresent && !numberOfRetriesReached) {
                        return retryIfNecessary(wrapWithCount(batchWrite$prime(unprocessedItems), numberOfRetries + 1 | 0));
                      } else {
                        return Promise.resolve(originalPromiseContent);
                      }
                    }));
      };
      var writeRequests = Belt_Array.map(items, (function (param) {
              var __x = {
                Item: insertTtl(param[1], param[2])
              };
              return {
                      PutRequest: __x
                    };
            }));
      var batchWriteItemRequestMap = Js_dict.fromArray(/* array */[/* tuple */[
              tableName,
              writeRequests
            ]]);
      var __x = retryIfNecessary(wrapWithCount(batchWrite$prime(batchWriteItemRequestMap), 0));
      return __x.then((function (param) {
                    if (hasUnprocessedItems(param[0])) {
                      Js_exn.raiseError("Still unprocessed items present after maxRetries(" + (String(maxRetries) + ")!"));
                    }
                    return Promise.resolve(/* Ok */Block.__(0, [/* () */0]));
                  }));
    });
}

function count(table) {
  return (function (id, fieldName, inc) {
      var tableName = table.name.get();
      console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".count: " + (String(tableName) + (", " + (String(id) + (", " + (String(fieldName) + (", " + (String(inc) + "")))))))));
      return DynamoDb_DocumentClient$AwsSdk.update({
                      TableName: tableName,
                      Key: {
                        id: id
                      },
                      UpdateExpression: "ADD #fieldName :inc",
                      ExpressionAttributeNames: Js_dict.fromList(/* :: */[
                            /* tuple */[
                              "#fieldName",
                              fieldName
                            ],
                            /* [] */0
                          ]),
                      ExpressionAttributeValues: {
                        ":inc": inc
                      },
                      ReturnValues: "UPDATED_NEW"
                    }).then((function (updateOutput) {
                      return Promise.resolve(/* Ok */Block.__(0, [updateOutput.Attributes.count]));
                    })).catch((function (err) {
                    console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".count: Error: Couldn\'t count on " + (String(tableName) + (": " + (String(err) + "")))));
                    return Promise.resolve(/* Error */Block.__(1, [/* NotCountedOnStorage */Block.__(2, [err.message])]));
                  }));
    });
}

function $$delete(table) {
  return (function (id, sort) {
      var tableName = table.name.get();
      console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws.delete: tableName, id, sort", table.name.get(), id, sort);
      return DynamoDb_DocumentClient$AwsSdk.deleteWithTableName(tableName, id, sort).then((function (param) {
                      console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".delete: deleted state for " + (String(id) + (" from " + (String(tableName) + "")))));
                      return Promise.resolve(/* Ok */Block.__(0, [/* () */0]));
                    })).catch((function (err) {
                    console.log("QueryDbStorage_DynamoDb_Runtime-ReventlessAws" + (".delete: Error: Couldn\'t delete state for " + (String(id) + (" from " + (String(tableName) + (": " + (String(err) + "")))))));
                    return Promise.resolve(/* Error */Block.__(1, [/* NotDeletedFromStorage */Block.__(3, [err.message])]));
                  }));
    });
}

exports.load = load;
exports.calcPurgeTime = calcPurgeTime;
exports.purgeTimeAttributeName = purgeTimeAttributeName;
exports.insertTtl = insertTtl;
exports.save = save;
exports.saveBatch = saveBatch;
exports.count = count;
exports.$$delete = $$delete;
/* Message-Reventless Not a pure module */
