{
  "name": "@reventless/reventless-aws",
  "version": "1.3.0-rescript.2",
  "description": "AWS adapters for Reventless",
  "author": "Atos",
  "license": "proprietary",
  "repository": {
    "type": "git",
    "url": "ssh://git@gitlab.com:reventless/reventless-universe.git"
  },
  "publishConfig": {
    "@reventless:registry": "https://gitlab.com/api/v4/projects/24127696/packages/npm/"
  },
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "rebuild": "npm run clean && npm run build",
    "deploy": "./scripts/deploy.sh",
    "test": "jest",
    "coverage": "nyc report --temp-directory=coverage --reporter=text-lcov | coveralls",
    "dev": "jest --watchAll",
    "watch:bsb": "bsb -make-world -w",
    "watch:jest": "jest --coverage --watchAll",
    "watch:screen": "screen -c .screenrc",
    "cloc": "cloc . --exclude-dir=node_modules,lib,milk,tmp-bs-platform --not-match-f=package.*\\.json"
  },
  "dependencies": {
    "@reventless/bs-aws-sdk": "^1.2.0-rescript.2",
    "@reventless/bs-pulumi-aws": "^1.2.0-rescript.2",
    "@reventless/bs-pulumi-pulumi": "^1.2.0-rescript.2",
    "@reventless/reventless": "^1.3.0-rescript.2",
    "@reventless/reventless-spec": "^1.3.0-rescript.2",
    "decco": "1.6.0"
  },
  "devDependencies": {
    "@glennsl/bs-jest": "^0.5.1",
    "bs-platform": "^8.4.2"
  },
  "peerDependencies": {
    "bs-platform": "^8.4.2"
  },
  "gitHead": "1016edd2eceed939132c050a8ec292dc898a405f"
}
