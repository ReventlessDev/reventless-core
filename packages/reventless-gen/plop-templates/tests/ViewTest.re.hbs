open Jest;

module {{properCase name}}ViewTest = 
  Reventless.ViewTest.Make({{properCase name}}, {{properCase name}}View);

open {{properCase name}}ViewTest;
open {{properCase name}}Fixtures;
open {{properCase name}};

describe("{{properCase name}}View:", () => {
  test("Added", () =>
    givenEvents([])
    |> whenEvent(Added)
    |> thenState(state)
  );

  test("Added (already existing)", () =>
    givenEvents([Added])
    |> whenEvent(Added)
    |> thenThrow
  );

});
