open Jest;

module {{properCase name}}BehaviorTest =
  Reventless.BehaviorTest.Make({{properCase name}}, {{properCase name}}Behavior);

open {{properCase name}}BehaviorTest;
open {{properCase name}}Fixtures;
open {{properCase name}};

describe("{{properCase name}}Behavior:", () => {
  test("Add", () =>
    givenEvents([]) //
    |> whenCmd(Add)
    |> thenEvent(Added)
  );

  test("Add (already existing)", () =>
    givenEvents([Added]) //
    |> whenCmd(Add)
    |> thenError(AlreadyExisting)
  );

});
