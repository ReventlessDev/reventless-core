let typesSchema = {|
type {{properCase name}} {
  id: ID!
  statusChange: StatusChange!
  status: [String!]!
}

type {{pluralize name}} {
  nextToken: String
  scannedCount: Int!
  items: [{{properCase name}}!]!
}
|};

let queriesSchema = {|
  {{camelCase name}}(id: ID!): {{properCase name}}
    @aws_auth(cognito_groups: ["Admin", "User"])
  every{{properCase name}}(nextToken: String, limit: Int): {{pluralize name}}!
    @aws_auth(cognito_groups: ["Admin", "User"])
|};

let mutationsSchema = {|
|};
