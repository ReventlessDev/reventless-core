$1
module {{properCase source}}To{{properCase target}}Mapping:
  EventMapping.T with module Target := {{properCase target}} = {
  module Source = {{properCase source}};

  let map =
    (. id, event, _queryEngine) =>
      switch (event) {
      | {{properCase source}}.Added => [|
          EventMapping.Publish(
            id->{{properCase target}}.Id.makeFromString,
            {{properCase target}}.Add,
          ),
        |]
      };
};
