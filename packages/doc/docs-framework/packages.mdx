---
id: packages
title: Monorepo Packages
description: The packages that make up the Reventless monorepo, grouped by role.
---

import PackagesReference from '@site/src/components/PackagesReference';

Reventless is organized as a monorepo of focused, composable packages. The list
below is generated from each package's `package.json` at build time, so it always
reflects the current workspace.

For where each kind of package lives and the conventions that govern it, see the
[contributing guide](./contributing.md).

<PackagesReference />
