// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Moment from "moment";
import Moment$1 from "moment";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

let Duration = {};

function duration(value, unit) {
  return Moment.default.duration(value, unit);
}

function durationMillis(value) {
  return Moment.default.duration(value);
}

function durationFormat(format) {
  return Moment.default.duration(format);
}

function add(moment, duration) {
  let clone = moment.clone();
  clone.add(duration);
  return clone;
}

function subtract(moment, duration) {
  let clone = moment.clone();
  clone.subtract(duration);
  return clone;
}

function startOf(moment, timeUnit) {
  let clone = moment.clone();
  clone.startOf(timeUnit);
  return clone;
}

function endOf(moment, timeUnit) {
  let clone = moment.clone();
  clone.endOf(timeUnit);
  return clone;
}

function setMillisecond(moment, millisecond) {
  let clone = moment.clone();
  clone.millisecond(millisecond);
  return clone;
}

function setSecond(moment, second) {
  let clone = moment.clone();
  clone.second(second);
  return clone;
}

function setMinute(moment, minute) {
  let clone = moment.clone();
  clone.minute(minute);
  return clone;
}

function setHour(moment, hour) {
  let clone = moment.clone();
  clone.hour(hour);
  return clone;
}

function setDate(moment, date) {
  let clone = moment.clone();
  clone.date(date);
  return clone;
}

function setDay(moment, day) {
  let clone = moment.clone();
  clone.day(day);
  return clone;
}

function setWeekday(moment, weekday) {
  let clone = moment.clone();
  clone.weekday(weekday);
  return clone;
}

function setIsoWeekday(moment, isoWeekday) {
  let clone = moment.clone();
  clone.isoWeekday(isoWeekday);
  return clone;
}

function setDayOfYear(moment, dayOfYear) {
  let clone = moment.clone();
  clone.dayOfYear(dayOfYear);
  return clone;
}

function setWeek(moment, week) {
  let clone = moment.clone();
  clone.week(week);
  return clone;
}

function setIsoWeek(moment, isoWeek) {
  let clone = moment.clone();
  clone.isoWeek(isoWeek);
  return clone;
}

function setQuarter(moment, quarter) {
  let clone = moment.clone();
  clone.quarter(quarter);
  return clone;
}

function setWeekYear(moment, weekYear) {
  let clone = moment.clone();
  clone.weekYear(weekYear);
  return clone;
}

function setIsoWeekYear(moment, isoWeekYear) {
  let clone = moment.clone();
  clone.weekYear(isoWeekYear);
  return clone;
}

function setMonth(moment, month) {
  let clone = moment.clone();
  clone.month(month);
  return clone;
}

function setYear(moment, year) {
  let clone = moment.clone();
  clone.year(year);
  return clone;
}

function toJSON(moment) {
  return Primitive_option.fromNull(moment.toJSON());
}

let Moment$2 = {
  add: add,
  subtract: subtract,
  startOf: startOf,
  endOf: endOf,
  setMillisecond: setMillisecond,
  setSecond: setSecond,
  setMinute: setMinute,
  setHour: setHour,
  setDate: setDate,
  setDay: setDay,
  setWeekday: setWeekday,
  setIsoWeekday: setIsoWeekday,
  setDayOfYear: setDayOfYear,
  setWeek: setWeek,
  setIsoWeek: setIsoWeek,
  setQuarter: setQuarter,
  setWeekYear: setWeekYear,
  setIsoWeekYear: setIsoWeekYear,
  setMonth: setMonth,
  setYear: setYear,
  toJSON: toJSON
};

function momentNow() {
  return Moment$1();
}

function momentDefaultFormat(value) {
  return Moment$1(value);
}

function momentWithFormat(value, format) {
  return Moment$1(value, format);
}

function momentWithDate(date) {
  return Moment$1(date);
}

function momentWithFormats(value, formats) {
  return Moment$1(value, formats);
}

function momentWithTimestampMS(timestamp) {
  return Moment$1(timestamp);
}

function momentWithComponents(components) {
  return Moment$1(components);
}

function momentUtcWithFormats(value, formats) {
  return Moment.default.utc(value, formats);
}

function momentUtcDefaultFormat(value) {
  return Moment.default.utc(value);
}

function momentWithUnix(timestamp) {
  return Moment$1(timestamp * 1000.0);
}

function momentUtc(format, value) {
  if (format !== undefined) {
    return Moment.default.utc(value, format);
  } else {
    return Moment.default.utc(value);
  }
}

function moment(format, value) {
  if (format !== undefined) {
    return Moment$1(value, format);
  } else {
    return Moment$1(value);
  }
}

export {
  Duration,
  duration,
  durationMillis,
  durationFormat,
  Moment$2 as Moment,
  momentNow,
  momentDefaultFormat,
  momentWithFormat,
  momentWithDate,
  momentWithFormats,
  momentWithTimestampMS,
  momentWithComponents,
  momentUtcWithFormats,
  momentUtcDefaultFormat,
  momentWithUnix,
  momentUtc,
  moment,
}
/* moment Not a pure module */
