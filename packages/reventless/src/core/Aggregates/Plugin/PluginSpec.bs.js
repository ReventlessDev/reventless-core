// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Block = require("bs-platform/lib/js/block.js");
var Decco = require("decco/src/Decco.js");
var Js_json = require("bs-platform/lib/js/js_json.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Plugin$ReventlessSpec = require("@reventless/reventless-spec/src/components/Plugin.bs.js");

function command_encode(v) {
  if (typeof v !== "number") {
    return [
            "Connect",
            Plugin$ReventlessSpec.pluginDefinition_encode(v[0])
          ];
  }
  switch (v) {
    case /* Heartbeat */0 :
        return ["Heartbeat"];
    case /* Disconnect */1 :
        return ["Disconnect"];
    case /* Activate */2 :
        return ["Activate"];
    case /* Deactivate */3 :
        return ["Deactivate"];
    
  }
}

function command_decode(v) {
  var jsonArr = Js_json.classify(v);
  if (typeof jsonArr === "number") {
    return Decco.error(undefined, "Not a variant", v);
  }
  if (jsonArr.tag !== /* JSONArray */3) {
    return Decco.error(undefined, "Not a variant", v);
  }
  var jsonArr$1 = jsonArr[0];
  if (jsonArr$1.length === 0) {
    return Decco.error(undefined, "Expected variant, found empty array", v);
  }
  var tagged = jsonArr$1.map(Js_json.classify);
  var match = Belt_Array.getExn(tagged, 0);
  if (typeof match !== "number" && !match.tag) {
    switch (match[0]) {
      case "Activate" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* Activate */2]);
          }
      case "Connect" :
          if (tagged.length !== 2) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          }
          var v0 = Plugin$ReventlessSpec.pluginDefinition_decode(Belt_Array.getExn(jsonArr$1, 1));
          if (!v0.tag) {
            return /* Ok */Block.__(0, [/* Connect */[v0[0]]]);
          }
          var e = v0[0];
          return /* Error */Block.__(1, [{
                      path: "[0]" + e.path,
                      message: e.message,
                      value: e.value
                    }]);
      case "Deactivate" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* Deactivate */3]);
          }
      case "Disconnect" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* Disconnect */1]);
          }
      case "Heartbeat" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* Heartbeat */0]);
          }
      default:
        
    }
  }
  return Decco.error(undefined, "Invalid variant constructor", Belt_Array.getExn(jsonArr$1, 0));
}

function event_encode(v) {
  if (typeof v === "number") {
    return ["UnknownPluginDetected"];
  }
  switch (v.tag | 0) {
    case /* Connected */0 :
        return [
                "Connected",
                Plugin$ReventlessSpec.pluginDefinition_encode(v[0])
              ];
    case /* Reconnected */1 :
        return [
                "Reconnected",
                Plugin$ReventlessSpec.pluginDefinition_encode(v[0])
              ];
    case /* Disconnected */2 :
        return [
                "Disconnected",
                Plugin$ReventlessSpec.pluginDefinition_encode(v[0])
              ];
    case /* Activated */3 :
        return [
                "Activated",
                Plugin$ReventlessSpec.pluginDefinition_encode(v[0])
              ];
    case /* Deactivated */4 :
        return [
                "Deactivated",
                Plugin$ReventlessSpec.pluginDefinition_encode(v[0])
              ];
    
  }
}

function event_decode(v) {
  var jsonArr = Js_json.classify(v);
  if (typeof jsonArr === "number") {
    return Decco.error(undefined, "Not a variant", v);
  }
  if (jsonArr.tag !== /* JSONArray */3) {
    return Decco.error(undefined, "Not a variant", v);
  }
  var jsonArr$1 = jsonArr[0];
  if (jsonArr$1.length === 0) {
    return Decco.error(undefined, "Expected variant, found empty array", v);
  }
  var tagged = jsonArr$1.map(Js_json.classify);
  var match = Belt_Array.getExn(tagged, 0);
  if (typeof match !== "number" && !match.tag) {
    switch (match[0]) {
      case "Activated" :
          if (tagged.length !== 2) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          }
          var v0 = Plugin$ReventlessSpec.pluginDefinition_decode(Belt_Array.getExn(jsonArr$1, 1));
          if (!v0.tag) {
            return /* Ok */Block.__(0, [/* Activated */Block.__(3, [v0[0]])]);
          }
          var e = v0[0];
          return /* Error */Block.__(1, [{
                      path: "[0]" + e.path,
                      message: e.message,
                      value: e.value
                    }]);
      case "Connected" :
          if (tagged.length !== 2) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          }
          var v0$1 = Plugin$ReventlessSpec.pluginDefinition_decode(Belt_Array.getExn(jsonArr$1, 1));
          if (!v0$1.tag) {
            return /* Ok */Block.__(0, [/* Connected */Block.__(0, [v0$1[0]])]);
          }
          var e$1 = v0$1[0];
          return /* Error */Block.__(1, [{
                      path: "[0]" + e$1.path,
                      message: e$1.message,
                      value: e$1.value
                    }]);
      case "Deactivated" :
          if (tagged.length !== 2) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          }
          var v0$2 = Plugin$ReventlessSpec.pluginDefinition_decode(Belt_Array.getExn(jsonArr$1, 1));
          if (!v0$2.tag) {
            return /* Ok */Block.__(0, [/* Deactivated */Block.__(4, [v0$2[0]])]);
          }
          var e$2 = v0$2[0];
          return /* Error */Block.__(1, [{
                      path: "[0]" + e$2.path,
                      message: e$2.message,
                      value: e$2.value
                    }]);
      case "Disconnected" :
          if (tagged.length !== 2) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          }
          var v0$3 = Plugin$ReventlessSpec.pluginDefinition_decode(Belt_Array.getExn(jsonArr$1, 1));
          if (!v0$3.tag) {
            return /* Ok */Block.__(0, [/* Disconnected */Block.__(2, [v0$3[0]])]);
          }
          var e$3 = v0$3[0];
          return /* Error */Block.__(1, [{
                      path: "[0]" + e$3.path,
                      message: e$3.message,
                      value: e$3.value
                    }]);
      case "Reconnected" :
          if (tagged.length !== 2) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          }
          var v0$4 = Plugin$ReventlessSpec.pluginDefinition_decode(Belt_Array.getExn(jsonArr$1, 1));
          if (!v0$4.tag) {
            return /* Ok */Block.__(0, [/* Reconnected */Block.__(1, [v0$4[0]])]);
          }
          var e$4 = v0$4[0];
          return /* Error */Block.__(1, [{
                      path: "[0]" + e$4.path,
                      message: e$4.message,
                      value: e$4.value
                    }]);
      case "UnknownPluginDetected" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* UnknownPluginDetected */0]);
          }
      default:
        
    }
  }
  return Decco.error(undefined, "Invalid variant constructor", Belt_Array.getExn(jsonArr$1, 0));
}

function error_encode(v) {
  switch (v) {
    case /* NotExisting */0 :
        return ["NotExisting"];
    case /* AlreadyConnected */1 :
        return ["AlreadyConnected"];
    case /* IsDisconnected */2 :
        return ["IsDisconnected"];
    case /* IsInactive */3 :
        return ["IsInactive"];
    
  }
}

function error_decode(v) {
  var jsonArr = Js_json.classify(v);
  if (typeof jsonArr === "number") {
    return Decco.error(undefined, "Not a variant", v);
  }
  if (jsonArr.tag !== /* JSONArray */3) {
    return Decco.error(undefined, "Not a variant", v);
  }
  var jsonArr$1 = jsonArr[0];
  if (jsonArr$1.length === 0) {
    return Decco.error(undefined, "Expected variant, found empty array", v);
  }
  var tagged = jsonArr$1.map(Js_json.classify);
  var match = Belt_Array.getExn(tagged, 0);
  if (typeof match !== "number" && !match.tag) {
    switch (match[0]) {
      case "AlreadyConnected" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* AlreadyConnected */1]);
          }
      case "IsDisconnected" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* IsDisconnected */2]);
          }
      case "IsInactive" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* IsInactive */3]);
          }
      case "NotExisting" :
          if (tagged.length !== 1) {
            return Decco.error(undefined, "Invalid number of arguments to variant constructor", v);
          } else {
            return /* Ok */Block.__(0, [/* NotExisting */0]);
          }
      default:
        
    }
  }
  return Decco.error(undefined, "Invalid variant constructor", Belt_Array.getExn(jsonArr$1, 0));
}

var name = "Plugin";

var Id;

var name_encode = Plugin$ReventlessSpec.name_encode;

var name_decode = Plugin$ReventlessSpec.name_decode;

var version_encode = Plugin$ReventlessSpec.version_encode;

var version_decode = Plugin$ReventlessSpec.version_decode;

var extensionPointDefinition_encode = Plugin$ReventlessSpec.extensionPointDefinition_encode;

var extensionPointDefinition_decode = Plugin$ReventlessSpec.extensionPointDefinition_decode;

var extensionDefinition_encode = Plugin$ReventlessSpec.extensionDefinition_encode;

var extensionDefinition_decode = Plugin$ReventlessSpec.extensionDefinition_decode;

var pluginDefinition_encode = Plugin$ReventlessSpec.pluginDefinition_encode;

var pluginDefinition_decode = Plugin$ReventlessSpec.pluginDefinition_decode;

exports.name = name;
exports.Id = Id;
exports.name_encode = name_encode;
exports.name_decode = name_decode;
exports.version_encode = version_encode;
exports.version_decode = version_decode;
exports.extensionPointDefinition_encode = extensionPointDefinition_encode;
exports.extensionPointDefinition_decode = extensionPointDefinition_decode;
exports.extensionDefinition_encode = extensionDefinition_encode;
exports.extensionDefinition_decode = extensionDefinition_decode;
exports.pluginDefinition_encode = pluginDefinition_encode;
exports.pluginDefinition_decode = pluginDefinition_decode;
exports.command_encode = command_encode;
exports.command_decode = command_decode;
exports.event_encode = event_encode;
exports.event_decode = event_decode;
exports.error_encode = error_encode;
exports.error_decode = error_decode;
/* No side effect */
