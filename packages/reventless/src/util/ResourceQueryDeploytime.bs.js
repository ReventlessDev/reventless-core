// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("bs-platform/lib/js/curry.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Belt_Option = require("bs-platform/lib/js/belt_Option.js");
var Caml_option = require("bs-platform/lib/js/caml_option.js");
var ResourceQuery$Reventless = require("./ResourceQuery.bs.js");

function find(resources, name) {
  return Belt_Array.reduce(resources, undefined, (function (result, resource) {
                var match = resource.name === name;
                if (match) {
                  return Caml_option.some(resource);
                } else {
                  return result;
                }
              }));
}

function eventTopicPublisherOfAllServices(services, serviceName) {
  return Belt_Option.map(find(services, serviceName), (function (service) {
                return service.aggregate.eventTopic.publisher;
              }));
}

function eventTopicPublisherOfAllServicesExn(services, serviceName) {
  return ResourceQuery$Reventless.unwrapResource(eventTopicPublisherOfAllServices(services, serviceName), "EventTopic", serviceName);
}

function readModelOfService(service) {
  return service.readModel;
}

function queryDbOfReadModel(readModel) {
  return readModel.queryDb;
}

function queryDbOfService(service) {
  return service.readModel.queryDb;
}

function allResolversMakers(services) {
  return Belt_Array.map(Belt_Array.map(services, queryDbOfService), (function (queryDb) {
                return queryDb.resolversMaker;
              }));
}

function queryDbStorageOfService(service) {
  return service.readModel.queryDb.storage;
}

function queryDbStorageOfAllServicesExn(services, serviceName) {
  return ResourceQuery$Reventless.unwrapResource(Belt_Option.map(find(services, serviceName), queryDbStorageOfService), "QueryDb", serviceName);
}

function allEventLogStorages(services, keep) {
  return Belt_Array.keepMap(services, (function (service) {
                var match = Curry._1(keep, service.name);
                if (match) {
                  return Caml_option.some(service.aggregate.eventLog.storage);
                }
                
              }));
}

function allQueryDbStorages(services, keep) {
  return Belt_Array.keepMap(services, (function (service) {
                var match = Curry._1(keep, service.name);
                if (match) {
                  return Caml_option.some(service.readModel.queryDb.storage);
                }
                
              }));
}

exports.find = find;
exports.eventTopicPublisherOfAllServices = eventTopicPublisherOfAllServices;
exports.eventTopicPublisherOfAllServicesExn = eventTopicPublisherOfAllServicesExn;
exports.readModelOfService = readModelOfService;
exports.queryDbOfReadModel = queryDbOfReadModel;
exports.queryDbOfService = queryDbOfService;
exports.allResolversMakers = allResolversMakers;
exports.queryDbStorageOfService = queryDbStorageOfService;
exports.queryDbStorageOfAllServicesExn = queryDbStorageOfAllServicesExn;
exports.allEventLogStorages = allEventLogStorages;
exports.allQueryDbStorages = allQueryDbStorages;
/* No side effect */
