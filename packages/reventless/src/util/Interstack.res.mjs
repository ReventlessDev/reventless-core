// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";

let coreStackReference = Stdlib_Option.map(new Pulumi.Config("core").get("stack"), stack => new Pulumi.StackReference(stack));

let stackDependencies = Stdlib_Option.getOr(new Pulumi.Config("interstack").getObject("dependencies"), []).map(stackName => new Pulumi.StackReference(stackName)).concat(Stdlib_Option.mapOr(coreStackReference, [], coreStack => [coreStack]));

function getOutputs(name) {
  return Pulumi.all(stackDependencies.map(stackRef => stackRef.getOutput(name))).apply(outputs => Stdlib_Array.filterMap(outputs, x => x));
}

let stackDependenciesTasks = getOutputs("tasks");

let stackDependenciesEventMappers = getOutputs("eventMappers");

function mergeMany(dependencies, locals) {
  return dependencies.apply(dependencies => locals.concat(dependencies));
}

function mergeTasks(extra) {
  return mergeMany(stackDependenciesTasks, extra);
}

function mergeEventMappers(extra) {
  return mergeMany(stackDependenciesEventMappers, extra);
}

export {
  coreStackReference,
  mergeTasks,
  mergeEventMappers,
}
/* coreStackReference Not a pure module */
