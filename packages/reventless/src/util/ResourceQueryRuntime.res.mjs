// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as ResourceQuery$Reventless from "./ResourceQuery.res.mjs";

function bucketNameOfAllTasks(tasks, taskName, bucketName) {
  return Stdlib_Option.map(Stdlib_Option.flatMap(tasks.find(task => task.name === taskName), task => Stdlib_Option.flatMap(task.bucketNames, buckets => buckets[bucketName])), bucket => bucket.get());
}

function bucketNameOfTaskExn(tasks, taskName, bucketName) {
  return ResourceQuery$Reventless.unwrapResource(bucketNameOfAllTasks(tasks, taskName, bucketName), "Bucket", taskName);
}

export {
  bucketNameOfAllTasks,
  bucketNameOfTaskExn,
}
/* No side effect */
