// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Math from "@rescript/runtime/lib/es6/Stdlib_Math.js";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

function filterRejected(results) {
  return results.map((result, idx) => [
    idx,
    result
  ]).filter(param => param[1].status === "rejected").map(param => [
    param[0],
    Stdlib_Option.getOr(Stdlib_Option.flatMap(Stdlib_Option.flatMap(param[1].reason, Stdlib_JsExn.fromException), exn => Stdlib_JsExn.message(exn)), "Unknown error")
  ]);
}

async function map(p, mapOk, mapExn) {
  let a;
  try {
    a = await p;
  } catch (raw_e) {
    return mapExn(Primitive_exceptions.internalToException(raw_e));
  }
  return mapOk(a);
}

async function mapOk(p, mapOk$1) {
  return mapOk$1(await p);
}

async function mapExn(p, mapExn$1) {
  try {
    return await p;
  } catch (raw_e) {
    return mapExn$1(Primitive_exceptions.internalToException(raw_e));
  }
}

function toUnit(p) {
  return mapOk(p, prim => {});
}

function make() {
  let res = {
    contents: _result => {}
  };
  let rej = {
    contents: _exn => {}
  };
  let promise = new Promise((resolve, reject) => {
    res.contents = resolve;
    rej.contents = reject;
  });
  return [
    promise,
    res.contents,
    rej.contents
  ];
}

async function onEndHandler(flush, resolve) {
  let exit = 0;
  let _res;
  try {
    _res = await flush();
    exit = 1;
  } catch (raw_e) {
    let e = Primitive_exceptions.internalToException(raw_e);
    if (e.RE_EXN_ID === "JsExn") {
      console.log("File \"Util_Promise.res\", line 54, characters 39-46", e._1);
    } else {
      throw e;
    }
  }
  if (exit === 1) {
    resolve();
  }
}

function finishTimeout(timeout) {
  let match = make();
  let resolve = match[1];
  setTimeout(() => resolve(), timeout);
  return match[0];
}

function finishRandomTimeout(min, max) {
  return finishTimeout(Stdlib_Math.Int.random(min, max));
}

export {
  filterRejected,
  map,
  mapOk,
  mapExn,
  toUnit,
  make,
  onEndHandler,
  finishTimeout,
  finishRandomTimeout,
}
/* Stdlib_JsExn Not a pure module */
