// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Component$Reventless from "../Component.res.mjs";
import * as ComponentType$Reventless from "../../ComponentType.res.mjs";
import * as EventCollector$Reventless from "./EventCollector.res.mjs";

function Make(RuntimeEnvironment) {
  return Channel => {
    let connect = (eventTopics, resources, runtime, eventCollector) => {
      let eventCollectorResource = Component$Reventless.toPulumiResource(eventCollector);
      let name = ComponentType$Reventless.name(Stdlib_Option.getOr(eventCollectorResource.__name, "Unnamed"), EventCollector$Reventless.componentType);
      Channel.connect(name, [{
          channel: eventCollector.channel,
          eventTopics: eventTopics,
          resources: resources
        }], runtime, {
        parent: eventCollectorResource
      });
    };
    let makeHandler = (eventCollector, eventsHandler) => {
      let channel = eventCollector.channel;
      return channel.handleChannelEvent(eventsHandler);
    };
    let make = (name, eventTopics, opts) => Component$Reventless.make(ComponentType$Reventless.toString(EventCollector$Reventless.componentType), name, (extra, extra$1) => {
      let opts_parent = Component$Reventless.toPulumiResource(extra);
      let opts = {
        parent: opts_parent
      };
      let name = ComponentType$Reventless.name(extra$1, EventCollector$Reventless.componentType);
      let channel = Channel.make(name, eventTopics, opts);
      extra.channel = channel;
      Component$Reventless.setOperations(extra, channel.enqueueEvent.apply(enqueueEvent => ({
        enqueueEvent: enqueueEvent
      })));
      return Component$Reventless.setOutputs(extra, {
        name: name,
        resources: channel.resources
      });
    }, opts);
    return {
      connect: connect,
      makeHandler: makeHandler,
      make: make
    };
  };
}

export {
  Make,
}
/* Component-Reventless Not a pure module */
