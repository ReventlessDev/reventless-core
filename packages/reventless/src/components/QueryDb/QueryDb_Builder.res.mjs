// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as QueryDb$Reventless from "./QueryDb.res.mjs";
import * as Component$Reventless from "../Component.res.mjs";
import * as ComponentType$Reventless from "../../ComponentType.res.mjs";
import * as QueryDb_Operations$Reventless from "./QueryDb_Operations.res.mjs";

function Make(Spec) {
  return Storage => (Resolvers => {
    let make = (api, apiRole, ttl, opts) => Component$Reventless.make(ComponentType$Reventless.toString(QueryDb$Reventless.componentType), Spec.name, (none, none$1) => {
      let opts_parent = Component$Reventless.toPulumiResource(none);
      let opts = {
        parent: opts_parent
      };
      let subIdField = Stdlib_Option.map(Spec.subIdConfig, config => config.subIdField);
      let storageName = ComponentType$Reventless.name(none$1, QueryDb$Reventless.componentType);
      let storage = Storage.make(storageName, Spec.config.indexes, subIdField, ttl, api, apiRole, opts);
      Component$Reventless.setOperations(none, storage.operations.apply(jsonOps => {
        let Operations = QueryDb_Operations$Reventless.Make(Spec)({
          jsonOps: jsonOps
        });
        return {
          load: Operations.load,
          save: Operations.save,
          saveBatch: Operations.saveBatch,
          count: Operations.count,
          delete: Operations.$$delete,
          deleteBatch: Operations.deleteBatch
        };
      }));
      let resolvers = Resolvers.make(none$1, api, apiRole, storage.dataSourceName, Spec.config.indexes, subIdField, Spec.config.idResolvers, Spec.config.idsResolvers, opts);
      return Component$Reventless.setOutputs(none, {
        resources: storage.resources.concat(resolvers.resources),
        resolversMaker: resolvers.resourcesMaker
      });
    }, opts);
    return {
      Spec: Spec,
      make: make
    };
  });
}

export {
  Make,
}
/* Component-Reventless Not a pure module */
