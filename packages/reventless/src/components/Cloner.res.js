// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Belt_Array = require("@rescript/std/lib/js/belt_Array.js");
var Belt_Option = require("@rescript/std/lib/js/belt_Option.js");
var Caml_option = require("@rescript/std/lib/js/caml_option.js");
var Pulumi = require("@pulumi/pulumi");
var Component$Reventless = require("./Component.res.js");
var ComponentType$Reventless = require("../ComponentType.res.js");

var noRunner = {
  resources: Pulumi.output([])
};

var Adapter = {
  noRunner: noRunner
};

function Make(Config, Runner) {
  var make = function (opts) {
    return Component$Reventless.make(ComponentType$Reventless.toString("Cloner"), ComponentType$Reventless.toString("Cloner"), (function (extra, extra$1) {
                  var api = Config.api;
                  var opts_parent = Caml_option.some(Component$Reventless.toPulumiResource(extra));
                  var opts = {
                    parent: opts_parent
                  };
                  var fullQualifiedStackName_organization = Belt_Option.getWithDefault(process.env.PULUMI_ORGANIZATION, "NO_ORGANIZATION");
                  var fullQualifiedStackName_project = Pulumi.getProject();
                  var fullQualifiedStackName_stack = Pulumi.getStack();
                  var fullQualifiedStackName = {
                    organization: fullQualifiedStackName_organization,
                    project: fullQualifiedStackName_project,
                    stack: fullQualifiedStackName_stack
                  };
                  var secretsConfig = new Pulumi.Config("secrets");
                  var secretUrns = Belt_Array.map([
                        "aws",
                        "pulumi",
                        "repository"
                      ], (function (str) {
                          return secretsConfig.get(str);
                        }));
                  var reventlessCiSecretUrn = secretsConfig.get("reventless-ci");
                  var runner;
                  if (secretUrns.length !== 3) {
                    console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
                    runner = noRunner;
                  } else {
                    var aws = secretUrns[0];
                    if (aws !== undefined) {
                      var pulumi = secretUrns[1];
                      if (pulumi !== undefined) {
                        var repository = secretUrns[2];
                        if (repository !== undefined && reventlessCiSecretUrn !== undefined) {
                          runner = Runner.make(extra$1, api, fullQualifiedStackName, Caml_option.valFromOption(reventlessCiSecretUrn), [
                                aws,
                                pulumi,
                                repository
                              ], opts);
                        } else {
                          console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
                          runner = noRunner;
                        }
                      } else {
                        console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
                        runner = noRunner;
                      }
                    } else {
                      console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
                      runner = noRunner;
                    }
                  }
                  return Component$Reventless.setOutputs(extra, {
                              resources: runner.resources
                            });
                }), opts);
  };
  return {
          make: make
        };
}

var componentType = "Cloner";

exports.componentType = componentType;
exports.Adapter = Adapter;
exports.Make = Make;
/* noRunner Not a pure module */
