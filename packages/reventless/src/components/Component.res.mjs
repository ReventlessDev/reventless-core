// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$ReventlessSpec from "@reventlessdev/reventless-spec/src/components/Component.res.mjs";

let registerOutputs = Component$ReventlessSpec.registerOutputs;

let setOutputs = Component$ReventlessSpec.setOutputs;

let outputs = Component$ReventlessSpec.outputs;

let wrappedOutputs = Component$ReventlessSpec.wrappedOutputs;

let setOperations = Component$ReventlessSpec.setOperations;

let operations = Component$ReventlessSpec.operations;

let toPulumiResource = Component$ReventlessSpec.toPulumiResource;

let fromPulumiResource = Component$ReventlessSpec.fromPulumiResource;

let make = Component$ReventlessSpec.make;

export {
  registerOutputs,
  setOutputs,
  outputs,
  wrappedOutputs,
  setOperations,
  operations,
  toPulumiResource,
  fromPulumiResource,
  make,
}
/* Component-ReventlessSpec Not a pure module */
