// Generated by ReScript, PLEASE EDIT WITH CARE

import * as DcbTag$ReventlessSpec from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let dcbTagId = DcbTag$ReventlessSpec.dcbTagId;

let string = DcbTag$ReventlessSpec.string;

let int = DcbTag$ReventlessSpec.int;

let isTagged = DcbTag$ReventlessSpec.isTagged;

let jsonValueToString = DcbTag$ReventlessSpec.jsonValueToString;

let extractTagsFromProperties = DcbTag$ReventlessSpec.extractTagsFromProperties;

let extractTagsFromJson = DcbTag$ReventlessSpec.extractTagsFromJson;

let extractTags = DcbTag$ReventlessSpec.extractTags;

let extractEventTypes = DcbTag$ReventlessSpec.extractEventTypes;

let extractTaggedFields = DcbTag$ReventlessSpec.extractTaggedFields;

export {
  dcbTagId,
  string,
  int,
  isTagged,
  jsonValueToString,
  extractTagsFromProperties,
  extractTagsFromJson,
  extractTags,
  extractEventTypes,
  extractTaggedFields,
}
/* DcbTag-ReventlessSpec Not a pure module */
