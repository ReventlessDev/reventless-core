// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_exn = require("@rescript/std/lib/js/js_exn.js");
var Js_dict = require("@rescript/std/lib/js/js_dict.js");
var Caml_obj = require("@rescript/std/lib/js/caml_obj.js");
var Belt_Array = require("@rescript/std/lib/js/belt_Array.js");
var Component = require("./Component").default;
var Belt_Option = require("@rescript/std/lib/js/belt_Option.js");
var Caml_option = require("@rescript/std/lib/js/caml_option.js");
var Belt_SetString = require("@rescript/std/lib/js/belt_SetString.js");
var Logger$Reventless = require("../util/Logger.res.js");
var Caml_js_exceptions = require("@rescript/std/lib/js/caml_js_exceptions.js");
var Message$Reventless = require("../Message.res.js");
var EventLog$Reventless = require("./EventLog.res.js");
var Component$Reventless = require("./Component.res.js");
var EventMapper$Reventless = require("./EventMapper.res.js");
var CommandTopic$Reventless = require("./CommandTopic.res.js");
var ComponentType$Reventless = require("../ComponentType.res.js");
var EventCollector$Reventless = require("./EventCollector.res.js");
var CommandGenerator$Reventless = require("./CommandGenerator.res.js");

function allEventTopics(allAggregates) {
  return Js_dict.map((function (aggregate) {
                return aggregate.eventLog.eventTopic;
              }), allAggregates);
}

function filterEventTopics(allAggregates, aggregateNames) {
  return Js_dict.fromArray(Belt_Array.keepMap(Belt_SetString.toArray(aggregateNames), (function (aggregateName) {
                    return Belt_Option.map(Js_dict.get(allAggregates, aggregateName), (function (aggregateOutput) {
                                  return [
                                          aggregateName,
                                          aggregateOutput.eventLog.eventTopic
                                        ];
                                }));
                  })));
}

function Make(Config, Spec, Behaviour, EventMappings, CommandGeneratorResolvers, CommandTopicConnector, EventLogStorage, EventTopicPublisher, EventCollectorConnector) {
  var partial_arg = CommandGenerator$Reventless.Make;
  var partial_arg$1 = function (param, param$1) {
    return partial_arg(Config, Spec, param, param$1);
  };
  var CommandGenerator = partial_arg$1(Behaviour, CommandGeneratorResolvers);
  var partial_arg_Id = Spec.Id;
  var partial_arg_command_encode = Spec.command_encode;
  var partial_arg_command_decode = Spec.command_decode;
  var partial_arg$2 = {
    Id: partial_arg_Id,
    command_encode: partial_arg_command_encode,
    command_decode: partial_arg_command_decode
  };
  var partial_arg$3 = CommandTopic$Reventless.Make;
  var CommandTopic = (function (param) {
        return partial_arg$3(partial_arg$2, param);
      })(CommandTopicConnector);
  var partial_arg_Id$1 = Spec.Id;
  var partial_arg_name = Spec.name;
  var partial_arg_event_encode = Spec.event_encode;
  var partial_arg_event_decode = Spec.event_decode;
  var partial_arg$4 = {
    Id: partial_arg_Id$1,
    name: partial_arg_name,
    event_encode: partial_arg_event_encode,
    event_decode: partial_arg_event_decode
  };
  var partial_arg$5 = EventLog$Reventless.Make;
  var partial_arg$6 = function (param, param$1) {
    return partial_arg$5(partial_arg$4, param, param$1);
  };
  var EventLog = partial_arg$6(EventLogStorage, EventTopicPublisher);
  var errorHandler = function (error, command, context) {
    var errorJson = JSON.stringify(Spec.error_encode(error));
    var commandJsonStr = JSON.stringify(Spec.command_encode(command));
    var id = context.id;
    Logger$Reventless.error("File \"Aggregate.res\", line 106, characters 11-18", undefined, undefined, "Behaviour error " + errorJson + " in " + Spec.name + "(" + id + "): Command: ", commandJsonStr);
    return [];
  };
  var groupTopicItemsById = function (topicItems) {
    var ids = Belt_Array.map(topicItems, (function (param) {
            return Spec.Id.toString(param.command.id);
          }));
    return Belt_Array.map(Belt_SetString.toArray(Belt_SetString.fromArray(ids)), (function (id) {
                  return [
                          Spec.Id.makeFromString(id),
                          Belt_Array.keep(topicItems, (function (param) {
                                  return Caml_obj.equal(param.command.id, Spec.Id.makeFromString(id));
                                }))
                        ];
                }));
  };
  var handleCommands = function (param) {
    var eventLogReplay = param[1];
    var eventLogAppend = param[0];
    return async function (allTopicItems) {
      var apply$p = function (stateOpt, $$event) {
        if (stateOpt !== undefined) {
          return Caml_option.some(Behaviour.apply(Caml_option.valFromOption(stateOpt), $$event));
        } else {
          return Caml_option.some(Behaviour.init($$event));
        }
      };
      var updateMeta = function (command$p) {
        var init = command$p.meta;
        return {
                service: init.service,
                time: Message$Reventless.nowAsISOString(),
                ip: init.ip,
                user: init.user,
                msgId: Message$Reventless.uuid(),
                correlationId: init.correlationId
              };
      };
      Logger$Reventless.debug("File \"Aggregate.res\", line 160, characters 24-31", undefined, undefined, "starting", "Aggregate.execCommands");
      return Belt_Array.concatMany(await Promise.all(Belt_Array.map(groupTopicItemsById(allTopicItems), (async function (param) {
                            var topicItems = param[1];
                            var id = param[0];
                            var history = await eventLogReplay(id);
                            var processCommand = async function (accP, command$p) {
                              var runBehaviour = function (param) {
                                var events = param[1];
                                var stateO = param[0];
                                if (stateO !== undefined) {
                                  var generatedEvents;
                                  try {
                                    generatedEvents = Behaviour.execute(Caml_option.valFromOption(stateO), command$p.command, {
                                          id: Spec.Id.toString(command$p.id),
                                          meta: command$p.meta
                                        }, errorHandler);
                                  }
                                  catch (raw_event){
                                    var $$event = Caml_js_exceptions.internalToOCamlException(raw_event);
                                    if ($$event.RE_EXN_ID === Message$Reventless.InvalidEvent) {
                                      Logger$Reventless.error("File \"Aggregate.res\", line 183, characters 36-43", undefined, undefined, "Behaviour.execute: InvalidEvent", $$event._1);
                                      generatedEvents = [];
                                    } else {
                                      throw $$event;
                                    }
                                  }
                                  return Promise.resolve({
                                              TAG: "Ok",
                                              _0: [
                                                Belt_Array.reduce(generatedEvents, stateO, apply$p),
                                                Belt_Array.concat(events, [[
                                                        generatedEvents,
                                                        updateMeta(command$p)
                                                      ]])
                                              ]
                                            });
                                }
                                var generatedEvents$1 = Behaviour.create(command$p.command, {
                                      id: Spec.Id.toString(command$p.id),
                                      meta: command$p.meta
                                    }, errorHandler);
                                return Promise.resolve({
                                            TAG: "Ok",
                                            _0: [
                                              Belt_Array.reduce(generatedEvents$1, undefined, apply$p),
                                              Belt_Array.concat(events, [[
                                                      generatedEvents$1,
                                                      updateMeta(command$p)
                                                    ]])
                                            ]
                                          });
                              };
                              var acc = await accP;
                              if (acc.TAG === "Ok") {
                                return await runBehaviour(acc._0);
                              } else {
                                return acc;
                              }
                            };
                            Logger$Reventless.debug("File \"Aggregate.res\", line 211, characters 28-35", undefined, undefined, "finished eventLogReplay for id", id);
                            Logger$Reventless.logCmdJsons("File \"Aggregate.res\", line 223, characters 36-43", undefined, Belt_Array.map(topicItems, (function (param) {
                                        return Message$Reventless.commandJsonOfCommand$p(Spec.Id.toString, Spec.command_encode, param.command);
                                      })), "Handling command");
                            var match = Belt_Array.unzip(Belt_Array.map(topicItems, (function (param) {
                                        return [
                                                param.reference,
                                                param.command
                                              ];
                                      })));
                            var references = match[0];
                            var result = await Belt_Array.reduce(match[1], Promise.resolve({
                                      TAG: "Ok",
                                      _0: [
                                        Belt_Array.reduce(history, undefined, apply$p),
                                        []
                                      ]
                                    }), processCommand);
                            var events;
                            events = result.TAG === "Ok" ? Belt_Array.concatMany(Belt_Array.map(result._0[1], (function (param) {
                                          var meta = param[1];
                                          return Belt_Array.map(param[0], (function ($$event) {
                                                        return {
                                                                id: id,
                                                                meta: meta,
                                                                event: $$event
                                                              };
                                                      }));
                                        }))) : Js_exn.raiseError(result._0);
                            if (events.length !== 0) {
                              var eventCount = String(events.length);
                              Logger$Reventless.debug(undefined, undefined, undefined, "Aggregate.handleCommands(" + Spec.Id.toString(id) + "): " + eventCount + " Event(s) generated:", Belt_Array.map(events, (function (event$p) {
                                          return Message$Reventless.variantNameOfJson(Spec.event_encode(event$p.event));
                                        })));
                              var match$1 = await eventLogAppend(history.length, id, events);
                              if (match$1.TAG === "Ok") {
                                Logger$Reventless.debug("File \"Aggregate.res\", line 261, characters 32-39", undefined, undefined, "finished eventLogAppend for id", Spec.Id.toString(id));
                                return Belt_Array.map(references, (function (reference) {
                                              return {
                                                      TAG: "Ok",
                                                      _0: reference
                                                    };
                                            }));
                              }
                              Logger$Reventless.error("File \"Aggregate.res\", line 264, characters 32-39", undefined, undefined, "failed eventLogAppend for id", Spec.Id.toString(id));
                              return Belt_Array.map(references, (function (reference) {
                                            return {
                                                    TAG: "Error",
                                                    _0: reference
                                                  };
                                          }));
                            }
                            Logger$Reventless.debug("File \"Aggregate.res\", line 247, characters 21-28", undefined, undefined, "handleCommands(" + Spec.Id.toString(id) + ")", "no Event generated");
                            return Belt_Array.map(references, (function (reference) {
                                          return {
                                                  TAG: "Ok",
                                                  _0: reference
                                                };
                                        }));
                          }))));
    };
  };
  var construct = function (self, name) {
    var opts_parent = Caml_option.some(self);
    var opts = {
      parent: opts_parent
    };
    var childName = ComponentType$Reventless.name(name, "Aggregate");
    var eventLog = EventLog.make(childName, opts);
    var handleCommands$1 = handleCommands([
          EventLog.append(eventLog),
          EventLog.replay(eventLog)
        ]);
    var commandTopic = CommandTopic.make(childName, handleCommands$1, undefined, undefined, opts);
    var commandGenerator = CommandGenerator.make(childName, CommandTopic.publish(commandTopic), opts);
    self.publishJsons = CommandTopic.publishJsons(commandTopic);
    self.addEventMapper = (function (none, none$1) {
        var EventCollector = EventCollector$Reventless.Make(EventCollectorConnector);
        var partial_arg_name = Spec.name;
        var partial_arg_Id = Spec.Id;
        var partial_arg_command_encode = Spec.command_encode;
        var partial_arg_command_decode = Spec.command_decode;
        var partial_arg = {
          name: partial_arg_name,
          Id: partial_arg_Id,
          command_encode: partial_arg_command_encode,
          command_decode: partial_arg_command_decode
        };
        var partial_arg$1 = EventMapper$Reventless.Make;
        var partial_arg$2 = function (param, param$1) {
          return partial_arg$1(partial_arg, param, param$1);
        };
        var EventMapper = partial_arg$2(EventCollector, EventMappings);
        var eventMapper = EventMappings.mappings.length !== 0 ? EventMapper.make(none, none$1, self.publishJsons, undefined, undefined, opts) : undefined;
        var newrecord = Caml_obj.obj_dup(Component$Reventless.extractOutputs(self));
        newrecord.eventMapper = Belt_Option.map(eventMapper, (function (eventMapper) {
                return Component$Reventless.extractOutputs(eventMapper);
              }));
        return newrecord;
      });
    var outputs = {
      name: name,
      commandGenerator: Component$Reventless.extractOutputs(commandGenerator),
      commandTopic: Component$Reventless.extractOutputs(commandTopic),
      eventLog: Component$Reventless.extractOutputs(eventLog)
    };
    self.setOutputs(outputs);
    return self.registerOutputs(outputs);
  };
  var make = function (opts) {
    var prim0 = ComponentType$Reventless.toString("Aggregate");
    var prim1 = Spec.name;
    return new Component(prim0, prim1, construct, opts);
  };
  return {
          Spec: Spec,
          make: make,
          publishJsons: (function (prim) {
              return prim.publishJsons;
            }),
          addEventMapper: (function (prim) {
              return prim.addEventMapper;
            })
        };
}

var ReventlessCommandTopic;

var componentType = "Aggregate";

exports.ReventlessCommandTopic = ReventlessCommandTopic;
exports.componentType = componentType;
exports.allEventTopics = allEventTopics;
exports.filterEventTopics = filterEventTopics;
exports.Make = Make;
/* ./Component Not a pure module */
