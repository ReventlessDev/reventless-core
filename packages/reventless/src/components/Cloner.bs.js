// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("bs-platform/lib/js/curry.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Component = require("./Component");
var Belt_Option = require("bs-platform/lib/js/belt_Option.js");
var Caml_option = require("bs-platform/lib/js/caml_option.js");
var Pulumi = require("@pulumi/pulumi");
var ComponentType$Reventless = require("../ComponentType.bs.js");

var noRunner = /* record */[/* resources */Pulumi.output(/* array */[])];

var Adapter = {
  noRunner: noRunner
};

function Make(Config) {
  return (function (Runner) {
      var construct = function (api, self, name) {
        var opts = {
          parent: self
        };
        var fullQualifiedStackName_000 = /* organization */Belt_Option.getWithDefault(process.env.PULUMI_ORGANIZATION, "NO_ORGANIZATION");
        var fullQualifiedStackName_001 = /* project */Pulumi.getProject();
        var fullQualifiedStackName_002 = /* stack */Pulumi.getStack();
        var fullQualifiedStackName = /* record */[
          fullQualifiedStackName_000,
          fullQualifiedStackName_001,
          fullQualifiedStackName_002
        ];
        var secretsConfig = new Pulumi.Config("secrets");
        var secretUrns = Belt_Array.map(/* array */[
              "aws",
              "pulumi",
              "repository"
            ], (function (param) {
                return secretsConfig.get(param);
              }));
        var reventlessCiSecretUrn = secretsConfig.get("reventless-ci");
        var runner;
        if (secretUrns.length !== 3) {
          console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
          runner = noRunner;
        } else {
          var match = secretUrns[0];
          if (match !== undefined) {
            var match$1 = secretUrns[1];
            if (match$1 !== undefined) {
              var match$2 = secretUrns[2];
              if (match$2 !== undefined && reventlessCiSecretUrn !== undefined) {
                runner = Curry._7(Runner.make, name, api, fullQualifiedStackName, Caml_option.valFromOption(reventlessCiSecretUrn), /* array */[
                      match,
                      match$1,
                      match$2
                    ], Caml_option.some(opts), /* () */0);
              } else {
                console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
                runner = noRunner;
              }
            } else {
              console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
              runner = noRunner;
            }
          } else {
            console.log("No ClonerRunner created because no secrets are configured in Pulumi config !");
            runner = noRunner;
          }
        }
        var self$1 = self;
        var outputs = {
          resources: runner[/* resources */0]
        };
        self$1.setOutputs(outputs);
        return self$1.registerOutputs(outputs);
      };
      var make = function (opts, param) {
        var partial_arg = Config.api;
        var prim = ComponentType$Reventless.toString(/* Cloner */20);
        var prim$1 = ComponentType$Reventless.toString(/* Cloner */20);
        var prim$2 = function (param, param$1) {
          return construct(partial_arg, param, param$1);
        };
        var prim$3 = opts;
        return new Component.default(prim, prim$1, prim$2, prim$3);
      };
      return {
              make: make
            };
    });
}

var componentType = /* Cloner */20;

exports.componentType = componentType;
exports.Adapter = Adapter;
exports.Make = Make;
/* noRunner Not a pure module */
