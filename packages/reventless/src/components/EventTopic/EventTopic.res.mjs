// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Belt_SetString from "@rescript/runtime/lib/es6/Belt_SetString.js";
import * as Adapter$Reventless from "../../adapter/Adapter.res.mjs";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

let NotPublishedToPublisher = /* @__PURE__ */Primitive_exceptions.create("EventTopic-Reventless.NotPublishedToPublisher");

function toResolvedOutputs(outputs) {
  return Adapter$Reventless.resourcesToUnwrappedOutput(outputs.resources).apply(resources => ({
    resources: resources.map(r => ({
      name: r.name,
      id: r.id,
      urn: r.urn,
      info: r.info,
      service: r.service
    }))
  }));
}

function allOutputsToResources(allOutputs) {
  return Object.values(allOutputs).map(eventTopic => eventTopic.resources).flat();
}

function filter(allEventTopics, sourceNames) {
  return Object.fromEntries(Stdlib_Array.filterMap(Belt_SetString.toArray(sourceNames), sourceName => Stdlib_Option.map(allEventTopics[sourceName], eventTopic => [
    sourceName,
    eventTopic
  ])));
}

function log(eventTopics, description) {
  Pulumi.all(Object.entries(Stdlib_Dict.mapValues(eventTopics, eventTopic => eventTopic.resources[0])).map(param => {
    let name = param[0];
    return param[1].service.apply(service => name + `(` + service + `)`);
  })).apply(topics => {
    console.log(description, topics.join(", "));
  });
}

let componentType = "EventTopic";

export {
  componentType,
  NotPublishedToPublisher,
  toResolvedOutputs,
  allOutputsToResources,
  filter,
  log,
}
/* @pulumi/pulumi Not a pure module */
