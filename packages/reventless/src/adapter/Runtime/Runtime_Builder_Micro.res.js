// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Core__Option = require("@rescript/core/src/Core__Option.res.js");
var Component$Reventless = require("../../components/Component.res.js");
var Heartbeat$Reventless = require("../../components/Heartbeat/Heartbeat.res.js");
var CommandTopic$Reventless = require("../../components/CommandTopic/CommandTopic.res.js");
var ComponentType$Reventless = require("../../ComponentType.res.js");
var EventCollector$Reventless = require("../../components/EventCollector/EventCollector.res.js");
var CommandGenerator$Reventless = require("../../components/CommandGenerator/CommandGenerator.res.js");
var SideEffectHandler$Reventless = require("../../components/SideEffectHandler/SideEffectHandler.res.js");

function Make(RuntimeEnvironment) {
  var forAggregateCommandGenerator = function (handler, memorySizeOpt, timeoutOpt, commandGenerator) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(commandGenerator);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, CommandGenerator$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forAggregateCommandTopic = function (handler, memorySizeOpt, timeoutOpt, commandTopic) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(commandTopic);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, CommandTopic$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forAggregateEventCollector = function (handler, memorySizeOpt, timeoutOpt, eventCollector) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(eventCollector);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, EventCollector$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forReadModelEventCollector = function (handler, memorySizeOpt, timeoutOpt, eventCollector) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(eventCollector);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, EventCollector$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forExtensionPointCommandTopic = function (handler, memorySizeOpt, timeoutOpt, commandTopic) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(commandTopic);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, CommandTopic$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forSideEffectHandlerEventCollector = function (handler, memorySizeOpt, timeoutOpt, eventCollector) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(eventCollector);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, SideEffectHandler$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forPluginEventCollector = function (handler, memorySizeOpt, timeoutOpt, eventCollector) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(eventCollector);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, EventCollector$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forPluginHeartbeat = function (handler, memorySizeOpt, timeoutOpt, heartbeat) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(heartbeat);
    return RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, Heartbeat$Reventless.componentType), handler, memorySize, timeout, {
                parent: resource
              });
  };
  var forDeadLetterQueue = function (handler, memorySizeOpt, timeoutOpt, plugin) {
    var memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
    var timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
    var resource = Component$Reventless.toPulumiResource(plugin);
    return RuntimeEnvironment.make(Core__Option.getOr(resource.__name, "DeadLetterQueue"), handler, memorySize, timeout, {
                parent: resource
              });
  };
  return {
          forAggregateCommandGenerator: forAggregateCommandGenerator,
          forAggregateCommandTopic: forAggregateCommandTopic,
          forAggregateEventCollector: forAggregateEventCollector,
          forReadModelEventCollector: forReadModelEventCollector,
          forExtensionPointCommandTopic: forExtensionPointCommandTopic,
          forSideEffectHandlerEventCollector: forSideEffectHandlerEventCollector,
          forPluginEventCollector: forPluginEventCollector,
          forPluginHeartbeat: forPluginHeartbeat,
          forDeadLetterQueue: forDeadLetterQueue
        };
}

exports.Make = Make;
/* Component-Reventless Not a pure module */
