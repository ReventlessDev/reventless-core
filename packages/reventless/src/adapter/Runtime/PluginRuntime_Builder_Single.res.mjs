// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Component$Reventless from "../../components/Component.res.mjs";
import * as Heartbeat$Reventless from "../../components/Heartbeat/Heartbeat.res.mjs";
import * as ComponentType$Reventless from "../../ComponentType.res.mjs";
import * as EventCollector$Reventless from "../../components/EventCollector/EventCollector.res.mjs";

function Make(RuntimeEnvironment) {
  return EventCollectorChannel => {
    let forPluginEventCollector = (handler, eventTopics, resources, memorySizeOpt, timeoutOpt, eventCollector) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$Reventless.toPulumiResource(eventCollector);
      let name = ComponentType$Reventless.nameOpt(resource.__name, EventCollector$Reventless.componentType);
      let opts_parent = resource;
      let opts = {
        parent: opts_parent
      };
      let runtime = RuntimeEnvironment.make(name, handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, {
        parent: resource
      });
      EventCollectorChannel.connect(name, [{
          channel: eventCollector.channel,
          eventTopics: eventTopics,
          resources: resources
        }], runtime, opts);
    };
    let forPluginHeartbeat = (handler, connect, memorySizeOpt, timeoutOpt, heartbeat) => {
      let memorySize = memorySizeOpt !== undefined ? memorySizeOpt : 1024;
      let timeout = timeoutOpt !== undefined ? timeoutOpt : 30;
      let resource = Component$Reventless.toPulumiResource(heartbeat);
      return connect(RuntimeEnvironment.make(ComponentType$Reventless.nameOpt(resource.__name, Heartbeat$Reventless.componentType), handler.apply(handler => RuntimeEnvironment.asEventHandler(handler)), memorySize, timeout, {
        parent: resource
      }));
    };
    let finish = () => {};
    return {
      EventCollectorChannel: EventCollectorChannel,
      forPluginEventCollector: forPluginEventCollector,
      forPluginHeartbeat: forPluginHeartbeat,
      finish: finish
    };
  };
}

export {
  Make,
}
/* Component-Reventless Not a pure module */
