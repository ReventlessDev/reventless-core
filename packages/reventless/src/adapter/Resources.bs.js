// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("bs-platform/lib/js/curry.js");
var Js_exn = require("bs-platform/lib/js/js_exn.js");
var Js_dict = require("bs-platform/lib/js/js_dict.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Caml_option = require("bs-platform/lib/js/caml_option.js");

function createResourceName(adapter, name) {
  return name + ("." + adapter);
}

function get(adapter, name, resources) {
  return Js_dict.get(resources, createResourceName(adapter, name));
}

function getExn(adapter, name, resources) {
  var param = (function (param) {
        var param$1 = param;
        var param$2 = resources;
        return get(adapter, param$1, param$2);
      })(name);
  if (param !== undefined) {
    return Caml_option.valFromOption(param);
  } else {
    var resources$1 = Object.keys(resources);
    return Js_exn.raiseError("Resource doesn\'t exist: " + (String(adapter) + (" " + (String(name) + (", resources: " + (String(resources$1) + ""))))));
  }
}

function set(adapter, name, resource, resources) {
  var param = (function (param) {
        var param$1 = param;
        var param$2 = resources;
        return get(adapter, param$1, param$2);
      })(name);
  if (param !== undefined) {
    var existing = JSON.stringify(Caml_option.valFromOption(param));
    var new_ = JSON.stringify(resource);
    return Js_exn.raiseError("Resource exists already: " + (String(adapter) + (" " + (String(name) + (", existing resource: " + (String(existing === undefined ? undefined : Caml_option.some(existing)) + (", new resource: " + (String(new_ === undefined ? undefined : Caml_option.some(new_)) + ""))))))));
  } else {
    resources[name + ("." + adapter)] = resource;
    return /* () */0;
  }
}

function filter(name, adapter, keep, resources) {
  return Belt_Array.keepMap(Js_dict.entries(resources), (function (param) {
                var match = param[0].endsWith(createResourceName(adapter, name)) && Curry._1(keep, name);
                if (match) {
                  return Caml_option.some(param[1]);
                }
                
              }));
}

exports.set = set;
exports.get = get;
exports.getExn = getExn;
exports.filter = filter;
/* No side effect */
