// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Message$Reventless from "./Message.res.mjs";
import * as MapperNto1$Reventless from "./MapperNto1.res.mjs";
import * as Projection$Reventless from "./Projection.res.mjs";
import * as Projection_Spec$ReventlessSpec from "@reventlessdev/reventless-spec/src/Projection_Spec.res.mjs";

function MakeGenericTargetFromStateTarget(StateTarget) {
  let decode = json => Message$Reventless.decode(json, StateTarget.stateSchema);
  let encode = value => Message$Reventless.encode(value, StateTarget.stateSchema);
  return {
    name: StateTarget.name,
    decode: decode,
    encode: encode
  };
}

function Make(Target) {
  return Mappings => {
    let decode = json => Message$Reventless.decode(json, Target.stateSchema);
    let encode = value => Message$Reventless.encode(value, Target.stateSchema);
    let GenericTarget_name = Target.name;
    let GenericTarget = {
      name: GenericTarget_name,
      decode: decode,
      encode: encode
    };
    let mappings = Mappings.mappings.map(M => {
      let Source = Projection$Reventless.Mapping.MakeGenericSource(M);
      let map = MapperNto1$Reventless.makeGenericMap(Source.decode$p, M.map);
      return {
        sourceName: M.sourceName,
        map: map
      };
    });
    return MapperNto1$Reventless.Mapper(Projection_Spec$ReventlessSpec)(GenericTarget)({
      mappings: mappings
    });
  };
}

let Spec;

export {
  Spec,
  MakeGenericTargetFromStateTarget,
  Make,
}
/* Message-Reventless Not a pure module */
