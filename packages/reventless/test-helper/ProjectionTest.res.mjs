// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as Belt_Array from "@rescript/runtime/lib/es6/Belt_Array.js";
import * as Belt_Option from "@rescript/runtime/lib/es6/Belt_Option.js";
import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as Primitive_string from "@rescript/runtime/lib/es6/Primitive_string.js";
import * as Projection$Reventless from "../src/Projection.res.mjs";
import * as TestFixtures$Reventless from "./TestFixtures.res.mjs";

function unpackPlainPartial(p) {
  return p.VAL;
}

function Make(Projection) {
  let testId = {
    contents: TestFixtures$Reventless.id
  };
  let meta = {
    contents: TestFixtures$Reventless.meta
  };
  let describeWithId = (description, id, fn) => {
    testId.contents = id;
    Jest.describe(description, fn);
  };
  let getSubId = state => Belt_Option.map(Projection.subIdConfig, param => param.getSubId(state));
  let hasSubId = (state, subId) => Belt_Option.getExn(getSubId(state)) === subId;
  let states = (store, id) => Belt_Option.getWithDefault(store[id], []);
  let setStates = (store, id, states) => {
    store[id] = states;
  };
  let updateState = (store, id, subId, newState) => Belt_Array.map(states(store, id), state => {
    if (hasSubId(state, subId)) {
      return newState;
    } else {
      return state;
    }
  });
  let deleteStates = (store, id) => {
    store[id] = [];
  };
  let deleteSubState = (store, id, subId, getSubId) => {
    store[id] = Stdlib_Option.getOr(Stdlib_Option.map(store[id], states => states.filter(state => Primitive_object.notequal(getSubId(state), subId))), []);
  };
  let update = async (store, events$p) => {
    let actions = events$p.map(event$p => Projection.map(event$p));
    await Projection$Reventless.handleActions(actions, {
      load: extra => Promise.resolve({
        TAG: "Ok",
        _0: states(store, extra)
      }),
      save: (extra, extra$1, extra$2, extra$3) => {
        let match = states(store, extra);
        let exit = 0;
        switch (extra$2) {
          case "Init" :
          case "Overwrite" :
            exit = 2;
            break;
          case "Any" :
            break;
        }
        if (exit === 2) {
          let len = match.length;
          if (len !== 1) {
            if (len !== 0) {
              return Promise.resolve({
                TAG: "Error",
                _0: "StaleState"
              });
            }
            if (extra$2 !== "Init") {
              return Promise.resolve({
                TAG: "Error",
                _0: "StaleState"
              });
            }
          } else if (extra$2 === "Init") {
            return Promise.resolve({
              TAG: "Error",
              _0: "StaleState"
            });
          }
        }
        setStates(store, extra, [extra$1]);
        return Promise.resolve({
          TAG: "Ok",
          _0: undefined
        });
      },
      saveBatch: extra => {
        extra.forEach(param => {
          let id = param[0];
          let newState = param[1];
          let subId = getSubId(newState);
          let match = subId !== undefined ? (
              Belt_Array.some(states(store, id), state => hasSubId(state, subId)) ? [
                  updateState(store, id, subId, newState),
                  []
                ] : [
                  states(store, id),
                  [newState]
                ]
            ) : [
              states(store, id),
              [newState]
            ];
          store[id] = match[0].concat(match[1]);
        });
        return Promise.resolve({
          TAG: "Ok",
          _0: undefined
        });
      },
      count: async (param, param$1, param$2) => ({
        TAG: "Ok",
        _0: 0
      }),
      delete: (extra, extra$1) => {
        let match = Projection.subIdConfig;
        if (extra$1 !== undefined) {
          if (match !== undefined) {
            deleteSubState(store, extra, extra$1[1], match.getSubId);
            return Promise.resolve({
              TAG: "Ok",
              _0: undefined
            });
          } else {
            return Promise.resolve({
              TAG: "Ok",
              _0: undefined
            });
          }
        } else {
          deleteStates(store, extra);
          return Promise.resolve({
            TAG: "Ok",
            _0: undefined
          });
        }
      },
      deleteBatch: extra => {
        extra.forEach(param => {
          let subId = param[1];
          let id = param[0];
          let match = Projection.subIdConfig;
          if (subId !== undefined) {
            if (match !== undefined) {
              return deleteSubState(store, id, subId[1], match.getSubId);
            } else {
              return;
            }
          } else {
            return deleteStates(store, id);
          }
        });
        return Promise.resolve({
          TAG: "Ok",
          _0: undefined
        });
      }
    }, Projection.subIdConfig);
    if (Projection.subIdConfig !== undefined) {
      return Stdlib_Dict.mapValues(store, states => states.toSorted((state1, state2) => Primitive_string.compare(getSubId(state1), getSubId(state2))));
    } else {
      return store;
    }
  };
  let givenEvents = events => update({}, events.map(event => ({
    id: testId.contents,
    meta: meta.contents,
    event: event
  })));
  let givenEventsWithTime = events => update({}, events.map(param => {
    let init = meta.contents;
    return {
      id: testId.contents,
      meta: {
        service: init.service,
        time: param[0],
        ip: init.ip,
        user: init.user,
        msgId: init.msgId,
        correlationId: init.correlationId
      },
      event: param[1]
    };
  }));
  let whenEvent = (store, event) => Jest.Expect.expect(async () => await update(await store, [{
      id: testId.contents,
      meta: meta.contents,
      event: event
    }]));
  let whenEvents = (store, events) => Jest.Expect.expect(async () => await update(await store, events.map(event => ({
    id: testId.contents,
    meta: meta.contents,
    event: event
  }))));
  let whenEventWithTime = (store, time, event) => Jest.Expect.expect(async () => {
    let init = meta.contents;
    return await update(await store, [{
        id: testId.contents,
        meta: {
          service: init.service,
          time: time,
          ip: init.ip,
          user: init.user,
          msgId: init.msgId,
          correlationId: init.correlationId
        },
        event: event
      }]);
  });
  let whenEventsWithTime = (store, events) => Jest.Expect.expect(async () => await update(await store, events.map(param => {
    let init = meta.contents;
    return {
      id: testId.contents,
      meta: {
        service: init.service,
        time: param[0],
        ip: init.ip,
        user: init.user,
        msgId: init.msgId,
        correlationId: init.correlationId
      },
      event: param[1]
    };
  })));
  let thenStates = async (p, expectedStates) => {
    let store = await p.VAL();
    return Jest.Expect.toEqual(Jest.Expect.expect([
      Object.keys(store).length,
      Object.keys(store)[0],
      Stdlib_Option.getOr(Object.values(store)[0], [])
    ]), [
      1,
      testId.contents,
      expectedStates
    ]);
  };
  let thenStatesWithId = async (p, id, expectedStates) => {
    let store = await p.VAL();
    return Jest.Expect.toEqual(Jest.Expect.expect([
      Object.keys(store).length,
      Object.keys(store)[0],
      Stdlib_Option.getOr(Object.values(store)[0], [])
    ]), [
      1,
      id,
      expectedStates
    ]);
  };
  let thenAllStates = async (p, expectedStore) => {
    let store = await p.VAL();
    return Jest.Expect.toEqual(Jest.Expect.expect(store), expectedStore);
  };
  let thenState = async (p, expectedState) => {
    let store = await p.VAL();
    return Jest.Expect.toEqual(Jest.Expect.expect([
      Object.keys(store).length,
      Object.keys(store)[0],
      Stdlib_Option.getOr(Object.values(store)[0], []).length,
      Object.values(store)[0][0]
    ]), [
      1,
      testId.contents,
      1,
      Primitive_option.some(expectedState)
    ]);
  };
  let thenStateWithId = async (p, id, expectedState) => {
    let store = await p.VAL();
    return Jest.Expect.toEqual(Jest.Expect.expect([
      Object.keys(store).length,
      Object.keys(store)[0],
      Stdlib_Option.getOr(Object.values(store)[0], []).length,
      Object.values(store)[0][0]
    ]), [
      1,
      id,
      1,
      Primitive_option.some(expectedState)
    ]);
  };
  let thenNoState = async p => {
    let store = await p.VAL();
    return Jest.Expect.toEqual(Jest.Expect.expect(Stdlib_Array.reduce(Object.values(store), 0, (acc, states) => acc + states.length | 0)), 0);
  };
  let thenThrow = async p => Jest.Expect.toThrow(p);
  let thenFail = async p => {
    let val;
    try {
      val = await p.VAL();
    } catch (exn) {
      return Jest.pass;
    }
    return Jest.fail("Expected Failure");
  };
  return {
    describe: Jest.describe,
    describeWithId: describeWithId,
    test: Jest.testPromise,
    givenEvents: givenEvents,
    givenEventsWithTime: givenEventsWithTime,
    whenEvent: whenEvent,
    whenEvents: whenEvents,
    whenEventWithTime: whenEventWithTime,
    whenEventsWithTime: whenEventsWithTime,
    thenStates: thenStates,
    thenStatesWithId: thenStatesWithId,
    thenAllStates: thenAllStates,
    thenState: thenState,
    thenStateWithId: thenStateWithId,
    thenNoState: thenNoState,
    thenThrow: thenThrow,
    thenFail: thenFail
  };
}

let handleActions = Projection$Reventless.handleActions;

export {
  unpackPlainPartial,
  handleActions,
  Make,
}
/* Jest Not a pure module */
