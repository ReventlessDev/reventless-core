// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Jest = require("@glennsl/rescript-jest/src/jest.bs.js");
var FTP$Reventless = require("../src/util/FTP.bs.js");

Jest.describe("FTP.SSH2 should", (function (param) {
        Jest.test("parse valid url, only url given", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            undefined,
                            ""
                          ]);
              }));
        Jest.test("parse valid url, url and relative path given", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net/some/path");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            undefined,
                            "some/path"
                          ]);
              }));
        Jest.test("parse valid url, url and absolute path given", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net//some/path");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            undefined,
                            "/some/path"
                          ]);
              }));
        Jest.test("parse valid url, url and port given", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net:8080");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            8080,
                            ""
                          ]);
              }));
        Jest.test("parse valid url, all parts given (relative path)", (function (param) {
                var t = FTP$Reventless.parseUrl("reventless.atos.net:8025/some/path");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "reventless.atos.net",
                            8025,
                            "some/path"
                          ]);
              }));
        Jest.test("parse valid url, all parts given (absolute path)", (function (param) {
                var t = FTP$Reventless.parseUrl("reventless.atos.net:8025//some/path");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "reventless.atos.net",
                            8025,
                            "/some/path"
                          ]);
              }));
        Jest.test("parse valid url, all parts given (relative path first)", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net/some/path:8080");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            8080,
                            "some/path"
                          ]);
              }));
        Jest.test("parse valid url, all parts given (absolute path first)", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net//some/path:8080");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            8080,
                            "/some/path"
                          ]);
              }));
        Jest.test("parse url with invalid port (relative path)", (function (param) {
                var t = FTP$Reventless.parseUrl("atos.net/some/path:invalidPort");
                return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                            "atos.net",
                            undefined,
                            "some/path"
                          ]);
              }));
        return Jest.test("parse url with invalid port (absolute path)", (function (param) {
                      var t = FTP$Reventless.parseUrl("atos.net//some/path:invalidPort");
                      return Jest.Expect.toEqual(Jest.Expect.expect(t), [
                                  "atos.net",
                                  undefined,
                                  "/some/path"
                                ]);
                    }));
      }));

/*  Not a pure module */
