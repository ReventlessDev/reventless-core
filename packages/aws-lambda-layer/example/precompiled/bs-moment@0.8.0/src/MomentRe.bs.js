// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Moment = require("moment");
var Caml_option = require("@rescript/std/lib/js/caml_option.js");

function asUnitOfTime(arg1, obj) {
  return obj.as(arg1);
}

var Duration = {
  asUnitOfTime: asUnitOfTime
};

function add(duration, moment) {
  var clone = moment.clone();
  clone.add(duration);
  return clone;
}

function subtract(duration, moment) {
  var clone = moment.clone();
  clone.subtract(duration);
  return clone;
}

function startOf(timeUnit, moment) {
  var clone = moment.clone();
  clone.startOf(timeUnit);
  return clone;
}

function endOf(timeUnit, moment) {
  var clone = moment.clone();
  clone.endOf(timeUnit);
  return clone;
}

function setMillisecond(millisecond, moment) {
  var clone = moment.clone();
  clone.millisecond(millisecond);
  return clone;
}

function setSecond(second, moment) {
  var clone = moment.clone();
  clone.second(second);
  return clone;
}

function setMinute(minute, moment) {
  var clone = moment.clone();
  clone.minute(minute);
  return clone;
}

function setHour(hour, moment) {
  var clone = moment.clone();
  clone.hour(hour);
  return clone;
}

function setDate(date, moment) {
  var clone = moment.clone();
  clone.date(date);
  return clone;
}

function setDay(day, moment) {
  var clone = moment.clone();
  clone.day(day);
  return clone;
}

function setWeekday(weekday, moment) {
  var clone = moment.clone();
  clone.weekday(weekday);
  return clone;
}

function setIsoWeekday(isoWeekday, moment) {
  var clone = moment.clone();
  clone.isoWeekday(isoWeekday);
  return clone;
}

function setDayOfYear(dayOfYear, moment) {
  var clone = moment.clone();
  clone.dayOfYear(dayOfYear);
  return clone;
}

function setWeek(week, moment) {
  var clone = moment.clone();
  clone.week(week);
  return clone;
}

function setIsoWeek(isoWeek, moment) {
  var clone = moment.clone();
  clone.isoWeek(isoWeek);
  return clone;
}

function setQuarter(quarter, moment) {
  var clone = moment.clone();
  clone.quarter(quarter);
  return clone;
}

function setWeekYear(weekYear, moment) {
  var clone = moment.clone();
  clone.weekYear(weekYear);
  return clone;
}

function setIsoWeekYear(isoWeekYear, moment) {
  var clone = moment.clone();
  clone.weekYear(isoWeekYear);
  return clone;
}

function setMonth(month, moment) {
  var clone = moment.clone();
  clone.month(month);
  return clone;
}

function setYear(year, moment) {
  var clone = moment.clone();
  clone.year(year);
  return clone;
}

function get(arg1, obj) {
  return obj.get(arg1);
}

function format(arg1, obj) {
  return obj.format(arg1);
}

function utc(arg1, obj) {
  return obj.utc(arg1);
}

function locale(arg1, obj) {
  return obj.locale(arg1);
}

function toJSON(moment) {
  return Caml_option.null_to_opt(moment.toJSON());
}

function toISOString(keepOffset, obj) {
  return obj.toISOString(keepOffset !== undefined ? Caml_option.valFromOption(keepOffset) : undefined);
}

var Moment$1 = {
  add: add,
  subtract: subtract,
  startOf: startOf,
  endOf: endOf,
  setMillisecond: setMillisecond,
  setSecond: setSecond,
  setMinute: setMinute,
  setHour: setHour,
  setDate: setDate,
  setDay: setDay,
  setWeekday: setWeekday,
  setIsoWeekday: setIsoWeekday,
  setDayOfYear: setDayOfYear,
  setWeek: setWeek,
  setIsoWeek: setIsoWeek,
  setQuarter: setQuarter,
  setWeekYear: setWeekYear,
  setIsoWeekYear: setIsoWeekYear,
  setMonth: setMonth,
  setYear: setYear,
  get: get,
  format: format,
  utc: utc,
  locale: locale,
  toJSON: toJSON,
  toISOString: toISOString
};

function momentWithUnix(timestamp) {
  return Moment(timestamp * 1000.0);
}

function momentUtc(format, value) {
  if (format !== undefined) {
    return Moment.utc(value, format);
  } else {
    return Moment.utc(value);
  }
}

function moment(format, value) {
  if (format !== undefined) {
    return Moment(value, format);
  } else {
    return Moment(value);
  }
}

exports.Duration = Duration;
exports.Moment = Moment$1;
exports.momentWithUnix = momentWithUnix;
exports.momentUtc = momentUtc;
exports.moment = moment;
/* moment Not a pure module */
