// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var FastCSV = require("../src/FastCSV.res.js");
var Js_dict = require("@rescript/std/lib/js/js_dict.js");
var FastCsv = require("fast-csv");

FastCsv.parseFile("x.csv", {
                delimiter: ";",
                headers: true
              }).transform(function (row, cb) {
              Js_dict.unsafeDeleteKey(row, "");
              return FastCSV.toValidTransformation(cb, row);
            }).on("data", (function (row) {
              console.log("parsed line:", row);
            })).on("data-invalid", (function (row, rowNumber, reason) {
            if (reason !== undefined) {
              console.log("line (" + String(rowNumber) + ") is invalid: " + reason, row);
            } else {
              console.log("line (" + String(rowNumber) + ") has too many/view records:", row);
            }
          })).on("error", (function (err) {
          console.log("error during parsing:", err.message);
        })).on("end", (function (rowCount) {
        console.log(String(rowCount) + " rows parsed");
      }));

/*  Not a pure module */
