// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";

describe("Duration — constructors", () => {
  test("millis returns a truthy Duration.t", () => {
    let d = Effect.Duration.millis(100);
    expect(d).toBeTruthy();
  });
  test("seconds returns a truthy Duration.t", () => {
    expect(Effect.Duration.seconds(1)).toBeTruthy();
  });
  test("minutes returns a truthy Duration.t", () => {
    expect(Effect.Duration.minutes(1)).toBeTruthy();
  });
  test("hours returns a truthy Duration.t", () => {
    expect(Effect.Duration.hours(1)).toBeTruthy();
  });
  test("days returns a truthy Duration.t", () => {
    expect(Effect.Duration.days(1)).toBeTruthy();
  });
  test("millis Duration can be used with Effect.sleep via TestClock", async () => {
    await Effect.Effect.runPromise(Effect.Effect.provide(Effect.Effect.flatMap(Effect.Effect.fork(Effect.Effect.sleep(Effect.Duration.millis(500))), fiber => Effect.Effect.zipRight(Effect.TestClock.adjust(Effect.Duration.millis(500)), Effect.Fiber.join(fiber))), Effect.TestContext.TestContext));
    expect(true).toBe(true);
  });
});

/*  Not a pure module */
