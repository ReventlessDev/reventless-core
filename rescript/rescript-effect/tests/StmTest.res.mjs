// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";

describe("Stm.TRef", () => {
  test("make + get returns the initial value", async () => {
    let tref = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.make(10)));
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.get(tref)));
    expect(v).toBe(10);
  });
  test("set + get reflects the new value", async () => {
    let tref = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.make(0)));
    Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.set(tref, 99)));
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.get(tref)));
    expect(v).toBe(99);
  });
  test("update applies a function to the value", async () => {
    let tref = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.make(5)));
    Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.update(tref, n => (n << 1))));
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.get(tref)));
    expect(v).toBe(10);
  });
  test("getAndUpdate returns the old value", async () => {
    let tref = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.make(3)));
    let old = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.getAndUpdate(tref, n => n + 1 | 0)));
    let new_ = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.get(tref)));
    expect(old).toBe(3);
    expect(new_).toBe(4);
  });
  test("modify returns computed result and updates the ref", async () => {
    let tref = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.make(7)));
    let result = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.modify(tref, n => [
      n * 10 | 0,
      n + 1 | 0
    ])));
    let new_ = Effect.Effect.runSync(Effect.STM.commit(Effect.TRef.get(tref)));
    expect(result).toBe(70);
    expect(new_).toBe(8);
  });
});

describe("Stm — STM operations", () => {
  test("succeed + commit produces the value", () => {
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.STM.succeed(42)));
    expect(v).toBe(42);
  });
  test("map transforms the value", () => {
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.STM.map(Effect.STM.succeed(3), n => (n << 1))));
    expect(v).toBe(6);
  });
  test("flatMap chains transactions", () => {
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.STM.flatMap(Effect.STM.succeed(4), n => Effect.STM.succeed(n + 1 | 0))));
    expect(v).toBe(5);
  });
  test("zipRight returns the second value", () => {
    let v = Effect.Effect.runSync(Effect.STM.commit(Effect.STM.zipRight(Effect.STM.succeed("a"), Effect.STM.succeed("b"))));
    expect(v).toBe("b");
  });
  test("fail + commit + runSyncExit is a failure", () => {
    let exit = Effect.Effect.runSyncExit(Effect.STM.commit(Effect.STM.fail("oops")));
    expect(Effect.Exit.isFailure(exit)).toBe(true);
  });
});

/*  Not a pure module */
