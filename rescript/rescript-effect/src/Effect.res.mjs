// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";
import * as EffectOption from "./EffectOption.res.mjs";

function tryPromise(onError, f) {
  return Effect.Effect.tryPromise({
    try: f,
    catch: onError
  });
}

function trySync(onError, f) {
  return Effect.Effect.try({
    try: f,
    catch: onError
  });
}

function option(effect) {
  return Effect.Effect.map(Effect.Effect.option(effect), EffectOption.toOption);
}

export {
  tryPromise,
  trySync,
  option,
}
/* effect Not a pure module */
