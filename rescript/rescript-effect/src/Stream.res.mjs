// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Chunk from "effect/Chunk";
import * as Effect from "effect/Effect";
import * as Option from "effect/Option";
import * as Stream from "effect/Stream";

function paginateEffect(initial, f) {
  return Stream.paginateChunkEffect(initial, s => Effect.map(f(s), param => [
    Chunk.fromIterable(param[0]),
    Option.fromNullable(param[1])
  ]));
}

function grouped(stream, n) {
  return Stream.map(Stream.grouped(stream, n), prim => Array.from(prim));
}

function runCollect(stream) {
  return Effect.map(Stream.runCollect(stream), prim => Array.from(prim));
}

function runHead(stream) {
  return Effect.map(Stream.runHead(stream), prim => Option.getOrUndefined(prim));
}

export {
  paginateEffect,
  grouped,
  runCollect,
  runHead,
}
/* effect/Chunk Not a pure module */
