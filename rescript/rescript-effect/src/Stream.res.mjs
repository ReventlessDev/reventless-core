// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Effect from "effect";

function paginateEffect(initial, f) {
  return Effect.Stream.paginateChunkEffect(initial, s => Effect.Effect.map(f(s), param => [
    Effect.Chunk.fromIterable(param[0]),
    Effect.Option.fromNullable(param[1])
  ]));
}

function runCollect(stream) {
  return Effect.Effect.map(Effect.Stream.runCollect(stream), prim => Array.from(prim));
}

function runHead(stream) {
  return Effect.Effect.map(Effect.Stream.runHead(stream), prim => Effect.Option.getOrUndefined(prim));
}

export {
  paginateEffect,
  runCollect,
  runHead,
}
/* effect Not a pure module */
