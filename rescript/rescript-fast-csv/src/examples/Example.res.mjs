// Generated by ReScript, PLEASE EDIT WITH CARE

import * as FastCSV from "../FastCSV.res.mjs";
import * as FastCsv from "fast-csv";
import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";

FastCsv.parseFile("x.csv", {
  delimiter: ";",
  headers: true
}).transform((row, cb) => {
  Stdlib_Dict.$$delete(row, "");
  return FastCSV.toValidTransformation(cb, row);
}).on("data", row => {
  console.log("parsed line:", row);
}).on("data-invalid", (row, rowNumber, reason) => {
  if (reason !== undefined) {
    console.log(`line (` + rowNumber.toString() + `) is invalid: ` + reason, row);
  } else {
    console.log(`line (` + rowNumber.toString() + `) has too many/view records:`, row);
  }
}).on("error", err => {
  console.log("error during parsing:", Stdlib_JsExn.message(err));
}).on("end", rowCount => {
  console.log(rowCount.toString() + ` rows parsed`);
});

/*  Not a pure module */
