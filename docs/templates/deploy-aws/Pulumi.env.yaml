config:
  aws:region: {{AWS_REGION}}
  # For plugin stacks, add platform stack reference:
  # platform:stack: {{ORG}}/{{PLATFORM_STACK_NAME}}/{{ENV}}
  # For cross-plugin dependencies:
  # interstack:
  #   dependencies:
  #     - {{ORG}}/{{DEPENDENCY_STACK}}/{{ENV}}
