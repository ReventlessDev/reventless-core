packages:
  - 'packages/*'
  - 'rescript/*'
  - 'reventless/*'
  - 'examples/**'
  # Codegen golden-output fixtures — buildable workspace members, paired with
  # the input fixtures at reventless-codegen/tests/fixtures/.
  - 'reventless/reventless-codegen/tests/golden/*/*'

# pnpm 10 blocks postinstall scripts by default; allowlist the ones we need.
# Keep this list in sync with the set of deps that ship native binaries or
# install-time codegen.
onlyBuiltDependencies:
  - '@pulumi/aws-native'
  - core-js
  - core-js-pure
  - cpu-features
  - esbuild
  - nx
  - protobufjs
  - rescript-relay
  - ssh2
  - sury-ppx
