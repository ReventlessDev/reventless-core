{
  "name": "reventless-monorepo",
  "private": true,
  "description": "Reventless Core - Event-sourced CQRS framework for serverless infrastructure",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/ReventlessDev/reventless-core.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "workspaces": [
    "packages/*",
    "rescript/*",
    "reventless/*",
    "examples/**"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "conventional-changelog-reventless": "file:./packages/conventional-changelog-reventless",
    "lerna": "^8.1.8",
    "rescript": "^12.1.0"
  },
  "scripts": {
    "build": "rescript build",
    "watch": "rescript watch",
    "clean": "rescript clean",
    "format": "rescript format",
    "test": "NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --silent",
    "test:watch": "NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --watchAll --silent",
    "test:watch:fail": "ONLY_FAILURES=1 NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --watchAll --silent",
    "dev": "concurrently --names rs,jest --prefix-colors cyan,yellow \"rescript watch\" \"NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --watchAll --silent\"",
    "dev:fail": "concurrently --names rs,jest --prefix-colors cyan,yellow \"rescript watch\" \"ONLY_FAILURES=1 NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --watchAll --silent\"",
    "test:integration": "echo 'No integration tests configured' || exit 0",
    "test:unit": "npm run test --workspaces --if-present",
    "test:coverage": "echo 'No coverage script configured' || exit 0"
  },
  "overrides": {
    "@docusaurus/theme-common": "3.9.2"
  },
  "version": ""
}
