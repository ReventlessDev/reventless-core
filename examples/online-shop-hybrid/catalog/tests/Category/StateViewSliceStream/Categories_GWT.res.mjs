// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Categories$CatalogPlugin from "../../../src/Category/StateViewSliceStream/Categories.res.mjs";
import * as Projection_GWT$ReventlessGwt from "@reventlessdev/reventless-gwt/src/Projection_GWT.res.mjs";
import * as Categories_Projection$CatalogPlugin from "../../../src/Category/StateViewSliceStream/Categories_Projection.res.mjs";

let include = Projection_GWT$ReventlessGwt.Make({
  name: Categories$CatalogPlugin.name,
  stateSchema: Categories$CatalogPlugin.stateSchema,
  consumedEventSchema: Categories$CatalogPlugin.consumedEventSchema,
  subIdConfig: undefined
})({
  project: Categories_Projection$CatalogPlugin.project
});

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenStateWithId = include.thenStateWithId;

describe("Categories StateViewSliceStream", () => {
  test("CategoryAdded creates a row", undefined, () => thenStateWithId(whenEvent(givenEvents([]), {
    TAG: "CategoryAdded",
    categoryId: "c1",
    name: "Electronics"
  }), "c1", {
    categoryId: "c1",
    name: "Electronics",
    archived: false
  }));
  test("CategoryRenamed updates the name", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "CategoryAdded",
      categoryId: "c1",
      name: "Electronics"
    }]), {
    TAG: "CategoryRenamed",
    categoryId: "c1",
    name: "Consumer Electronics"
  }), "c1", {
    categoryId: "c1",
    name: "Consumer Electronics",
    archived: false
  }));
  test("CategoryArchived sets archived flag", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "CategoryAdded",
      categoryId: "c1",
      name: "Electronics"
    }]), {
    TAG: "CategoryArchived",
    categoryId: "c1"
  }), "c1", {
    categoryId: "c1",
    name: "Electronics",
    archived: true
  }));
});

let Spec = include.Spec;

let describeWithId = include.describeWithId;

let todo = include.todo;

let whenEvents = include.whenEvents;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenState = include.thenState;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  Spec,
  describe,
  describeWithId,
  todo,
  test,
  givenEvents,
  whenEvent,
  whenEvents,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
