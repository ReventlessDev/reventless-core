// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

function evolve(state, event) {
  if (typeof event !== "object") {
    if (event === "CategoryArchived") {
      return {
        exists: state.exists,
        archived: true,
        currentImageUrl: state.currentImageUrl
      };
    } else {
      return {
        exists: state.exists,
        archived: false,
        currentImageUrl: state.currentImageUrl
      };
    }
  } else if (event.TAG === "CategoryAdded") {
    return {
      exists: true,
      archived: false,
      currentImageUrl: event.categoryImage
    };
  } else {
    return {
      exists: state.exists,
      archived: state.archived,
      currentImageUrl: event.categoryImage
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "CategoryNotFound"
    };
  }
  if (state.archived) {
    return {
      TAG: "Error",
      _0: "CategoryAlreadyArchived"
    };
  }
  let categoryImage = command.categoryImage;
  if (Primitive_object.equal(categoryImage, state.currentImageUrl)) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "CategoryImageChanged",
          categoryId: command.categoryId,
          categoryImage: categoryImage
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  archived: false,
  currentImageUrl: undefined
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/StateChangeSlice/ChangeCategoryImage_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
