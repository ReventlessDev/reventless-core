// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

let initialState_attached = [];

let initialState_altTexts = [];

let initialState = {
  exists: false,
  archived: false,
  attached: initialState_attached,
  primary: undefined,
  altTexts: initialState_altTexts
};

function evolve(state, event) {
  if (typeof event !== "object") {
    switch (event) {
      case "CategoryAdded" :
        return {
          exists: true,
          archived: false,
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
      case "CategoryArchived" :
        return {
          exists: state.exists,
          archived: true,
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
      case "CategoryUnarchived" :
        return {
          exists: state.exists,
          archived: false,
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
    }
  } else {
    switch (event.TAG) {
      case "CategoryImageAttached" :
        let categoryImage = event.categoryImage;
        if (state.attached.includes(categoryImage)) {
          return state;
        } else {
          return {
            exists: state.exists,
            archived: state.archived,
            attached: state.attached.concat([categoryImage]),
            primary: state.primary,
            altTexts: state.altTexts
          };
        }
      case "CategoryImageRemoved" :
        let categoryImage$1 = event.categoryImage;
        return {
          exists: state.exists,
          archived: state.archived,
          attached: state.attached.filter(r => r !== categoryImage$1),
          primary: Primitive_object.equal(state.primary, categoryImage$1) ? undefined : state.primary,
          altTexts: state.altTexts.filter(param => param[0] !== categoryImage$1)
        };
      case "CategoryPrimaryImageSet" :
        return {
          exists: state.exists,
          archived: state.archived,
          attached: state.attached,
          primary: event.categoryImage,
          altTexts: state.altTexts
        };
      case "CategoryImageAltTextSet" :
        let categoryImage$2 = event.categoryImage;
        return {
          exists: state.exists,
          archived: state.archived,
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts.filter(param => param[0] !== categoryImage$2).concat([[
              categoryImage$2,
              event.altText
            ]])
        };
    }
  }
}

function effectivePrimary(state) {
  let p = state.primary;
  if (p !== undefined) {
    return p;
  } else {
    return state.attached[0];
  }
}

function altTextOf(state, ref) {
  return Stdlib_Option.map(state.altTexts.find(param => param[0] === ref), param => param[1]);
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "CategoryNotFound"
    };
  }
  if (state.archived) {
    return {
      TAG: "Error",
      _0: "CategoryAlreadyArchived"
    };
  }
  switch (command.TAG) {
    case "AttachCategoryImage" :
      let categoryImage = command.categoryImage;
      if (state.attached.includes(categoryImage)) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "CategoryImageAttached",
              categoryId: command.categoryId,
              categoryImage: categoryImage,
              altText: command.altText
            }]
        };
      }
    case "RemoveCategoryImage" :
      let categoryImage$1 = command.categoryImage;
      if (state.attached.includes(categoryImage$1)) {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "CategoryImageRemoved",
              categoryId: command.categoryId,
              categoryImage: categoryImage$1
            }]
        };
      } else {
        return {
          TAG: "Ok",
          _0: []
        };
      }
    case "SetPrimaryCategoryImage" :
      let categoryImage$2 = command.categoryImage;
      if (state.attached.includes(categoryImage$2)) {
        if (Primitive_object.equal(effectivePrimary(state), categoryImage$2)) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "CategoryPrimaryImageSet",
                categoryId: command.categoryId,
                categoryImage: categoryImage$2
              }]
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: "CategoryImageNotAttached"
        };
      }
    case "SetCategoryImageAltText" :
      let categoryImage$3 = command.categoryImage;
      if (!state.attached.includes(categoryImage$3)) {
        return {
          TAG: "Error",
          _0: "CategoryImageNotAttached"
        };
      }
      let altText = command.altText;
      if (Primitive_object.equal(altTextOf(state, categoryImage$3), altText)) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "CategoryImageAltTextSet",
              categoryId: command.categoryId,
              categoryImage: categoryImage$3,
              altText: altText
            }]
        };
      }
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/StateChangeSlice/CategoryImages_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  effectivePrimary,
  altTextOf,
  decide,
  moduleUrl,
}
/* No side effect */
