// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event === "CategoryAdded") {
    return {
      exists: true,
      archived: false
    };
  } else {
    return {
      exists: state.exists,
      archived: true
    };
  }
}

function decide(state, command) {
  if (state.exists) {
    return {
      TAG: "Error",
      _0: "CategoryAlreadyExists"
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "CategoryAdded",
          categoryId: command.categoryId,
          name: command.name,
          imageUrl: command.imageUrl
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  archived: false
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/StateChangeSlice/AddCategory_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
