// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "CategoryAdded",
    name: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "CategoryRenamed",
    name: s.m(Sury.string)
  })),
  Sury.literal("CategoryArchived"),
  Sury.literal("CategoryUnarchived")
]);

let commandSchema = Sury.$schema(s => ({
  TAG: "RenameCategory",
  categoryId: s.m(DcbTag$Reventless.string),
  name: s.m(Sury.string)
}));

let errorSchema = Sury.union([
  Sury.literal("CategoryNotFound"),
  Sury.literal("CategoryAlreadyArchived")
]);

let eventSchema = Sury.$schema(s => ({
  TAG: "CategoryRenamed",
  categoryId: s.m(DcbTag$Reventless.string),
  name: s.m(Sury.string)
}));

let commandSchema$1 = Api$ReventlessInfra.markAllowedStates(commandSchema, [[
    "RenameCategory",
    ["Listed"]
  ]]);

function commandAuthorization(command) {
  return {
    TAG: "AllowGroups",
    _0: [
      "Admin",
      "Merchandiser"
    ]
  };
}

function commandTransition(param) {
  return "Unrestricted";
}

let name = "RenameCategory";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/StateChangeSlice/RenameCategory.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  errorSchema,
  eventSchema,
  commandSchema$1 as commandSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
}
/* consumedEventSchema Not a pure module */
