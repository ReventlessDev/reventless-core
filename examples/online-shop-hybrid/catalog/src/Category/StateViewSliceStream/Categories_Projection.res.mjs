// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

function heldRef(state) {
  return Stdlib_Option.map(state.categoryImage, held => held.ref);
}

function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "CategoryAdded" :
      let categoryId = event.categoryId;
      return [{
          TAG: "Set",
          _0: categoryId,
          _1: {
            categoryId: categoryId,
            name: event.name,
            shelfStatus: "Listed"
          }
        }];
    case "CategoryRenamed" :
      let name = event.name;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.name = name;
            return newrecord;
          }
        }];
    case "CategoryImageAttached" :
      let altText = event.altText;
      let categoryImage = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.categoryImage = {
              ref: categoryImage,
              altText: altText
            };
            return newrecord;
          }
        }];
    case "CategoryImageRemoved" :
      let categoryImage$1 = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            if (!Primitive_object.equal(heldRef(state), categoryImage$1)) {
              return state;
            }
            let newrecord = {...state};
            newrecord.categoryImage = undefined;
            return newrecord;
          }
        }];
    case "CategoryImageAltTextSet" :
      let altText$1 = event.altText;
      let categoryImage$2 = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let held = state.categoryImage;
            if (held === undefined) {
              return state;
            }
            if (held.ref !== categoryImage$2) {
              return state;
            }
            let newrecord = {...state};
            let newrecord$1 = {...held};
            newrecord.categoryImage = (newrecord$1.altText = altText$1, newrecord$1);
            return newrecord;
          }
        }];
    case "CategoryArchived" :
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Archived";
            return newrecord;
          }
        }];
    case "CategoryUnarchived" :
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Listed";
            return newrecord;
          }
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/StateViewSliceStream/Categories_Projection.res.mjs";

export {
  Spec,
  heldRef,
  project,
  moduleUrl,
}
/* No side effect */
