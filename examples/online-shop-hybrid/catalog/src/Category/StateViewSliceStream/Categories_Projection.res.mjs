// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";
import * as Attachments_Rules$TraitAttachments from "@reventlessdev/trait-attachments/src/Attachments_Rules.res.mjs";

function withPrimary(state, chosen) {
  let newrecord = {...state};
  newrecord.categoryImage = Attachments_Rules$TraitAttachments.primaryOf(chosen, state.categoryImages.map(a => a.categoryImage));
  return newrecord;
}

function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "CategoryAdded" :
      let categoryId = event.categoryId;
      return [{
          TAG: "Set",
          _0: categoryId,
          _1: {
            categoryId: categoryId,
            name: event.name,
            shelfStatus: "Listed",
            categoryImages: []
          }
        }];
    case "CategoryRenamed" :
      let name = event.name;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.name = name;
            return newrecord;
          }
        }];
    case "CategoryImageAttached" :
      let altText = event.altText;
      let categoryImage = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            if (state.categoryImages.some(a => a.categoryImage === categoryImage)) {
              return state;
            }
            let newrecord = {...state};
            return withPrimary((newrecord.categoryImages = state.categoryImages.concat([{
                categoryImage: categoryImage,
                altText: altText
              }]), newrecord), state.categoryImage);
          }
        }];
    case "CategoryImageRemoved" :
      let categoryImage$1 = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            return withPrimary((newrecord.categoryImages = state.categoryImages.filter(a => a.categoryImage !== categoryImage$1), newrecord), Primitive_object.equal(state.categoryImage, categoryImage$1) ? undefined : state.categoryImage);
          }
        }];
    case "CategoryPrimaryImageSet" :
      let categoryImage$2 = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.categoryImage = categoryImage$2;
            return newrecord;
          }
        }];
    case "CategoryImageAltTextSet" :
      let altText$1 = event.altText;
      let categoryImage$3 = event.categoryImage;
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.categoryImages = state.categoryImages.map(a => {
              if (a.categoryImage !== categoryImage$3) {
                return a;
              }
              let newrecord = {...a};
              newrecord.altText = altText$1;
              return newrecord;
            });
            return newrecord;
          }
        }];
    case "CategoryArchived" :
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Archived";
            return newrecord;
          }
        }];
    case "CategoryUnarchived" :
      return [{
          TAG: "Update",
          _0: event.categoryId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Listed";
            return newrecord;
          }
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/StateViewSliceStream/Categories_Projection.res.mjs";

export {
  Spec,
  withPrimary,
  project,
  moduleUrl,
}
/* No side effect */
