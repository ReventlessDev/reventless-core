// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

let stateSchema = S.union([
  S.literal("NotCreated"),
  S.schema(s => ({
    TAG: "Active",
    name: s.m(S.string)
  })),
  S.literal("Archived")
]);

function evolve(state, event) {
  if (typeof state !== "object") {
    if (state === "NotCreated" && typeof event === "object" && event.TAG === "Added") {
      return {
        TAG: "Active",
        name: event.name
      };
    } else {
      return state;
    }
  } else if (typeof event !== "object") {
    return "Archived";
  } else {
    return {
      TAG: "Active",
      name: event.name
    };
  }
}

function decide(state, command) {
  if (typeof state !== "object") {
    if (state === "NotCreated") {
      if (typeof command !== "object" || command.TAG !== "Add") {
        return {
          TAG: "Error",
          _0: "CategoryNotFound"
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "Added",
              name: command.name
            }]
        };
      }
    } else if (typeof command !== "object") {
      return {
        TAG: "Ok",
        _0: []
      };
    } else {
      return {
        TAG: "Error",
        _0: "CategoryAlreadyArchived"
      };
    }
  }
  if (typeof command !== "object") {
    return {
      TAG: "Ok",
      _0: ["Archived"]
    };
  }
  if (command.TAG === "Add") {
    return {
      TAG: "Error",
      _0: "CategoryAlreadyExists"
    };
  }
  let name = command.name;
  if (name === state.name) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "Renamed",
          name: name
        }]
    };
  }
}

let Spec;

let initialState = "NotCreated";

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Category/Aggregate/CategoryBehavior.res.mjs";

export {
  Spec,
  stateSchema,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* stateSchema Not a pure module */
