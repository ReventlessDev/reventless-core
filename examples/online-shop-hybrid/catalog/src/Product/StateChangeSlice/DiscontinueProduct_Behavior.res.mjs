// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  switch (event) {
    case "ProductAdded" :
      return {
        exists: true,
        shelf: "Listed"
      };
    case "ProductArchived" :
      return {
        exists: state.exists,
        shelf: "Archived"
      };
    case "ProductUnarchived" :
      return {
        exists: state.exists,
        shelf: "Listed"
      };
    case "ProductDiscontinued" :
      return {
        exists: state.exists,
        shelf: "Discontinued"
      };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  let theId = command.productId;
  let match = state.shelf;
  switch (match) {
    case "Listed" :
    case "Archived" :
      return {
        TAG: "Ok",
        _0: [{
            TAG: "ProductDiscontinued",
            productId: theId
          }]
      };
    case "Discontinued" :
      return {
        TAG: "Ok",
        _0: []
      };
  }
}

let Spec;

let initialState = {
  exists: false,
  shelf: "Listed"
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/DiscontinueProduct_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
