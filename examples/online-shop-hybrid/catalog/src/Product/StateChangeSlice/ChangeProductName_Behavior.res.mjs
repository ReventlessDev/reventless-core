// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (typeof event === "object") {
    if (event.TAG === "ProductAdded") {
      return {
        exists: true,
        shelf: "Listed",
        currentName: event.name
      };
    } else {
      return {
        exists: state.exists,
        shelf: state.shelf,
        currentName: event.name
      };
    }
  }
  switch (event) {
    case "ProductArchived" :
      return {
        exists: state.exists,
        shelf: "Archived",
        currentName: state.currentName
      };
    case "ProductUnarchived" :
      return {
        exists: state.exists,
        shelf: "Listed",
        currentName: state.currentName
      };
    case "ProductDiscontinued" :
      return {
        exists: state.exists,
        shelf: "Discontinued",
        currentName: state.currentName
      };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  if (state.shelf === "Discontinued") {
    return {
      TAG: "Error",
      _0: "ProductIsDiscontinued"
    };
  }
  let name = command.name;
  if (name === state.currentName) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductNameChanged",
          productId: command.productId,
          name: name
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  shelf: "Listed",
  currentName: ""
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/ChangeProductName_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
