// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

function evolve(state, event) {
  if (event.TAG === "ProductAdded") {
    return {
      exists: true,
      currentPrice: event.price
    };
  } else {
    return {
      exists: state.exists,
      currentPrice: event.price
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  let price = command.price;
  if (Primitive_object.equal(state.currentPrice, price)) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductPriceChanged",
          productId: command.productId,
          price: price
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  currentPrice: undefined
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/ChangeProductPrice_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
