// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

let initialState_liveCategoryIds = [];

let initialState_categoryNames = [];

let initialState = {
  exists: false,
  liveCategoryIds: initialState_liveCategoryIds,
  categoryNames: initialState_categoryNames
};

function naming(pairs, categoryId, name) {
  return pairs.filter(param => param[0] !== categoryId).concat([[
      categoryId,
      name
    ]]);
}

function evolve(state, event) {
  switch (event.TAG) {
    case "ProductAdded" :
      return {
        exists: true,
        liveCategoryIds: state.liveCategoryIds,
        categoryNames: state.categoryNames
      };
    case "CategoryAdded" :
      let categoryId = event.categoryId;
      return {
        exists: state.exists,
        liveCategoryIds: state.liveCategoryIds.includes(categoryId) ? state.liveCategoryIds : state.liveCategoryIds.concat([categoryId]),
        categoryNames: naming(state.categoryNames, categoryId, event.name)
      };
    case "CategoryRenamed" :
      return {
        exists: state.exists,
        liveCategoryIds: state.liveCategoryIds,
        categoryNames: naming(state.categoryNames, event.categoryId, event.name)
      };
    case "CategoryArchived" :
      let categoryId$1 = event.categoryId;
      return {
        exists: state.exists,
        liveCategoryIds: state.liveCategoryIds.filter(id => id !== categoryId$1),
        categoryNames: state.categoryNames
      };
  }
}

function decide(state, command) {
  if (state.exists) {
    return {
      TAG: "Error",
      _0: "ProductAlreadyExists"
    };
  }
  let categoryId = command.categoryId;
  if (!state.liveCategoryIds.includes(categoryId)) {
    return {
      TAG: "Error",
      _0: "CategoryNotFound"
    };
  }
  let categoryName = Stdlib_Option.map(state.categoryNames.find(param => param[0] === categoryId), param => param[1]);
  return {
    TAG: "Ok",
    _0: [{
        TAG: "ProductAdded",
        productId: command.productId,
        name: command.name,
        description: command.description,
        price: command.price,
        categoryId: categoryId,
        categoryName: categoryName
      }]
  };
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/AddProduct_Behavior.res.mjs";

export {
  Spec,
  initialState,
  naming,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
