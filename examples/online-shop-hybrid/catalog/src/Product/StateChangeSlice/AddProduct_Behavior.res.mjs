// Generated by ReScript, PLEASE EDIT WITH CARE


let initialState_addedProductIds = [];

let initialState_liveCategoryIds = [];

let initialState = {
  addedProductIds: initialState_addedProductIds,
  liveCategoryIds: initialState_liveCategoryIds
};

function evolve(state, event) {
  switch (event.TAG) {
    case "ProductAdded" :
      let productId = event.productId;
      return {
        addedProductIds: state.addedProductIds.includes(productId) ? state.addedProductIds : state.addedProductIds.concat([productId]),
        liveCategoryIds: state.liveCategoryIds
      };
    case "CategoryAdded" :
      let categoryId = event.categoryId;
      return {
        addedProductIds: state.addedProductIds,
        liveCategoryIds: state.liveCategoryIds.includes(categoryId) ? state.liveCategoryIds : state.liveCategoryIds.concat([categoryId])
      };
    case "CategoryArchived" :
      let categoryId$1 = event.categoryId;
      return {
        addedProductIds: state.addedProductIds,
        liveCategoryIds: state.liveCategoryIds.filter(id => id !== categoryId$1)
      };
  }
}

function decide(state, command) {
  let productId = command.productId;
  if (state.addedProductIds.includes(productId)) {
    return {
      TAG: "Error",
      _0: "ProductAlreadyExists"
    };
  }
  let categoryId = command.categoryId;
  if (state.liveCategoryIds.includes(categoryId)) {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductAdded",
          productId: productId,
          name: command.name,
          description: command.description,
          price: command.price,
          categoryId: categoryId
        }]
    };
  } else {
    return {
      TAG: "Error",
      _0: "CategoryNotFound"
    };
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/AddProduct_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
