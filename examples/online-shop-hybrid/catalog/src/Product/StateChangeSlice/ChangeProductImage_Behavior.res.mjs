// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event.TAG === "ProductAdded") {
    return {
      exists: true,
      currentImageUrl: event.imageUrl
    };
  } else {
    return {
      exists: state.exists,
      currentImageUrl: event.imageUrl
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  let imageUrl = command.imageUrl;
  if (imageUrl === state.currentImageUrl) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductImageChanged",
          productId: command.productId,
          imageUrl: imageUrl
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  currentImageUrl: ""
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/ChangeProductImage_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
