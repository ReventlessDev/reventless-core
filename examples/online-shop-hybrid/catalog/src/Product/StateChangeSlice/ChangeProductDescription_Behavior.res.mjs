// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (typeof event === "object") {
    if (event.TAG === "ProductAdded") {
      return {
        exists: true,
        shelf: "Listed",
        currentDescription: event.description
      };
    } else {
      return {
        exists: state.exists,
        shelf: state.shelf,
        currentDescription: event.description
      };
    }
  }
  switch (event) {
    case "ProductArchived" :
      return {
        exists: state.exists,
        shelf: "Archived",
        currentDescription: state.currentDescription
      };
    case "ProductUnarchived" :
      return {
        exists: state.exists,
        shelf: "Listed",
        currentDescription: state.currentDescription
      };
    case "ProductDiscontinued" :
      return {
        exists: state.exists,
        shelf: "Discontinued",
        currentDescription: state.currentDescription
      };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  if (state.shelf === "Discontinued") {
    return {
      TAG: "Error",
      _0: "ProductIsDiscontinued"
    };
  }
  let description = command.description;
  if (description === state.currentDescription) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductDescriptionChanged",
          productId: command.productId,
          description: description
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  shelf: "Listed",
  currentDescription: ""
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/ChangeProductDescription_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
