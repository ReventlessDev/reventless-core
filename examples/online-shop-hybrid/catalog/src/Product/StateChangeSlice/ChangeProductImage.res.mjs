// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as StorageRef$Reventless from "@reventlessdev/reventless-spec/src/semantic/StorageRef.res.mjs";

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    imageUrl: s.m(S.option(S.string))
  })),
  S.schema(s => ({
    TAG: "ProductImageChanged",
    imageUrl: s.m(S.string)
  }))
]);

let commandSchema = S.schema(s => ({
  TAG: "ChangeProductImage",
  productId: s.m(DcbTag$Reventless.string),
  imageUrl: s.m(StorageRef$Reventless.forStore(undefined, "productImages"))
}));

let errorSchema = S.literal("ProductNotFound");

let eventSchema = S.schema(s => ({
  TAG: "ProductImageChanged",
  productId: s.m(DcbTag$Reventless.string),
  imageUrl: s.m(StorageRef$Reventless.forStore(undefined, "productImages"))
}));

function commandAuthorization(command) {
  return {
    TAG: "AllowGroups",
    _0: ["Admin"]
  };
}

let name = "ChangeProductImage";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/ChangeProductImage.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
}
/* consumedEventSchema Not a pure module */
