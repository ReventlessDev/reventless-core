// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.literal("ProductAdded"),
  Sury.literal("ProductArchived"),
  Sury.literal("ProductUnarchived"),
  Sury.literal("ProductDiscontinued")
]);

let commandSchema = Sury.$schema(s => ({
  TAG: "DiscontinueProduct",
  productId: s.m(DcbTag$Reventless.string)
}));

let errorSchema = Sury.literal("ProductNotFound");

let eventSchema = Sury.$schema(s => ({
  TAG: "ProductDiscontinued",
  productId: s.m(DcbTag$Reventless.string)
}));

function commandTransition(command) {
  return {
    TAG: "Moves",
    _0: [
      "Listed",
      "Archived"
    ],
    _1: "Discontinued"
  };
}

function commandAuthorization(command) {
  return {
    TAG: "AllowGroups",
    _0: [
      "Admin",
      "Merchandiser"
    ]
  };
}

let traits = [];

let name = "DiscontinueProduct";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/DiscontinueProduct.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  commandTransition,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  traits,
}
/* consumedEventSchema Not a pure module */
