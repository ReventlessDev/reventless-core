// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

let initialState_attached = [];

let initialState_altTexts = [];

let initialState = {
  exists: false,
  shelf: "Listed",
  attached: initialState_attached,
  primary: undefined,
  altTexts: initialState_altTexts
};

function evolve(state, event) {
  if (typeof event !== "object") {
    switch (event) {
      case "ProductArchived" :
        return {
          exists: state.exists,
          shelf: "Archived",
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
      case "ProductUnarchived" :
        return {
          exists: state.exists,
          shelf: "Listed",
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
      case "ProductDiscontinued" :
        return {
          exists: state.exists,
          shelf: "Discontinued",
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
    }
  } else {
    switch (event.TAG) {
      case "ProductAdded" :
        return {
          exists: true,
          shelf: state.shelf,
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts
        };
      case "ProductImageAttached" :
        let productImage = event.productImage;
        if (state.attached.includes(productImage)) {
          return state;
        } else {
          return {
            exists: state.exists,
            shelf: state.shelf,
            attached: state.attached.concat([productImage]),
            primary: state.primary,
            altTexts: state.altTexts
          };
        }
      case "ProductImageRemoved" :
        let productImage$1 = event.productImage;
        return {
          exists: state.exists,
          shelf: state.shelf,
          attached: state.attached.filter(r => r !== productImage$1),
          primary: Primitive_object.equal(state.primary, productImage$1) ? undefined : state.primary,
          altTexts: state.altTexts.filter(param => param[0] !== productImage$1)
        };
      case "ProductPrimaryImageSet" :
        return {
          exists: state.exists,
          shelf: state.shelf,
          attached: state.attached,
          primary: event.productImage,
          altTexts: state.altTexts
        };
      case "ProductImageAltTextSet" :
        let productImage$2 = event.productImage;
        return {
          exists: state.exists,
          shelf: state.shelf,
          attached: state.attached,
          primary: state.primary,
          altTexts: state.altTexts.filter(param => param[0] !== productImage$2).concat([[
              productImage$2,
              event.altText
            ]])
        };
    }
  }
}

function effectivePrimary(state) {
  let p = state.primary;
  if (p !== undefined) {
    return p;
  } else {
    return state.attached[0];
  }
}

function altTextOf(state, ref) {
  return Stdlib_Option.map(state.altTexts.find(param => param[0] === ref), param => param[1]);
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  if (state.shelf === "Discontinued") {
    return {
      TAG: "Error",
      _0: "ProductIsDiscontinued"
    };
  }
  switch (command.TAG) {
    case "AttachProductImage" :
      let productImage = command.productImage;
      if (state.attached.includes(productImage)) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "ProductImageAttached",
              productId: command.productId,
              productImage: productImage,
              altText: command.altText
            }]
        };
      }
    case "RemoveProductImage" :
      let productImage$1 = command.productImage;
      if (state.attached.includes(productImage$1)) {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "ProductImageRemoved",
              productId: command.productId,
              productImage: productImage$1
            }]
        };
      } else {
        return {
          TAG: "Ok",
          _0: []
        };
      }
    case "SetPrimaryProductImage" :
      let productImage$2 = command.productImage;
      if (state.attached.includes(productImage$2)) {
        if (Primitive_object.equal(effectivePrimary(state), productImage$2)) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "ProductPrimaryImageSet",
                productId: command.productId,
                productImage: productImage$2
              }]
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: "ProductImageNotAttached"
        };
      }
    case "SetProductImageAltText" :
      let productImage$3 = command.productImage;
      if (!state.attached.includes(productImage$3)) {
        return {
          TAG: "Error",
          _0: "ProductImageNotAttached"
        };
      }
      let altText = command.altText;
      if (Primitive_object.equal(altTextOf(state, productImage$3), altText)) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "ProductImageAltTextSet",
              productId: command.productId,
              productImage: productImage$3,
              altText: altText
            }]
        };
      }
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/ProductImages_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  effectivePrimary,
  altTextOf,
  decide,
  moduleUrl,
}
/* No side effect */
