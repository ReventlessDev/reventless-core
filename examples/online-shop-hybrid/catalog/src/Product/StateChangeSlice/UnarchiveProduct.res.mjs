// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";

let consumedEventSchema = S.union([
  S.literal("ProductAdded"),
  S.literal("ProductArchived"),
  S.literal("ProductUnarchived"),
  S.literal("ProductDiscontinued")
]);

let commandSchema = S.schema(s => ({
  TAG: "UnarchiveProduct",
  productId: s.m(DcbTag$Reventless.string)
}));

let errorSchema = S.union([
  S.literal("ProductNotFound"),
  S.literal("ProductIsDiscontinued")
]);

let eventSchema = S.schema(s => ({
  TAG: "ProductUnarchived",
  productId: s.m(DcbTag$Reventless.string)
}));

let commandSchema$1 = Api$ReventlessInfra.markAllowedStates(commandSchema, [[
    "UnarchiveProduct",
    ["Archived"]
  ]]);

let commandSchema$2 = Api$ReventlessInfra.markTargetState(commandSchema$1, [[
    "UnarchiveProduct",
    "Listed"
  ]]);

function commandAuthorization(command) {
  return {
    TAG: "AllowGroups",
    _0: [
      "Admin",
      "Merchandiser"
    ]
  };
}

let name = "UnarchiveProduct";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateChangeSlice/UnarchiveProduct.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  errorSchema,
  eventSchema,
  commandSchema$2 as commandSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
}
/* consumedEventSchema Not a pure module */
