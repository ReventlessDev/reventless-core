// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Money$Reventless from "@reventlessdev/reventless-spec/src/semantic/Money.res.mjs";
import * as Currency$Reventless from "@reventlessdev/reventless-spec/src/semantic/Currency.res.mjs";

function translate(input) {
  let why = Currency$Reventless.fromString(input.currency);
  if (why.TAG === "Ok") {
    if (input.unitPrice <= 0) {
      return {
        TAG: "Error",
        _0: "Price must be positive"
      };
    } else if (input.sku === "") {
      return {
        TAG: "Error",
        _0: "SKU is required"
      };
    } else if (input.category === "") {
      return {
        TAG: "Error",
        _0: "Category is required"
      };
    } else {
      return {
        TAG: "Ok",
        _0: [[
            input.sku,
            {
              TAG: "AddProduct",
              productId: input.sku,
              name: input.title,
              description: input.desc,
              price: Money$Reventless.make(input.unitPrice, why._0),
              categoryId: input.category
            }
          ]]
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: why._0
    };
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/InboundTranslationSlice/ImportProduct_Translation.res.mjs";

export {
  Spec,
  translate,
  moduleUrl,
}
/* Money-Reventless Not a pure module */
