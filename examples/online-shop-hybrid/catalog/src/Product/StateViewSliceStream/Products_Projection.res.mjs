// Generated by ReScript, PLEASE EDIT WITH CARE


function project(event) {
  switch (event.TAG) {
    case "ProductAdded" :
      let productId = event.productId;
      return [{
          TAG: "Set",
          _0: productId,
          _1: {
            productId: productId,
            name: event.name,
            description: event.description,
            price: event.price,
            categoryId: event.categoryId,
            sku: undefined
          }
        }];
    case "ProductNameChanged" :
      let name = event.name;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: name,
            description: state.description,
            price: state.price,
            categoryId: state.categoryId,
            sku: state.sku
          })
        }];
    case "ProductDescriptionChanged" :
      let description = event.description;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: description,
            price: state.price,
            categoryId: state.categoryId,
            sku: state.sku
          })
        }];
    case "ProductPriceChanged" :
      let price = event.price;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: price,
            categoryId: state.categoryId,
            sku: state.sku
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateViewSliceStream/Products_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
