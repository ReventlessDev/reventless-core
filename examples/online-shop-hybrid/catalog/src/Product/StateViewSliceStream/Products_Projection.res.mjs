// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Attachments_Rules$TraitAttachments from "@reventlessdev/trait-attachments/src/Attachments_Rules.res.mjs";

function primaryFirst(state, chosen) {
  return {
    productId: state.productId,
    name: state.name,
    description: state.description,
    price: state.price,
    productImages: Attachments_Rules$TraitAttachments.primaryFirst(chosen, state.productImages, a => a.ref),
    categoryId: state.categoryId,
    shelfStatus: state.shelfStatus
  };
}

function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "ProductAdded" :
      let productId = event.productId;
      return [{
          TAG: "Set",
          _0: productId,
          _1: {
            productId: productId,
            name: event.name,
            description: event.description,
            price: event.price,
            productImages: [],
            categoryId: event.categoryId,
            shelfStatus: "Listed"
          }
        }];
    case "ProductPriceChanged" :
      let price = event.price;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: price,
            productImages: state.productImages,
            categoryId: state.categoryId,
            shelfStatus: state.shelfStatus
          })
        }];
    case "ProductNameChanged" :
      let name = event.name;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: name,
            description: state.description,
            price: state.price,
            productImages: state.productImages,
            categoryId: state.categoryId,
            shelfStatus: state.shelfStatus
          })
        }];
    case "ProductDescriptionChanged" :
      let description = event.description;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: description,
            price: state.price,
            productImages: state.productImages,
            categoryId: state.categoryId,
            shelfStatus: state.shelfStatus
          })
        }];
    case "ProductImageAttached" :
      let altText = event.altText;
      let productImage = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            if (state.productImages.some(a => a.ref === productImage)) {
              return state;
            } else {
              return {
                productId: state.productId,
                name: state.name,
                description: state.description,
                price: state.price,
                productImages: state.productImages.concat([{
                    ref: productImage,
                    altText: altText
                  }]),
                categoryId: state.categoryId,
                shelfStatus: state.shelfStatus
              };
            }
          }
        }];
    case "ProductImageRemoved" :
      let productImage$1 = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: state.price,
            productImages: state.productImages.filter(a => a.ref !== productImage$1),
            categoryId: state.categoryId,
            shelfStatus: state.shelfStatus
          })
        }];
    case "ProductPrimaryImageSet" :
      let productImage$2 = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => primaryFirst(state, productImage$2)
        }];
    case "ProductImageAltTextSet" :
      let altText$1 = event.altText;
      let productImage$3 = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: state.price,
            productImages: state.productImages.map(a => {
              if (a.ref !== productImage$3) {
                return a;
              }
              let newrecord = {...a};
              newrecord.altText = altText$1;
              return newrecord;
            }),
            categoryId: state.categoryId,
            shelfStatus: state.shelfStatus
          })
        }];
    case "ProductArchived" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: state.price,
            productImages: state.productImages,
            categoryId: state.categoryId,
            shelfStatus: "Archived"
          })
        }];
    case "ProductUnarchived" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: state.price,
            productImages: state.productImages,
            categoryId: state.categoryId,
            shelfStatus: "Listed"
          })
        }];
    case "ProductDiscontinued" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: state.price,
            productImages: state.productImages,
            categoryId: state.categoryId,
            shelfStatus: "Discontinued"
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateViewSliceStream/Products_Projection.res.mjs";

export {
  Spec,
  primaryFirst,
  project,
  moduleUrl,
}
/* Attachments_Rules-TraitAttachments Not a pure module */
