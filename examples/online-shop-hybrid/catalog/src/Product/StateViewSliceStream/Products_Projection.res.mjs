// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";
import * as Attachments_Rules$TraitAttachments from "@reventlessdev/trait-attachments/src/Attachments_Rules.res.mjs";

function withPrimary(state, chosen) {
  let match = Attachments_Rules$TraitAttachments.primaryWithAltText(chosen, state.productImages, a => a.productImage, a => a.altText);
  let newrecord = {...state};
  newrecord.productImageAltText = match[1];
  newrecord.productImage = match[0];
  return newrecord;
}

function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "ProductAdded" :
      let productId = event.productId;
      return [{
          TAG: "Set",
          _0: productId,
          _1: {
            productId: productId,
            name: event.name,
            description: event.description,
            price: event.price,
            productImages: [],
            categoryId: event.categoryId,
            shelfStatus: "Listed"
          }
        }];
    case "ProductPriceChanged" :
      let price = event.price;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            newrecord.price = price;
            return newrecord;
          }
        }];
    case "ProductNameChanged" :
      let name = event.name;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            newrecord.name = name;
            return newrecord;
          }
        }];
    case "ProductDescriptionChanged" :
      let description = event.description;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            newrecord.description = description;
            return newrecord;
          }
        }];
    case "ProductImageAttached" :
      let altText = event.altText;
      let productImage = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            if (state.productImages.some(a => a.productImage === productImage)) {
              return state;
            }
            let newrecord = {...state};
            return withPrimary((newrecord.productImages = state.productImages.concat([{
                productImage: productImage,
                altText: altText
              }]), newrecord), state.productImage);
          }
        }];
    case "ProductImageRemoved" :
      let productImage$1 = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            return withPrimary((newrecord.productImages = state.productImages.filter(a => a.productImage !== productImage$1), newrecord), Primitive_object.equal(state.productImage, productImage$1) ? undefined : state.productImage);
          }
        }];
    case "ProductPrimaryImageSet" :
      let productImage$2 = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => withPrimary(state, productImage$2)
        }];
    case "ProductImageAltTextSet" :
      let altText$1 = event.altText;
      let productImage$3 = event.productImage;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            return withPrimary((newrecord.productImages = state.productImages.map(a => {
              if (a.productImage !== productImage$3) {
                return a;
              }
              let newrecord = {...a};
              newrecord.altText = altText$1;
              return newrecord;
            }), newrecord), state.productImage);
          }
        }];
    case "ProductArchived" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Archived";
            return newrecord;
          }
        }];
    case "ProductUnarchived" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Listed";
            return newrecord;
          }
        }];
    case "ProductDiscontinued" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => {
            let newrecord = {...state};
            newrecord.shelfStatus = "Discontinued";
            return newrecord;
          }
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateViewSliceStream/Products_Projection.res.mjs";

export {
  Spec,
  withPrimary,
  project,
  moduleUrl,
}
/* Attachments_Rules-TraitAttachments Not a pure module */
