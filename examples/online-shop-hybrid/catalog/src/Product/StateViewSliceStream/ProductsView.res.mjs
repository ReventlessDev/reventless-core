// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    productId: s.m(S.string),
    name: s.m(S.string),
    description: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "ProductNameChanged",
    productId: s.m(S.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ProductDescriptionChanged",
    productId: s.m(S.string),
    description: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ProductPriceChanged",
    productId: s.m(S.string),
    price: s.m(S.float)
  }))
]);

let stateSchema = S.schema(s => ({
  productId: s.m(S.string),
  name: s.m(S.string),
  description: s.m(S.string),
  price: s.m(S.float)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "Products";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/Product/StateViewSliceStream/ProductsView.res.mjs";

export {
  name,
  Id,
  consumedEventSchema,
  stateSchema,
  config,
  subIdConfig,
  moduleUrl,
}
/* consumedEventSchema Not a pure module */
