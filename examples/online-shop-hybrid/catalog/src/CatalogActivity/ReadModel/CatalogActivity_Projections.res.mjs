// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Projection$Reventless from "@reventlessdev/reventless-spec/src/types/Projection.res.mjs";
import * as Category$CatalogPlugin from "../../Category/Aggregate/Category.res.mjs";
import * as CatalogActivity$CatalogPlugin from "./CatalogActivity.res.mjs";

let M = Projection$Reventless.Mappings.Make({
  Id: Id$Reventless.$$String,
  name: CatalogActivity$CatalogPlugin.name,
  stateSchema: CatalogActivity$CatalogPlugin.stateSchema,
  subIdConfig: undefined
});

let name = "CatalogDcbEventLog";

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    productId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string),
    description: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "ProductRenamed",
    productId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  }))
]);

let CatalogDcbSource = {
  Id: undefined,
  name: name,
  eventSchema: eventSchema
};

function project(param) {
  let event = param.event;
  let id = param.id;
  if (typeof event !== "object") {
    return {
      TAG: "Update",
      _0: id,
      _1: state => ({
        name: state.name,
        kind: state.kind,
        lastChange: "Archived"
      })
    };
  }
  if (event.TAG === "Added") {
    return {
      TAG: "Set",
      _0: id,
      _1: {
        name: event.name,
        kind: "Category",
        lastChange: "Added"
      }
    };
  }
  let name = event.name;
  return {
    TAG: "Update",
    _0: id,
    _1: state => ({
      name: name,
      kind: state.kind,
      lastChange: "Renamed"
    })
  };
}

let CategoryActivityMapping = Projection$Reventless.Mapping.Make({
  Id: Id$Reventless.$$String,
  name: Category$CatalogPlugin.name,
  eventSchema: Category$CatalogPlugin.eventSchema
})({
  Id: Id$Reventless.$$String,
  name: CatalogActivity$CatalogPlugin.name,
  stateSchema: CatalogActivity$CatalogPlugin.stateSchema,
  subIdConfig: undefined
})({
  project: project
});

function project$1(param) {
  let event = param.event;
  if (event.TAG === "ProductAdded") {
    return {
      TAG: "Set",
      _0: event.productId,
      _1: {
        name: event.name,
        kind: "Product",
        lastChange: "Added"
      }
    };
  }
  let name = event.name;
  return {
    TAG: "Update",
    _0: event.productId,
    _1: state => ({
      name: name,
      kind: state.kind,
      lastChange: "Renamed"
    })
  };
}

let ProductActivityMapping = Projection$Reventless.Mapping.Make({
  Id: Id$Reventless.$$String,
  name: name,
  eventSchema: eventSchema
})({
  Id: Id$Reventless.$$String,
  name: CatalogActivity$CatalogPlugin.name,
  stateSchema: CatalogActivity$CatalogPlugin.stateSchema,
  subIdConfig: undefined
})({
  project: project$1
});

let mappings = [
  CategoryActivityMapping,
  ProductActivityMapping
];

let Target;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/CatalogActivity/ReadModel/CatalogActivity_Projections.res.mjs";

export {
  Target,
  M,
  CatalogDcbSource,
  CategoryActivityMapping,
  ProductActivityMapping,
  mappings,
  moduleUrl,
}
/* M Not a pure module */
