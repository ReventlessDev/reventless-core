// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "ProductDemandRecorded",
    orderId: s.m(DcbTag$Reventless.string)
  })),
  Sury.$schema(s => ({
    TAG: "ProductDemandRevoked",
    orderId: s.m(DcbTag$Reventless.string)
  }))
]);

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "RecordDemand",
    productId: s.m(DcbTag$Reventless.partition),
    orderId: s.m(DcbTag$Reventless.string)
  })),
  Sury.$schema(s => ({
    TAG: "RevokeDemand",
    productId: s.m(DcbTag$Reventless.partition),
    orderId: s.m(DcbTag$Reventless.string)
  }))
]);

let errorSchema = Sury.$unit;

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "ProductDemandRecorded",
    productId: s.m(DcbTag$Reventless.partition),
    orderId: s.m(DcbTag$Reventless.string)
  })),
  Sury.$schema(s => ({
    TAG: "ProductDemandRevoked",
    productId: s.m(DcbTag$Reventless.partition),
    orderId: s.m(DcbTag$Reventless.string)
  }))
]);

let commandSchema$1 = Api$ReventlessInfra.markNoApi(commandSchema);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

let name = "RecordProductDemand";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-catalog/src/ProductDemand/StateChangeSlice/RecordProductDemand.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  errorSchema,
  eventSchema,
  commandSchema$1 as commandSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
}
/* consumedEventSchema Not a pure module */
