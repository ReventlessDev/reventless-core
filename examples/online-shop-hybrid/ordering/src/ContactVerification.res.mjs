// Generated by ReScript, PLEASE EDIT WITH CARE


function channelsFor(contact) {
  if (contact.TAG === "ByEmail") {
    return ["Email"];
  } else {
    return ["Sms"];
  }
}

function toRecipient(contact) {
  if (contact.TAG === "ByEmail") {
    return {
      TAG: "ToEmail",
      _0: contact._0
    };
  } else {
    return {
      TAG: "ToSms",
      _0: contact._0
    };
  }
}

let emailPolicy = {
  tokenTtl: 86400,
  maxAttempts: 5,
  resendCooldown: 60,
  proofLength: 32,
  proofAlphabet: "UrlSafe"
};

let smsPolicy = {
  tokenTtl: 600,
  maxAttempts: 3,
  resendCooldown: 60,
  proofLength: 6,
  proofAlphabet: "Digits"
};

function policyFor(channel) {
  switch (channel) {
    case "Email" :
      return emailPolicy;
    case "Sms" :
      return smsPolicy;
    case "Push" :
      return;
  }
}

export {
  channelsFor,
  toRecipient,
  emailPolicy,
  smsPolicy,
  policyFor,
}
/* No side effect */
