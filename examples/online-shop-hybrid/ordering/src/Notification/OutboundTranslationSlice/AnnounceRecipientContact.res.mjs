// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Notification$TraitNotification from "@reventlessdev/trait-notification/src/Notification.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Registered",
    email: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailUpdated",
    email: s.m(Sury.string)
  }))
]);

let outboundItemSchema = Sury.$schema(s => ({
  recipientId: s.m(Sury.string),
  email: s.m(Sury.string)
}));

let inboundCommandSchema = Sury.$schema(s => ({
  TAG: "AnnounceRecipient",
  recipientId: s.m(DcbTag$Reventless.string),
  email: s.m(Sury.string)
}));

let sourceNames = ["Customer"];

let capabilityNeeds = [];

let traits = [Notification$TraitNotification.declaration];

let name = "AnnounceRecipientContact";

let Id;

let maxRetries = 3;

let heartbeatInterval = 60;

let targetName = "NotificationPreferences";

let externalSystem;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Notification/OutboundTranslationSlice/AnnounceRecipientContact.res.mjs";

export {
  name,
  Id,
  consumedEventSchema,
  outboundItemSchema,
  inboundCommandSchema,
  maxRetries,
  heartbeatInterval,
  targetName,
  sourceNames,
  externalSystem,
  capabilityNeeds,
  traits,
  moduleUrl,
}
/* consumedEventSchema Not a pure module */
