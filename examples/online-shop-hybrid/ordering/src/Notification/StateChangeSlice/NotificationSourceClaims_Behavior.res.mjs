// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Notification_Rules$TraitNotification from "@reventlessdev/trait-notification/src/Notification_Rules.res.mjs";

function evolve(state, event) {
  if (event.TAG === "NotificationSourceClaimed") {
    return Notification_Rules$TraitNotification.evolve(state, {
      TAG: "Claimed",
      source: event.sourceId,
      by: event.by
    });
  } else {
    return Notification_Rules$TraitNotification.evolve(state, {
      TAG: "Released",
      source: event.sourceId
    });
  }
}

function posture(_category, _channel) {
  return false;
}

function named(fact) {
  switch (fact.TAG) {
    case "Claimed" :
      return {
        TAG: "NotificationSourceClaimed",
        sourceId: fact.source,
        by: fact.by
      };
    case "Released" :
      return {
        TAG: "NotificationSourceReleased",
        sourceId: fact.source
      };
    default:
      return;
  }
}

function through(state, op) {
  let facts = Notification_Rules$TraitNotification.decide(state, op, posture);
  if (facts.TAG === "Ok") {
    return {
      TAG: "Ok",
      _0: Stdlib_Array.filterMap(facts._0, named)
    };
  } else {
    return {
      TAG: "Error",
      _0: "ClaimRefused"
    };
  }
}

function decide(state, command) {
  if (command.TAG === "ClaimNotificationSource") {
    return through(state, {
      TAG: "Claim",
      source: command.sourceId,
      by: command.by
    });
  } else {
    return through(state, {
      TAG: "Release",
      source: command.sourceId
    });
  }
}

let Spec;

let Rules;

let initialState = Notification_Rules$TraitNotification.empty;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Notification/StateChangeSlice/NotificationSourceClaims_Behavior.res.mjs";

export {
  Spec,
  Rules,
  initialState,
  evolve,
  posture,
  named,
  through,
  decide,
  moduleUrl,
}
/* No side effect */
