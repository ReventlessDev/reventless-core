// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";
import * as Notification$TraitNotification from "@reventlessdev/trait-notification/src/Notification.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "NotificationSourceClaimed",
    sourceId: s.m(DcbTag$Reventless.string),
    by: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "NotificationSourceReleased",
    sourceId: s.m(DcbTag$Reventless.string)
  }))
]);

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "ClaimNotificationSource",
    sourceId: s.m(DcbTag$Reventless.string),
    by: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "ReleaseNotificationSource",
    sourceId: s.m(DcbTag$Reventless.string)
  }))
]);

let errorSchema = Sury.literal("ClaimRefused");

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "NotificationSourceClaimed",
    sourceId: s.m(DcbTag$Reventless.string),
    by: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "NotificationSourceReleased",
    sourceId: s.m(DcbTag$Reventless.string)
  }))
]);

let traits = [Notification$TraitNotification.declaration];

let commandSchema$1 = Api$ReventlessInfra.markNoApiVariants(commandSchema, [
  "ClaimNotificationSource",
  "ReleaseNotificationSource"
]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Unrestricted";
}

let name = "NotificationSourceClaims";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Notification/StateChangeSlice/NotificationSourceClaims.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  errorSchema,
  eventSchema,
  traits,
  commandSchema$1 as commandSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
}
/* consumedEventSchema Not a pure module */
