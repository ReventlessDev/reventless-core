// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";

let initialState_choices = [];

let initialState = {
  email: undefined,
  choices: initialState_choices
};

function defaultPosture(category, channel) {
  switch (category) {
    case "OrderConfirmation" :
      switch (channel) {
        case "Email" :
          return true;
        case "Sms" :
        case "Push" :
          return false;
      }
    case "ShippingUpdate" :
      switch (channel) {
        case "Email" :
          return true;
        case "Sms" :
        case "Push" :
          return false;
      }
    case "Marketing" :
      return false;
  }
}

function enabled(state, category, channel) {
  let match = state.choices.find(c => {
    if (c.category === category) {
      return c.channel === channel;
    } else {
      return false;
    }
  });
  if (match !== undefined) {
    return match.enabled;
  } else {
    return defaultPosture(category, channel);
  }
}

function withChoice(state, category, channel, isEnabled) {
  return {
    email: state.email,
    choices: state.choices.filter(c => c.category !== category || c.channel !== channel).concat([{
        category: category,
        channel: channel,
        enabled: isEnabled
      }])
  };
}

function addressFor(state, channel) {
  switch (channel) {
    case "Email" :
      return state.email;
    case "Sms" :
    case "Push" :
      return;
  }
}

function evolve(state, event) {
  switch (event.TAG) {
    case "RecipientAnnounced" :
      return {
        email: event.email,
        choices: state.choices
      };
    case "NotificationSubscribed" :
      return withChoice(state, event.category, event.channel, true);
    case "NotificationUnsubscribed" :
      return withChoice(state, event.category, event.channel, false);
  }
}

let allChannels = [
  "Email",
  "Sms",
  "Push"
];

function decide(state, command) {
  switch (command.TAG) {
    case "AnnounceRecipient" :
      let email = command.email;
      if (Primitive_object.equal(state.email, email)) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "RecipientAnnounced",
              recipientId: command.recipientId,
              email: email
            }]
        };
      }
    case "Subscribe" :
      let channel = command.channel;
      let category = command.category;
      let match = state.email;
      if (match !== undefined) {
        if (enabled(state, category, channel)) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "NotificationSubscribed",
                recipientId: command.recipientId,
                category: category,
                channel: channel
              }]
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: "RecipientUnknown"
        };
      }
    case "Unsubscribe" :
      let channel$1 = command.channel;
      let category$1 = command.category;
      let match$1 = state.email;
      if (match$1 !== undefined) {
        if (enabled(state, category$1, channel$1)) {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "NotificationUnsubscribed",
                recipientId: command.recipientId,
                category: category$1,
                channel: channel$1
              }]
          };
        } else {
          return {
            TAG: "Ok",
            _0: []
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: "RecipientUnknown"
        };
      }
    case "RequestNotification" :
      let body = command.body;
      let subject = command.subject;
      let reference = command.reference;
      let category$2 = command.category;
      let recipientId = command.recipientId;
      let match$2 = state.email;
      if (match$2 === undefined) {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "NotificationUndeliverable",
              recipientId: recipientId,
              category: category$2,
              reference: reference
            }]
        };
      }
      let addressed = Stdlib_Array.filterMap(allChannels.filter(channel => enabled(state, category$2, channel)), channel => Stdlib_Option.map(addressFor(state, channel), address => ({
        TAG: "NotificationRequested",
        recipientId: recipientId,
        category: category$2,
        reference: reference,
        channel: channel,
        address: address,
        subject: subject,
        body: body
      })));
      if (addressed.length !== 0) {
        return {
          TAG: "Ok",
          _0: addressed
        };
      } else if (allChannels.some(channel => enabled(state, category$2, channel))) {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "NotificationUndeliverable",
              recipientId: recipientId,
              category: category$2,
              reference: reference
            }]
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "NotificationSuppressed",
              recipientId: recipientId,
              category: category$2,
              reference: reference
            }]
        };
      }
    case "RecordDelivery" :
      return {
        TAG: "Ok",
        _0: [{
            TAG: "NotificationDelivered",
            recipientId: command.recipientId,
            reference: command.reference,
            providerRef: command.providerRef
          }]
      };
    case "RecordDeliveryFailure" :
      return {
        TAG: "Ok",
        _0: [{
            TAG: "NotificationFailed",
            recipientId: command.recipientId,
            reference: command.reference,
            reason: command.reason
          }]
      };
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Notification/StateChangeSlice/NotificationPreferences_Behavior.res.mjs";

export {
  Spec,
  initialState,
  defaultPosture,
  enabled,
  withChoice,
  addressFor,
  evolve,
  allChannels,
  decide,
  moduleUrl,
}
/* No side effect */
