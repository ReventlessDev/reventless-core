// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";

function project(param) {
  let meta = param.meta;
  let event = param.event;
  switch (event.TAG) {
    case "OrderPlaced" :
      let lines = event.lines;
      let orderId = event.orderId;
      return [{
          TAG: "Set",
          _0: orderId,
          _1: {
            orderId: orderId,
            customerId: event.customerId,
            productIds: event.productIds,
            placedAt: meta.time,
            shippedAt: "",
            lines: lines,
            total: event.total,
            itemCount: Stdlib_Array.reduce(lines, 0, (count, line) => count + line.quantity | 0),
            lifecycle: "Placed",
            shippingMethod: event.shippingMethod,
            deliveryWindow: event.deliveryWindow,
            firstProductName: event.firstProductName,
            firstProductImage: event.firstProductImage
          }
        }];
    case "OrderShipped" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => {
            let newrecord = {...state};
            newrecord.lifecycle = "Shipped";
            newrecord.shippedAt = meta.time;
            return newrecord;
          }
        }];
    case "OrderCancelled" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => {
            let newrecord = {...state};
            newrecord.lifecycle = "Cancelled";
            return newrecord;
          }
        }];
    case "OrderReopened" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => {
            let newrecord = {...state};
            newrecord.lifecycle = "Placed";
            return newrecord;
          }
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Order/StateViewStream/Orders_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
