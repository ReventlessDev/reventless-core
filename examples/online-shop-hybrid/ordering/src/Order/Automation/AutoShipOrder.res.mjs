// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as OrderId$OrderingPlugin from "../OrderId.res.mjs";

let todoItemSchema = Sury.$schema(s => ({
  orderId: s.m(OrderId$OrderingPlugin.schema)
}));

let commandSchema = Sury.$schema(s => ({
  TAG: "ShipOrder",
  orderId: s.m(DcbTag$Reventless.mark(OrderId$OrderingPlugin.schema))
}));

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Undeclared";
}

let traits = [];

let name = "AutoShipOrder";

let Id;

let maxRetries = 3;

let heartbeatInterval = 60;

let targetName = "ShipOrder";

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Order/Automation/AutoShipOrder.res.mjs";

export {
  name,
  Id,
  todoItemSchema,
  commandSchema,
  maxRetries,
  heartbeatInterval,
  targetName,
  moduleUrl,
  commandAuthorization,
  commandTransition,
  traits,
}
/* todoItemSchema Not a pure module */
