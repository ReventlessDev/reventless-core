// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (typeof event !== "object") {
    if (event === "OrderPlaced") {
      return {
        exists: true,
        cancelled: false,
        refunded: false
      };
    } else {
      return {
        exists: state.exists,
        cancelled: true,
        refunded: state.refunded
      };
    }
  } else {
    return {
      exists: state.exists,
      cancelled: state.cancelled,
      refunded: true
    };
  }
}

function decide(state, command) {
  if (state.exists) {
    if (state.cancelled) {
      if (state.refunded) {
        return {
          TAG: "Error",
          _0: "RefundAlreadyIssued"
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "RefundIssued",
              orderId: command.orderId,
              reason: command.reason
            }]
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: "OrderNotCancelled"
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: "OrderNotFound"
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  cancelled: false,
  refunded: false
};

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Order/StateChangeSlice/RefundOrder_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
