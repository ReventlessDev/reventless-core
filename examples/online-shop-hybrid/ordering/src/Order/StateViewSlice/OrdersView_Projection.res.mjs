// Generated by ReScript, PLEASE EDIT WITH CARE


function project(event) {
  switch (event.TAG) {
    case "OrderPlaced" :
      let orderId = event.orderId;
      return [{
          TAG: "Set",
          _0: orderId,
          _1: {
            orderId: orderId,
            customerId: event.customerId,
            productId: event.productId,
            status: "placed"
          }
        }];
    case "OrderShipped" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productId: state.productId,
            status: "shipped"
          })
        }];
    case "OrderCancelled" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productId: state.productId,
            status: "cancelled"
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Order/StateViewSlice/OrdersView_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
