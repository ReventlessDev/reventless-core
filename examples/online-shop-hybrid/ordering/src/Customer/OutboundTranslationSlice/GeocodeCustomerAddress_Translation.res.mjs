// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";
import * as AddressGeocoding_Translate$TraitAddressGeocoding from "@reventlessdev/trait-address-geocoding/src/AddressGeocoding_Translate.res.mjs";

function collect(event, sourceId) {
  if (event.TAG === "Registered") {
    let address = event.address;
    return [[
        sourceId + `:` + address,
        {
          customerId: sourceId,
          address: address
        }
      ]];
  }
  let address$1 = event.address;
  return [[
      sourceId + `:` + address$1,
      {
        customerId: sourceId,
        address: address$1
      }
    ]];
}

async function translate(_id, item, capabilities) {
  return Stdlib_Result.map(await AddressGeocoding_Translate$TraitAddressGeocoding.translate(item.address, capabilities, (point, resolvedFrom) => ({
    TAG: "SetLocation",
    location: point,
    resolvedFrom: resolvedFrom
  }), (subject, reason) => ({
    TAG: "MarkAddressUnresolvable",
    address: subject,
    reason: reason
  })), command => [
    item.customerId,
    command
  ]);
}

function onExhausted(_id, item, lastError) {
  return [
    item.customerId,
    {
      TAG: "MarkAddressUnresolvable",
      address: item.address,
      reason: AddressGeocoding_Translate$TraitAddressGeocoding.exhaustedReason(lastError)
    }
  ];
}

let Spec;

let Geocode;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Customer/OutboundTranslationSlice/GeocodeCustomerAddress_Translation.res.mjs";

export {
  Spec,
  collect,
  Geocode,
  translate,
  onExhausted,
  moduleUrl,
}
/* AddressGeocoding_Translate-TraitAddressGeocoding Not a pure module */
