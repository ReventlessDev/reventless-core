// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Registered",
    email: s.m(Sury.string),
    address: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailUpdated",
    email: s.m(Sury.string)
  }))
]);

let outboundItemSchema = Sury.$schema(s => ({
  customerId: s.m(Sury.string),
  email: s.m(Sury.string)
}));

let inboundCommandSchema = Sury.$schema(s => ({
  TAG: "IssueEmailChallenge",
  customerId: s.m(DcbTag$Reventless.string),
  email: s.m(Sury.string),
  purpose: s.m(Sury.string),
  proofHash: s.m(Sury.string),
  issuedAt: s.m(Sury.string)
}));

let sourceNames = ["Customer"];

let capabilityNeeds = ["Messaging"];

let traits = [];

let name = "VerifyCustomerEmail";

let Id;

let maxRetries = 3;

let heartbeatInterval = 60;

let targetName = "EmailVerificationChallenges";

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Customer/OutboundTranslation/VerifyCustomerEmail.res.mjs";

let externalSystem;

export {
  name,
  Id,
  consumedEventSchema,
  outboundItemSchema,
  inboundCommandSchema,
  maxRetries,
  heartbeatInterval,
  targetName,
  sourceNames,
  capabilityNeeds,
  moduleUrl,
  externalSystem,
  traits,
}
/* consumedEventSchema Not a pure module */
