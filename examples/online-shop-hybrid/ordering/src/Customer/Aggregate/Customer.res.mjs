// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as AddressGeocoding$TraitAddressGeocoding from "@reventlessdev/trait-address-geocoding/src/AddressGeocoding.res.mjs";

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Register",
    email: s.m(Sury.string),
    address: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "UpdateEmail",
    email: s.m(Sury.string)
  })),
  Sury.literal("Deactivate"),
  Sury.literal("Reactivate")
].concat(AddressGeocoding$TraitAddressGeocoding.addressCommandsSchema.type === "anyOf" ? AddressGeocoding$TraitAddressGeocoding.addressCommandsSchema.anyOf : [AddressGeocoding$TraitAddressGeocoding.addressCommandsSchema]).concat(AddressGeocoding$TraitAddressGeocoding.reportCommandsSchema.type === "anyOf" ? AddressGeocoding$TraitAddressGeocoding.reportCommandsSchema.anyOf : [AddressGeocoding$TraitAddressGeocoding.reportCommandsSchema]));

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Registered",
    email: s.m(Sury.string),
    address: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailUpdated",
    email: s.m(Sury.string)
  })),
  Sury.literal("Deactivated"),
  Sury.literal("Reactivated")
].concat(AddressGeocoding$TraitAddressGeocoding.eventsSchema.type === "anyOf" ? AddressGeocoding$TraitAddressGeocoding.eventsSchema.anyOf : [AddressGeocoding$TraitAddressGeocoding.eventsSchema]));

let errorSchema = Sury.union([
  Sury.literal("CustomerAlreadyRegistered"),
  Sury.literal("CustomerNotFound"),
  Sury.literal("CustomerAlreadyDeactivated")
]);

function commandTransition(command) {
  if (typeof command !== "object") {
    if (command === "Deactivate") {
      return {
        TAG: "Moves",
        _0: ["Active"],
        _1: "Deactivated"
      };
    } else {
      return {
        TAG: "Moves",
        _0: ["Deactivated"],
        _1: "Active"
      };
    }
  }
  switch (command.TAG) {
    case "Register" :
    case "SetLocation" :
    case "MarkAddressUnresolvable" :
      return "Unrestricted";
    default:
      return {
        TAG: "Guards",
        _0: ["Active"]
      };
  }
}

let traits = [AddressGeocoding$TraitAddressGeocoding.declaration];

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

let name = "Customer";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Customer/Aggregate/Customer.res.mjs";

export {
  name,
  Id,
  commandSchema,
  eventSchema,
  errorSchema,
  commandTransition,
  traits,
  moduleUrl,
  commandAuthorization,
}
/* commandSchema Not a pure module */
