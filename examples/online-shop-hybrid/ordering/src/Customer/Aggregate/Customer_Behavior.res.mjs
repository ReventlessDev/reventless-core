// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";
import * as GeoPoint$Reventless from "@reventlessdev/reventless-spec/src/semantic/GeoPoint.res.mjs";

let stateSchema = S.union([
  S.literal("NotCreated"),
  S.schema(s => ({
    TAG: "Active",
    email: s.m(S.string),
    address: s.m(S.string),
    location: s.m(S.option(GeoPoint$Reventless.schema)),
    locationResolvedFrom: s.m(S.option(S.string))
  })),
  S.literal("Deactivated")
]);

function evolve(state, event) {
  if (typeof state !== "object") {
    if (state === "NotCreated" && typeof event === "object" && event.TAG === "Registered") {
      return {
        TAG: "Active",
        email: event.email,
        address: event.address,
        location: undefined,
        locationResolvedFrom: undefined
      };
    } else {
      return state;
    }
  }
  if (typeof event !== "object") {
    return "Deactivated";
  }
  switch (event.TAG) {
    case "Registered" :
      return {
        TAG: "Active",
        email: event.email,
        address: event.address,
        location: state.location,
        locationResolvedFrom: state.locationResolvedFrom
      };
    case "EmailUpdated" :
      return {
        TAG: "Active",
        email: event.email,
        address: state.address,
        location: state.location,
        locationResolvedFrom: state.locationResolvedFrom
      };
    case "AddressUpdated" :
      return {
        TAG: "Active",
        email: state.email,
        address: event.address,
        location: undefined,
        locationResolvedFrom: undefined
      };
    case "LocationSet" :
      return {
        TAG: "Active",
        email: state.email,
        address: state.address,
        location: event.location,
        locationResolvedFrom: event.resolvedFrom
      };
    case "AddressLocated" :
      let address = event.address;
      return {
        TAG: "Active",
        email: state.email,
        address: address,
        location: event.location,
        locationResolvedFrom: address
      };
    case "AddressUnresolvable" :
      return {
        TAG: "Active",
        email: state.email,
        address: state.address,
        location: undefined,
        locationResolvedFrom: event.address
      };
  }
}

function decide(state, command) {
  if (typeof state !== "object") {
    if (state === "NotCreated") {
      if (typeof command !== "object" || command.TAG !== "Register") {
        return {
          TAG: "Error",
          _0: "CustomerNotFound"
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "Registered",
              email: command.email,
              address: command.address
            }]
        };
      }
    }
    if (typeof command !== "object") {
      return {
        TAG: "Ok",
        _0: []
      };
    }
    switch (command.TAG) {
      case "SetLocation" :
      case "MarkAddressUnresolvable" :
        return {
          TAG: "Ok",
          _0: []
        };
      default:
        return {
          TAG: "Error",
          _0: "CustomerAlreadyDeactivated"
        };
    }
  } else {
    if (typeof command !== "object") {
      return {
        TAG: "Ok",
        _0: ["Deactivated"]
      };
    }
    switch (command.TAG) {
      case "Register" :
        return {
          TAG: "Error",
          _0: "CustomerAlreadyRegistered"
        };
      case "UpdateEmail" :
        let email = command.email;
        if (email === state.email) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "EmailUpdated",
                email: email
              }]
          };
        }
      case "UpdateAddress" :
        let address = command.address;
        if (address === state.address) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "AddressUpdated",
                address: address
              }]
          };
        }
      case "SetAddressLocation" :
        let location = command.location;
        let address$1 = command.address;
        if (address$1 === state.address && Primitive_object.equal(state.location, location)) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "AddressLocated",
                address: address$1,
                location: location
              }]
          };
        }
      case "SetLocation" :
        let resolvedFrom = command.resolvedFrom;
        if (resolvedFrom !== state.address) {
          return {
            TAG: "Ok",
            _0: []
          };
        }
        let location$1 = command.location;
        if (Primitive_object.equal(state.location, location$1) && Primitive_object.equal(state.locationResolvedFrom, resolvedFrom)) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "LocationSet",
                location: location$1,
                resolvedFrom: resolvedFrom
              }]
          };
        }
      case "MarkAddressUnresolvable" :
        let address$2 = command.address;
        if (address$2 !== state.address) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else if (Primitive_object.equal(state.locationResolvedFrom, address$2)) {
          return {
            TAG: "Ok",
            _0: []
          };
        } else {
          return {
            TAG: "Ok",
            _0: [{
                TAG: "AddressUnresolvable",
                address: address$2,
                reason: command.reason
              }]
          };
        }
    }
  }
}

let Spec;

let initialState = "NotCreated";

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Customer/Aggregate/Customer_Behavior.res.mjs";

let snapshot;

export {
  Spec,
  stateSchema,
  initialState,
  evolve,
  decide,
  moduleUrl,
  snapshot,
}
/* stateSchema Not a pure module */
