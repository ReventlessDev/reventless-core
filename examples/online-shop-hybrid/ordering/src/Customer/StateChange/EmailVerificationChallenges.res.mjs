// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Api$ReventlessInfra from "@reventlessdev/reventless-infra/src/components/Api.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "EmailChallengeIssued",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string),
    purpose: s.m(Sury.string),
    proofHash: s.m(Sury.string),
    issuedAt: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailProofAccepted",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailProofRefused",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string),
    reason: s.m(Sury.string)
  }))
]);

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "IssueEmailChallenge",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string),
    purpose: s.m(Sury.string),
    proofHash: s.m(Sury.string),
    issuedAt: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "SubmitEmailProof",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string),
    proofHash: s.m(Sury.string),
    presentedAt: s.m(Sury.string)
  }))
]);

let errorSchema = Sury.literal("NoChallengeOutstanding");

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "EmailChallengeIssued",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string),
    purpose: s.m(Sury.string),
    proofHash: s.m(Sury.string),
    issuedAt: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailProofAccepted",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailProofRefused",
    customerId: s.m(DcbTag$Reventless.string),
    email: s.m(Sury.string),
    reason: s.m(Sury.string)
  }))
]);

let commandSchema$1 = Api$ReventlessInfra.markNoApiVariants(commandSchema, ["IssueEmailChallenge"]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Undeclared";
}

let traits = [];

let name = "EmailVerificationChallenges";

let Id;

let moduleUrl = "@reventlessdev/online-shop-hybrid-ordering/src/Customer/StateChange/EmailVerificationChallenges.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  errorSchema,
  eventSchema,
  commandSchema$1 as commandSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
  traits,
}
/* consumedEventSchema Not a pure module */
