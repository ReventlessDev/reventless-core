// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as ShipOrder_Behavior$OrderingPlugin from "../../src/Order/StateChangeSlice/ShipOrder_Behavior.res.mjs";
import * as PlaceOrder_Behavior$OrderingPlugin from "../../src/Order/StateChangeSlice/PlaceOrder_Behavior.res.mjs";
import * as CancelOrder_Behavior$OrderingPlugin from "../../src/Order/StateChangeSlice/CancelOrder_Behavior.res.mjs";

Jest.describe("PlaceOrder:", () => {
  let withProducts = productIds => {
    let s = new Set();
    productIds.forEach(id => {
      s.add(id);
    });
    return {
      placedOrderIds: new Set(),
      availableProductIds: s
    };
  };
  Jest.describe("evolve", () => {
    Jest.test("OrderPlaced records the orderId in placedOrderIds", () => {
      let state = PlaceOrder_Behavior$OrderingPlugin.evolve(withProducts(["prod-1"]), {
        TAG: "OrderPlaced",
        orderId: "ord-1"
      });
      return Jest.Expect.toBe(Jest.Expect.expect(state.placedOrderIds.has("ord-1")), true);
    });
    Jest.test("CatalogProductSynced adds to availableProductIds", () => {
      let state = PlaceOrder_Behavior$OrderingPlugin.evolve(PlaceOrder_Behavior$OrderingPlugin.initialState, {
        TAG: "CatalogProductSynced",
        productId: "prod-1"
      });
      return Jest.Expect.toBe(Jest.Expect.expect(state.availableProductIds.has("prod-1")), true);
    });
  });
  Jest.describe("decide", () => {
    Jest.test("with available products produces OrderPlaced", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder_Behavior$OrderingPlugin.decide(withProducts([
      "prod-1",
      "prod-2"
    ]), {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productId: [
        "prod-1",
        "prod-2"
      ]
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "OrderPlaced",
          orderId: "ord-1",
          customerId: "cust-1",
          productId: [
            "prod-1",
            "prod-2"
          ]
        }]
    }));
    Jest.test("with missing products returns ProductsNotAvailable", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder_Behavior$OrderingPlugin.decide(withProducts(["prod-1"]), {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productId: [
        "prod-1",
        "prod-2"
      ]
    })), {
      TAG: "Error",
      _0: {
        TAG: "ProductsNotAvailable",
        missing: ["prod-2"]
      }
    }));
    Jest.test("on existing order returns OrderAlreadyPlaced", () => {
      let state = withProducts(["prod-1"]);
      state.placedOrderIds.add("ord-1");
      return Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder_Behavior$OrderingPlugin.decide(state, {
        TAG: "PlaceOrder",
        orderId: "ord-1",
        customerId: "cust-1",
        productId: ["prod-1"]
      })), {
        TAG: "Error",
        _0: "OrderAlreadyPlaced"
      });
    });
    Jest.test("OrderPlaced from a sibling order does not block this order", () => {
      let state = withProducts(["prod-1"]);
      state.placedOrderIds.add("ord-other");
      return Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder_Behavior$OrderingPlugin.decide(state, {
        TAG: "PlaceOrder",
        orderId: "ord-1",
        customerId: "cust-1",
        productId: ["prod-1"]
      })), {
        TAG: "Ok",
        _0: [{
            TAG: "OrderPlaced",
            orderId: "ord-1",
            customerId: "cust-1",
            productId: ["prod-1"]
          }]
      });
    });
  });
});

Jest.describe("ShipOrder:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent order returns OrderNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder_Behavior$OrderingPlugin.decide(ShipOrder_Behavior$OrderingPlugin.initialState, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderNotFound"
  }));
  Jest.test("on placed order produces OrderShipped", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder_Behavior$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: false
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "OrderShipped",
        orderId: "ord-1"
      }]
  }));
  Jest.test("on shipped order is idempotent (no events)", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder_Behavior$OrderingPlugin.decide({
    exists: true,
    shipped: true,
    cancelled: false
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on cancelled order returns OrderAlreadyCancelled", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder_Behavior$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: true
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderAlreadyCancelled"
  }));
}));

Jest.describe("CancelOrder:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent order returns OrderNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder_Behavior$OrderingPlugin.decide(CancelOrder_Behavior$OrderingPlugin.initialState, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderNotFound"
  }));
  Jest.test("on placed order produces OrderCancelled with productIds", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder_Behavior$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: false,
    productId: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "OrderCancelled",
        orderId: "ord-1",
        productId: ["prod-1"]
      }]
  }));
  Jest.test("on cancelled order is idempotent (no events)", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder_Behavior$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: true,
    productId: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on shipped order returns OrderAlreadyShipped", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder_Behavior$OrderingPlugin.decide({
    exists: true,
    shipped: true,
    cancelled: false,
    productId: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderAlreadyShipped"
  }));
}));

/*  Not a pure module */
