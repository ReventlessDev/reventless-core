// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as ShipOrder$OrderingPlugin from "../../src/Order/StateChangeSlice/ShipOrder.res.mjs";
import * as PlaceOrder$OrderingPlugin from "../../src/Order/StateChangeSlice/PlaceOrder.res.mjs";
import * as CancelOrder$OrderingPlugin from "../../src/Order/StateChangeSlice/CancelOrder.res.mjs";

Jest.describe("PlaceOrder:", () => {
  let withProducts = productIds => {
    let s = new Set();
    productIds.forEach(id => {
      s.add(id);
    });
    return {
      exists: false,
      availableProductIds: s
    };
  };
  Jest.describe("evolve", () => {
    Jest.test("OrderPlaced sets exists=true", () => {
      let state = PlaceOrder$OrderingPlugin.evolve(withProducts(["prod-1"]), "OrderPlaced");
      return Jest.Expect.toBe(Jest.Expect.expect(state.exists), true);
    });
    Jest.test("CatalogProductSynced adds to availableProductIds", () => {
      let state = PlaceOrder$OrderingPlugin.evolve(PlaceOrder$OrderingPlugin.initialState, {
        TAG: "CatalogProductSynced",
        productId: "prod-1"
      });
      return Jest.Expect.toBe(Jest.Expect.expect(state.availableProductIds.has("prod-1")), true);
    });
  });
  Jest.describe("decide", () => {
    Jest.test("with available products produces OrderPlaced", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.decide(withProducts([
      "prod-1",
      "prod-2"
    ]), {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productId: [
        "prod-1",
        "prod-2"
      ]
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "OrderPlaced",
          orderId: "ord-1",
          customerId: "cust-1",
          productId: [
            "prod-1",
            "prod-2"
          ]
        }]
    }));
    Jest.test("with missing products returns ProductsNotAvailable", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.decide(withProducts(["prod-1"]), {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productId: [
        "prod-1",
        "prod-2"
      ]
    })), {
      TAG: "Error",
      _0: {
        TAG: "ProductsNotAvailable",
        missing: ["prod-2"]
      }
    }));
    Jest.test("on existing order returns OrderAlreadyPlaced", () => {
      let state = withProducts(["prod-1"]);
      return Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.decide({
        exists: true,
        availableProductIds: state.availableProductIds
      }, {
        TAG: "PlaceOrder",
        orderId: "ord-1",
        customerId: "cust-1",
        productId: ["prod-1"]
      })), {
        TAG: "Error",
        _0: "OrderAlreadyPlaced"
      });
    });
  });
});

Jest.describe("ShipOrder:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent order returns OrderNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide(ShipOrder$OrderingPlugin.initialState, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderNotFound"
  }));
  Jest.test("on placed order produces OrderShipped", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: false
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "OrderShipped",
        orderId: "ord-1"
      }]
  }));
  Jest.test("on shipped order is idempotent (no events)", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide({
    exists: true,
    shipped: true,
    cancelled: false
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on cancelled order returns OrderAlreadyCancelled", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: true
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderAlreadyCancelled"
  }));
}));

Jest.describe("CancelOrder:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent order returns OrderNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide(CancelOrder$OrderingPlugin.initialState, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderNotFound"
  }));
  Jest.test("on placed order produces OrderCancelled with productIds", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: false,
    productId: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "OrderCancelled",
        orderId: "ord-1",
        productId: ["prod-1"]
      }]
  }));
  Jest.test("on cancelled order is idempotent (no events)", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: true,
    productId: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on shipped order returns OrderAlreadyShipped", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide({
    exists: true,
    shipped: true,
    cancelled: false,
    productId: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderAlreadyShipped"
  }));
}));

/*  Not a pure module */
