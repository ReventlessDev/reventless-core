// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as ShipOrder$OrderingPlugin from "../../src/Order/StateChangeSlice/ShipOrder.res.mjs";
import * as PlaceOrder$OrderingPlugin from "../../src/Order/StateChangeSlice/PlaceOrder.res.mjs";
import * as CancelOrder$OrderingPlugin from "../../src/Order/StateChangeSlice/CancelOrder.res.mjs";

Jest.describe("PlaceOrder:", () => {
  Jest.describe("reduce", () => {
    Jest.test("OrderPlaced sets exists=true", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.reduce(PlaceOrder$OrderingPlugin.initialDecisionModel, {
      TAG: "OrderPlaced",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: ["prod-1"]
    })), {
      exists: true
    }));
    Jest.test("other events do not change model", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.reduce(PlaceOrder$OrderingPlugin.initialDecisionModel, {
      TAG: "OrderShipped",
      orderId: "ord-1"
    })), PlaceOrder$OrderingPlugin.initialDecisionModel));
  });
  Jest.describe("decide", () => {
    Jest.test("on non-existent order produces OrderPlaced", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.decide(PlaceOrder$OrderingPlugin.initialDecisionModel, {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: [
        "prod-1",
        "prod-2"
      ]
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "OrderPlaced",
          orderId: "ord-1",
          customerId: "cust-1",
          productIds: [
            "prod-1",
            "prod-2"
          ]
        }]
    }));
    Jest.test("on existing order returns OrderAlreadyPlaced", () => Jest.Expect.toEqual(Jest.Expect.expect(PlaceOrder$OrderingPlugin.decide({
      exists: true
    }, {
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: ["prod-1"]
    })), {
      TAG: "Error",
      _0: "OrderAlreadyPlaced"
    }));
  });
});

Jest.describe("ShipOrder:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent order returns OrderNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide(ShipOrder$OrderingPlugin.initialDecisionModel, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderNotFound"
  }));
  Jest.test("on placed order produces OrderShipped", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: false
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "OrderShipped",
        orderId: "ord-1"
      }]
  }));
  Jest.test("on shipped order is idempotent (no events)", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide({
    exists: true,
    shipped: true,
    cancelled: false
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on cancelled order returns OrderAlreadyCancelled", () => Jest.Expect.toEqual(Jest.Expect.expect(ShipOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: true
  }, {
    TAG: "ShipOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderAlreadyCancelled"
  }));
}));

Jest.describe("CancelOrder:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent order returns OrderNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide(CancelOrder$OrderingPlugin.initialDecisionModel, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderNotFound"
  }));
  Jest.test("on placed order produces OrderCancelled with productIds", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: false,
    productIds: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "OrderCancelled",
        orderId: "ord-1",
        productIds: ["prod-1"]
      }]
  }));
  Jest.test("on cancelled order is idempotent (no events)", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide({
    exists: true,
    shipped: false,
    cancelled: true,
    productIds: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on shipped order returns OrderAlreadyShipped", () => Jest.Expect.toEqual(Jest.Expect.expect(CancelOrder$OrderingPlugin.decide({
    exists: true,
    shipped: true,
    cancelled: false,
    productIds: ["prod-1"]
  }, {
    TAG: "CancelOrder",
    orderId: "ord-1"
  })), {
    TAG: "Error",
    _0: "OrderAlreadyShipped"
  }));
}));

/*  Not a pure module */
