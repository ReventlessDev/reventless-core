config:
  aws:region: eu-west-1
  # Disposable dev stack: opt into `pnpm run seed:reset`. Prod stacks omit this.
  reventless:wipeable: "true"
  platform:stack: reventless/online-shop-hybrid-platform-aws/alpha
  interstack:
    dependencies:
      - reventless/online-shop-hybrid-catalog-aws/alpha
