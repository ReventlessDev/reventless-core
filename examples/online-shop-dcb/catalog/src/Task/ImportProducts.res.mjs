// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Message$Reventless from "@reventlessdev/reventless-spec/src/types/Message.res.mjs";
import * as ImportProduct$CatalogPlugin from "../Product/InboundTranslationSlice/ImportProduct.res.mjs";

function importCallback(eventName, key) {
  if (!eventName.includes("ObjectCreated")) {
    return Promise.resolve([]);
  }
  console.log("[ImportProducts] Processing file: " + key);
  let meta_time = Date.now().toString();
  let meta_ip = "";
  let meta_user = "system";
  let meta = {
    service: "ImportProducts",
    time: meta_time,
    ip: meta_ip,
    user: meta_user,
    msgId: key,
    correlationId: key
  };
  let demoInput_desc = "Imported from " + key;
  let demoInput = {
    sku: key,
    title: "Imported Product",
    desc: demoInput_desc,
    unitPrice: 999,
    currency: "USD"
  };
  return Promise.resolve([{
      TAG: "PublishCommands",
      _0: ImportProduct$CatalogPlugin.name,
      _1: [{
          id: key,
          meta: meta,
          commandJson: Message$Reventless.encode(demoInput, ImportProduct$CatalogPlugin.externalInputSchema)
        }]
    }]);
}

function setup(_queryEngine, _queryBucketName, _opts) {
  return {
    buckets: [{
        bucketMode: "Read",
        callback: importCallback
      }]
  };
}

let name = "ImportProducts";

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Task/ImportProducts.res.mjs";

export {
  name,
  importCallback,
  setup,
  moduleUrl,
}
/* Message-Reventless Not a pure module */
