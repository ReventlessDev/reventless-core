// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as ProductId$CatalogSpec from "@reventlessdev/online-shop-dcb-catalog-spec/src/ProductId.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "ProductAdded",
    description: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "ProductDescriptionChanged",
    description: s.m(Sury.string)
  }))
]);

let commandSchema = Sury.$schema(s => ({
  TAG: "ChangeProductDescription",
  productId: s.m(DcbTag$Reventless.mark(ProductId$CatalogSpec.schema)),
  description: s.m(Sury.string)
}));

let errorSchema = Sury.literal("ProductNotFound");

let eventSchema = Sury.$schema(s => ({
  TAG: "ProductDescriptionChanged",
  productId: s.m(DcbTag$Reventless.mark(ProductId$CatalogSpec.schema)),
  description: s.m(Sury.string)
}));

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Undeclared";
}

let traits = [];

let name = "ChangeProductDescription";

let Id;

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateChange/ChangeProductDescription.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
  traits,
}
/* consumedEventSchema Not a pure module */
