// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event.TAG === "ProductAdded") {
    return {
      exists: true,
      currentDescription: event.description
    };
  } else {
    return {
      exists: state.exists,
      currentDescription: event.description
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  let description = command.description;
  if (description === state.currentDescription) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductDescriptionChanged",
          productId: command.productId,
          description: description
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  currentDescription: ""
};

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateChangeSlice/ChangeProductDescription_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
