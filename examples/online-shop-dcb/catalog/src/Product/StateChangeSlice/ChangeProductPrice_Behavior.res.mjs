// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event.TAG === "ProductAdded") {
    return {
      exists: true,
      currentPrice: event.price
    };
  } else {
    return {
      exists: state.exists,
      currentPrice: event.price
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  let price = command.price;
  if (price === state.currentPrice) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductPriceChanged",
          productId: command.productId,
          price: price
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  currentPrice: 0.0
};

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateChangeSlice/ChangeProductPrice_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
