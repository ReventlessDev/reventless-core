// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "ProductAdded",
    price: s.m(Sury.float)
  })),
  Sury.$schema(s => ({
    TAG: "ProductPriceChanged",
    price: s.m(Sury.float)
  }))
]);

let commandSchema = Sury.$schema(s => ({
  TAG: "ChangeProductPrice",
  productId: s.m(DcbTag$Reventless.string),
  price: s.m(Sury.float)
}));

let errorSchema = Sury.literal("ProductNotFound");

let eventSchema = Sury.$schema(s => ({
  TAG: "ProductPriceChanged",
  productId: s.m(DcbTag$Reventless.string),
  price: s.m(Sury.float)
}));

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Unrestricted";
}

let traits = [];

let name = "ChangeProductPrice";

let Id;

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateChangeSlice/ChangeProductPrice.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
  traits,
}
/* consumedEventSchema Not a pure module */
