// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event.TAG === "ProductAdded") {
    return {
      exists: true,
      currentName: event.name
    };
  } else {
    return {
      exists: state.exists,
      currentName: event.name
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "ProductNotFound"
    };
  }
  let name = command.name;
  if (name === state.currentName) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "ProductNameChanged",
          productId: command.productId,
          name: name
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  currentName: ""
};

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateChangeSlice/ChangeProductName_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
