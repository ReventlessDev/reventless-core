// Generated by ReScript, PLEASE EDIT WITH CARE


function project(event) {
  switch (event.TAG) {
    case "ProductAdded" :
      let productId = event.productId;
      return [{
          TAG: "Set",
          _0: productId,
          _1: {
            productId: productId,
            name: event.name,
            description: event.description,
            price: event.price
          }
        }];
    case "ProductNameChanged" :
      let name = event.name;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: name,
            description: state.description,
            price: state.price
          })
        }];
    case "ProductDescriptionChanged" :
      let description = event.description;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: description,
            price: state.price
          })
        }];
    case "ProductPriceChanged" :
      let price = event.price;
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: state => ({
            productId: state.productId,
            name: state.name,
            description: state.description,
            price: price
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateViewSlice/ProductsView_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
