// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_int from "@rescript/runtime/lib/es6/Primitive_int.js";

function project(event) {
  switch (event.TAG) {
    case "ProductAdded" :
      let name = event.name;
      let productId = event.productId;
      return [{
          TAG: "UpdateWithDefault",
          _0: productId,
          _1: {
            productId: productId,
            name: name,
            orderCount: 0
          },
          _2: s => ({
            productId: s.productId,
            name: name,
            orderCount: s.orderCount
          })
        }];
    case "ProductDemandRecorded" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: s => ({
            productId: s.productId,
            name: s.name,
            orderCount: s.orderCount + 1 | 0
          })
        }];
    case "ProductDemandRevoked" :
      return [{
          TAG: "Update",
          _0: event.productId,
          _1: s => ({
            productId: s.productId,
            name: s.name,
            orderCount: Primitive_int.max(0, s.orderCount - 1 | 0)
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/Product/StateViewSlice/ProductDemandView_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
