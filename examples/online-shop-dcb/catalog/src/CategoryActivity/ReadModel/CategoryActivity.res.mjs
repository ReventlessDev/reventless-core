// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let kindSchema = S.union([
  S.literal("Category"),
  S.literal("Product")
]);

let changeSchema = S.union([
  S.literal("Added"),
  S.literal("Renamed"),
  S.literal("Archived")
]);

let stateSchema = S.schema(s => ({
  name: s.m(S.string),
  kind: s.m(kindSchema),
  lastChange: s.m(changeSchema)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "CategoryActivity";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/CategoryActivity/ReadModel/CategoryActivity.res.mjs";

let authorization = "AllowAuthenticated";

let visibility = "Public";

export {
  name,
  Id,
  kindSchema,
  changeSchema,
  stateSchema,
  config,
  subIdConfig,
  moduleUrl,
  authorization,
  visibility,
}
/* kindSchema Not a pure module */
