// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Id$Reventless from "@reventlessdev/reventless-spec/src/types/Id.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as Projection$Reventless from "@reventlessdev/reventless-spec/src/types/Projection.res.mjs";
import * as CategoryActivity$CatalogPlugin from "./CategoryActivity.res.mjs";

let M = Projection$Reventless.Mappings.Make({
  Id: Id$Reventless.$$String,
  name: CategoryActivity$CatalogPlugin.name,
  stateSchema: CategoryActivity$CatalogPlugin.stateSchema,
  subIdConfig: undefined
});

let name = "CatalogDcbEventLog";

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "CategoryAdded",
    categoryId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "CategoryRenamed",
    categoryId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "CategoryArchived",
    categoryId: s.m(DcbTag$Reventless.string)
  }))
]);

let CategoryEvents = {
  Id: undefined,
  name: name,
  eventSchema: eventSchema
};

let name$1 = "CatalogDcbEventLog";

let eventSchema$1 = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    productId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string),
    description: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "ProductNameChanged",
    productId: s.m(DcbTag$Reventless.string),
    name: s.m(S.string)
  }))
]);

let ProductEvents = {
  Id: undefined,
  name: name$1,
  eventSchema: eventSchema$1
};

function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "CategoryAdded" :
      return {
        TAG: "Set",
        _0: event.categoryId,
        _1: {
          name: event.name,
          kind: "Category",
          lastChange: "Added"
        }
      };
    case "CategoryRenamed" :
      let name = event.name;
      return {
        TAG: "Update",
        _0: event.categoryId,
        _1: state => ({
          name: name,
          kind: state.kind,
          lastChange: "Renamed"
        })
      };
    case "CategoryArchived" :
      return {
        TAG: "Update",
        _0: event.categoryId,
        _1: state => ({
          name: state.name,
          kind: state.kind,
          lastChange: "Archived"
        })
      };
  }
}

let CategoryActivityMapping = Projection$Reventless.Mapping.Make({
  Id: Id$Reventless.$$String,
  name: name,
  eventSchema: eventSchema
})({
  Id: Id$Reventless.$$String,
  name: CategoryActivity$CatalogPlugin.name,
  stateSchema: CategoryActivity$CatalogPlugin.stateSchema,
  subIdConfig: undefined
})({
  project: project
});

function project$1(param) {
  let event = param.event;
  if (event.TAG === "ProductAdded") {
    return {
      TAG: "Set",
      _0: event.productId,
      _1: {
        name: event.name,
        kind: "Product",
        lastChange: "Added"
      }
    };
  }
  let name = event.name;
  return {
    TAG: "Update",
    _0: event.productId,
    _1: state => ({
      name: name,
      kind: state.kind,
      lastChange: "Renamed"
    })
  };
}

let ProductActivityMapping = Projection$Reventless.Mapping.Make({
  Id: Id$Reventless.$$String,
  name: name$1,
  eventSchema: eventSchema$1
})({
  Id: Id$Reventless.$$String,
  name: CategoryActivity$CatalogPlugin.name,
  stateSchema: CategoryActivity$CatalogPlugin.stateSchema,
  subIdConfig: undefined
})({
  project: project$1
});

let mappings = [
  CategoryActivityMapping,
  ProductActivityMapping
];

let Target;

let moduleUrl = "@reventlessdev/online-shop-dcb-catalog/src/CategoryActivity/ReadModel/CategoryActivity_Projections.res.mjs";

export {
  Target,
  M,
  CategoryEvents,
  ProductEvents,
  CategoryActivityMapping,
  ProductActivityMapping,
  mappings,
  moduleUrl,
}
/* M Not a pure module */
