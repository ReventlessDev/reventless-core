// Generated by ReScript, PLEASE EDIT WITH CARE

import * as ProductDemand$CatalogPlugin from "../../../src/Product/StateViewSlice/ProductDemand.res.mjs";
import * as Projection_GWT$ReventlessGwt from "@reventlessdev/reventless-gwt/src/Projection_GWT.res.mjs";
import * as ProductDemand_Projection$CatalogPlugin from "../../../src/Product/StateViewSlice/ProductDemand_Projection.res.mjs";

let include = Projection_GWT$ReventlessGwt.Make({
  name: ProductDemand$CatalogPlugin.name,
  stateSchema: ProductDemand$CatalogPlugin.stateSchema,
  consumedEventSchema: ProductDemand$CatalogPlugin.consumedEventSchema,
  subIdConfig: undefined
})({
  project: ProductDemand_Projection$CatalogPlugin.project
});

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenStateWithId = include.thenStateWithId;

describe("ProductDemand StateViewSlice", () => {
  test("ProductAdded initialises a row with orderCount = 0", undefined, () => thenStateWithId(whenEvent(givenEvents([]), {
    TAG: "ProductAdded",
    productId: "p1",
    name: "Laptop"
  }), "p1", {
    productId: "p1",
    name: "Laptop",
    orderCount: 0
  }));
  test("ProductDemandRecorded increments orderCount", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "ProductAdded",
      productId: "p1",
      name: "Laptop"
    }]), {
    TAG: "ProductDemandRecorded",
    productId: "p1"
  }), "p1", {
    productId: "p1",
    name: "Laptop",
    orderCount: 1
  }));
  test("ProductDemandRevoked decrements orderCount", undefined, () => thenStateWithId(whenEvent(givenEvents([
    {
      TAG: "ProductAdded",
      productId: "p1",
      name: "Laptop"
    },
    {
      TAG: "ProductDemandRecorded",
      productId: "p1"
    },
    {
      TAG: "ProductDemandRecorded",
      productId: "p1"
    }
  ]), {
    TAG: "ProductDemandRevoked",
    productId: "p1"
  }), "p1", {
    productId: "p1",
    name: "Laptop",
    orderCount: 1
  }));
  test("ProductDemandRevoked clamps orderCount at zero", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "ProductAdded",
      productId: "p1",
      name: "Laptop"
    }]), {
    TAG: "ProductDemandRevoked",
    productId: "p1"
  }), "p1", {
    productId: "p1",
    name: "Laptop",
    orderCount: 0
  }));
});

let Spec = include.Spec;

let describeWithId = include.describeWithId;

let todo = include.todo;

let whenEvents = include.whenEvents;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenState = include.thenState;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  Spec,
  describe,
  describeWithId,
  todo,
  test,
  givenEvents,
  whenEvent,
  whenEvents,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
