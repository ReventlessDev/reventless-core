// Generated by ReScript, PLEASE EDIT WITH CARE


let initialState_productIds = [];

let initialState = {
  exists: false,
  shipped: false,
  cancelled: false,
  productIds: initialState_productIds
};

function evolve(state, event) {
  if (typeof event !== "object") {
    if (event === "OrderShipped") {
      return {
        exists: state.exists,
        shipped: true,
        cancelled: state.cancelled,
        productIds: state.productIds
      };
    } else {
      return {
        exists: state.exists,
        shipped: state.shipped,
        cancelled: true,
        productIds: state.productIds
      };
    }
  } else {
    return {
      exists: true,
      shipped: false,
      cancelled: false,
      productIds: event.productIds
    };
  }
}

function decide(state, command) {
  if (state.exists) {
    if (state.shipped) {
      return {
        TAG: "Error",
        _0: "OrderAlreadyShipped"
      };
    } else if (state.cancelled) {
      return {
        TAG: "Ok",
        _0: []
      };
    } else {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "OrderCancelled",
            orderId: command.orderId,
            productIds: state.productIds
          }]
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: "OrderNotFound"
    };
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Order/StateChangeSlice/CancelOrder_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
