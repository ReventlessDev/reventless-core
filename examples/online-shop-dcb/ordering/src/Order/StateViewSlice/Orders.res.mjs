// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let lifecycleSchema = S.union([
  S.literal("Placed"),
  S.literal("Shipped"),
  S.literal("Cancelled")
]);

let stateSchema = S.schema(s => ({
  orderId: s.m(S.string),
  customerId: s.m(S.string),
  productIds: s.m(S.array(S.string)),
  lifecycle: s.m(lifecycleSchema)
}));

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "OrderPlaced",
    orderId: s.m(DcbTag$Reventless.string),
    customerId: s.m(DcbTag$Reventless.string),
    productIds: s.m(S.array(DcbTag$Reventless.stringForKey("productId")))
  })),
  S.schema(s => ({
    TAG: "OrderShipped",
    orderId: s.m(DcbTag$Reventless.string)
  })),
  S.schema(s => ({
    TAG: "OrderCancelled",
    orderId: s.m(DcbTag$Reventless.string)
  }))
]);

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "Orders";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Order/StateViewSlice/Orders.res.mjs";

let authorization = "AllowAuthenticated";

let visibility = "Public";

export {
  name,
  Id,
  lifecycleSchema,
  stateSchema,
  consumedEventSchema,
  config,
  subIdConfig,
  moduleUrl,
  authorization,
  visibility,
}
/* lifecycleSchema Not a pure module */
