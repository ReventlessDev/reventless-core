// Generated by ReScript, PLEASE EDIT WITH CARE


function project(param) {
  let event = param.event;
  switch (event.TAG) {
    case "OrderPlaced" :
      let orderId = event.orderId;
      return [{
          TAG: "Set",
          _0: orderId,
          _1: {
            orderId: orderId,
            customerId: event.customerId,
            productIds: event.productIds,
            lifecycle: "Placed"
          }
        }];
    case "OrderShipped" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productIds: state.productIds,
            lifecycle: "Shipped"
          })
        }];
    case "OrderCancelled" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productIds: state.productIds,
            lifecycle: "Cancelled"
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Order/StateViewSlice/Orders_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
