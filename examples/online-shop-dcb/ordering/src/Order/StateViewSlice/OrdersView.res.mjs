// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as OrderingEventLog$OrderingPlugin from "../../Plugin/OrderingEventLog.res.mjs";

let stateSchema = S.schema(s => ({
  orderId: s.m(S.string),
  customerId: s.m(S.string),
  productIds: s.m(S.array(S.string)),
  status: s.m(S.string)
}));

function project(param, event) {
  switch (event.TAG) {
    case "OrderPlaced" :
      let orderId = event.orderId;
      return [{
          TAG: "Set",
          _0: orderId,
          _1: {
            orderId: orderId,
            customerId: event.customerId,
            productIds: event.productIds,
            status: "placed"
          }
        }];
    case "OrderShipped" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productIds: state.productIds,
            status: "shipped"
          })
        }];
    case "OrderCancelled" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productIds: state.productIds,
            status: "cancelled"
          })
        }];
    default:
      return [];
  }
}

let name = "OrdersView";

let DcbEventLogSpec;

let eventSchema = OrderingEventLog$OrderingPlugin.eventSchema;

export {
  name,
  DcbEventLogSpec,
  eventSchema,
  stateSchema,
  project,
}
/* stateSchema Not a pure module */
