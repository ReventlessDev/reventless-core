// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let stateSchema = S.schema(s => ({
  orderId: s.m(S.string),
  customerId: s.m(S.string),
  productIds: s.m(S.array(S.string)),
  status: s.m(S.string)
}));

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "OrderPlaced",
    orderId: s.m(DcbTag$Reventless.string),
    customerId: s.m(DcbTag$Reventless.string),
    productIds: s.m(S.array(DcbTag$Reventless.string))
  })),
  S.schema(s => ({
    TAG: "OrderShipped",
    orderId: s.m(DcbTag$Reventless.string)
  })),
  S.schema(s => ({
    TAG: "OrderCancelled",
    orderId: s.m(DcbTag$Reventless.string)
  }))
]);

function project(event) {
  switch (event.TAG) {
    case "OrderPlaced" :
      let orderId = event.orderId;
      return [{
          TAG: "Set",
          _0: orderId,
          _1: {
            orderId: orderId,
            customerId: event.customerId,
            productIds: event.productIds,
            status: "placed"
          }
        }];
    case "OrderShipped" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productIds: state.productIds,
            status: "shipped"
          })
        }];
    case "OrderCancelled" :
      return [{
          TAG: "Update",
          _0: event.orderId,
          _1: state => ({
            orderId: state.orderId,
            customerId: state.customerId,
            productIds: state.productIds,
            status: "cancelled"
          })
        }];
  }
}

let name = "Orders";

let Id;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Order/StateViewSlice/OrdersView.res.mjs";

export {
  name,
  Id,
  stateSchema,
  consumedEventSchema,
  project,
  moduleUrl,
}
/* stateSchema Not a pure module */
