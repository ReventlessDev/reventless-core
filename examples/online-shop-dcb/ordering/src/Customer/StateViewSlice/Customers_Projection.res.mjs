// Generated by ReScript, PLEASE EDIT WITH CARE


function project(event) {
  switch (event.TAG) {
    case "CustomerRegistered" :
      let customerId = event.customerId;
      return [{
          TAG: "Set",
          _0: customerId,
          _1: {
            customerId: customerId,
            email: event.email,
            address: event.address,
            deactivated: false
          }
        }];
    case "EmailChanged" :
      let email = event.email;
      return [{
          TAG: "Update",
          _0: event.customerId,
          _1: state => ({
            customerId: state.customerId,
            email: email,
            address: state.address,
            deactivated: state.deactivated
          })
        }];
    case "AddressChanged" :
      let address = event.address;
      return [{
          TAG: "Update",
          _0: event.customerId,
          _1: state => ({
            customerId: state.customerId,
            email: state.email,
            address: address,
            deactivated: state.deactivated
          })
        }];
    case "CustomerDeactivated" :
      return [{
          TAG: "Update",
          _0: event.customerId,
          _1: state => ({
            customerId: state.customerId,
            email: state.email,
            address: state.address,
            deactivated: true
          })
        }];
  }
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateViewSlice/Customers_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
