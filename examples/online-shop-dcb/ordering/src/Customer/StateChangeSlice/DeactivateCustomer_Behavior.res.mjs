// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event === "CustomerRegistered") {
    return {
      exists: true,
      deactivated: false
    };
  } else {
    return {
      exists: state.exists,
      deactivated: true
    };
  }
}

function decide(state, command) {
  if (state.exists) {
    if (state.deactivated) {
      return {
        TAG: "Ok",
        _0: []
      };
    } else {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "CustomerDeactivated",
            customerId: command.customerId
          }]
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: "CustomerNotFound"
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  deactivated: false
};

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateChangeSlice/DeactivateCustomer_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
