// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(_state, event) {
  return {
    exists: true
  };
}

function decide(state, command) {
  if (state.exists) {
    return {
      TAG: "Error",
      _0: "CustomerAlreadyRegistered"
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "CustomerRegistered",
          customerId: command.customerId,
          email: command.email,
          address: command.address
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false
};

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateChangeSlice/RegisterCustomer_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
