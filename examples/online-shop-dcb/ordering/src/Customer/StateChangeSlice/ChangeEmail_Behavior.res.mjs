// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (typeof event !== "object") {
    return {
      exists: state.exists,
      deactivated: true,
      currentEmail: state.currentEmail
    };
  } else if (event.TAG === "CustomerRegistered") {
    return {
      exists: true,
      deactivated: false,
      currentEmail: event.email
    };
  } else {
    return {
      exists: state.exists,
      deactivated: state.deactivated,
      currentEmail: event.email
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "CustomerNotFound"
    };
  }
  if (state.deactivated) {
    return {
      TAG: "Error",
      _0: "CustomerAlreadyDeactivated"
    };
  }
  let email = command.email;
  if (email === state.currentEmail) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "EmailChanged",
          customerId: command.customerId,
          email: email
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  deactivated: false,
  currentEmail: ""
};

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateChangeSlice/ChangeEmail_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
