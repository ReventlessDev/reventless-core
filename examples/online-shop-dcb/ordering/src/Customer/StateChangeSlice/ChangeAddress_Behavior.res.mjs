// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (typeof event !== "object") {
    return {
      exists: state.exists,
      deactivated: true,
      currentAddress: state.currentAddress
    };
  } else if (event.TAG === "CustomerRegistered") {
    return {
      exists: true,
      deactivated: false,
      currentAddress: event.address
    };
  } else {
    return {
      exists: state.exists,
      deactivated: state.deactivated,
      currentAddress: event.address
    };
  }
}

function decide(state, command) {
  if (!state.exists) {
    return {
      TAG: "Error",
      _0: "CustomerNotFound"
    };
  }
  if (state.deactivated) {
    return {
      TAG: "Error",
      _0: "CustomerAlreadyDeactivated"
    };
  }
  let address = command.address;
  if (address === state.currentAddress) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "AddressChanged",
          customerId: command.customerId,
          address: address
        }]
    };
  }
}

let Spec;

let initialState = {
  exists: false,
  deactivated: false,
  currentAddress: ""
};

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateChangeSlice/ChangeAddress_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
