// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let consumedEventSchema = S.union([
  S.schema(s => ({
    TAG: "CustomerRegistered",
    email: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "EmailChanged",
    email: s.m(S.string)
  })),
  S.literal("CustomerDeactivated")
]);

let commandSchema = S.schema(s => ({
  TAG: "ChangeEmail",
  customerId: s.m(DcbTag$Reventless.string),
  email: s.m(S.string)
}));

let errorSchema = S.union([
  S.literal("CustomerNotFound"),
  S.literal("CustomerAlreadyDeactivated")
]);

let eventSchema = S.schema(s => ({
  TAG: "EmailChanged",
  customerId: s.m(DcbTag$Reventless.string),
  email: s.m(S.string)
}));

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

let name = "ChangeEmail";

let Id;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateChangeSlice/ChangeEmail.res.mjs";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  moduleUrl,
  commandAuthorization,
}
/* consumedEventSchema Not a pure module */
