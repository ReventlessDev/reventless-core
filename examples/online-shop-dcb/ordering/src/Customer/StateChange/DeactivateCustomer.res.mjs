// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as CustomerId$OrderingPlugin from "../CustomerId.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.literal("CustomerRegistered"),
  Sury.literal("CustomerDeactivated")
]);

let commandSchema = Sury.$schema(s => ({
  TAG: "DeactivateCustomer",
  customerId: s.m(DcbTag$Reventless.mark(CustomerId$OrderingPlugin.schema))
}));

let errorSchema = Sury.literal("CustomerNotFound");

let eventSchema = Sury.$schema(s => ({
  TAG: "CustomerDeactivated",
  customerId: s.m(DcbTag$Reventless.mark(CustomerId$OrderingPlugin.schema))
}));

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Undeclared";
}

let traits = [];

let name = "DeactivateCustomer";

let Id;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/Customer/StateChange/DeactivateCustomer.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
  traits,
}
/* consumedEventSchema Not a pure module */
