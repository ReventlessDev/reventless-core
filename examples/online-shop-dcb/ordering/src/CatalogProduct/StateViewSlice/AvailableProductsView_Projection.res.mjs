// Generated by ReScript, PLEASE EDIT WITH CARE


function project(event) {
  if (event.TAG === "CatalogProductSynced") {
    let productId = event.productId;
    return [{
        TAG: "Set",
        _0: productId,
        _1: {
          productId: productId,
          name: event.name,
          price: event.price
        }
      }];
  }
  let price = event.price;
  return [{
      TAG: "Update",
      _0: event.productId,
      _1: p => ({
        productId: p.productId,
        name: p.name,
        price: price
      })
    }];
}

let Spec;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/CatalogProduct/StateViewSlice/AvailableProductsView_Projection.res.mjs";

export {
  Spec,
  project,
  moduleUrl,
}
/* No side effect */
