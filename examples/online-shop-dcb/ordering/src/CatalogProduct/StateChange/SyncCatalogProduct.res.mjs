// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as DcbTag$Reventless from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";
import * as ProductId$CatalogSpec from "@reventlessdev/online-shop-dcb-catalog-spec/src/ProductId.res.mjs";

let consumedEventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "CatalogProductSynced",
    name: s.m(Sury.string),
    price: s.m(Sury.float)
  })),
  Sury.$schema(s => ({
    TAG: "CatalogProductPriceChanged",
    price: s.m(Sury.float)
  }))
]);

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "SyncNewProduct",
    productId: s.m(DcbTag$Reventless.mark(ProductId$CatalogSpec.schema)),
    name: s.m(Sury.string),
    price: s.m(Sury.float)
  })),
  Sury.$schema(s => ({
    TAG: "ChangeSyncedPrice",
    productId: s.m(DcbTag$Reventless.mark(ProductId$CatalogSpec.schema)),
    price: s.m(Sury.float)
  }))
]);

let errorSchema = Sury.$unit;

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "CatalogProductSynced",
    productId: s.m(DcbTag$Reventless.mark(ProductId$CatalogSpec.schema)),
    name: s.m(Sury.string),
    price: s.m(Sury.float)
  })),
  Sury.$schema(s => ({
    TAG: "CatalogProductPriceChanged",
    productId: s.m(DcbTag$Reventless.mark(ProductId$CatalogSpec.schema)),
    price: s.m(Sury.float)
  }))
]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Undeclared";
}

let traits = [];

let name = "SyncCatalogProduct";

let Id;

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/CatalogProduct/StateChange/SyncCatalogProduct.res.mjs";

let readConsistency = "EscalateOnRetry";

export {
  name,
  Id,
  consumedEventSchema,
  commandSchema,
  errorSchema,
  eventSchema,
  moduleUrl,
  commandAuthorization,
  readConsistency,
  commandTransition,
  traits,
}
/* consumedEventSchema Not a pure module */
