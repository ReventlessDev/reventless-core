// Generated by ReScript, PLEASE EDIT WITH CARE


function evolve(state, event) {
  if (event.TAG === "CatalogProductSynced") {
    return {
      name: event.name,
      price: event.price
    };
  } else {
    return {
      name: state.name,
      price: event.price
    };
  }
}

function decide(state, command) {
  if (command.TAG === "SyncNewProduct") {
    let price = command.price;
    let name = command.name;
    if (state.name === name && state.price === price) {
      return {
        TAG: "Ok",
        _0: []
      };
    } else {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "CatalogProductSynced",
            productId: command.productId,
            name: name,
            price: price
          }]
      };
    }
  }
  let price$1 = command.price;
  if (state.price === price$1) {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "CatalogProductPriceChanged",
          productId: command.productId,
          price: price$1
        }]
    };
  }
}

let Spec;

let initialState = {
  name: "",
  price: 0.0
};

let moduleUrl = "@reventlessdev/online-shop-dcb-ordering/src/CatalogProduct/StateChangeSlice/SyncCatalogProduct_Behavior.res.mjs";

export {
  Spec,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* No side effect */
