// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Customers$OrderingPlugin from "../../../src/Customer/StateViewSlice/Customers.res.mjs";
import * as Projection_GWT$ReventlessGwt from "@reventlessdev/reventless-gwt/src/Projection_GWT.res.mjs";
import * as Customers_Projection$OrderingPlugin from "../../../src/Customer/StateViewSlice/Customers_Projection.res.mjs";

let include = Projection_GWT$ReventlessGwt.Make({
  name: Customers$OrderingPlugin.name,
  stateSchema: Customers$OrderingPlugin.stateSchema,
  consumedEventSchema: Customers$OrderingPlugin.consumedEventSchema,
  subIdConfig: undefined
})({
  project: Customers_Projection$OrderingPlugin.project
});

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenStateWithId = include.thenStateWithId;

describe("Customers StateViewSlice", () => {
  test("CustomerRegistered creates a row", undefined, () => thenStateWithId(whenEvent(givenEvents([]), {
    TAG: "CustomerRegistered",
    customerId: "c1",
    email: "alice@x.y",
    address: "123 Main"
  }), "c1", {
    customerId: "c1",
    email: "alice@x.y",
    address: "123 Main",
    deactivated: false
  }));
  test("EmailChanged updates the email", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "CustomerRegistered",
      customerId: "c1",
      email: "alice@x.y",
      address: "123 Main"
    }]), {
    TAG: "EmailChanged",
    customerId: "c1",
    email: "alice2@x.y"
  }), "c1", {
    customerId: "c1",
    email: "alice2@x.y",
    address: "123 Main",
    deactivated: false
  }));
  test("AddressChanged updates the address", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "CustomerRegistered",
      customerId: "c1",
      email: "alice@x.y",
      address: "123 Main"
    }]), {
    TAG: "AddressChanged",
    customerId: "c1",
    address: "789 Pine"
  }), "c1", {
    customerId: "c1",
    email: "alice@x.y",
    address: "789 Pine",
    deactivated: false
  }));
  test("CustomerDeactivated sets deactivated flag", undefined, () => thenStateWithId(whenEvent(givenEvents([{
      TAG: "CustomerRegistered",
      customerId: "c1",
      email: "alice@x.y",
      address: "123 Main"
    }]), {
    TAG: "CustomerDeactivated",
    customerId: "c1"
  }), "c1", {
    customerId: "c1",
    email: "alice@x.y",
    address: "123 Main",
    deactivated: true
  }));
});

let Spec = include.Spec;

let describeWithId = include.describeWithId;

let whenEvents = include.whenEvents;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenState = include.thenState;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  Spec,
  describe,
  describeWithId,
  test,
  givenEvents,
  whenEvent,
  whenEvents,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
