// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

let commandSchema = S.union([
  S.schema(s => ({
    TAG: "AddProduct",
    productId: s.m(S.string),
    name: s.m(S.string),
    description: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "UpdateProductName",
    productId: s.m(S.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "UpdateProductDescription",
    productId: s.m(S.string),
    description: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "UpdateProductPrice",
    productId: s.m(S.string),
    price: s.m(S.float)
  }))
]);

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "ProductAdded",
    productId: s.m(S.string),
    name: s.m(S.string),
    description: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "ProductNameUpdated",
    productId: s.m(S.string),
    name: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ProductDescriptionUpdated",
    productId: s.m(S.string),
    description: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "ProductPriceUpdated",
    productId: s.m(S.string),
    price: s.m(S.float)
  }))
]);

let errorSchema = S.union([
  S.literal("ProductAlreadyExists"),
  S.literal("ProductNotFound")
]);

let Id;

let name = "Product";

export {
  Id,
  name,
  commandSchema,
  eventSchema,
  errorSchema,
}
/* commandSchema Not a pure module */
