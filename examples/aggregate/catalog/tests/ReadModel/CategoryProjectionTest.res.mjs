// Generated by ReScript, PLEASE EDIT WITH CARE

import * as ProjectionTest$Reventless from "@reventlessdev/reventless/test-helper/ProjectionTest.res.mjs";
import * as CategoriesProjections$ReventlessdevAggregateExampleCatalog from "../../src/ReadModel/CategoriesProjections.res.mjs";

let include = ProjectionTest$Reventless.Make(CategoriesProjections$ReventlessdevAggregateExampleCatalog.CategoryMapping);

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenState = include.thenState;

describe("CategoryProjection:", () => {
  test("CategoryAdded sets initial read model state", undefined, () => thenState(whenEvent(givenEvents([]), {
    TAG: "CategoryAdded",
    categoryId: "cat-1",
    name: "Electronics"
  }), {
    categoryId: "cat-1",
    name: "Electronics",
    archived: false
  }));
  test("CategoryRenamed after creation updates name", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "CategoryAdded",
      categoryId: "cat-1",
      name: "Electronics"
    }]), {
    TAG: "CategoryRenamed",
    categoryId: "cat-1",
    name: "Consumer Electronics"
  }), {
    categoryId: "cat-1",
    name: "Consumer Electronics",
    archived: false
  }));
  test("CategoryArchived after creation sets archived flag", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "CategoryAdded",
      categoryId: "cat-1",
      name: "Electronics"
    }]), {
    TAG: "CategoryArchived",
    categoryId: "cat-1"
  }), {
    categoryId: "cat-1",
    name: "Electronics",
    archived: true
  }));
});

let describeWithId = include.describeWithId;

let givenEventsWithTime = include.givenEventsWithTime;

let whenEvents = include.whenEvents;

let whenEventWithTime = include.whenEventWithTime;

let whenEventsWithTime = include.whenEventsWithTime;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenStateWithId = include.thenStateWithId;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  describe,
  describeWithId,
  test,
  givenEvents,
  givenEventsWithTime,
  whenEvent,
  whenEvents,
  whenEventWithTime,
  whenEventsWithTime,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
