// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Platform$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/Platform.res.mjs";
import * as TestRunner$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/test/TestRunner.res.mjs";
import * as GraphQL_Server$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/adapter/GraphQL_Server.res.mjs";
import * as InMemory_PluginSpec$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/adapter/InMemory_PluginSpec.res.mjs";
import * as CatalogPlugin$ReventlessdevExampleAggregateCatalog from "@reventlessdev/example-aggregate-catalog/src/CatalogPlugin.res.mjs";
import * as OrderingPlugin$ReventlessdevExampleAggregateOrdering from "@reventlessdev/example-aggregate-ordering/src/OrderingPlugin.res.mjs";

TestRunner$ReventlessInMemory.setup();

let Platform = Platform$ReventlessInMemory.Make({});

let Catalog = CatalogPlugin$ReventlessdevExampleAggregateCatalog.Make(Platform);

let Ordering = OrderingPlugin$ReventlessdevExampleAggregateOrdering.Make(Platform);

let scheduler = Platform.makeScheduler();

let catalogPlugin = Catalog.make(scheduler, undefined, undefined);

let orderingPlugin = Ordering.make(scheduler, undefined, undefined);

let core = Platform.Core.make("1.0.0", [], [], [], scheduler, undefined, undefined, InMemory_PluginSpec$ReventlessInMemory.resourceNaming, undefined);

Platform.makePlatform(undefined, core, [
  catalogPlugin,
  orderingPlugin
]);

if (Stdlib_Option.isSome(process.env["GRAPHQL_DEBUG"])) {
  GraphQL_Server$ReventlessInMemory.printDiagnostics();
}

export {
  Platform,
  Catalog,
  Ordering,
  scheduler,
  catalogPlugin,
  orderingPlugin,
  core,
}
/*  Not a pure module */
