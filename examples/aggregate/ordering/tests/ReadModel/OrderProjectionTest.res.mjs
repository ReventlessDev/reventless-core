// Generated by ReScript, PLEASE EDIT WITH CARE

import * as ProjectionTest$Reventless from "@reventlessdev/reventless/test-helper/ProjectionTest.res.mjs";
import * as OrdersProjections$ReventlessdevExampleAggregateOrdering from "../../src/ReadModel/OrdersProjections.res.mjs";

let include = ProjectionTest$Reventless.Make(OrdersProjections$ReventlessdevExampleAggregateOrdering.OrderMapping);

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenState = include.thenState;

describe("OrderProjection:", () => {
  test("OrderPlaced sets initial read model state", undefined, () => thenState(whenEvent(givenEvents([]), {
    TAG: "OrderPlaced",
    orderId: "ord-1",
    customerId: "cust-1",
    productIds: [
      "prod-1",
      "prod-2"
    ]
  }), {
    orderId: "ord-1",
    customerId: "cust-1",
    productIds: [
      "prod-1",
      "prod-2"
    ],
    status: "placed"
  }));
  test("OrderShipped after placing updates status to shipped", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "OrderPlaced",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: ["prod-1"]
    }]), {
    TAG: "OrderShipped",
    orderId: "ord-1"
  }), {
    orderId: "ord-1",
    customerId: "cust-1",
    productIds: ["prod-1"],
    status: "shipped"
  }));
  test("OrderCancelled after placing updates status to cancelled", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "OrderPlaced",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: ["prod-1"]
    }]), {
    TAG: "OrderCancelled",
    orderId: "ord-1"
  }), {
    orderId: "ord-1",
    customerId: "cust-1",
    productIds: ["prod-1"],
    status: "cancelled"
  }));
});

let describeWithId = include.describeWithId;

let givenEventsWithTime = include.givenEventsWithTime;

let whenEvents = include.whenEvents;

let whenEventWithTime = include.whenEventWithTime;

let whenEventsWithTime = include.whenEventsWithTime;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenStateWithId = include.thenStateWithId;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  describe,
  describeWithId,
  test,
  givenEvents,
  givenEventsWithTime,
  whenEvent,
  whenEvents,
  whenEventWithTime,
  whenEventsWithTime,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
