// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Message$Reventless from "@reventlessdev/reventless-spec/src/types/Message.res.mjs";
import * as TestRunner$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/test/TestRunner.res.mjs";
import * as CommandTopic$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/reexport/CommandTopic.res.mjs";
import * as InMemory_Bus$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/adapter/InMemory_Bus.res.mjs";
import * as DcbEventLog_Builder$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/components/DcbEventLog_Builder.res.mjs";
import * as ShipOrder$ReventlessdevExampleDcbOrdering from "../../src/Order/StateChangeSlice/ShipOrder.res.mjs";
import * as PlaceOrder$ReventlessdevExampleDcbOrdering from "../../src/Order/StateChangeSlice/PlaceOrder.res.mjs";
import * as CancelOrder$ReventlessdevExampleDcbOrdering from "../../src/Order/StateChangeSlice/CancelOrder.res.mjs";
import * as StateChangeSlice_Builder$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/components/StateChangeSlice_Builder.res.mjs";
import * as UpdateEmail$ReventlessdevExampleDcbOrdering from "../../src/Customer/StateChangeSlice/UpdateEmail.res.mjs";
import * as UpdateAddress$ReventlessdevExampleDcbOrdering from "../../src/Customer/StateChangeSlice/UpdateAddress.res.mjs";
import * as OrderingEventLog$ReventlessdevExampleDcbOrdering from "../../src/Plugin/OrderingEventLog.res.mjs";
import * as RegisterCustomer$ReventlessdevExampleDcbOrdering from "../../src/Customer/StateChangeSlice/RegisterCustomer.res.mjs";
import * as DeactivateCustomer$ReventlessdevExampleDcbOrdering from "../../src/Customer/StateChangeSlice/DeactivateCustomer.res.mjs";

let Bus = InMemory_Bus$ReventlessInMemory.Make({});

let capturedEventCount = {
  contents: 0
};

Bus.subscribeToEvents("OrderingEventTopic", async (param, param$1, param$2) => {
  capturedEventCount.contents = capturedEventCount.contents + 1 | 0;
});

TestRunner$ReventlessInMemory.setup();

let DcbEventLogMaker = DcbEventLog_Builder$ReventlessInMemory.Make(Bus);

let OrderingEventLogMaker = DcbEventLogMaker.Make(OrderingEventLog$ReventlessdevExampleDcbOrdering);

let eventLog = OrderingEventLogMaker.make("Ordering", undefined);

let RegisterCustomerMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: RegisterCustomer$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: RegisterCustomer$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: RegisterCustomer$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: RegisterCustomer$ReventlessdevExampleDcbOrdering.reduce,
  decide: RegisterCustomer$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: RegisterCustomer$ReventlessdevExampleDcbOrdering.commandSchema
});

let UpdateEmailMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: UpdateEmail$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: UpdateEmail$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: UpdateEmail$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: UpdateEmail$ReventlessdevExampleDcbOrdering.reduce,
  decide: UpdateEmail$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: UpdateEmail$ReventlessdevExampleDcbOrdering.commandSchema
});

let UpdateAddressMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: UpdateAddress$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: UpdateAddress$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: UpdateAddress$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: UpdateAddress$ReventlessdevExampleDcbOrdering.reduce,
  decide: UpdateAddress$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: UpdateAddress$ReventlessdevExampleDcbOrdering.commandSchema
});

let DeactivateCustomerMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: DeactivateCustomer$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: DeactivateCustomer$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: DeactivateCustomer$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: DeactivateCustomer$ReventlessdevExampleDcbOrdering.reduce,
  decide: DeactivateCustomer$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: DeactivateCustomer$ReventlessdevExampleDcbOrdering.commandSchema
});

let PlaceOrderMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: PlaceOrder$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: PlaceOrder$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: PlaceOrder$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: PlaceOrder$ReventlessdevExampleDcbOrdering.reduce,
  decide: PlaceOrder$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: PlaceOrder$ReventlessdevExampleDcbOrdering.commandSchema
});

let ShipOrderMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: ShipOrder$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: ShipOrder$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: ShipOrder$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: ShipOrder$ReventlessdevExampleDcbOrdering.reduce,
  decide: ShipOrder$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: ShipOrder$ReventlessdevExampleDcbOrdering.commandSchema
});

let CancelOrderMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: CancelOrder$ReventlessdevExampleDcbOrdering.name,
  DcbEventLogSpec: OrderingEventLog$ReventlessdevExampleDcbOrdering,
  errorSchema: CancelOrder$ReventlessdevExampleDcbOrdering.errorSchema,
  initialDecisionModel: CancelOrder$ReventlessdevExampleDcbOrdering.initialDecisionModel,
  reduce: CancelOrder$ReventlessdevExampleDcbOrdering.reduce,
  decide: CancelOrder$ReventlessdevExampleDcbOrdering.decide,
  commandSchema: CancelOrder$ReventlessdevExampleDcbOrdering.commandSchema
});

async function publishJsons(cmdJsons) {
  await Promise.all(cmdJsons.map(async cmdJson => {
    let dict = cmdJson.commandJson;
    let typeName;
    typeName = typeof dict === "object" && dict !== null && !Array.isArray(dict) ? Stdlib_Option.getOr(Stdlib_Option.flatMap(dict["TAG"], j => {
        if (typeof j === "string") {
          return j;
        }
      }), "") : "";
    let fullBody = Object.fromEntries([
      [
        "id",
        cmdJson.id
      ],
      [
        "meta",
        S.reverseConvertToJsonOrThrow(cmdJson.meta, Message$Reventless.metaSchema)
      ],
      [
        "command",
        cmdJson.commandJson
      ]
    ]);
    let handlers = CommandTopic$ReventlessInMemory.getHandlers(typeName);
    await Promise.all(handlers.map(async entry => {
      await entry.handler([{
          command: fullBody,
          reference: cmdJson.id
        }]);
    }));
  }));
}

let publishJsonsOutput = Pulumi.output(publishJsons);

let _registerCustomerSlice = RegisterCustomerMaker.make(eventLog, publishJsonsOutput, undefined);

let _updateEmailSlice = UpdateEmailMaker.make(eventLog, publishJsonsOutput, undefined);

let _updateAddressSlice = UpdateAddressMaker.make(eventLog, publishJsonsOutput, undefined);

let _deactivateCustomerSlice = DeactivateCustomerMaker.make(eventLog, publishJsonsOutput, undefined);

let _placeOrderSlice = PlaceOrderMaker.make(eventLog, publishJsonsOutput, undefined);

let _shipOrderSlice = ShipOrderMaker.make(eventLog, publishJsonsOutput, undefined);

let _cancelOrderSlice = CancelOrderMaker.make(eventLog, publishJsonsOutput, undefined);

let testMeta = {
  service: "example-dcb-ordering-test",
  time: "2024-01-01T00:00:00.000Z",
  ip: "127.0.0.1",
  user: "testuser",
  msgId: "msg-001",
  correlationId: "corr-001"
};

async function dispatch(commandJson, entityId) {
  return await publishJsons([{
      id: entityId,
      meta: testMeta,
      commandJson: commandJson
    }]);
}

describe("Ordering DCB E2E:", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(OrderingEventLogMaker.operations(eventLog));
  });
  beforeEach(() => {
    capturedEventCount.contents = 0;
  });
  test("RegisterCustomer publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "RegisterCustomer",
      customerId: "cust-1",
      email: "alice@example.com",
      address: "123 Main St"
    }, RegisterCustomer$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "cust-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate RegisterCustomer produces 0 events (CustomerAlreadyRegistered)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "RegisterCustomer",
      customerId: "cust-1",
      email: "duplicate@example.com",
      address: "Dup"
    }, RegisterCustomer$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "cust-1");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("UpdateEmail on existing customer publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "UpdateEmail",
      customerId: "cust-1",
      email: "alice2@example.com"
    }, UpdateEmail$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "cust-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("DeactivateCustomer publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "DeactivateCustomer",
      customerId: "cust-1"
    }, DeactivateCustomer$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "cust-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate DeactivateCustomer is idempotent (0 events)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "DeactivateCustomer",
      customerId: "cust-1"
    }, DeactivateCustomer$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "cust-1");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("PlaceOrder publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: [
        "prod-1",
        "prod-2"
      ]
    }, PlaceOrder$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "ord-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate PlaceOrder produces 0 events (OrderAlreadyPlaced)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "PlaceOrder",
      orderId: "ord-1",
      customerId: "cust-1",
      productIds: ["prod-1"]
    }, PlaceOrder$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "ord-1");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("ShipOrder on placed order publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "ShipOrder",
      orderId: "ord-1"
    }, ShipOrder$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "ord-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate ShipOrder is idempotent (0 events)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "ShipOrder",
      orderId: "ord-1"
    }, ShipOrder$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "ord-1");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("CancelOrder on non-existent order produces 0 events (OrderNotFound)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "CancelOrder",
      orderId: "no-such-order"
    }, CancelOrder$ReventlessdevExampleDcbOrdering.commandSchema);
    await dispatch(cmd, "no-such-order");
    expect(capturedEventCount.contents).toBe(0);
  });
});

export {
  Bus,
  capturedEventCount,
  DcbEventLogMaker,
  OrderingEventLogMaker,
  eventLog,
  RegisterCustomerMaker,
  UpdateEmailMaker,
  UpdateAddressMaker,
  DeactivateCustomerMaker,
  PlaceOrderMaker,
  ShipOrderMaker,
  CancelOrderMaker,
  publishJsons,
  publishJsonsOutput,
  _registerCustomerSlice,
  _updateEmailSlice,
  _updateAddressSlice,
  _deactivateCustomerSlice,
  _placeOrderSlice,
  _shipOrderSlice,
  _cancelOrderSlice,
  testMeta,
  dispatch,
}
/* Bus Not a pure module */
