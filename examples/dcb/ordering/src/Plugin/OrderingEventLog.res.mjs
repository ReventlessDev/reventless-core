// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as DcbTag$ReventlessSpec from "@reventlessdev/reventless-spec/src/components/DcbTag.res.mjs";

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "CustomerRegistered",
    customerId: s.m(DcbTag$ReventlessSpec.string),
    email: s.m(S.string),
    address: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "EmailUpdated",
    customerId: s.m(DcbTag$ReventlessSpec.string),
    email: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "AddressUpdated",
    customerId: s.m(DcbTag$ReventlessSpec.string),
    address: s.m(S.string)
  })),
  S.schema(s => ({
    TAG: "CustomerDeactivated",
    customerId: s.m(DcbTag$ReventlessSpec.string)
  })),
  S.schema(s => ({
    TAG: "OrderPlaced",
    orderId: s.m(DcbTag$ReventlessSpec.string),
    customerId: s.m(S.string),
    productIds: s.m(S.array(S.string))
  })),
  S.schema(s => ({
    TAG: "OrderShipped",
    orderId: s.m(DcbTag$ReventlessSpec.string)
  })),
  S.schema(s => ({
    TAG: "OrderCancelled",
    orderId: s.m(DcbTag$ReventlessSpec.string)
  }))
]);

export {
  eventSchema,
}
/* eventSchema Not a pure module */
