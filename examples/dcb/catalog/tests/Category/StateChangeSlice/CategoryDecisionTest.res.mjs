// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as AddCategory$ReventlessdevExampleDcbCatalog from "../../../src/Category/StateChangeSlice/AddCategory.res.mjs";
import * as RenameCategory$ReventlessdevExampleDcbCatalog from "../../../src/Category/StateChangeSlice/RenameCategory.res.mjs";
import * as ArchiveCategory$ReventlessdevExampleDcbCatalog from "../../../src/Category/StateChangeSlice/ArchiveCategory.res.mjs";

Jest.describe("AddCategory:", () => {
  Jest.describe("reduce", () => {
    Jest.test("CategoryAdded sets exists=true", () => Jest.Expect.toEqual(Jest.Expect.expect(AddCategory$ReventlessdevExampleDcbCatalog.reduce(AddCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "CategoryAdded",
      categoryId: "c1",
      name: "Electronics"
    })), {
      exists: true,
      archived: false
    }));
    Jest.test("CategoryArchived sets archived=true", () => Jest.Expect.toEqual(Jest.Expect.expect(AddCategory$ReventlessdevExampleDcbCatalog.reduce({
      exists: true,
      archived: false
    }, {
      TAG: "CategoryArchived",
      categoryId: "c1"
    })), {
      exists: true,
      archived: true
    }));
    Jest.test("Product events do not change model", () => Jest.Expect.toEqual(Jest.Expect.expect(AddCategory$ReventlessdevExampleDcbCatalog.reduce(AddCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "ProductAdded",
      productId: "p1",
      name: "Laptop",
      description: "A laptop",
      price: 999.99
    })), AddCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel));
  });
  Jest.describe("decide", () => {
    Jest.test("on non-existent category produces CategoryAdded", () => Jest.Expect.toEqual(Jest.Expect.expect(AddCategory$ReventlessdevExampleDcbCatalog.decide(AddCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "AddCategory",
      categoryId: "c1",
      name: "Electronics"
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "CategoryAdded",
          categoryId: "c1",
          name: "Electronics"
        }]
    }));
    Jest.test("on existing category returns CategoryAlreadyExists", () => Jest.Expect.toEqual(Jest.Expect.expect(AddCategory$ReventlessdevExampleDcbCatalog.decide({
      exists: true,
      archived: false
    }, {
      TAG: "AddCategory",
      categoryId: "c1",
      name: "Electronics"
    })), {
      TAG: "Error",
      _0: "CategoryAlreadyExists"
    }));
  });
});

Jest.describe("RenameCategory:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent category returns CategoryNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(RenameCategory$ReventlessdevExampleDcbCatalog.decide(RenameCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
    TAG: "RenameCategory",
    categoryId: "c1",
    name: "Consumer Electronics"
  })), {
    TAG: "Error",
    _0: "CategoryNotFound"
  }));
  Jest.test("on archived category returns CategoryAlreadyArchived", () => Jest.Expect.toEqual(Jest.Expect.expect(RenameCategory$ReventlessdevExampleDcbCatalog.decide({
    exists: true,
    archived: true
  }, {
    TAG: "RenameCategory",
    categoryId: "c1",
    name: "Consumer Electronics"
  })), {
    TAG: "Error",
    _0: "CategoryAlreadyArchived"
  }));
  Jest.test("on active category produces CategoryRenamed", () => Jest.Expect.toEqual(Jest.Expect.expect(RenameCategory$ReventlessdevExampleDcbCatalog.decide({
    exists: true,
    archived: false
  }, {
    TAG: "RenameCategory",
    categoryId: "c1",
    name: "Consumer Electronics"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "CategoryRenamed",
        categoryId: "c1",
        name: "Consumer Electronics"
      }]
  }));
}));

Jest.describe("ArchiveCategory:", () => Jest.describe("decide", () => {
  Jest.test("on non-existent category returns CategoryNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(ArchiveCategory$ReventlessdevExampleDcbCatalog.decide(ArchiveCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
    TAG: "ArchiveCategory",
    categoryId: "c1"
  })), {
    TAG: "Error",
    _0: "CategoryNotFound"
  }));
  Jest.test("on already archived category returns Ok([]) (idempotent)", () => Jest.Expect.toEqual(Jest.Expect.expect(ArchiveCategory$ReventlessdevExampleDcbCatalog.decide({
    exists: true,
    archived: true
  }, {
    TAG: "ArchiveCategory",
    categoryId: "c1"
  })), {
    TAG: "Ok",
    _0: []
  }));
  Jest.test("on active category produces CategoryArchived", () => Jest.Expect.toEqual(Jest.Expect.expect(ArchiveCategory$ReventlessdevExampleDcbCatalog.decide({
    exists: true,
    archived: false
  }, {
    TAG: "ArchiveCategory",
    categoryId: "c1"
  })), {
    TAG: "Ok",
    _0: [{
        TAG: "CategoryArchived",
        categoryId: "c1"
      }]
  }));
}));

/*  Not a pure module */
