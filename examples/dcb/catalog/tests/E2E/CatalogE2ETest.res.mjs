// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Pulumi from "@pulumi/pulumi";
import * as Message$Reventless from "@reventlessdev/reventless-spec/src/types/Message.res.mjs";
import * as TestRunner$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/test/TestRunner.res.mjs";
import * as CommandTopic$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/reexport/CommandTopic.res.mjs";
import * as InMemory_Bus$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/adapter/InMemory_Bus.res.mjs";
import * as DcbEventLog_Builder$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/components/DcbEventLog_Builder.res.mjs";
import * as AddProduct$ReventlessdevExampleDcbCatalog from "../../src/Product/StateChangeSlice/AddProduct.res.mjs";
import * as AddCategory$ReventlessdevExampleDcbCatalog from "../../src/Category/StateChangeSlice/AddCategory.res.mjs";
import * as StateChangeSlice_Builder$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/components/StateChangeSlice_Builder.res.mjs";
import * as RenameCategory$ReventlessdevExampleDcbCatalog from "../../src/Category/StateChangeSlice/RenameCategory.res.mjs";
import * as ArchiveCategory$ReventlessdevExampleDcbCatalog from "../../src/Category/StateChangeSlice/ArchiveCategory.res.mjs";
import * as CatalogEventLog$ReventlessdevExampleDcbCatalog from "../../src/Plugin/CatalogEventLog.res.mjs";
import * as UpdateProductName$ReventlessdevExampleDcbCatalog from "../../src/Product/StateChangeSlice/UpdateProductName.res.mjs";
import * as UpdateProductPrice$ReventlessdevExampleDcbCatalog from "../../src/Product/StateChangeSlice/UpdateProductPrice.res.mjs";
import * as UpdateProductDescription$ReventlessdevExampleDcbCatalog from "../../src/Product/StateChangeSlice/UpdateProductDescription.res.mjs";

let Bus = InMemory_Bus$ReventlessInMemory.Make({});

let capturedEventCount = {
  contents: 0
};

Bus.subscribeToEvents("CatalogEventTopic", async (param, param$1, param$2) => {
  capturedEventCount.contents = capturedEventCount.contents + 1 | 0;
});

TestRunner$ReventlessInMemory.setup();

let DcbEventLogMaker = DcbEventLog_Builder$ReventlessInMemory.Make(Bus);

let CatalogEventLogMaker = DcbEventLogMaker.Make(CatalogEventLog$ReventlessdevExampleDcbCatalog);

let eventLog = CatalogEventLogMaker.make("Catalog", undefined);

let AddProductMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: AddProduct$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: AddProduct$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: AddProduct$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: AddProduct$ReventlessdevExampleDcbCatalog.reduce,
  decide: AddProduct$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: AddProduct$ReventlessdevExampleDcbCatalog.commandSchema
});

let UpdateProductNameMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: UpdateProductName$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: UpdateProductName$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: UpdateProductName$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: UpdateProductName$ReventlessdevExampleDcbCatalog.reduce,
  decide: UpdateProductName$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: UpdateProductName$ReventlessdevExampleDcbCatalog.commandSchema
});

let UpdateProductDescriptionMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: UpdateProductDescription$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: UpdateProductDescription$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: UpdateProductDescription$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: UpdateProductDescription$ReventlessdevExampleDcbCatalog.reduce,
  decide: UpdateProductDescription$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: UpdateProductDescription$ReventlessdevExampleDcbCatalog.commandSchema
});

let UpdateProductPriceMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: UpdateProductPrice$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: UpdateProductPrice$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: UpdateProductPrice$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: UpdateProductPrice$ReventlessdevExampleDcbCatalog.reduce,
  decide: UpdateProductPrice$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: UpdateProductPrice$ReventlessdevExampleDcbCatalog.commandSchema
});

let AddCategoryMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: AddCategory$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: AddCategory$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: AddCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: AddCategory$ReventlessdevExampleDcbCatalog.reduce,
  decide: AddCategory$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: AddCategory$ReventlessdevExampleDcbCatalog.commandSchema
});

let RenameCategoryMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: RenameCategory$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: RenameCategory$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: RenameCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: RenameCategory$ReventlessdevExampleDcbCatalog.reduce,
  decide: RenameCategory$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: RenameCategory$ReventlessdevExampleDcbCatalog.commandSchema
});

let ArchiveCategoryMaker = StateChangeSlice_Builder$ReventlessInMemory.Make({
  name: ArchiveCategory$ReventlessdevExampleDcbCatalog.name,
  DcbEventLogSpec: CatalogEventLog$ReventlessdevExampleDcbCatalog,
  errorSchema: ArchiveCategory$ReventlessdevExampleDcbCatalog.errorSchema,
  initialDecisionModel: ArchiveCategory$ReventlessdevExampleDcbCatalog.initialDecisionModel,
  reduce: ArchiveCategory$ReventlessdevExampleDcbCatalog.reduce,
  decide: ArchiveCategory$ReventlessdevExampleDcbCatalog.decide,
  commandSchema: ArchiveCategory$ReventlessdevExampleDcbCatalog.commandSchema
});

async function publishJsons(cmdJsons) {
  await Promise.all(cmdJsons.map(async cmdJson => {
    let dict = cmdJson.commandJson;
    let typeName;
    typeName = typeof dict === "object" && dict !== null && !Array.isArray(dict) ? Stdlib_Option.getOr(Stdlib_Option.flatMap(dict["TAG"], j => {
        if (typeof j === "string") {
          return j;
        }
      }), "") : "";
    let fullBody = Object.fromEntries([
      [
        "id",
        cmdJson.id
      ],
      [
        "meta",
        S.reverseConvertToJsonOrThrow(cmdJson.meta, Message$Reventless.metaSchema)
      ],
      [
        "command",
        cmdJson.commandJson
      ]
    ]);
    let handlers = CommandTopic$ReventlessInMemory.getHandlers(typeName);
    await Promise.all(handlers.map(async entry => {
      await entry.handler([{
          command: fullBody,
          reference: cmdJson.id
        }]);
    }));
  }));
}

let publishJsonsOutput = Pulumi.output(publishJsons);

let _addProductSlice = AddProductMaker.make(eventLog, publishJsonsOutput, undefined);

let _updateProductNameSlice = UpdateProductNameMaker.make(eventLog, publishJsonsOutput, undefined);

let _updateProductDescriptionSlice = UpdateProductDescriptionMaker.make(eventLog, publishJsonsOutput, undefined);

let _updateProductPriceSlice = UpdateProductPriceMaker.make(eventLog, publishJsonsOutput, undefined);

let _addCategorySlice = AddCategoryMaker.make(eventLog, publishJsonsOutput, undefined);

let _renameCategorySlice = RenameCategoryMaker.make(eventLog, publishJsonsOutput, undefined);

let _archiveCategorySlice = ArchiveCategoryMaker.make(eventLog, publishJsonsOutput, undefined);

let testMeta = {
  service: "example-dcb-catalog-test",
  time: "2024-01-01T00:00:00.000Z",
  ip: "127.0.0.1",
  user: "testuser",
  msgId: "msg-001",
  correlationId: "corr-001"
};

async function dispatch(commandJson, entityId) {
  return await publishJsons([{
      id: entityId,
      meta: testMeta,
      commandJson: commandJson
    }]);
}

describe("Catalog DCB E2E:", () => {
  beforeAll(async () => {
    await TestRunner$ReventlessInMemory.resolve(CatalogEventLogMaker.operations(eventLog));
  });
  beforeEach(() => {
    capturedEventCount.contents = 0;
  });
  test("AddProduct publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "AddProduct",
      productId: "prod-1",
      name: "Laptop",
      description: "A laptop",
      price: 999.99
    }, AddProduct$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "prod-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate AddProduct produces 0 events (ProductAlreadyExists)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "AddProduct",
      productId: "prod-1",
      name: "Duplicate",
      description: "Dup",
      price: 1.0
    }, AddProduct$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "prod-1");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("UpdateProductName on existing product publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "UpdateProductName",
      productId: "prod-1",
      name: "Gaming Laptop"
    }, UpdateProductName$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "prod-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("UpdateProductName on non-existent product produces 0 events (ProductNotFound)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "UpdateProductName",
      productId: "no-such-product",
      name: "Ghost"
    }, UpdateProductName$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "no-such-product");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("AddCategory publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "AddCategory",
      categoryId: "cat-1",
      name: "Electronics"
    }, AddCategory$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "cat-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate AddCategory produces 0 events (CategoryAlreadyExists)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "AddCategory",
      categoryId: "cat-1",
      name: "Duplicate"
    }, AddCategory$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "cat-1");
    expect(capturedEventCount.contents).toBe(0);
  });
  test("RenameCategory on existing category publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "RenameCategory",
      categoryId: "cat-1",
      name: "Consumer Electronics"
    }, RenameCategory$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "cat-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("ArchiveCategory publishes 1 event", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "ArchiveCategory",
      categoryId: "cat-1"
    }, ArchiveCategory$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "cat-1");
    expect(capturedEventCount.contents).toBe(1);
  });
  test("duplicate ArchiveCategory is idempotent (0 events)", async () => {
    let cmd = Message$Reventless.encode({
      TAG: "ArchiveCategory",
      categoryId: "cat-1"
    }, ArchiveCategory$ReventlessdevExampleDcbCatalog.commandSchema);
    await dispatch(cmd, "cat-1");
    expect(capturedEventCount.contents).toBe(0);
  });
});

export {
  Bus,
  capturedEventCount,
  DcbEventLogMaker,
  CatalogEventLogMaker,
  eventLog,
  AddProductMaker,
  UpdateProductNameMaker,
  UpdateProductDescriptionMaker,
  UpdateProductPriceMaker,
  AddCategoryMaker,
  RenameCategoryMaker,
  ArchiveCategoryMaker,
  publishJsons,
  publishJsonsOutput,
  _addProductSlice,
  _updateProductNameSlice,
  _updateProductDescriptionSlice,
  _updateProductPriceSlice,
  _addCategorySlice,
  _renameCategorySlice,
  _archiveCategorySlice,
  testMeta,
  dispatch,
}
/* Bus Not a pure module */
