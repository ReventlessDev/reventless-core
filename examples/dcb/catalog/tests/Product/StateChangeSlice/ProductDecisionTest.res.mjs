// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Jest from "@glennsl/rescript-jest/src/jest.res.mjs";
import * as AddProduct$ReventlessdevExampleDcbCatalog from "../../../src/Product/StateChangeSlice/AddProduct.res.mjs";
import * as UpdateProductName$ReventlessdevExampleDcbCatalog from "../../../src/Product/StateChangeSlice/UpdateProductName.res.mjs";
import * as UpdateProductPrice$ReventlessdevExampleDcbCatalog from "../../../src/Product/StateChangeSlice/UpdateProductPrice.res.mjs";
import * as UpdateProductDescription$ReventlessdevExampleDcbCatalog from "../../../src/Product/StateChangeSlice/UpdateProductDescription.res.mjs";

Jest.describe("AddProduct:", () => {
  Jest.describe("reduce", () => {
    Jest.test("ProductAdded sets exists=true", () => Jest.Expect.toEqual(Jest.Expect.expect(AddProduct$ReventlessdevExampleDcbCatalog.reduce(AddProduct$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "ProductAdded",
      productId: "p1",
      name: "Laptop",
      description: "A laptop",
      price: 999.99
    })), {
      exists: true
    }));
    Jest.test("other events do not change model", () => Jest.Expect.toEqual(Jest.Expect.expect(AddProduct$ReventlessdevExampleDcbCatalog.reduce(AddProduct$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "CategoryAdded",
      categoryId: "c1",
      name: "Books"
    })), AddProduct$ReventlessdevExampleDcbCatalog.initialDecisionModel));
  });
  Jest.describe("decide", () => {
    Jest.test("on non-existent product produces ProductAdded", () => Jest.Expect.toEqual(Jest.Expect.expect(AddProduct$ReventlessdevExampleDcbCatalog.decide(AddProduct$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "AddProduct",
      productId: "p1",
      name: "Laptop",
      description: "A laptop",
      price: 999.99
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "ProductAdded",
          productId: "p1",
          name: "Laptop",
          description: "A laptop",
          price: 999.99
        }]
    }));
    Jest.test("on existing product returns ProductAlreadyExists", () => Jest.Expect.toEqual(Jest.Expect.expect(AddProduct$ReventlessdevExampleDcbCatalog.decide({
      exists: true
    }, {
      TAG: "AddProduct",
      productId: "p1",
      name: "Laptop",
      description: "A laptop",
      price: 999.99
    })), {
      TAG: "Error",
      _0: "ProductAlreadyExists"
    }));
  });
});

Jest.describe("UpdateProductName:", () => {
  let existingModel = {
    exists: true,
    currentName: "Laptop"
  };
  Jest.describe("reduce", () => {
    Jest.test("ProductAdded sets exists=true and currentName", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductName$ReventlessdevExampleDcbCatalog.reduce(UpdateProductName$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "ProductAdded",
      productId: "p1",
      name: "Laptop",
      description: "A laptop",
      price: 999.99
    })), {
      exists: true,
      currentName: "Laptop"
    }));
    Jest.test("ProductNameUpdated updates currentName", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductName$ReventlessdevExampleDcbCatalog.reduce(existingModel, {
      TAG: "ProductNameUpdated",
      productId: "p1",
      name: "Gaming Laptop"
    })), {
      exists: true,
      currentName: "Gaming Laptop"
    }));
  });
  Jest.describe("decide", () => {
    Jest.test("on non-existent product returns ProductNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductName$ReventlessdevExampleDcbCatalog.decide(UpdateProductName$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "UpdateProductName",
      productId: "p1",
      name: "Gaming Laptop"
    })), {
      TAG: "Error",
      _0: "ProductNotFound"
    }));
    Jest.test("same name produces no events (idempotent)", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductName$ReventlessdevExampleDcbCatalog.decide(existingModel, {
      TAG: "UpdateProductName",
      productId: "p1",
      name: "Laptop"
    })), {
      TAG: "Ok",
      _0: []
    }));
    Jest.test("new name produces ProductNameUpdated", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductName$ReventlessdevExampleDcbCatalog.decide(existingModel, {
      TAG: "UpdateProductName",
      productId: "p1",
      name: "Gaming Laptop"
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "ProductNameUpdated",
          productId: "p1",
          name: "Gaming Laptop"
        }]
    }));
  });
});

Jest.describe("UpdateProductDescription:", () => {
  let existingModel = {
    exists: true,
    currentDescription: "A laptop"
  };
  Jest.describe("decide", () => {
    Jest.test("on non-existent product returns ProductNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductDescription$ReventlessdevExampleDcbCatalog.decide(UpdateProductDescription$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "UpdateProductDescription",
      productId: "p1",
      description: "A high-end laptop"
    })), {
      TAG: "Error",
      _0: "ProductNotFound"
    }));
    Jest.test("same description produces no events (idempotent)", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductDescription$ReventlessdevExampleDcbCatalog.decide(existingModel, {
      TAG: "UpdateProductDescription",
      productId: "p1",
      description: "A laptop"
    })), {
      TAG: "Ok",
      _0: []
    }));
    Jest.test("new description produces ProductDescriptionUpdated", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductDescription$ReventlessdevExampleDcbCatalog.decide(existingModel, {
      TAG: "UpdateProductDescription",
      productId: "p1",
      description: "A high-end laptop"
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "ProductDescriptionUpdated",
          productId: "p1",
          description: "A high-end laptop"
        }]
    }));
  });
});

Jest.describe("UpdateProductPrice:", () => {
  let existingModel = {
    exists: true,
    currentPrice: 999.99
  };
  Jest.describe("decide", () => {
    Jest.test("on non-existent product returns ProductNotFound", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductPrice$ReventlessdevExampleDcbCatalog.decide(UpdateProductPrice$ReventlessdevExampleDcbCatalog.initialDecisionModel, {
      TAG: "UpdateProductPrice",
      productId: "p1",
      price: 899.99
    })), {
      TAG: "Error",
      _0: "ProductNotFound"
    }));
    Jest.test("same price produces no events (idempotent)", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductPrice$ReventlessdevExampleDcbCatalog.decide(existingModel, {
      TAG: "UpdateProductPrice",
      productId: "p1",
      price: 999.99
    })), {
      TAG: "Ok",
      _0: []
    }));
    Jest.test("new price produces ProductPriceUpdated", () => Jest.Expect.toEqual(Jest.Expect.expect(UpdateProductPrice$ReventlessdevExampleDcbCatalog.decide(existingModel, {
      TAG: "UpdateProductPrice",
      productId: "p1",
      price: 899.99
    })), {
      TAG: "Ok",
      _0: [{
          TAG: "ProductPriceUpdated",
          productId: "p1",
          price: 899.99
        }]
    }));
  });
});

/*  Not a pure module */
