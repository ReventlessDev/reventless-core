// Generated by ReScript, PLEASE EDIT WITH CARE

import * as OrdersProjections$OrderingPlugin from "../../src/ReadModel/OrdersProjections.res.mjs";
import * as MultiSourceProjection_GWT$ReventlessGwt from "@reventlessdev/reventless-gwt/src/MultiSourceProjection_GWT.res.mjs";

let include = MultiSourceProjection_GWT$ReventlessGwt.Make(OrdersProjections$OrderingPlugin.OrderMapping);

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenState = include.thenState;

describe("OrderProjection:", () => {
  test("Placed sets initial read model state", undefined, () => thenState(whenEvent(givenEvents([]), {
    TAG: "Placed",
    customerId: "cust-1",
    productIds: [
      "prod-1",
      "prod-2"
    ]
  }), {
    customerId: "cust-1",
    productIds: [
      "prod-1",
      "prod-2"
    ],
    status: "placed"
  }));
  test("Shipped after placing updates status to shipped", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "Placed",
      customerId: "cust-1",
      productIds: ["prod-1"]
    }]), "Shipped"), {
    customerId: "cust-1",
    productIds: ["prod-1"],
    status: "shipped"
  }));
  test("Cancelled after placing updates status to cancelled", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "Placed",
      customerId: "cust-1",
      productIds: ["prod-1"]
    }]), {
    TAG: "Cancelled",
    productIds: ["prod-1"]
  }), {
    customerId: "cust-1",
    productIds: ["prod-1"],
    status: "cancelled"
  }));
});

let describeWithId = include.describeWithId;

let givenEventsWithTime = include.givenEventsWithTime;

let whenEvents = include.whenEvents;

let whenEventWithTime = include.whenEventWithTime;

let whenEventsWithTime = include.whenEventsWithTime;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenStateWithId = include.thenStateWithId;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  describe,
  describeWithId,
  test,
  givenEvents,
  givenEventsWithTime,
  whenEvent,
  whenEvents,
  whenEventWithTime,
  whenEventsWithTime,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
