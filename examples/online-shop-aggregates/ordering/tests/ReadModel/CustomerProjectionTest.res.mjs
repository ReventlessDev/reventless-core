// Generated by ReScript, PLEASE EDIT WITH CARE

import * as ProjectionTest$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/test/ProjectionTest.res.mjs";
import * as CustomersProjections$OrderingPlugin from "../../src/ReadModel/CustomersProjections.res.mjs";

let include = ProjectionTest$ReventlessInMemory.Make(CustomersProjections$OrderingPlugin.CustomerMapping);

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenEvent = include.whenEvent;

let thenState = include.thenState;

describe("CustomerProjection:", () => {
  test("Registered sets initial read model state", undefined, () => thenState(whenEvent(givenEvents([]), {
    TAG: "Registered",
    email: "alice@example.com",
    address: "123 Main St"
  }), {
    customerId: "id",
    email: "alice@example.com",
    address: "123 Main St",
    deactivated: false
  }));
  test("EmailUpdated after registration updates email", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "Registered",
      email: "alice@example.com",
      address: "123 Main St"
    }]), {
    TAG: "EmailUpdated",
    email: "alice2@example.com"
  }), {
    customerId: "id",
    email: "alice2@example.com",
    address: "123 Main St",
    deactivated: false
  }));
  test("AddressUpdated after registration updates address", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "Registered",
      email: "alice@example.com",
      address: "123 Main St"
    }]), {
    TAG: "AddressUpdated",
    address: "789 Pine Rd"
  }), {
    customerId: "id",
    email: "alice@example.com",
    address: "789 Pine Rd",
    deactivated: false
  }));
  test("Deactivated after registration sets deactivated flag", undefined, () => thenState(whenEvent(givenEvents([{
      TAG: "Registered",
      email: "alice@example.com",
      address: "123 Main St"
    }]), "Deactivated"), {
    customerId: "id",
    email: "alice@example.com",
    address: "123 Main St",
    deactivated: true
  }));
});

let describeWithId = include.describeWithId;

let givenEventsWithTime = include.givenEventsWithTime;

let whenEvents = include.whenEvents;

let whenEventWithTime = include.whenEventWithTime;

let whenEventsWithTime = include.whenEventsWithTime;

let thenStates = include.thenStates;

let thenStatesWithId = include.thenStatesWithId;

let thenAllStates = include.thenAllStates;

let thenStateWithId = include.thenStateWithId;

let thenNoState = include.thenNoState;

let thenThrow = include.thenThrow;

let thenFail = include.thenFail;

export {
  describe,
  describeWithId,
  test,
  givenEvents,
  givenEventsWithTime,
  whenEvent,
  whenEvents,
  whenEventWithTime,
  whenEventsWithTime,
  thenStates,
  thenStatesWithId,
  thenAllStates,
  thenState,
  thenStateWithId,
  thenNoState,
  thenThrow,
  thenFail,
}
/* include Not a pure module */
