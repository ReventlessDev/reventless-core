// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

let stateSchema = S.union([
  S.literal("NotCreated"),
  S.schema(s => ({
    TAG: "Created",
    name: s.m(S.string),
    price: s.m(S.float)
  }))
]);

function evolve(state, event) {
  if (typeof state !== "object") {
    if (event.TAG === "Synced") {
      return {
        TAG: "Created",
        name: event.name,
        price: event.price
      };
    } else {
      return state;
    }
  } else if (event.TAG === "Synced") {
    return {
      TAG: "Created",
      name: event.name,
      price: event.price
    };
  } else {
    return {
      TAG: "Created",
      name: state.name,
      price: event.price
    };
  }
}

function decide(state, command) {
  if (typeof state !== "object") {
    if (command.TAG === "Sync") {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "Synced",
            name: command.name,
            price: command.price
          }]
      };
    } else {
      return {
        TAG: "Ok",
        _0: []
      };
    }
  } else if (command.TAG === "Sync") {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "PriceUpdated",
          price: command.price
        }]
    };
  }
}

let Spec;

let initialState = "NotCreated";

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/Aggregate/CatalogProductBehavior.res.mjs";

export {
  Spec,
  stateSchema,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* stateSchema Not a pure module */
