// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as CatalogProduct$OrderingPlugin from "./CatalogProduct.res.mjs";

let stateSchema = S.union([
  S.literal("NotCreated"),
  S.schema(s => ({
    TAG: "Created",
    name: s.m(S.string),
    price: s.m(S.float)
  }))
]);

let resolverConfig_fields = [];

let resolverConfig = {
  commandSchema: CatalogProduct$OrderingPlugin.commandSchema,
  fields: resolverConfig_fields
};

let moduleUrl = import.meta.url;

function evolve(state, event) {
  if (typeof state !== "object") {
    if (event.TAG === "Synced") {
      return {
        TAG: "Created",
        name: event.name,
        price: event.price
      };
    } else {
      return state;
    }
  } else if (event.TAG === "Synced") {
    return {
      TAG: "Created",
      name: event.name,
      price: event.price
    };
  } else {
    return {
      TAG: "Created",
      name: state.name,
      price: event.price
    };
  }
}

function decide(state, command) {
  if (typeof state !== "object") {
    if (command.TAG === "Sync") {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "Synced",
            name: command.name,
            price: command.price
          }]
      };
    } else {
      return {
        TAG: "Ok",
        _0: []
      };
    }
  } else if (command.TAG === "Sync") {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "PriceUpdated",
          price: command.price
        }]
    };
  }
}

let Spec;

let initialState = "NotCreated";

export {
  Spec,
  stateSchema,
  resolverConfig,
  moduleUrl,
  initialState,
  evolve,
  decide,
}
/* stateSchema Not a pure module */
