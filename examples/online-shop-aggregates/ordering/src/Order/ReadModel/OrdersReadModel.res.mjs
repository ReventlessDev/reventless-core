// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let statusSchema = S.union([
  S.literal("Placed"),
  S.literal("Shipped"),
  S.literal("Cancelled")
]);

let stateSchema = S.schema(s => ({
  customerId: s.m(S.string),
  productIds: s.m(S.array(S.string)),
  status: s.m(statusSchema)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "Orders";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/Order/ReadModel/OrdersReadModel.res.mjs";

export {
  name,
  Id,
  statusSchema,
  stateSchema,
  config,
  subIdConfig,
  moduleUrl,
}
/* statusSchema Not a pure module */
