// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as ReadModel$Reventless from "@reventlessdev/reventless-spec/src/components/ReadModel.res.mjs";

let lifecycleSchema = S.union([
  S.literal("Placed"),
  S.literal("Shipped"),
  S.literal("Cancelled"),
  S.literal("Refunded")
]);

let stateSchema = S.schema(s => ({
  customerId: s.m(S.string),
  productIds: s.m(S.array(S.string)),
  lifecycle: s.m(lifecycleSchema)
}));

let config = ReadModel$Reventless.config(undefined, undefined, undefined);

let name = "Orders";

let Id;

let subIdConfig;

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/Order/ReadModel/Orders.res.mjs";

let authorization = "AllowAuthenticated";

let visibility = "Public";

export {
  name,
  Id,
  lifecycleSchema,
  stateSchema,
  config,
  subIdConfig,
  moduleUrl,
  authorization,
  visibility,
}
/* lifecycleSchema Not a pure module */
