// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Order$OrderingPlugin from "../Aggregate/Order.res.mjs";
import * as Orders$OrderingPlugin from "./Orders.res.mjs";
import * as Projection$Reventless from "@reventlessdev/reventless-spec/src/types/Projection.res.mjs";
import * as OrderId$OrderingPlugin from "../OrderId.res.mjs";

let M = Projection$Reventless.Mappings.Make({
  Id: OrderId$OrderingPlugin,
  name: Orders$OrderingPlugin.name,
  stateSchema: Orders$OrderingPlugin.stateSchema,
  subIdConfig: undefined
});

function project(param) {
  let event = param.event;
  let id = param.id;
  if (typeof event !== "object") {
    return {
      TAG: "Update",
      _0: id,
      _1: state => ({
        customerId: state.customerId,
        productIds: state.productIds,
        lifecycle: "Shipped"
      })
    };
  }
  switch (event.TAG) {
    case "Placed" :
      return {
        TAG: "Set",
        _0: id,
        _1: {
          customerId: event.customerId,
          productIds: event.productIds,
          lifecycle: "Placed"
        }
      };
    case "Cancelled" :
      return {
        TAG: "Update",
        _0: id,
        _1: state => ({
          customerId: state.customerId,
          productIds: state.productIds,
          lifecycle: "Cancelled"
        })
      };
    case "Refunded" :
      return {
        TAG: "Update",
        _0: id,
        _1: state => ({
          customerId: state.customerId,
          productIds: state.productIds,
          lifecycle: "Refunded"
        })
      };
  }
}

let OrderMapping = Projection$Reventless.Mapping.Make({
  Id: OrderId$OrderingPlugin,
  name: Order$OrderingPlugin.name,
  eventSchema: Order$OrderingPlugin.eventSchema
})({
  Id: OrderId$OrderingPlugin,
  name: Orders$OrderingPlugin.name,
  stateSchema: Orders$OrderingPlugin.stateSchema,
  subIdConfig: undefined
})({
  project: project
});

let mappings = [OrderMapping];

let Target;

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/Order/ReadModel/Orders_Projections.res.mjs";

export {
  Target,
  M,
  OrderMapping,
  mappings,
  moduleUrl,
}
/* M Not a pure module */
