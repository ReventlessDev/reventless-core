// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";

let stateSchema = Sury.union([
  Sury.literal("NotCreated"),
  Sury.$schema(s => ({
    TAG: "Created",
    name: s.m(Sury.string),
    price: s.m(Sury.float)
  }))
]);

function evolve(state, event) {
  if (typeof state !== "object") {
    if (event.TAG === "Synced") {
      return {
        TAG: "Created",
        name: event.name,
        price: event.price
      };
    } else {
      return state;
    }
  } else if (event.TAG === "Synced") {
    return {
      TAG: "Created",
      name: event.name,
      price: event.price
    };
  } else {
    return {
      TAG: "Created",
      name: state.name,
      price: event.price
    };
  }
}

function decide(state, command) {
  if (typeof state !== "object") {
    if (command.TAG === "Sync") {
      return {
        TAG: "Ok",
        _0: [{
            TAG: "Synced",
            name: command.name,
            price: command.price
          }]
      };
    } else {
      return {
        TAG: "Ok",
        _0: []
      };
    }
  } else if (command.TAG === "Sync") {
    return {
      TAG: "Ok",
      _0: []
    };
  } else {
    return {
      TAG: "Ok",
      _0: [{
          TAG: "PriceUpdated",
          price: command.price
        }]
    };
  }
}

let Spec;

let initialState = "NotCreated";

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/CatalogProduct/Aggregate/CatalogProduct_Behavior.res.mjs";

let snapshot;

export {
  Spec,
  stateSchema,
  initialState,
  evolve,
  decide,
  moduleUrl,
  snapshot,
}
/* stateSchema Not a pure module */
