// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

let commandSchema = S.union([
  S.schema(s => ({
    TAG: "Sync",
    name: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "UpdatePrice",
    price: s.m(S.float)
  }))
]);

let eventSchema = S.union([
  S.schema(s => ({
    TAG: "Synced",
    name: s.m(S.string),
    price: s.m(S.float)
  })),
  S.schema(s => ({
    TAG: "PriceUpdated",
    price: s.m(S.float)
  }))
]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

let name = "CatalogProduct";

let Id;

let errorSchema = S.unit;

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/CatalogProduct/Aggregate/CatalogProduct.res.mjs";

export {
  name,
  Id,
  commandSchema,
  eventSchema,
  errorSchema,
  moduleUrl,
  commandAuthorization,
}
/* commandSchema Not a pure module */
