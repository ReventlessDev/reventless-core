// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as Email$Reventless from "@reventlessdev/reventless-spec/src/semantic/Email.res.mjs";

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Register",
    email: s.m(Email$Reventless.schema),
    address: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "UpdateEmail",
    email: s.m(Email$Reventless.schema)
  })),
  Sury.$schema(s => ({
    TAG: "UpdateAddress",
    address: s.m(Sury.string)
  })),
  Sury.literal("Deactivate")
]);

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Registered",
    email: s.m(Sury.string),
    address: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "EmailUpdated",
    email: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "AddressUpdated",
    address: s.m(Sury.string)
  })),
  Sury.literal("Deactivated")
]);

let errorSchema = Sury.union([
  Sury.literal("CustomerAlreadyRegistered"),
  Sury.literal("CustomerNotFound"),
  Sury.literal("CustomerAlreadyDeactivated")
]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Unrestricted";
}

let traits = [];

let name = "Customer";

let Id;

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/Customer/Aggregate/Customer.res.mjs";

export {
  name,
  Id,
  commandSchema,
  eventSchema,
  errorSchema,
  moduleUrl,
  commandAuthorization,
  commandTransition,
  traits,
}
/* commandSchema Not a pure module */
