// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";

let stateSchema = S.union([
  S.literal("NotCreated"),
  S.schema(s => ({
    TAG: "Active",
    email: s.m(S.string),
    address: s.m(S.string)
  })),
  S.literal("Deactivated")
]);

function evolve(state, event) {
  if (typeof state !== "object") {
    if (state === "NotCreated" && typeof event === "object" && event.TAG === "Registered") {
      return {
        TAG: "Active",
        email: event.email,
        address: event.address
      };
    } else {
      return state;
    }
  }
  if (typeof event !== "object") {
    return "Deactivated";
  }
  switch (event.TAG) {
    case "Registered" :
      return {
        TAG: "Active",
        email: event.email,
        address: event.address
      };
    case "EmailUpdated" :
      return {
        TAG: "Active",
        email: event.email,
        address: state.address
      };
    case "AddressUpdated" :
      return {
        TAG: "Active",
        email: state.email,
        address: event.address
      };
  }
}

function decide(state, command) {
  if (typeof state !== "object") {
    if (state === "NotCreated") {
      if (typeof command !== "object" || command.TAG !== "Register") {
        return {
          TAG: "Error",
          _0: "CustomerNotFound"
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "Registered",
              email: command.email,
              address: command.address
            }]
        };
      }
    } else if (typeof command !== "object") {
      return {
        TAG: "Ok",
        _0: []
      };
    } else {
      return {
        TAG: "Error",
        _0: "CustomerAlreadyDeactivated"
      };
    }
  }
  if (typeof command !== "object") {
    return {
      TAG: "Ok",
      _0: ["Deactivated"]
    };
  }
  switch (command.TAG) {
    case "Register" :
      return {
        TAG: "Error",
        _0: "CustomerAlreadyRegistered"
      };
    case "UpdateEmail" :
      let email = command.email;
      if (email === state.email) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "EmailUpdated",
              email: email
            }]
        };
      }
    case "UpdateAddress" :
      let address = command.address;
      if (address === state.address) {
        return {
          TAG: "Ok",
          _0: []
        };
      } else {
        return {
          TAG: "Ok",
          _0: [{
              TAG: "AddressUpdated",
              address: address
            }]
        };
      }
  }
}

let Spec;

let initialState = "NotCreated";

let moduleUrl = "@reventlessdev/online-shop-aggregates-ordering/src/Customer/Aggregate/Customer_Behavior.res.mjs";

export {
  Spec,
  stateSchema,
  initialState,
  evolve,
  decide,
  moduleUrl,
}
/* stateSchema Not a pure module */
