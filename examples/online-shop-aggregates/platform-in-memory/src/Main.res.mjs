// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Plugin$CatalogPlugin from "@reventlessdev/online-shop-aggregates-catalog/src/Plugin.res.mjs";
import * as Plugin$OrderingPlugin from "@reventlessdev/online-shop-aggregates-ordering/src/Plugin.res.mjs";
import * as PackageVersion$Reventless from "@reventlessdev/reventless-spec/src/PackageVersion.res.mjs";
import * as Platform$ReventlessInMemory from "@reventlessdev/reventless-in-memory/src/Platform.res.mjs";

let Platform = Platform$ReventlessInMemory.Make({});

let Catalog = Plugin$CatalogPlugin.Make(Platform);

let Ordering = Plugin$OrderingPlugin.Make(Platform);

function make() {
  return Catalog.make(process.env["CATALOG_UI_BUNDLE_URL"]);
}

let CatalogMaker = {
  make: make
};

function make$1() {
  return Ordering.make(process.env["ORDERING_UI_BUNDLE_URL"]);
}

let OrderingMaker = {
  make: make$1
};

Platform.makePlatform(PackageVersion$Reventless.fromCwd(), [
  CatalogMaker,
  OrderingMaker
]);

Platform.startServers();

export {
  Platform,
  Catalog,
  Ordering,
  CatalogMaker,
  OrderingMaker,
}
/* Platform Not a pure module */
