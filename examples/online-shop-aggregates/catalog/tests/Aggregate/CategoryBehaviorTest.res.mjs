// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Category$CatalogPlugin from "../../src/Aggregate/Category.res.mjs";
import * as Behavior_GWT$ReventlessGwt from "@reventlessdev/reventless-gwt/src/Behavior_GWT.res.mjs";
import * as CategoryBehavior$CatalogPlugin from "../../src/Aggregate/CategoryBehavior.res.mjs";

let include = Behavior_GWT$ReventlessGwt.Make({
  name: Category$CatalogPlugin.name,
  commandSchema: Category$CatalogPlugin.commandSchema,
  eventSchema: Category$CatalogPlugin.eventSchema,
  errorSchema: Category$CatalogPlugin.errorSchema
})({
  Spec: {
    name: Category$CatalogPlugin.name,
    commandSchema: Category$CatalogPlugin.commandSchema,
    eventSchema: Category$CatalogPlugin.eventSchema,
    errorSchema: Category$CatalogPlugin.errorSchema
  },
  initialState: CategoryBehavior$CatalogPlugin.initialState,
  evolve: CategoryBehavior$CatalogPlugin.evolve,
  decide: CategoryBehavior$CatalogPlugin.decide,
  moduleUrl: CategoryBehavior$CatalogPlugin.moduleUrl
});

let describe = include.describe;

let test = include.test;

let givenEvents = include.givenEvents;

let whenCmd = include.whenCmd;

let thenEvent = include.thenEvent;

let thenNoEvent = include.thenNoEvent;

let thenError = include.thenError;

describe("CategoryBehavior:", () => {
  describe("Add", () => {
    test("on new aggregate produces Added", () => thenEvent(whenCmd(givenEvents([]), {
      TAG: "Add",
      name: "Electronics"
    }), {
      TAG: "Added",
      name: "Electronics"
    }));
    test("on existing aggregate returns CategoryAlreadyExists error", () => thenError(whenCmd(givenEvents([{
        TAG: "Added",
        name: "Electronics"
      }]), {
      TAG: "Add",
      name: "Electronics 2"
    }), "CategoryAlreadyExists"));
  });
  describe("Rename", () => {
    test("on non-existent aggregate returns CategoryNotFound error", () => thenError(whenCmd(givenEvents([]), {
      TAG: "Rename",
      name: "New Name"
    }), "CategoryNotFound"));
    test("on active category produces Renamed", () => thenEvent(whenCmd(givenEvents([{
        TAG: "Added",
        name: "Electronics"
      }]), {
      TAG: "Rename",
      name: "Consumer Electronics"
    }), {
      TAG: "Renamed",
      name: "Consumer Electronics"
    }));
    test("on archived category returns CategoryAlreadyArchived error", () => thenError(whenCmd(givenEvents([
      {
        TAG: "Added",
        name: "Electronics"
      },
      "Archived"
    ]), {
      TAG: "Rename",
      name: "New Name"
    }), "CategoryAlreadyArchived"));
  });
  describe("Archive", () => {
    test("on active category produces Archived", () => thenEvent(whenCmd(givenEvents([{
        TAG: "Added",
        name: "Electronics"
      }]), "Archive"), "Archived"));
    test("on archived category is idempotent (produces no events)", () => thenNoEvent(whenCmd(givenEvents([
      {
        TAG: "Added",
        name: "Electronics"
      },
      "Archived"
    ]), "Archive")));
  });
});

let Spec = include.Spec;

let thenCompareEvent = include.thenCompareEvent;

let thenEventWithError = include.thenEventWithError;

let thenEvents = include.thenEvents;

let thenCompareEvents = include.thenCompareEvents;

let thenEventsWithError = include.thenEventsWithError;

export {
  Spec,
  describe,
  test,
  givenEvents,
  whenCmd,
  thenEvent,
  thenCompareEvent,
  thenNoEvent,
  thenEventWithError,
  thenEvents,
  thenCompareEvents,
  thenEventsWithError,
  thenError,
}
/* include Not a pure module */
