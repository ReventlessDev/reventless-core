// Generated by ReScript, PLEASE EDIT WITH CARE


function mapIncomingCommand(_id, _command, _meta) {
  return [];
}

let mapOutgoingEvent = (id, event, _meta, _queryEngine) => {
  switch (event.TAG) {
    case "Added" :
      return [{
          TAG: "PublishEvent",
          _0: id,
          _1: {
            TAG: "ProductBecameAvailable",
            productId: id,
            name: event.name,
            price: event.price
          }
        }];
    case "PriceUpdated" :
      return [{
          TAG: "PublishEvent",
          _0: id,
          _1: {
            TAG: "ProductPriceChanged",
            productId: id,
            price: event.price
          }
        }];
    default:
      return [];
  }
};

let publishedEvents = [
  {
    name: "ProductBecameAvailable",
    fromEventTypes: ["Added"]
  },
  {
    name: "ProductPriceChanged",
    fromEventTypes: ["PriceUpdated"]
  }
];

let acceptedCommands = [];

let name = "Products";

let Id;

let ExtensionPoint;

let Delegate;

let moduleUrl = "@reventlessdev/online-shop-aggregates-catalog/src/ExtensionPoint/Products_ExtensionPointMapping.res.mjs";

export {
  name,
  Id,
  ExtensionPoint,
  Delegate,
  mapIncomingCommand,
  mapOutgoingEvent,
  moduleUrl,
  publishedEvents,
  acceptedCommands,
}
/* No side effect */
