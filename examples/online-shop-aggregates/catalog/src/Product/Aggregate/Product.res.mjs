// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Sury from "sury";
import * as StorageRef$Reventless from "@reventlessdev/reventless-spec/src/semantic/StorageRef.res.mjs";

let commandSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Add",
    name: s.m(Sury.string),
    description: s.m(Sury.string),
    price: s.m(Sury.float),
    imageUrl: s.m(StorageRef$Reventless.forStore(undefined, "productImages"))
  })),
  Sury.$schema(s => ({
    TAG: "UpdateName",
    name: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "UpdateDescription",
    description: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "UpdatePrice",
    price: s.m(Sury.float)
  })),
  Sury.$schema(s => ({
    TAG: "UpdateImage",
    imageUrl: s.m(StorageRef$Reventless.forStore(undefined, "productImages"))
  }))
]);

let eventSchema = Sury.union([
  Sury.$schema(s => ({
    TAG: "Added",
    name: s.m(Sury.string),
    description: s.m(Sury.string),
    price: s.m(Sury.float),
    imageUrl: s.m(StorageRef$Reventless.forStore(undefined, "productImages"))
  })),
  Sury.$schema(s => ({
    TAG: "NameUpdated",
    name: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "DescriptionUpdated",
    description: s.m(Sury.string)
  })),
  Sury.$schema(s => ({
    TAG: "PriceUpdated",
    price: s.m(Sury.float)
  })),
  Sury.$schema(s => ({
    TAG: "ImageUpdated",
    imageUrl: s.m(StorageRef$Reventless.forStore(undefined, "productImages"))
  }))
]);

let errorSchema = Sury.union([
  Sury.literal("ProductAlreadyExists"),
  Sury.literal("ProductNotFound")
]);

function commandAuthorization(param) {
  return "AllowAuthenticated";
}

function commandTransition(param) {
  return "Unrestricted";
}

let traits = [];

let name = "Product";

let Id;

let moduleUrl = "@reventlessdev/online-shop-aggregates-catalog/src/Product/Aggregate/Product.res.mjs";

export {
  name,
  Id,
  commandSchema,
  eventSchema,
  errorSchema,
  moduleUrl,
  commandAuthorization,
  commandTransition,
  traits,
}
/* commandSchema Not a pure module */
