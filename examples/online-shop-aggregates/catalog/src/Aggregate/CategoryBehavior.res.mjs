// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Message$Reventless from "@reventlessdev/reventless-spec/src/types/Message.res.mjs";
import * as Category$CatalogPlugin from "./Category.res.mjs";

let stateSchema = S.union([
  S.schema(s => ({
    TAG: "Active",
    name: s.m(S.string)
  })),
  S.literal("Archived")
]);

let resolverConfig_fields = [];

let resolverConfig = {
  commandSchema: Category$CatalogPlugin.commandSchema,
  fields: resolverConfig_fields
};

let moduleUrl = import.meta.url;

function init(event) {
  if (typeof event !== "object") {
    throw {
      RE_EXN_ID: Message$Reventless.InvalidEvent,
      _1: Message$Reventless.encode(event, Category$CatalogPlugin.eventSchema),
      Error: new Error()
    };
  }
  if (event.TAG === "Added") {
    return {
      TAG: "Active",
      name: event.name
    };
  }
  throw {
    RE_EXN_ID: Message$Reventless.InvalidEvent,
    _1: Message$Reventless.encode(event, Category$CatalogPlugin.eventSchema),
    Error: new Error()
  };
}

function apply(state, event) {
  if (typeof state !== "object") {
    return state;
  } else if (typeof event !== "object") {
    return "Archived";
  } else {
    return {
      TAG: "Active",
      name: event.name
    };
  }
}

function create(command, _context, errorHandler) {
  if (typeof command !== "object" || command.TAG !== "Add") {
    return errorHandler("CategoryNotFound", command, _context);
  } else {
    return [{
        TAG: "Added",
        name: command.name
      }];
  }
}

function execute(state, command, context, errorHandler) {
  if (typeof state !== "object") {
    if (typeof command !== "object") {
      return [];
    } else {
      return errorHandler("CategoryAlreadyArchived", command, context);
    }
  }
  if (typeof command !== "object") {
    return ["Archived"];
  }
  if (command.TAG === "Add") {
    return errorHandler("CategoryAlreadyExists", command, context);
  }
  let name = command.name;
  if (name === state.name) {
    return [];
  } else {
    return [{
        TAG: "Renamed",
        name: name
      }];
  }
}

let Spec;

export {
  Spec,
  stateSchema,
  resolverConfig,
  moduleUrl,
  init,
  apply,
  create,
  execute,
}
/* stateSchema Not a pure module */
