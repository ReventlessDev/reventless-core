// Generated by ReScript, PLEASE EDIT WITH CARE

import * as S from "sury/src/S.res.mjs";
import * as Message$Reventless from "@reventlessdev/reventless-spec/src/types/Message.res.mjs";
import * as Product$CatalogPlugin from "./Product.res.mjs";

let stateSchema = S.schema(s => ({
  name: s.m(S.string),
  description: s.m(S.string),
  price: s.m(S.float)
}));

let resolverConfig_fields = [];

let resolverConfig = {
  commandSchema: Product$CatalogPlugin.commandSchema,
  fields: resolverConfig_fields
};

function init(event) {
  if (event.TAG === "Added") {
    return {
      name: event.name,
      description: event.description,
      price: event.price
    };
  }
  throw {
    RE_EXN_ID: Message$Reventless.InvalidEvent,
    _1: Message$Reventless.encode(event, Product$CatalogPlugin.eventSchema),
    Error: new Error()
  };
}

function apply(state, event) {
  switch (event.TAG) {
    case "Added" :
      return {
        name: event.name,
        description: event.description,
        price: event.price
      };
    case "NameUpdated" :
      return {
        name: event.name,
        description: state.description,
        price: state.price
      };
    case "DescriptionUpdated" :
      return {
        name: state.name,
        description: event.description,
        price: state.price
      };
    case "PriceUpdated" :
      return {
        name: state.name,
        description: state.description,
        price: event.price
      };
  }
}

function create(command, _context, errorHandler) {
  if (command.TAG === "Add") {
    return [{
        TAG: "Added",
        name: command.name,
        description: command.description,
        price: command.price
      }];
  } else {
    return errorHandler("ProductNotFound", command, _context);
  }
}

function execute(state, command, context, errorHandler) {
  switch (command.TAG) {
    case "Add" :
      return errorHandler("ProductAlreadyExists", command, context);
    case "UpdateName" :
      let name = command.name;
      if (name === state.name) {
        return [];
      } else {
        return [{
            TAG: "NameUpdated",
            name: name
          }];
      }
    case "UpdateDescription" :
      let description = command.description;
      if (description === state.description) {
        return [];
      } else {
        return [{
            TAG: "DescriptionUpdated",
            description: description
          }];
      }
    case "UpdatePrice" :
      let price = command.price;
      if (price === state.price) {
        return [];
      } else {
        return [{
            TAG: "PriceUpdated",
            price: price
          }];
      }
  }
}

let Spec;

export {
  Spec,
  stateSchema,
  resolverConfig,
  init,
  apply,
  create,
  execute,
}
/* stateSchema Not a pure module */
